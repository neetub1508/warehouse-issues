TITLE: [Warehouse] P2-01 · Adjustments with a value-based approval threshold, the insufficient-stock log and the blocked-move queue
LABELS: task,warehouse,phase-p2,warehouse
issue: 18
---
Part of __P2__ · Module **`warehouse`** · Migrations **V510030, V510034, V511201, V511231** · Screens **WS-089 Stock Adjustments**, **WS-096 Insufficient Stock & Lost Sales**, **WS-097 Blocked Movements Queue** · `V510030` also creates **`wh_adjustment_approval_policies`** (`RA-008`)

## Scope

> **Round-2 addition — `Y-009`.** **Unidentified found stock has no home.** Add a
> **`FOUND_UNIDENTIFIED`** reason code with `requires_note = true` and `affects_demand_history =
> false`, and one line: unidentified stock is held at a named quarantine location under a
> placeholder item until identified, then moved out by an ordinary two-line transfer. **`WONTFIX`
> with a stated reason is a legitimate answer here** — a warehouse that refuses to record what it
> cannot name is a defensible policy. What is not defensible is leaving the question unanswered,
> so that the first operator to find an unlabelled pallet invents the answer at the terminal.
>
> **ANSWERED, 2026-09-18 (P2-01 review) — in half.** `V510030` seeds `FOUND_UNIDENTIFIED` in the
> `ADJUSTMENT` context with `requires_note = true` and `affects_demand_history = false`, and its own
> verification block **raises** if that row is absent (`V510030:523`, `:811-813`), so the reason-code
> half of `Y-009` is closed and enforced by the migration itself. **The quarantine sentence is not
> answered**: no named quarantine location, no placeholder item and no move-out-by-ordinary-transfer
> rule exists anywhere in the build or in the screen contract. It is flagged open here rather than
> presumed — an operator reading the ladder today is told which code to pick and nothing about where
> the pallet goes.

The first application-layer writer that is not a receipt. Three objects, two migrations.

**`V510030` — WH-20 — `wh_stock_adjustments`, `wh_stock_adjustment_lines`.**
Header: `adjustment_number` (gapless, from `whb_number_series`), `warehouse_id`, `owner_id`,
`adjustment_type` (`POSITIVE`/`NEGATIVE`/`MIXED`), `reason_code_id` **NOT NULL**, `total_lines`,
`total_value_impact`, `requires_approval`, `approved_by`, `approved_at`, `status`,
`posted_movement_id` (a **bare UUID**, not an FK — §1.9).
Line: **one `location_id`, one `stock_status_code`, one `owner_id` per line** — the from/to shape of
`accessory_inventory_transactions` is *not* re-homed here; the two-sidedness lives in the movement,
not in the document. Plus `lot_id`, `serial_id`, `lpn_id`, `duty_status`, `system_quantity`,
`actual_quantity`, `variance_quantity`, `uom_code`, `unit_cost`, `variance_value`, `reason_code_id`,
`movement_id` (bare).

- **The approval threshold is expressed by value as well as by quantity** (`FR-145`). *A one-unit
  variance on a washer is noise; a one-unit variance on an engine control unit is an investigation.*
  Both thresholds live on configuration, both are evaluated at submit, and `requires_approval` is
  the stored outcome so the decision is reproducible.
- **The reason code is mandatory and comes from the catalogue** — an FK into `whb_reason_codes`,
  never a free-text field. There is no free-text reason column anywhere on either table.
- **`affects_demand_history` on the reason code** (`FR-146`) decides whether the movement this
  adjustment posts feeds `wh_demand_history`. A write-off or a warranty issue must not inflate the
  reorder point. The column lives on `whb_reason_codes` (`P0-04`, `V500004`); this task is its first
  consumer and the first place it is read.
- **Scrap and write-off are explicit value-destroying movements** (`FR-164`) with a reason, an
  approval and a movement — never a silent quantity decrement.

> **Round-3 fold — `RA-008` · `RF-002` (`GAP-REGISTER-R3.md` §2.1, §2.6, §4.2).**
> (1) **The two thresholds live in a ladder, not in prose** (`RA-008`). `V510030` also creates
> **`wh_adjustment_approval_policies`**: nullable `warehouse_id`, `owner_id`, `item_category_id` and
> `reason_code_id`, a computed `specificity`, `threshold_quantity` (`DECIMAL(18,4)`),
> `threshold_value` (`DECIMAL(19,4)`) and effective dates. It resolves **most-specific-first, mirroring
> `whb_gl_posting_rules` exactly**, as `Z-001` requires of every ladder. The all-null row is seeded from
> the install default `warehouse.adjustment.default_threshold_value` (`P0-15`'s `V501100`). *Submit*
> evaluates both thresholds of the resolved row and stores `requires_approval`. WS-089 shows the
> resolved threshold and **which row supplied it**, and offers the **Test resolution** modal `WS-051`
> already defines; the rows are maintained from a WS-089 toolbar action, not a new screen id.
> **Amendment, 2026-09-18 (P2-01 review), recorded from the build.** The toolbar action is the whole
> maintenance surface and it is served by WS-089's own controller:
> `GET/POST/PUT/PATCH /warehouse/inventory/adjustments/approval-policies` under
> `wh_adjustment_approval_policies:view` and `:manage`, with `POST /approval-policies/test` behind the
> **Test resolution** modal. **Both axes compare the same way — `|figure| >= threshold`, absolute,
> either one routes** (one method, `WhAdjustmentApprovalPolicyResolver.breaches`). The consequence on a
> default install is worth stating plainly, because it reads as a surprise otherwise: the all-null rung
> is seeded `threshold_value = 0` from `warehouse.adjustment.default_threshold_value` (`V501100`) and
> `threshold_quantity` **NULL**, so **every adjustment carrying a value impact routes to approval out
> of the box** — which is precisely what `V501100`'s own description says (*"Zero makes every valued
> adjustment need approval"*). *Posts on submit* begins at the first rung given a non-zero
> `threshold_value`. **The review's premise of a strict `>` on the value axis, and of a zero-value
> adjustment posting on submit, does not match the built comparison**; the set records the build, and a
> strict `>` on either axis would be a change to `FR-145`, not a reading of it.
> (2) **An `ALLOW` breach may issue with no open cost layer** (`RF-002`). What that issue costs — a
> negative `ESTIMATED` layer, consumed first by the covering receipt with a cost-variance movement — is
> `P2-16`'s rule. This task's `wh_insufficient_stock_log` row is what makes such an issue findable.

**The adjustment ladder** (`RJ-004`). It is R24 Appendix A, adopted by `GAP-REGISTER-R4.md` §2.3,
in §0.11's columns. Below both thresholds, *Submit* approves and posts in one transaction — which is
what *"posts on submit"* means.

> **Amendment, 2026-09-18 (P2-01 review).** *Post* was written as *"one movement through the port"*.
> **A `MIXED` adjustment cannot post one movement**: `ADJUST_UP` and `ADJUST_DOWN` carry different
> `cost_basis_default` — `ACTUAL` and `WEIGHTED_AVERAGE` (`V500003:217-218`) — so one movement cannot
> value both directions, and this task's own line shape already gives the **line** a bare
> `movement_id` separate from the header's `posted_movement_id`, which only a per-direction split
> needs. The build's reading conforms and the row above is amended to it: **one movement per direction
> present**, `ADJUST_UP` first, every line stamped with the movement of its own direction, and
> `posted_movement_id` = the first of them. A `POSITIVE` or `NEGATIVE` adjustment therefore still
> posts exactly one. Recorded in `docs/contracts/movement-post-reverse.contract.md` §7.

| Table | From | To | Verb | Actor (permission) | Guard | Terminal? |
|---|---|---|---|---|---|---|
| `wh_stock_adjustments` | — | `DRAFT` | Add | `wh_stock_adjustments:create` | — | no |
| `wh_stock_adjustments` | `DRAFT` | `SUBMITTED` | Submit | `wh_stock_adjustments:submit` | a catalogue reason code on the header and every line; both thresholds evaluated and `requires_approval` stored | no |
| `wh_stock_adjustments` | `SUBMITTED` | `APPROVED` | *(automatic below both thresholds)* · Approve | `wh_stock_adjustments:approve` | above either threshold, approver ≠ submitter (`FR-408`) | no |
| `wh_stock_adjustments` | `SUBMITTED` | `REJECTED` | Reject | `wh_stock_adjustments:approve` | reason recorded | **yes** |
| `wh_stock_adjustments` | `APPROVED` | `POSTED` | Post | `wh_stock_adjustments:post` | **one movement per direction present** (`ADJUST_UP` then `ADJUST_DOWN`), each line stamped with its own `movement_id` and `posted_movement_id` = the first; in the submit transaction when auto-approved | **yes** |
| `wh_stock_adjustments` | `DRAFT` · `SUBMITTED` | `CANCELLED` | Cancel | `wh_stock_adjustments:cancel` | nothing posted | **yes** |

**The verbs** (`RJ-005`). Before round 4, §10.2 listed only `:approve`, so anybody with `:edit` could
submit, post or cancel an adjustment.
- `V511201` seeds `wh_stock_adjustments:submit`, `:post` and `:cancel`.
- `V511231` inserts their `permission_dependencies` rows. Each verb requires the resource's `:view`,
  and approval never requires execution or the reverse (§10.2).

**`V510034` — WH-24 — `wh_insufficient_stock_log`, `wh_blocked_movements`.**

`wh_insufficient_stock_log` is **one table for two views of the same event** (`FR-015`, `FR-257`):
every `WARN`/`ALLOW` breach of the negative-stock policy *and* every lost sale captured at the
counter or the job-issue screen. Columns: `warehouse_id`, `item_id`, `owner_id`,
`requested_quantity`, `available_quantity`, `source_type`, `source_id`, `actor_user_id`,
`occurred_at`, `policy_applied` (`BLOCK`/`WARN`/`ALLOW`), `is_lost_sale`,
`lost_sale_reason_code_id`, `captured_manually`. Ledger-style: **no audit columns, no export
audit columns** — grid↔export parity means the export has none either.

`wh_blocked_movements` makes **a physical move the system rejected a first-class object**
(`FR-028`). *Refusing the transaction does not un-move the goods.* Columns:
`warehouse_id`, `attempted_movement_type_code`, `attempted_payload` **`TEXT`** (never `JSONB`),
`rejection_code` (from the `FR-039` stable vocabulary), `rejection_detail`, `actor_user_id`,
`device_id`, `occurred_at`, `resolved_at`, `resolution_action`, `resulting_movement_id` (bare).
The queue holds the goods in a `PENDING_RESOLUTION` disposition and routes to a supervisor who can
**force with an approval and a reason**.

**Who writes the row — a split delivery, in `MPR-T4-10`'s shape (amendment, 2026-09-18, P2-01
review).** *"The rows are raised by whatever refused the move"* names no mechanism, and a queue with
no producer never fills. The producer is a port, exactly as the insufficient-stock log's is:
`warehouse-base` declares **`WhbBlockedMovementRecorder`** and hands it every refused physical move
(the attempted movement type, the attempted payload as `TEXT`, the `FR-039` rejection code and
detail, the actor and the device) as its refusal leaves the writer; the **row** (`wh_blocked_movements`,
`V510034`) is written by `warehouse`'s adapter implementing that port, mirroring
`WhbInsufficientStockRecorder` / `WhInsufficientStockRecorderAdapter` bean for bean. Zero or more
beans: with none registered the refusal is audit-logged and no row is written, so a refusal is never
silent and never invents a producer. `MPR-T4-10`'s wording — *"base hands every breach to the port and
audits it; the log row waits on the module that implements the recorder"* — is the wording to copy.

**Which callers raise a row, and which deliberately do not (ruling, 2026-09-18, P2-01 re-verify).**
The producer (`WhbBlockedMovementReporter`) is wired into **one** caller: the movement port,
`WhbMovementPoster.post` — the EDI / handheld / device envelope path, including each member of a
batch, which reaches it one by one. **The stock-adjustment path does not raise a row and must not be
wired to**, although it calls `WhbStockLedgerWriter.post` directly and can be refused by the same
qualifying codes. `WS-097` is the queue of **physical** moves refused at a device or a port — *the
pallet is already off the rack and in somebody's hands*, which is why the refused payload, the actor
and the device are first-class evidence a supervisor dispositions. A **document** refused at its own
screen (WS-089 *Post*, and every other screen-driven writer call) leaves the goods where they were:
the operator is standing at the form, the refusal is on it, and the document itself is the queue —
it stays `SUBMITTED`/`APPROVED` and is posted again after the fix. Queueing it too would make two
queues over one fact, and a supervisor would have to clear a `wh_blocked_movements` row for an
adjustment that the adjustment screen already shows as unposted. `WhbMovementPoster` is therefore the
only call site of `report`, `device_id` is meaningful on every row, and adding a second caller is a
product decision, not a wiring omission.

**Screens.**

| id | Table · Scope | Ref | Notes |
|---|---|---|---|
| **WS-089** | `wh_stock_adjustments` · `WAREHOUSE_STOCK_ADJUSTMENT` | Service Vehicle | Multi-tab modal (header + line editor). Actions: Add · Submit · **Approve / Reject** (`wh_stock_adjustments:approve`) · Post · Cancel · ~~Print~~ (**struck 2026-09-18, P2-01 review**: no warehouse screen implements a print action, module-wide; document rendering and reprint are `P2-14`'s own screens, so a Print button here would be a second mechanism. If it is wanted it becomes its own task). **Statistics:** open · awaiting approval · posted this period · net value impact this period — **filter-aware, therefore no cache name** (`FR-395`) |
| **WS-096** | `wh_insufficient_stock_log` · `WAREHOUSE_INSUFFICIENT_STOCK` | Customer | Read-only + Export |
| **WS-097** | `wh_blocked_movements` · `WAREHOUSE_BLOCKED_MOVEMENT` | Service Vehicle | View attempted payload · **Force with approval** (`wh_blocked_movements:force`) · Resolve · Discard |

Filters, exactly as `BUILD-SPEC-SCREENS.md` §3.2 states them — WS-089:
`warehouseId` → `ownerId` → `reasonCodeId` · `adjustmentType` · `status` **multiselect** ·
`requiresApproval` · `approvedBy` typeahead · `createdFrom`/`createdTo` **`date`** pair ·
`valueImpactMin`/`valueImpactMax` number pair. There is **no `daterange` filter type** — every
from→to is two keys and **both** go in the `COMMON_FILTER_CONFIGS` allowlist.

**Mobile.** ~~`mobile/src/screens/whStockAdjustment` — create and submit. **Approval is desk-only and
the mobile screen says so** rather than hiding the button. `mobile/src/screens/whBlockedMovement` —
the operator who was refused must be able to see why and raise the force request.~~
— **SUPERSEDED** by the no-mobile decision — *"warehouse has no mobile app"* (user scope
rule 2026-09-11, recorded in classic's `warehouse-base/INSTALL.md` §11 and asserted by
`warehouse-base/frontend/src/__tests__/warehouseMobileDecisionGate.test.ts`, which fails the build
if a `wh*` screen appears under `mobile/`). Nothing under `mobile/` is edited by any warehouse
task, so there is no counterpart to ship and nothing is owed here; the screen stays recorded as
the intended surface for whenever the decision is reversed, which means editing `INSTALL.md` first.
`none` for WS-096 (controller report) was already stated as a decision, and after the supersede
`none` reads for all three screens.

## Requirements closed
`FR-015` · `FR-028` · `FR-145` · `FR-146` · `FR-164`

## Scenarios closed
- `WH-SC-249` — the refusal creates a blocked-move row with the attempted payload, the operator and a resolution path
- `WH-SC-170` — the refusal's own setup: a device syncs a genuinely valid 14:10 issue of 5 `EA` from a pick face a 16:30 counter sale has since emptied. **Sufficiency is evaluated against the balance as it stands at posting, never as it stood at `occurred_at`** — `409` `INSUFFICIENT_STOCK` on `lines[0].quantity`, and `WH-SC-249` is what happens next (`X-041`, `X-023`)
- `WH-SC-283` — the counter and the job-issue screen both hit the insufficient-stock guard and both write a lost-sale row
- `WH-SC-122` — a transit loss posts as a reason-coded value-destroying movement (`FR-164`), shared with `P2-02`
- `WH-SC-282` — the write-off is excluded from demand history by the reason code's `affects_demand_history` (`FR-146`), shared with `P2-15`
- `WH-SC-108` — the count's automatic variance posting carries `FR-145`'s catalogue reason code, shared with `P2-04`

## Closes
`E-045` · `C-045` (`FR-015`) · `S-089` (`FR-028`) · `T-010` `E-033` `E-034` `T-090` (`FR-145`) ·
`E-066` (`FR-146`) · `F-052` (`FR-164`) · `Y-009` · `RJ-005` · `RA-008` `RF-002`


## Traps

- **`C-045` is the idea to copy and the code is not.** `accessories` already ships the good half of
  this: a global setting choosing *block* versus *issue and mark pending*, plus
  `accessory_insufficient_stock_logs` and a report
  (`accessories/…/AccessoryStockEnforcementSettingService.java:17,31`;
  `AccessoryIssuanceService.java:101`). Ship the same switch as `WAREHOUSE_ALLOW_NEGATIVE_STOCK`.
  **Do not import the code** — it is FK-bound to `accessory_products` (`V30130:34-36`).
- **`C-025` — a ledger port must never return quietly.** `InventoryStockAdjustmentService.java:56-60`
  silently no-ops when no stock-level row exists, and `AccessoryIssuanceService.java:277-279`
  swallows non-`BusinessException` failures and continues. Every refusal here is an exception with a
  stable code; every success returns a movement id the caller stores.
- **`C-021` — do not mutate a balance.** `StockReceiptService.java:373-411` moves the accessories
  balance and writes **no** transaction row at all. The adjustment document here writes **no**
  position row; it posts a movement through the port and the position follows (`D-4`, `L-4`).
- **`T-1` — never `cascade = CascadeType.ALL` on the line collection.**
  `accessories/…/entity/SalesOrder.java:161,166` is the shape to avoid; 105 occurrences repo-wide.
  A `getLines().clear()` on a posted adjustment would issue `DELETE`s that `I-2` rejects, so the
  code works on drafts and fails in production.
- **`T-2` — a JPA field initialiser beats the column `DEFAULT`.**
  `accessories/…/entity/StockLevel.java:58-64` writes `0` and the DB default is never observed.
  Decide `total_value_impact` and the quantity columns in one place.
- **`T-6` / `FR-395`** — every one of the three filter scopes goes into
  `platform/frontend/src/utils/filterUtils.ts` (`:346` onward, 213 scopes today) or the filter UI
  silently does nothing. The **statistics strip gets no cache name**:
  `CacheConfiguration.java:190-196` records that exact mistake with its issue numbers
  (`neetub1508/classic#790`, `#791`).
- **`T-8`** — `grid_preferences` needs **both** `default_columns` and `default_filters` populated as
  `'[…]'::jsonb`; `filter_definitions.default_visible = true` alone builds nothing
  (`V18:12`, `V229:50-51`).
- **`CM-4` / `T-12`** — the table is **`filter_definitions`**, created in `V229`. There is no
  `grid_filter_definitions`; that name fails at Flyway and crash-loops the backend. And every
  module's migrations are physically flattened into one directory at build time
  (`Dockerfile.backend:140-181`), so a duplicate version is a startup failure, not a merge conflict.
- **`attempted_payload` is `TEXT`, not `JSONB`.** `D-3`/§1.6: no JSONB on a new `wh_` business table.
  The exception is `grid_preferences`, which is a platform table.
- **Precision.** `variance_quantity` `DECIMAL(18,4)` · `unit_cost` `DECIMAL(19,6)` (**per-unit beats
  the word `value`**) · `variance_value` and `total_value_impact` `DECIMAL(19,4)`. `OD-7`.

## Acceptance
- [ ] An adjustment with no `reason_code_id` is refused by the service **before commit**, with a field-level error on `reason_code_id` — and there is no free-text reason field anywhere on the request DTO
- [ ] A one-unit variance under the value threshold posts on submit; a one-unit variance over the **value** threshold routes to approval even though the **quantity** threshold was not breached
- [ ] `requires_approval` is stored, not recomputed at read time
- [ ] `V510030` creates `wh_adjustment_approval_policies`; resolution is most-specific-first like `whb_gl_posting_rules`, the all-null row carries `warehouse.adjustment.default_threshold_value`, and WS-089 shows the resolved threshold, the row that supplied it and a **Test resolution** modal (`RA-008`)
- [ ] Posting writes **no** row in `whb_stock_positions` directly; the position moves only because the movement posted
- [ ] **OPEN against `P2-15` (warehouse-issues#106), not closable here.** This task is `affects_demand_history`'s first reader and stamps it onto the movement it posts; the row that proves *"`wh_demand_history` does not count it"* cannot be written while **`wh_demand_history` does not exist** — no table, no migration, only comments naming it. `P2-15` creates it and owns the consumption assertion, in `P0-03`'s split form (*"the log row and the report wait on P2-01"*). What is provable here — the flag is read at submit and travels to the movement — is proved by this task
- [ ] A reason code with `affects_demand_history = false` produces a movement that `wh_demand_history` does not count — *the assertion above; carried against `P2-15`*
- [ ] A rejected physical move produces a `wh_blocked_movements` row carrying the attempted payload, the rejection code, the actor and the device; forcing it requires a distinct permission, a reason and an approver
- [ ] Every `WARN`/`ALLOW` breach writes one `wh_insufficient_stock_log` row and appears on WS-096
- [ ] All three filter scopes exist in `COMMON_FILTER_CONFIGS`; **no `statistics.*` cache name is registered** for WS-089's strip
- [ ] ~~`mobile/src/screens/whStockAdjustment` creates and submits, and states that approval is desk-only~~ — **SUPERSEDED** by the no-mobile decision (*"warehouse has no mobile app"*, user scope rule 2026-09-11; see this task's **Mobile** section). There is no counterpart to ship and nothing is owed here
- [ ] The adjustment ladder has no state without an inbound transition and no non-terminal state without an outbound one; *Submit*, *Post* and *Cancel* are each refused to a user holding only `wh_stock_adjustments:edit` (`RJ-004`, `RJ-005`)
- [ ] **OPEN against `P2-02` (warehouse-issues#24), not closable here.** `RJ-002`'s transit exception needs a transfer with a per-transfer transit location to aim at, and `wh_transfer_orders` is `P2-02`'s schema (itself blocked behind `P1-17`), so no fixture in this task can reach the SITE-B/transit case. The site scope this task **does** enforce — a storekeeper is refused on any location outside their own sites — is proved here; the transit exception is carried against `P2-02`, which already holds the twin checkbox for the port's batch-replay path (`MPR-GRD-33` note, `MPR-OPEN-20`)
- [ ] A SITE-B storekeeper posts a transit-loss adjustment against a transfer's transit location at SITE-A, and is refused on any other SITE-A location (`RJ-002`, `P1-18`) — *the case above; carried against `P2-02`*
- [ ] An unresolved `wh_blocked_movements` row older than `warehouse.blocked_movement.alert_minutes` (WS-097 `ageMinutes`) raises **one** alert to the warehouse manager and clears on resolution, never resolving, posting or forcing anything, from a job declared in `WhbJobCatalogue` with a `@Scheduled` trigger and a `whb_job_runs` row per run, and the `DATED-OBLIGATION-REGISTER.md` row flips from *owed* to *deployed* in the same merge (`FR-165`, `FR-398`; re-homed from closed `P1-20` by the `P0-13` C9 fix, #104)
- [ ] The export of a filtered, sorted grid reproduces **that** filter and **that** sort - `getEntitiesForExport` consumes its filters map and a sort key, ratcheted by the module's `ExportServiceContractTest` (`P2-29` Rule 1, `FR-400`)
- [ ] **Scenarios** `WH-SC-249` · `WH-SC-283` · `WH-SC-122` · `WH-SC-282` · `WH-SC-108`
