TITLE: [Warehouse] P1-20 · App permissions, menus, grid configuration wave 1, and the shared-registry edits
LABELS: task,warehouse,phase-p1,warehouse
issue: 145
---
Part of __P1__ · Module **`warehouse` + `warehouse-base` + `platform`** · Migrations **V501050–V501069, V511000, V511001, V511010, V511020–V511059, V511200** · Screens **none of its own — it configures every P1 grid**

## Scope

### Adopted solution and configuration contract — 2026-09-11

Apply this task’s relevant behavior from [Global settings and resolved behaviour](https://github.com/neetub1508/warehouse-issues/blob/main/docs/GLOBAL-SETTINGS-DECISIONS.md), adopted by DECISIONS. This resolves earlier open-question/blocked-on wording for the decisions it names; external capability availability remains a testable gate. Preserve this task's existing module, migration and requirement ownership. P0-15/P1-20 own the shared settings implementation; other tasks consume it. Do not build unrelated settings controls or later-version capabilities here.

- `warehouse.accounting.mode`: default **STANDALONE**; allowed STANDALONE / INTEGRATED. Integrated requires tested contract; disable pauses new delivery, preserves accepted and pending envelopes for reconciliation.
- `warehouse.tax.mode`: default **MANUAL_EVIDENCE**; allowed MANUAL_EVIDENCE / PROVIDER / NONE. Provider profile required for PROVIDER. Manual mode cannot finalize a taxable invoice without an external document; stock-only receipt remains supported.
- `warehouse.cost.exchange_rate_source`: default **SOURCE_RATE**; allowed SOURCE_RATE / PROVIDER_RATE. Provider failure blocks valuation posting requiring a rate. Explicit authorized source rate with evidence is a separate selection, never silent fallback.
- `warehouse.labels.lpn_format`: default **INTERNAL_CODE128**; allowed INTERNAL_CODE128 / GS1_128. GS1 unavailable until allocator/parser readiness; profile includes valid prefix and counter range. Never reset or recycle issued identifiers.
- `warehouse.receiving.over_receipt_percent`: default **0**; allowed decimal 0–100 inclusive. Use PO line, PO header, item, warehouse, global in that order; NULL inherits, explicit zero blocks over-receipt; percentage, not fraction.
- `warehouse.expiry.near_expiry_days`: default **45**; allowed integer 0–3650. Item-site then item then global; zero means only today. Alert uses site business date, never changes expiry or FEFO eligibility.
- `warehouse.approval.pending_alert_hours`: default **24**; allowed integer 1–720. Escalate a pending approval after elapsed hours; never auto-approve. One alert per pending episode.
- `warehouse.tasks.blocked_alert_minutes`: default **15**; allowed integer 1–10080. Alert supervisor after elapsed blocked duration; retain task state and ownership.
- `warehouse.dropship.enabled`: default **false**; allowed boolean; v2 capability required. Disable blocks new drop-ship orders; linked in-flight confirmations, reversals and reconciliation remain available.
- `warehouse.regulated_profile`: default **NONE**; allowed NONE / tested profile identifier. A profile supplies validated licence, traceability, recall and retention rules. An incomplete profile cannot be enabled or advertised as supported.


> **Round-4 addition — `RJ-005` · `RH-002` · `RK-004` · `RJ-016`.** **(1) The P1 transition verbs**
> (`RJ-005`): `V511000` adds `wh_receiving_sessions:complete`/`:cancel`, `wh_goods_receipts:cancel`,
> `wh_quality_inspections:approve` (scrap-disposition approval, approver ≠ disposer, `FR-408`) and
> `wh_receipt_reversals:post`, each with its `→ :view` row in `V511001` — the ladders in `P1-13`,
> `P1-14` and `P1-16` name them. **(2) The app scope tier** (`RH-002`): for every app management
> resource `V511000` seeds `<resource>:view:all` and `<resource>:view:branch` beside `:view`, and
> `V511001` their dependency rows; ADMIN and AUDITOR get `:view:all`, Branch Admin gets `:view:branch`
> and **never** `:view:all`, the warehouse operational bundles get `:view:branch`. The pair is what
> `P1-18`'s wrapper of `branchScopeService.resolveStrict(...)` reads. **(3) Two `V511200` defaults**:
> `warehouse.replenishment.default_schedule` (nightly), read by `P2-15`'s scheduled run (`RK-004`), and
> `warehouse.count.default_tolerance_pct` / `warehouse.count.default_tolerance_value`, inherited by a
> count with no programme (`P2-04`, `RJ-016`).

- **`V501050`–`V501069`** — **base** grid configuration, **wave 2**: the P1 grids that live in
  `warehouse-base` (items, categories, UoMs, identifiers, packaging, conversions, lots, serials,
  LPNs, genealogy, counterparties, counterparty roles, warehouses, locations, location types,
  number series, numbers issued, import batches, channels, external refs, category ownership,
  external snapshots, item × site settings, supplier sources, supersessions).
  *(§2.9 divergence 1: `DATA-MODEL.md` `WHB-74` allocates `V501020`–`V501099` as one block; this plan
  sub-allocates it across four tasks. `P0-15` took `V501020`–`V501049`.)*
- **`V511000`** — `permissions` + **verb permissions** + the **`:view:all`/`:view:branch` tier pair**
  (`RH-002`) for the app resources; ADMIN and AUDITOR grants.
- **`V511001`** — `permission_dependencies`, **`INSERT` only**.
- **`V511010`** — the app menu tree, `menu_translations` **en/fr/hi**, `menu_permissions`, every
  insert **`WHERE NOT EXISTS`**-guarded.
- **`V511020`–`V511059`** — **app** grid configuration, one migration per grid: purchase orders,
  receiving sessions, goods receipts, receipt reversals, inspection plans, quality inspections,
  putaway rules, putaway tasks, dock doors.
- **`V511200`** — `admin_settings` seed, category `WAREHOUSE`, app-owned keys, including
  `warehouse.replenishment.default_schedule` (`RK-004`) and `warehouse.count.default_tolerance_pct` /
  `_value` (`RJ-016`).
- **Every filter scope in `COMMON_FILTER_CONFIGS`** and **every cache name in
  `CacheConfiguration.java`** (`FR-432`) — **and the expiry, count and reservation-expiry jobs
  registered with them.**
- **The native-query timestamp mapper that handles all four types a driver may return and logs a
  warning on a fifth** (`FR-438`).

## The two platform files, and the honest limit

`COMMON_FILTER_CONFIGS` is a TypeScript `const` at
`platform/frontend/src/utils/filterUtils.ts:346` with **213** scopes; `CacheConfiguration.java` has
**235** registered names. **Neither has a per-module extension point**, and **every warehouse grid
edits both** — in every module, including every adapter. That is why the ratchet can only ever be
*"zero commits to `warehouse-base`"*, **never** *"zero commits to `platform`"* (`D-10` corollary,
`FR-355`, `G-047`). **Plan the serialisation of these two files rather than discovering it in a merge
conflict.**

> **Round-3 fold — `RE-003` (`GAP-REGISTER-R3.md` §2.5, §4.5).** **The honest limit is three platform
> files, not two:** `filterUtils.ts`, `CacheConfiguration.java` **and
> `platform/frontend/src/app/dashboard/admin-settings/page.tsx`**, which gains a thirteenth tab — an
> `AdminSettingsWarehouseTab.tsx` beside `id: 'docOcrAi'` (`:145-152`), invisible in a deployment
> without the module — gated on `whb_settings:edit` (seeded in `V501000`) and checked **in addition to**
> the platform's `admin_settings:edit`. No other screen exists for the install keys `P0-15`'s `V501100`
> seeds. `MODULE-INTEGRATION.md` §12 records the third file (and `RB-005`'s fourth).

## The verb permissions this wave adds

`wh_purchase_orders:approve` · `wh_receipt_reversals:approve` ·
`wh_quality_inspections:disposition` · `wh_goods_receipts:post` ·
`wh_putaway_tasks:complete` · `wh_receiving_sessions:complete` · `wh_receiving_sessions:cancel` ·
`wh_goods_receipts:cancel` · `wh_quality_inspections:approve` · `wh_receipt_reversals:post` (the last
five from `RJ-005`) — each with its `→ :view` dependency row, and **no reverse row** (`FR-408`,
`P0-15`). `wh_goods_receipts:reverse` was struck on 2026-09-14: reversing a receipt is
`wh_receipt_reversals:create` → `:approve` → `:post` (`BUILD-SPEC-SCREENS.md` §10.2,
`docs/contracts/receipt-qc-putaway.contract.md` `RQP-OPEN-10`); `wh_putaway_tasks:complete` stays and now
has its §10.2 row (`RQP-OPEN-11`).

## Requirements closed
`FR-432` `FR-433` `FR-438`

## Scenarios closed
`WH-SC-247` `WH-SC-255`

## Closes
`C-042` `C-043` · `P-006` · `G-047` · `T-070` · `RE-003`

## Traps

- **`grid_filter_definitions` does not exist.** The table is **`filter_definitions`**
  (`platform/…/V229__Add_filter_definitions_table.sql:8`). Inserting into the wrong name **fails at
  Flyway and crash-loops the backend** — and the prior art names the wrong table in **six documents,
  fourteen times** (`P-006`).
- **`grid_preferences.default_columns` AND `default_filters`, both, as `'[…]'::jsonb`.**
  `default_columns` is `NOT NULL DEFAULT '[]'` from `V18:12`; `default_filters` was added at
  `V229:51` and is **nullable**, which is why it gets forgotten. `filter_definitions.default_visible
  = true` alone does **not** populate the strip (R1 §8 trap `T-8`). **Never patch these later with
  `REPLACE` on a text literal** — a known whitespace trap in this repo.
- **A required column not in `default_columns` bricks every grid-preference save**, silently, for
  that grid only. `is_required` on **at most one identity column per grid**, plus
  BUILD-SPEC-SCREENS §9.4's `DO $$ … RAISE EXCEPTION` assertion in the same migration.
- **`filter_definitions.position` has no uniqueness** and defaults to 0. Number from 1, contiguously,
  in reading order; when a later migration inserts into a shipped grid, **renumber the tail in the
  same migration** rather than reusing a number.
- **A filter key absent from `COMMON_FILTER_CONFIGS.{SCOPE}` is silently dropped** by
  `convertFiltersForApi()` and the filter appears to do nothing (`filterUtils.ts:346`, R1 §8 trap
  `T-6`). Grep the scope before adding a field.
- **`dateOnly` versus `date` is load-bearing.** `filterUtils.ts:49-56`: `date` shifts to UTC day
  boundaries for `TIMESTAMP` columns, *"which moves the 'from' bound to the previous calendar day for
  users east of UTC."* Every statutory, expiry, manufacture, count, order and posting date is a SQL
  `DATE` (`FR-327`) and is **`dateOnly`**; `occurred_at`, `recorded_at` and `created_at` are `date`.
- **There is no `daterange` type** (`filterUtils.ts:30`). Every from→to filter is **two keys**, both
  in the allowlist.
- **An unregistered cache name throws `IllegalArgumentException` on the FIRST call, not at startup**
  (`CacheConfiguration.java:375-377`). **And do not register a `statistics.*` name for a
  filter-aware strip** — `:190-196` records that exact mistake (issues `neetub1508/classic#790`,
  `#791`): the names cached nothing while reading as though they did (`FR-395`, `C-043`).
- **The API service must forward every filter key.** An enumerating `getForManagement` silently drops
  unknown keys, so a scope entry plus a controller parameter is still not enough.
- **`permission_dependencies` is a platform table — `INSERT` only, never `CREATE TABLE`** (`V248`,
  `C-017`), and the column is **`dependent_permission_id`**.
- **Menu inserts guarded `WHERE NOT EXISTS`, never `ON CONFLICT DO NOTHING`** (`FR-410`, R1 §8 trap
  `T-9`).
- **Hibernate native-query timestamps come back as `OffsetDateTime`, `Instant`,
  `java.sql.Timestamp` or `LocalDateTime`**, and returning `null` for an unhandled type is **silent
  data loss** — cells render blank though the DB has data. Copy
  `UserActivityLogQueryService.java:288-307`, which **logs a warning on an unknown type** so the
  runtime type is discoverable (`FR-438`, R1 §8 trap `T-4`).
- **The three jobs must be registered with the columns they read**, not after: reservation expiry
  (`whb_reservations.expires_at`, `P0-09`), lot expiry (`whb_lots.expiry_date`, `P1-07`) and cycle
  count (`whb_item_site_settings.count_frequency_class`, `P1-03`). A threshold column with no job is
  a defect **at merge** (`FR-165`), and each must appear on **WS-064 Job Runs** (`P0-13`).
- **This task's module cell is `app+platform` in the plan, but it owns `V501050`–`V501069` in the
  `warehouse-base` band.** That is §2.9 divergence 1 working as declared; the migrations are
  base-band files and must live in the `warehouse-base` module's migration directory. Recorded in
  `DEFECTS-FOUND.md`.

## Acceptance

- [ ] `CONFIG-CASE-01` in the adopted contract passes for this task's owned workflow only, including its refusal/retry behavior; reuse shared infrastructure tests and record evidence in the implementation PR.
- [ ] `CONFIG-CASE-02` in the adopted contract passes for this task's owned workflow only, including its refusal/retry behavior; reuse shared infrastructure tests and record evidence in the implementation PR.
- [ ] `CONFIG-CASE-03` in the adopted contract passes for this task's owned workflow only, including its refusal/retry behavior; reuse shared infrastructure tests and record evidence in the implementation PR.
- [ ] `CONFIG-CASE-07` in the adopted contract passes for this task's owned workflow only, including its refusal/retry behavior; reuse shared infrastructure tests and record evidence in the implementation PR.
- [ ] No setting rewrites posted history, bypasses permissions/period locks or silently enables an unavailable capability.
- [ ] Every P1 grid ships `grid_column_definitions`, **`filter_definitions`** (never
      `grid_filter_definitions`) and `grid_preferences` with **both** JSONB arrays
- [ ] The required-column assertion runs in every grid migration and fails when deliberately violated
- [ ] Filter positions are contiguous from 1 within each grid
- [ ] Every P1 filter scope is in `COMMON_FILTER_CONFIGS`, **and** the API service forwards every key
      — proved by a filter that visibly changes the result set on each grid (`WH-SC-247`)
- [ ] Every `dateOnly` filter is `dateOnly` and every `date` filter is `date`, checked against
      `FR-327`'s column list
- [ ] Every `dropdown.*` cache name is registered; **no `statistics.*` name is registered for any
      warehouse strip**
- [ ] App permissions and verb permissions are seeded with ADMIN and AUDITOR grants; AUDITOR holds
      **no** verb permission; dependency rows are `INSERT`ed with `dependent_permission_id`
- [ ] The five `RJ-005` verbs exist with `→ :view` rows, and each gates its ladder transition in
      `P1-13`, `P1-14` and `P1-16` (`RJ-005`)
- [ ] Every app management resource has `:view:all` and `:view:branch` with dependency rows; ADMIN
      and AUDITOR hold `:view:all`; Branch Admin holds `:view:branch` and no `:view:all` (`RH-002`)
- [ ] `V511200` seeds `warehouse.replenishment.default_schedule` and both
      `warehouse.count.default_tolerance_*` keys (`RK-004`, `RJ-016`)
- [ ] Menus are `WHERE NOT EXISTS`-guarded with `en`/`fr`/`hi` translations
- [ ] The native-query timestamp mapper handles all four types and **logs a warning on a fifth**
      (`WH-SC-255`)
- [ ] The reservation-expiry, lot-expiry and cycle-count jobs are registered and produce
      `whb_job_runs` rows
- [ ] The warehouse settings tab renders only when the module is installed, and saving requires both
      `whb_settings:edit` and `admin_settings:edit` (`RE-003`)
- [ ] **Scenarios** `WH-SC-247` · `WH-SC-255`
