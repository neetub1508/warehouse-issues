TITLE: [Warehouse] P2-09 · Discrete picking to a real staging location, and dispatch as the only inventory-relief event
LABELS: task,warehouse,phase-p2,warehouse
issue: 65
---
Part of __P2__ · Module **`warehouse`** · Migrations **V510041, V511064** · Screens **WS-102 Pick Tasks**

## Scope

> **Round-2 addition — `FR-449`, `Y-003`.** This task makes a pick move stock to **a real, countable
> staging location** — which creates a state the set had no exit from: an order **cancelled after the
> pick and before the ship confirm** leaves units in staging with their reservation gone, belonging to
> nothing, counted by nobody, and reported nowhere. **A cancellation after pick-to-staging must offer
> a de-stage path** — a put-back movement from staging to the source or to a putaway task — and where
> it cannot, it is **refused with `409 STAGED_STOCK`** naming the staging location and the quantity.
> Silently cancelling is the one option that is not available.

**`V510041` — WH-31 — `wh_pick_tasks`.** The **1:1 pick extension of `whb_tasks`** (`P0-10`,
`V500034`) — `D-3`'s worked example of a `wh_` table sitting over a `whb_` one, and the fix for
`DATA-MODEL.md` §3.4's defect **X-4**. It does **not** duplicate `whb_tasks`' lifecycle timestamps;
it adds the pick-specific columns and points at the task.

**The one sentence the whole team holds: dispatch is the inventory-relief event and it is the only
one** (`FR-189`). `RJ-003` adds its second half: ***for a transfer, the relief is into transit.***
*Every alternative reading produces a different set of bugs.* Write it into the service javadoc, the
screen help text and the API docs.

**Pick moves stock to a real, countable staging location** (`FR-188`): `−qty` at the pick face /
`+qty` at `STAGE-OUT-01`, a genuine `whb_locations` row with `counts_as_on_hand = true` — **not** a
virtual location and **not** off the books. *If stock is relieved at pick, picked-not-shipped goods
vanish from on-hand while physically present, and a count of the staging lane disagrees with the
position table.*

**The reservation is re-attached to the staged stock inside the pick's own writer transaction**
(`RJ-015`):
1. The row that held the pick face is released, with `released_at` set and reason `PICKED`. It is
   never deleted.
2. A new row is created at `STAGE-OUT-01` with the same holder quad and the same `HARD` type.

Done in two transactions, `I-6`'s `quantity_available >= 0` fires on the source position between them.
**The staged reservation is `HARD` and carries no expiry** (`RJ-007`). It ends only by the ship
confirm's consume, a cancel or an explicit release, so no job can release stock mid-pick or in
staging.

**Ship confirm relieves from staging to the virtual customer location and closes the reservation in
one transaction** — `−qty` at `STAGE-OUT-01` / `+qty` at `VIRT-CUSTOMER`, the reservation closes,
the demand line's `shipped_quantity` advances. **One transaction.**

**Transfers and vendor returns use the same pick and the same ship confirm** (`RJ-003`). Only the
relief target differs:
- **A `TRANSFER` demand order** posts `TRANSFER_DEPART`: `−qty` at `STAGE-OUT-01` / `+qty` at the
  transfer's own `IN_TRANSIT-<transfer number>` location. That location is a child of **this** site
  (`RJ-002`, `P2-02`). The transfer moves to `IN_TRANSIT` in the same transaction. The stock is still
  on the sender's books — in transit, not relieved to a customer.
- **A `VENDOR_RETURN` demand order** relieves to the site's `SUPPLIER` virtual location, and the
  supplier return moves to `DISPATCHED` (`P2-13`).

There is no transfer-side or supplier-return-side pick or dispatch anywhere.

**A short pick is a first-class outcome, not a silently reduced quantity** (`FR-185`). It:
1. records the shortfall against an **exception code**;
2. **releases the unmet reservation**;
3. offers **re-allocate / emergency replenish / short the line**;
4. under a per-site switch, **auto-creates a cycle-count task for the location** — *a short pick is
   the highest-quality signal of an inventory error a warehouse ever gets*.

> **Round-3 fold — `RA-006` · `RB-008` (`GAP-REGISTER-R3.md` §2.1, §2.2).**
> - **Emergency replenish is built in v1, not withdrawn** (`RA-006`). It creates a
>   **`wh_transfer_orders` row of type `BIN_TO_BIN`** from the resolved reserve location to the short
>   pick face. The row is pre-filled with the shortfall and carries the pick task's exception code as
>   its reference. That is one existing v1 object and one existing v1 screen (WS-090), with no new table.
> - `FR-255`'s replenishment task engine and WS-142 stay v1.1 and supersede it.
> - *Build* is the documented default. Striking *emergency replenish* from `FR-185`'s v1 offer instead
>   is a product call (`GAP-REGISTER-R3.md` §5.8), and whichever is chosen is written down.
> - **The per-site auto-count switch of step 4 has a home**: the `admin_settings` key
>   `warehouse.short_pick.auto_count`, seeded in `P0-15`'s `V501100` (`RA-006`).
> - **Every enumerated select on WS-090, WS-097 and WS-098 states its options** (`RB-008`):
>   - an `ageOverDays` select offers the one shared ageing list, 7 · 15 · 30 · 60 · 90 · 180 · 365 days;
>   - `rejectionCode` reads `FR-039`'s vocabulary — fetched, never re-enumerated.

**v1 is discrete picking only, and the deferral is stated rather than discovered** (`FR-187`,
`FR-186`): v1 ships **Release on the demand header** (`P2-08`); **batch pick is v1.1 (`P3-06`)**;
cluster, zone, pick-and-pass, pick-to-carton and put-wall are v2/v3. The `pick_method` column
carries `DISCRETE`/`BATCH`/`CLUSTER`/`ZONE` from v1 so the later methods are a new value, not a
re-key. `wh_pick_tasks.wave_id` is nullable in v1.

**WS-102** · `wh_pick_tasks` · `WAREHOUSE_PICK_TASK` · Service Vehicle reference.
Columns `taskNumber`, `orderNumber`, `waveNumber`, `pickMethod`, `itemCode`, `lotCode`,
`serialNumber`, `sourceLocationCode`, `requestedQuantity`, `pickedQuantity`, `status`,
`assignedToName`, `startedAt`, `completedAt`, `exceptionCode`.
Filters `warehouseId` → `zoneLocationId` → `status` **multiselect** · `assignedTo` typeahead ·
`pickMethod` select · `waveId` select · `orderNumber` text · **`shortPickOnly`** boolean.
Actions Assign · Complete · **Short pick** · Cancel.
**The pick task carries `owner_id` from v1** (`P0-10`), so cross-client waving is possible later
without a re-key.

**Mobile.** The v1.1 RF pick screen (**WS-232**, `P3`) is a **separate screen family**, not an
`EntityListScreen` instance (`FR-217`, `FR-220`). ~~In v1 `mobile/src/screens/whPickTask` offers the
list, assign and complete; short pick is available because the operator who was short is the one who
knows. State that explicitly.~~
— **SUPERSEDED** by the no-mobile decision — *"warehouse has no mobile app"* (user scope
rule 2026-09-11, recorded in classic's `warehouse-base/INSTALL.md` §11 and asserted by
`warehouse-base/frontend/src/__tests__/warehouseMobileDecisionGate.test.ts`, which fails the build
if a `wh*` screen appears under `mobile/`). Nothing under `mobile/` is edited by any warehouse
task, so there is no counterpart to ship and nothing is owed here; the screen stays recorded as
the intended surface for whenever the decision is reversed, which means editing `INSTALL.md` first.

## Requirements closed
`FR-185` · `FR-187` *(the v1 Release half; the wave object is `P3-06`)* · `FR-188` · `FR-189` · `FR-449`

## Scenarios closed
- `WH-SC-054` — the v1 exit-criterion pick: stock moves to `STAGE-OUT-01`, a **real countable location**; total on-hand is unchanged and a count of the lane finds 144
- `WH-SC-056` — ship confirm relieves staging to `VIRT-CUSTOMER`, closes the reservation and advances `shipped` **in one transaction**; on-hand falls exactly now and not a moment earlier
- `WH-SC-093` — a short pick records the shortfall against an exception code and releases the unmet reservation
- `WH-SC-099` — Release exists on the demand header in v1 and the wave deferral is stated
- `WH-SC-107` — each despatched serial is recorded on the despatch movement line and its custody updates
- `WH-SC-129` — customer delivery posts nothing, because despatch already relieved

## Closes
`T-045` `F-029` (`FR-185`) · `T-044` (`FR-187`) · `T-046` `P-038` (`FR-188`, `FR-189`) · `Y-003` ·
`RA-006` (`FR-185`) · `RB-008`

## Traps

- **`P-038` is the workflow spine and it is prior art worth adopting.** Pick Confirm
  (`Allocated → Picked`) → pack → shipment `READY_FOR_TRANSPORT` → carrier → e-invoice → e-way bill
  → gate pass → **Dispatch (`Packed → Shipped` — only here)**. R6's single most valuable line is
  *"Dispatch is the inventory-relief event and it is the only one"*, which is `T-046` expressed as a
  rule a team can hold.
- **The staging location is `counts_as_on_hand = true`.** If it is modelled as one of `FR-084`'s
  virtual locations, `WH-SC-054`'s count of the lane fails and the whole point is lost.
- **Ship confirm is one transaction, and the reservation close is inside it.** `T-046` is exactly
  the failure of doing the relief and the reservation close in two.
- **`wh_pick_tasks` is a 1:1 extension, not a copy.** §3.4 defect X-4 is the shape where the
  application table re-declares `assigned_at`/`started_at`/`completed_at` and the two drift.
  `whb_tasks` owns the lifecycle timestamps; `P2-21`'s KPIs read them from there.
- **A short pick that reduces the requested quantity is the defect.** `requested_quantity` is
  immutable on the task; `picked_quantity` and `exception_code` carry the truth.
- **The auto-created cycle-count task is behind a per-site switch and it creates a `wh_counts`
  document** (`P2-04`), not an ad-hoc adjustment.
- **`T-6`** — `WAREHOUSE_PICK_TASK` into `COMMON_FILTER_CONFIGS`, including `shortPickOnly`.
- **`PP-7`** — the pick-task grid is ledger-adjacent; enforce a bounded default scope in v1.
- **`T-16` — controllers must live in `ai.warehouse.controller…`** or
  `UserActivityTrackingAspect`'s pointcut (`:59`, `within(ai..controller..*)`) cannot see them and
  the pick never appears in the activity log.

## Acceptance
- [ ] After a pick, total on-hand is unchanged and a count of the staging lane agrees with `whb_stock_positions` to the unit
- [ ] Ship confirm's relief, reservation close and `shipped_quantity` advance are in **one** transaction — asserted by forcing a failure after the relief and observing all three roll back
- [ ] Nothing anywhere relieves inventory at pick; `grep` finds exactly one relief site
- [ ] A short pick writes an exception code, releases the unmet reservation, and leaves `requested_quantity` untouched
- [ ] With the site switch on, a short pick creates a `wh_counts` document for the location
- [ ] `pick_method` admits `BATCH`/`CLUSTER`/`ZONE` as values from v1 and `wave_id` is nullable, so `P3-06` adds rows and not columns
- [ ] `wh_pick_tasks` declares **no** duplicate of `whb_tasks`' lifecycle timestamps
- [ ] The demand header's **Release** action exists and the screen states that waving is v1.1
- [ ] Cancelling an order whose lines are in staging either de-stages them by a real movement or returns `409 STAGED_STOCK` naming the location and quantity
- [ ] After a de-stage the staging location's balance for that item is zero and the units are available (or on a putaway task) — asserted from the position table, not the document
- [ ] No code path exists that cancels an order and leaves a non-zero staging balance, asserted by test
- [ ] A pick releases the source-position reservation with reason `PICKED` and creates the staged one with the same holder quad **in the pick's writer transaction** — `I-6` never fires between them (`RJ-015`)
- [ ] The staged reservation is `HARD` with no expiry, and the reservation-expiry job leaves it untouched (`RJ-007`)
- [ ] Ship confirm on a `TRANSFER` demand order posts `TRANSFER_DEPART` into that transfer's `IN_TRANSIT-<number>` location **at this site** and moves the transfer to `IN_TRANSIT`; on a `VENDOR_RETURN` it relieves to `SUPPLIER` and moves the supplier return to `DISPATCHED` — `grep` finds no transfer-side or supplier-return-side dispatch (`RJ-003`)
- [ ] *Emergency replenish* on a short pick creates a `BIN_TO_BIN` `wh_transfer_orders` row from the resolved reserve location to the pick face, pre-filled with the shortfall and referencing the exception code, and it appears on WS-090 (`RA-006`)
- [ ] The auto-count on a short pick fires only where `warehouse.short_pick.auto_count` is on (`RA-006`)
- [ ] Every enumerated select on WS-090, WS-097 and WS-098 has a stated option list or a fetched source, and `rejectionCode` reads `FR-039`'s vocabulary (`RB-008`)
- [ ] The export of a filtered, sorted grid reproduces **that** filter and **that** sort - `getEntitiesForExport` consumes its filters map and a sort key, ratcheted by the module's `ExportServiceContractTest` (`P2-29` Rule 1, `FR-400`)
- [ ] **Scenarios** `WH-SC-054` · `WH-SC-056` · `WH-SC-093` · `WH-SC-099` · `WH-SC-107` · `WH-SC-129`
