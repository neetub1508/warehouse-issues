TITLE: [Warehouse] P1-16 · Receipt reversal as an action, not a data fix
LABELS: task,warehouse,phase-p1,warehouse
---
Part of __P1__ · Module **`warehouse`** · Migrations **V510018** · Screens **WS-078 Receipt Reversals**

## Scope

- **`V510018`** — `wh_receipt_reversals`, `wh_receipt_reversal_lines`.
- **Reversal is an action, not a data fix** (`FR-131`): it generates a **`REVERSAL` movement linked to
  the original**, **decrements the PO line's received quantity**, and **leaves both documents
  visible**. There is no delete, no edit and no "correct the GRN".
- **The reversal is blocked once the stock has moved on.** If the received units have been put away
  and picked, or their status changed, or they have left the site, the reversal is refused with a
  message naming what blocks it.

**`DATA-MODEL.md` §2.2 (`WH-09`) is the authority on both column sets and must be re-read immediately
before the migration is written.**

## Why this is a separate task from `P1-13`

Because the tempting implementation is an `UPDATE` on the GRN and a compensating write to the
position — and **both are forbidden**. `L-2` makes posted lines immutable in three layers, `L-3`
makes correction a reversal, and `FR-436` allows exactly one writer. A reversal screen that exists
before anyone needs one is what prevents a support engineer inventing the data fix.

## Screen — WS-078 · Receipt Reversals

`/warehouse/inbound/receipt-reversals` · **Service Vehicle** · `gridIdentifier`
`wh_receipt_reversals` · filter scope `WAREHOUSE_RECEIPT_REVERSAL` ·
`wh_receipt_reversals:view|create|export` **+ `:approve`** · statistics `—` (filter-aware, `FR-395`).

- **Columns:** `reversalNumber`, `grnNumber`, `reasonCodeName`, `requestedByName`, `approvedByName`,
  `approvedAt`, `status`, **`reversalMovementSequenceNo`**, audit quartet + names.
- **Filters:** `warehouseId` · `status` select · `reasonCodeId` select ·
  `requestedFrom`/`To` `date` pair.
- **Actions:** **Request** · **Approve** (`wh_receipt_reversals:approve`; **the approver may not be
  the requester**, `FR-408`) · **Post**.
- **Mobile:** `none` — an approval workflow performed at a desk with the GRN and the movement
  register open. **Stated as a decision** (`FR-218`).

The reversal number comes from `P1-09`'s gapless series, and the reversal movement carries **its own
idempotency key** and a **mandatory catalogue reason code** (`FR-035`, `P0-08`).

## Requirements closed
`FR-131`

## Scenarios closed
`WH-SC-081` `WH-SC-082`

## Closes
`P-027` · `T-020` · `E-022`

## Traps

- **There is no edit modal for a posted GRN, and there must not be one** (`P1-13`). The moment one
  exists, correction stops being reversal and `L-3` is dead in practice while still true in the
  contract.
- **The reversal is a movement, not a compensating position write.** It goes through the single
  writer service, mirrors the original lines, sets `reversal_of_movement_id` and marks the original
  reversed (`FR-005`, `FR-035`). `whb_stock_positions` is a cache and is updated as a consequence,
  never directly.
- **Reversing a reversal is refused** (`FR-035`). The `/reverse` endpoint enforces it; this screen
  must not offer a path around it.
- **The PO line's `received_quantity` must be decremented in the same transaction.** If it is not,
  the PO shows a receipt that no longer exists, and the reorder logic and the three-way match both
  read the wrong number.
- **Blocking conditions must be *named*, not summarised.** *"Cannot reverse: 12 EA of `OF-1120` lot
  `L-2609` have been picked on shipment `SH-0042`"* is actionable; *"stock has moved"* is not.
- **A reversal into a `CLOSED` period is refused, including this one** (`FR-020`, `L-8`). A soft-close
  override is the sanctioned path and it is recorded (`P0-07`).
- **The approver may not be the requester** (`FR-408`), and the dependency row is
  `(wh_receipt_reversals:approve → wh_receipt_reversals:view)` — **the reverse row must not exist**
  (`P0-15`).
- **`cascade = CascadeType.ALL` on `wh_receipt_reversals → _lines` will look natural and is the
  wrong mapping** for anything that fronts ledger rows — 105 occurrences of that trap repo-wide
  (R1 §8 trap `T-1`).
- **One grid × two platform files**; `reasonCodeId` fetches from `whb_reason_codes` and must not be
  re-closed as a TypeScript union (`OD-5`, `FR-380`).

## Acceptance
- [ ] A reversal produces a **`REVERSAL` movement linked to the original**, with mirrored lines and a
      mandatory reason code (`WH-SC-081`)
- [ ] The PO line's `received_quantity` is decremented in the same transaction
- [ ] **Both documents remain visible** — the GRN is not deleted, hidden or edited
- [ ] A reversal is **refused once the stock has moved on**, and the refusal **names the movement
      that blocks it** (`WH-SC-082`)
- [ ] Reversing a reversal is refused
- [ ] A reversal into a `CLOSED` period is refused; a `SOFT_CLOSED` period admits it only with the
      recorded override
- [ ] The approver may not be the requester, and no `:view → :approve` dependency row exists
- [ ] There is **no edit modal for a posted GRN** anywhere in the product, proved by grep
- [ ] **Scenarios** `WH-SC-081` · `WH-SC-082`
