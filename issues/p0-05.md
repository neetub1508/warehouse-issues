TITLE: [Warehouse] P0-05 · The remaining ledger-facing registries, and status change as a balanced movement
LABELS: task,warehouse,phase-p0,warehouse-base
issue: 49
---
Part of __P0__ · Module **`warehouse-base`** · Migrations **V500005, V500006, V500008, V500010** · Screens **WS-004 Stock Statuses**, **WS-008 Task Types**, **WS-010 Item Types**, **WS-012 Dispositions**, **WS-013 Attribute Keys**, **WS-014 Condition Codes**

## **These migrations precede `PNR-1` (`V500030`, `P0-02`) and must land before it**

`whb_stock_movement_lines.stock_status_code` is `NOT NULL` and FKs to `whb_stock_statuses(code)`;
`stock_status_code` is also a member of the nine-part position key. **`V500005` must run before
`V500030`.** `whb_condition_codes` and `whb_attribute_keys` are referenced by the line and by
`whb_movement_line_attributes` in the same migration. **`DATA-MODEL.md` §2.1 is the authority on the
exact column set of every table here and must be re-read immediately before the migration is
written** — a behaviour flag missing from a registry row is a behaviour the ledger can never express.

## Scope

> **Round-2 addition — `FR-448`, `Y-002`.** A status change against stock carrying an **open
> reservation** had no stated outcome and no error code anywhere in the set. It has one now, and it
> belongs here because this is the task that builds *status change as a balanced two-line movement at
> the same location*: the change is **refused with `409 RESERVED_STOCK`** naming the reservation,
> unless the caller passes an explicit release intent **and** holds `whb_reservations:release`, in
> which case the reservation is released first and the release is recorded. **`FR-160`'s nightly
> reservation-expiry job is itself a status change and never releases silently** — it takes the same
> path, records the same release, and shows on `WS-064` as a job run with a count.

> **Round-4 addition — `RL-001` · `RL-006` · `RL-007` · `RL-013`.** (1) **`V500005` also creates
> `whb_duty_statuses`** — registry **15**, the §1.8 shape, behaviour columns `is_duty_paid`,
> `is_allocatable_to_domestic_demand`, `requires_licence`, `commingle_group` (`RL-001`). **Base seeds
> `DOMESTIC` only**; `warehouse-india` seeds `BONDED`, `MOOWR`, `SEZ`, `FTWZ` and `EXPORT_UNDER_BOND`
> (`P4-07`, `V540140`). **`BONDED` leaves the stock-status seed** — one fact, one axis; a customs hold
> is a `wh_hold_types` row. It must precede `V500021` and `V500030`, which FK `duty_status` to it.
> Round 4 allocates it no screen. (2) **`V500010` creates `whb_code_lists` + `whb_code_list_values`**
> (`list_code`, `code`, `name`, `owning_module`, `is_system`, `sort_order`, `is_active`) — one generic
> open list for low-behaviour vocabularies, seedable by any module under `P0-04`'s guarded idiom; a
> referencing column carries a constant `<col>_list` column and a composite FK (`RL-006`). V1 seeds
> eleven lists: `DEMAND_TYPE`, `RETURN_TYPE`, `CHANNEL_KIND`, `ACTOR_TYPE`, `PRINT_TEMPLATE_KIND`,
> `PRINT_FORMAT`, `IDENTIFIER_TYPE`, `ATTRIBUTE_SUBJECT`, `ADDRESS_ROLE`, `CUSTODY_ROLE`,
> `BARCODE_FORMAT`. **The classification rule:** every enumerated column is `REGISTRY` (FK by code, no
> `CHECK`), `CODE-LIST` (composite FK, no `CHECK`) or `CLOSED-SYSTEM` (`OD-5` union and `CHECK`
> allowed — **except on the two partitioned ledger tables, which carry no `CHECK (… IN …)` at all**);
> `DATA-MODEL.md` §2.1.1 carries the table. (3) `whb_attribute_keys.applies_to` becomes the
> **`ATTRIBUTE_SUBJECT`** list — `ITEM`, `MOVEMENT_LINE`, `LOT`, `SERIAL`, `LPN`, `LOCATION`,
> `COUNTERPARTY`, `DOCUMENT`; no `EVENT` subject (`RL-007`). `V500010` also creates
> **`whb_entity_attribute_values`** (`entity_kind`, `entity_id`, `attribute_key_id`, `value_string`,
> `value_number`, `value_date`, `value_boolean`; `uk(entity_kind, entity_id, attribute_key_id)`) for
> the non-partitioned subjects. **Install-created keys on WS-013 (`owning_module = 'INSTALL'`) are the
> product's custom-field answer** (`RL-007`). (4) **`I-18` enumerates seventeen registries plus every
> `CODE-LIST` column** (`RL-001`, `RL-006`). (5) Every code-keyed FK is `ON UPDATE RESTRICT`
> (`RL-013`).

Six registries, one shape (`DATA-MODEL.md` §1.8), **no `CHECK (x IN (…))` on any of them** (`D-10`,
`I-18`):

- **`V500005`** — `whb_stock_statuses` with its **nine behaviour flags** (`is_on_hand`,
  `is_available_to_promise`, `is_allocatable`, `is_pickable`, `is_shippable`, `is_countable`,
  `is_owned_asset`, `requires_reason_to_enter`, `requires_reason_to_leave`) plus **`badge_variant`**;
  `whb_condition_codes` (`is_sellable`, `is_repairable`, `grade_rank`,
  `default_disposition_code`); and `whb_duty_statuses`, seeded `DOMESTIC` only — **no `BONDED` row in
  the stock-status seed** (`RL-001`).
- **`V500006`** — `whb_location_types` + seed, **including every virtual and transit type**, with
  `is_physical`, `is_stock_holding`, `is_virtual_counterparty`, `is_mobile`, `is_transit`,
  `allows_mixed_owner`, `requires_assigned_user`, `default_location_level`. `P1-05`'s `V500013`
  seeds the virtual **locations** themselves; this migration seeds the **types** they need.
- **`V500008`** — `whb_item_types` + seed, including `PACKAGING`, `RETURNABLE_EQUIPMENT`, `TYRE`,
  `FUEL` (the logistics-seam types of `FR-337`), with `is_stocked`, `is_serial_default`,
  `is_lot_default`, `is_returnable_equipment`, `is_asset_shaped`, `is_value_only`.
- **`V500010`** — `whb_task_types` (`is_directed`, `default_priority`, `interleavable`,
  `labour_standard_minutes`, `required_resource_type`), `whb_dispositions` (**including
  `return_type`** from the `RETURN_TYPE` code list, `movement_type_code`, `target_stock_status_code`,
  `requires_inspection`, `emits_credit_signal`), `whb_attribute_keys` (`value_type`, `applies_to`
  from the `ATTRIBUTE_SUBJECT` code list, `is_filterable`, `is_exportable`), plus `whb_code_lists`,
  `whb_code_list_values` and `whb_entity_attribute_values` (`RL-006`, `RL-007`).
- **Status change as a balanced two-line movement at the same location** (`FR-103`) with a mandatory
  reason code. Quarantine never requires a physical move, and the status change is still a ledger
  event with a counter-side. This is the behaviour half of the task; the writer service of `P0-03`
  executes it.

> **Round-3 fold — `RE-005` (`GAP-REGISTER-R3.md` §2.5).** **The seed value lists are
> `IRREVERSIBLE.md` §5's v1 seed column**, cited by row, reproduced here and corrected as the register
> requires. **Row 4** stock statuses — `AVAILABLE`, `QC_HOLD`, `DAMAGED`, `QUARANTINE`, `EXPIRED`,
> `BLOCKED`, `CORE_UNGRADED` (no `BONDED`, `RL-001`) **plus `PENDING_RESOLUTION`** (`is_on_hand = true`,
> `is_allocatable = false`, `is_shippable = false`, `requires_reason_to_leave = true`), which `P2-12` and
> `FR-028` consume, **and `RETURNED`** — `WH-SC-057` and `DATA-MODEL.md` §`wh_return_receipt_lines`
> both require a dedicated return status, *"never straight to `AVAILABLE`"*, so it is v1 seed and not
> a `P2` addition. **Nine, not eight**; `IRREVERSIBLE.md` §5 row 4 has been amended to match. **Row 5** location types — §4.5's list, **gaining `OPENING_BALANCE`,
> `COUNT_VARIANCE` and `CONSUMED`**, and `COST_OF_SALES` for the cost-of-sales virtual location `P1-05`
> seeds (`RF-003`). **Row 10** item types — `STOCK`, `NON_STOCK`, `SERVICE`, `CORE`, `CONSUMABLE`,
> `PACKAGING`, `RETURNABLE_EQUIPMENT`, `TYRE`, `FUEL`, with **`KIT` replaced by `KIT_STOCKED` and
> `KIT_PHANTOM`** and **`ASSET` added** (`FR-049`). **Row 8** task types — `PUTAWAY`, `PICK`, `REPLEN`,
> `COUNT`, `MOVE`, `LOAD`, `PACK`, `QC`, `VAS`. **Row 12** dispositions — `RESTOCK_SELLABLE`,
> `RESTOCK_UNSELLABLE`, `REFURB`, `REPACK`, `SCRAP`, `RTV`, `DONATE`, `HOLD_FOR_CLIENT`,
> `RETURN_TO_CLIENT`. **Row 13** attribute keys — temperature, cold-chain excursion, COA ref, channel
> order id, GS1 AI codes. A code that appears in a requirement, a scenario fixture or another task and not in the seed is a merge blocker — the rule `FR-165` applies to threshold columns.

## Screens — six Department-shape catalogues

| | Table | Filter scope | Triage filters |
|---|---|---|---|
| WS-004 Stock Statuses | `whb_stock_statuses` | `WAREHOUSE_STOCK_STATUS` | `is_allocatable` · `is_on_hand` |
| WS-008 Task Types | `whb_task_types` | `WAREHOUSE_TASK_TYPE` | `is_directed` · `required_resource_type` select |
| WS-010 Item Types | `whb_item_types` | `WAREHOUSE_ITEM_TYPE` | `is_stocked` |
| WS-012 Dispositions | `whb_dispositions` | `WAREHOUSE_DISPOSITION` | `requires_inspection` |
| WS-013 Attribute Keys | `whb_attribute_keys` | `WAREHOUSE_ATTRIBUTE_KEY` | `value_type` select · `applies_to` select |
| WS-014 Condition Codes | `whb_condition_codes` | `WAREHOUSE_CONDITION_CODE` | `is_sellable` |

`gridIdentifier` = the table name. Permission `<table>:view|create|edit|delete|export`. Caches
`dropdown.<entityCamelCase>`; statistics **`—` (filter-aware, `FR-395`)**. `code` immutable after
create. **Mobile: `none` for all six** — administered from a desk.

> **The `OD-5` fetch obligation is NOT carried "into every mobile screen".** That sentence stood here
> until the **2026-09-11 no-mobile decision**: warehouse ships no mobile screens, so there is no
> mobile screen for the obligation to reach and the line described work nobody could do or check.
> The obligation itself is unchanged and lives where it is enforceable — the **web** screens fetch
> every vocabulary from its catalogue endpoint and compile none of it in. If a warehouse mobile
> surface is ever built, the obligation travels with it then.

> **BUILD-SPEC-SCREENS §1 marks WS-010, WS-013 and WS-014 `v1 · P1` and WS-012 `v1 · P2`.**
> `IMPLEMENTATION-PLAN.md` §2.1 assigns all six to this task, and §1.1's fifteen-screen P0 list names
> only WS-004 and WS-008. **Followed the plan's §2 Screens column**, because the DDL is in this
> task's migrations and a registry table with no maintenance screen is a vocabulary only a migration
> can extend. Recorded in `DEFECTS-FOUND.md`.

## Requirements closed
`FR-102` `FR-103` `FR-270` `FR-274` · `FR-448`

## Scenarios closed
`WH-SC-138` `WH-SC-147` `WH-SC-202`

**`WH-SC-200` is NOT closed here and has been withdrawn.** Its three returns are a customer return,
an RTO and a unit from a **recalled** lot, and the third has no data behind it anywhere: there is no
recall register, no lot recall flag and nothing a disposition could consult. Seeding the
`RETURN_TYPE` code list — which is what this task actually did — closes the first third and is not
the scenario. **`P5-14` owns it** (`wh_recalls`, `wh_recall_lines`, `V510211`), and the obligation is
recorded there.

## Closes
`C-028` `C-029` · `P-007` · `T-003` · `Y-002` · `RL-006` `RL-007` · `RE-005`

## Traps

- **`badge_variant` is a column on the registry row, not a frontend map.** `FR-381`: a status badge's
  variant comes from the row and i18n **falls back to the registry row's `name`** when a translation
  key misses. A hard-coded `statusVariantMap` in TypeScript re-closes the vocabulary the backend
  opened — the same defect `OD-5` names.
- **The nine flags are nine because collapsing them loses information.** `is_allocatable` and
  `is_pickable` are different questions (allocatable-but-not-pickable is a hold at a blocked
  location); `is_on_hand` and `is_available_to_promise` are different questions (in-transit counts as
  on-hand and does not promise). The prior art's `location_type`/`zone_type` `CHECK`s were dropped
  and recreated with completely different value sets 36 versions after creation — the flags are what
  make a new status row usable without code.
- **`whb_dispositions.return_type` lands here, in v1, even though full reverse logistics is v2.**
  `A-1` moved **basic** returns into v1/P2, and the disposition row is what the return receipt writes
  against (`FR-270`, `FR-274`).
- **The warehouse emits the disposition and never decides a refund** (`FR-274`). There is no refund
  screen, no refund amount and no payment path in any warehouse module. Do not add one to this
  registry.
- **`whb_attribute_keys` is what makes `FR-026` enforceable.** An unregistered attribute key is a
  column nobody can filter or export; `whb_movement_line_attributes` accepts registered keys only.
  This is the alternative to the JSONB the prior art put on eleven business tables (`P-007`,
  `WMS_DATABASE_DESIGN.md:99` made it a convention).
- **Six grids × two platform files.** Six `COMMON_FILTER_CONFIGS` scopes in `filterUtils.ts` and six
  `dropdown.*` names in `CacheConfiguration.java`. An unregistered cache name throws
  `IllegalArgumentException` on the **first call**, not at startup
  (`CacheConfiguration.java:375-377`).

## Acceptance
- [ ] A quarantine hold is a **two-line movement at the same location** with a mandatory reason code
      and no physical move (`WH-SC-138`)
- [ ] A **new** stock status added as a catalogue row renders with the badge variant from its own row
      and, with no i18n key, falls back to the row's `name` — with no frontend change (`WH-SC-147`)
- [ ] Adding a `CHECK (… IN (…))` to any of the six columns fails `WarehouseBaseCouplingTest`
- [ ] `whb_dispositions` carries `movement_type_code` and `target_stock_status_code` on every seeded
      row, and the warehouse offers **no** refund path anywhere (`WH-SC-202`)
      — **the columns are DATA here and are read by nothing.** Disposition-driven status change is
      `P2-12`'s (Returns v1): it plans the return's second movement from the disposition row rather
      than from a literal. Recorded on `issues/p2-12.md`. This task does not close that half.
- [ ] `V500005`, `V500006`, `V500008` and `V500010` all run **before** `V500030`, proved by the
      migration ordering test
- [ ] Six grids, six filter scopes, six cache names, `en`/`fr`/`hi` on every label
- [ ] A status change against stock with an open reservation returns `409 RESERVED_STOCK` naming the reservation
- [ ] The same change with an explicit release intent and `whb_reservations:release` succeeds, releases the reservation first, and records the release
- [ ] The same change with the release intent but **without** the permission returns `403`, and the reservation is untouched
- [ ] `FR-160`'s expiry path releases through `StockStatusChangePlanner.planExpiry()`, records each
      release, and no silent release exists in the diff — asserted by grep
      — **but there is no scheduler.** No `@Scheduled` bean, cron row or consumer calls `planExpiry()`
      anywhere in the product, so nothing runs nightly and nothing reports a count on `WS-064`. This
      task ships the DECISION; the trigger and its `whb_job_runs` row belong to **`P0-13`**'s job
      register (`P0-09` column · `P1-20` registration). Recorded on `issues/p0-13.md`.
- [ ] `whb_movement_types` carries `default_stock_status_code`, `allowed_from_status_codes` and
      `allowed_to_status_codes` (`FR-003`, added to `V500003` in place), surfaced on `WS-001`'s form,
      grid and export, and `StockStatusChangePlanner.validateStatuses()` refuses a pair outside
      either list with **`422 STATUS_TRANSITION_NOT_ALLOWED`** (`MPR-GRD-18`). An absent or empty
      list means **any** — the open default every v1 row carries — and a unit test covers a refused
      pair, an allowed pair and the empty-means-any case
- [ ] The stock status's `requires_reason_to_enter` / `requires_reason_to_leave` flags are **read by
      the planner**, not only by the mapper and the export: entering or leaving a status that
      declares the reason load-bearing requires a note (`MPR-GRD-18`)
- [ ] `whb_reservations:release` is **seeded** (`V500005`) and granted to `ADMIN` and `ADMIN_GROUP`
      and not to `AUDITOR`. Without the row the `FR-448` release path is a permanent `403` for every
      user. Populating `StockStatusChangeCommand.callerAuthorities` from the authenticated principal
      — never from caller-supplied data — is `P0-03`'s writer service and `P0-09`'s release path;
      recorded on both
- [ ] `RETURNED` is in the `V500005` seed and `IRREVERSIBLE.md` §5 row 4 names **nine** codes, not
      eight (`WH-SC-057`, `DATA-MODEL.md` §`wh_return_receipt_lines`)
- [ ] `PRINT_TEMPLATE_KIND` carries **eleven** values and no `SHELF_LABEL`
      (`BUILD-SPEC-SCREENS.md`:2080-2084, `DATA-MODEL.md`:542,1337) — `LOCATION_LABEL` covers the
      subject, and the list is open for an install that genuinely differs
- [ ] `whb_duty_statuses` exists in `V500005` with `DOMESTIC` as its only base seed, and no `BONDED`
      stock-status row exists (`RL-001`)
- [ ] `whb_code_lists` carries the eleven v1 lists; another module adds a `DEMAND_TYPE` value by seed
      with zero commits to `warehouse-base`, and a colliding code fails under the guarded idiom
      (`RL-006`)
- [ ] `whb_entity_attribute_values` exists with its four typed value columns and
      `uk(entity_kind, entity_id, attribute_key_id)` (`RL-007`)
      — **DDL only. This task ships no entity, repository or service for it, so nothing can write a
      row and the storing half of the box is UNMET here.** `P1-13` is the first writer (`LOT`-kind
      attributes on the GRN line) and owns the persistence layer. When it is built, a second value
      for one `(entity_kind, entity_id, attribute_key_id)` must return a **field-level error naming
      the key**, not the raw unique-constraint violation — that reaches the browser as an opaque
      `500` on a GRN post. Recorded on `issues/p1-13.md`.
- [ ] `WarehouseBaseCouplingTest` enumerates seventeen registries plus every `CODE-LIST` column
      (`I-18`, `RL-001`, `RL-006`), and no code-keyed FK is `ON UPDATE CASCADE` (`RL-013`)
- [ ] The `V500005`, `V500006`, `V500008` and `V500010` seeds equal `IRREVERSIBLE.md` §5 rows 4, 5, 10,
      8, 12 and 13 as corrected above — `PENDING_RESOLUTION`, `KIT_STOCKED`, `KIT_PHANTOM` and `ASSET`
      present, no bare `KIT`, and `OPENING_BALANCE`, `COUNT_VARIANCE`, `CONSUMED` and `COST_OF_SALES`
      location types present (`RE-005`)
- [ ] **Scenarios** `WH-SC-138` · `WH-SC-147` · `WH-SC-202` (`WH-SC-200` withdrawn — see *Scenarios closed*)
