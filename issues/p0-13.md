TITLE: [Warehouse] P0-13 · The ledger is the audit trail — hash-chained events, job runs, and the dated-obligation register
LABELS: task,warehouse,phase-p0,warehouse-base
---
Part of __P0__ · Module **`warehouse-base`** · Migrations **V500043** · Screens **WS-063 Warehouse Audit Events**, **WS-064 Job Runs**

## Why warehouse does not use the platform activity log

The platform's activity log is **asynchronous, out of transaction, and swallows its own exceptions**
(`FR-427`). For a stock ledger that is not an audit trail — it is a best-effort log that is silently
incomplete precisely when something went wrong. Warehouse owns
`whb_audit_events` + `whb_audit_event_changes` with an **append-only hash chain**, written in the same
transaction as the fact it records.

## Scope

- **`V500043`** — `whb_audit_events`, `whb_audit_event_changes`, `whb_job_runs`.
- **The append-only hash chain**: `sequence_no`, `prev_hash`, `payload_hash`, a `BEFORE UPDATE OR
  DELETE` reject trigger, a chain-head row taken with `INSERT … ON CONFLICT DO UPDATE … RETURNING`,
  the "sequence must be head + 1" rule, the hash recomputed **in SQL** and cross-checked against the
  Java writer, and a verifier that names the break. The template exists and is recent:
  `accounting-base/V600111` throughout — `:38-44`, `:364-386`, `:660-690` (`C-038`).
- **`on_behalf_of_actor_id` from the first migration** (`FR-409`). Support impersonation ships in
  v1.1 and the **feature is platform's** (`PP-4`); the **column is warehouse's and it is v1**.
  Without it, **year one — the year the first audit covers — cannot answer "was this posted by the
  user or by support acting as them", forever.**
- **`whb_job_runs`** — one row per scheduled-job execution: `job_code`, `started_at`, `finished_at`,
  `status`, `records_read`, `records_written`, `error_detail`, `duration_seconds`.
- **The dated-obligation register** (`FR-165`): **every threshold column in the product is listed
  with the scheduled job that reads it, and a column with no job is a merge-blocking defect.** The
  register is an artefact in the repository, and **WS-064 is its enforcement surface** — a job with
  no run record cannot be proved to have run.

## The v1 dated obligations, named

Each ships **with** its job, in the merge that creates the column:

| Threshold column | Job | Owning task |
|---|---|---|
| `whb_reservations.expires_at` | reservation-expiry | `P0-09` column · `P1-20` registration · `P2-07` notification |
| `whb_stock_position_snapshots` daily run | position snapshot | `P0-03` |
| position drift | `L-4` rebuild + drift alert | `P0-03` |
| `whb_outbox_subscriptions.max_attempts`, `.backoff_seconds` | outbox publisher | `P0-11` |
| `admin_settings warehouse.port.rejected_queue_threshold` | port rejected-queue alert | `P0-08` |
| `whb_stock_movements` monthly partitions | partition creation | `P0-02` |
| `whb_lots.expiry_date` / `best_before_date` / `use_by_date` | expiry state change + notify at `expiry − n` | `P1-07` columns · `P2-05` job |
| `whb_item_site_settings.count_frequency_class` | cycle-count scheduling | `P1-03` column · `P2-04` job |

**A dated obligation with no actor is a defect at the moment it is merged, not when it is noticed.**

## Screens

| | WS-063 · Warehouse Audit Events | WS-064 · Job Runs |
|---|---|---|
| Route | `/warehouse/platform/audit-events` | `/warehouse/platform/job-runs` |
| Reference | **Customer** | **Customer** |
| `gridIdentifier` | `whb_audit_events` | `whb_job_runs` |
| Filter scope | `WAREHOUSE_AUDIT_EVENT` | `WAREHOUSE_JOB_RUN` |
| Permission | `whb_audit_events:view|export` | `whb_job_runs:view|export` + **`:trigger`** |
| Columns | `sequenceNo`, `entityType`, `entityId`, `action`, `actorUserName`, **`onBehalfOfActorName`**, `occurredAt`, `summary`, `prevHash`, `payloadHash` | `jobCode`, `startedAt`, `finishedAt`, `status`, `recordsRead`, `recordsWritten`, `errorDetail`, `durationSeconds` |
| Filters | `entityType` → `action` · `actorUserId` typeahead · `occurredFrom`/`To` `date` pair · `entityId` text | `jobCode` select · `status` select · `startedFrom`/`To` `date` pair · **`failedOnly` boolean** |
| Actions | View → **expansion row** of `whb_audit_event_changes` (field, old, new) | View · **Run now** (`whb_job_runs:trigger`) |
| Audit columns | **none** — append-only, hash-chained, ledger-style; **the actor *is* the content** | **none** |
| Mobile | `none` — an audit trail is a controller and support surface (`FR-218`) | `none` |

`whb_audit_event_changes` has **no grid of its own**; it is the expansion row on WS-063.

> **BUILD-SPEC-SCREENS §1 marks both `v1 · P1`;** `IMPLEMENTATION-PLAN.md` §2.1 assigns them here and
> §1.1's fifteen-screen list omits them. **Followed the plan** — `V500043` is this task's migration
> and every P0 task after `P0-02` writes audit rows. Recorded in `DEFECTS-FOUND.md`.

## Requirements closed
`FR-165` `FR-409` `FR-427`

`FR-409` is multi-phase (`P0·P3`): this task owns the **column**; the impersonation **feature** is
platform's (`PP-4`) and the consuming UI is `P3` (plan §8.2).

## Scenarios closed
`WH-SC-299`

## Closes
`C-038` `C-048` · `P-023` · `T-017`

*Added 2026-09-02 (`GAP-REGISTER.md` §5.2). These were `COVERED-uncited` **BLOCKER**s — owned only through an FRD row's `Closes` column, so a builder reading this file alone never met them:*
`S-092`

## Traps

- **`on_behalf_of_actor_id` must be in the *first* audit migration.** `whb_audit_events` is
  append-only and hash-chained, so a column added later is `NULL` on every existing row and
  **impersonated actions are indistinguishable from the customer's own, forever** (`FR-409`,
  `PP-4`, and the accounting precedent at `acc_audit_events.on_behalf_of_actor_id`). The feature not
  existing yet is **not** a reason to omit the column — it is the reason to add it.
- **`CREATE CONSTRAINT TRIGGER` has exactly one precedent in this repo**
  (`accounting-base/V600111:812-815`), which also records the two PostgreSQL rules: a constraint
  trigger must be `AFTER` and must be `FOR EACH ROW` (`C-037`). The plain append-only reject trigger
  here needs no deferral, but the hash-chain head lock does need the
  `INSERT … ON CONFLICT DO UPDATE … RETURNING` idiom of `:673-690`, and its cost is stated in that
  file: *"from its first audit write to COMMIT, a mutating transaction holds this row's lock, so
  mutating transactions for ONE COMPANY serialise."* Decide the chain scope deliberately.
- **Do not join or redefine `all_activity_history`.** It is **dealer-owned** (`dealer/V20412:354`,
  `dealer/V20885:85-177`) and `accessories` explicitly opted out (`accessories/V30379:7-9`).
  Warehouse owns its **own** view — `whb_activity_history`, `V500054`, built in `P1-11` (`C-048`,
  `FR-428`).
- **`PlatformLogger.audit` is not this.** The audit-log convention in CLAUDE.md writes to the
  platform log; `FR-427` requires an in-transaction row. Do both; do not substitute one for the
  other.
- **A `whb_job_runs` row must be written even when the job fails**, with `error_detail` — a job that
  crashes and writes nothing is indistinguishable from a job that was never scheduled, which is the
  exact failure `FR-165` exists to catch.
- **`failedOnly` defaults true is wrong here** — a supervisor opening Job Runs wants the last run of
  every job, not only the failures. Default it false and make `status` a select. (Contrast WS-043,
  where `unresolvedOnly` **does** default true.)
- **Both grids need `default_columns` AND `default_filters`**, two `COMMON_FILTER_CONFIGS` scopes,
  and **no `statistics.*` cache name** — the strips are filter-aware (`FR-395`,
  `CacheConfiguration.java:190-196`, issues `neetub1508/classic#790`, `#791`).
- **Hash columns are `CHAR(64)` lower-case hex.** Mixing case between the SQL recomputation and the
  Java writer breaks the cross-check and looks like tampering.

## Acceptance
- [ ] `UPDATE` and `DELETE` on `whb_audit_events` are refused by the database to every actor
      including `ADMIN`
- [ ] The chain verifier **names the break** between two sequence numbers when a row is removed by
      direct SQL (`WH-SC-299`)
- [ ] `on_behalf_of_actor_id` exists in `V500043` and is written by at least one path, even though
      the impersonation feature is v1.1
- [ ] Every mutation to a warehouse entity writes an audit event **in the same transaction**, with
      its field-level changes in `whb_audit_event_changes`
- [ ] The **dated-obligation register** exists as an artefact, lists every threshold column in P0 and
      P1 with its job, and a CI check fails the build when a threshold column has no entry
- [ ] Every scheduled job writes a `whb_job_runs` row on success **and on failure**, and appears on
      WS-064
- [ ] **Run now** triggers a job and produces a run record with `recordsRead`/`recordsWritten`
- [ ] **Scenarios** `WH-SC-299`
