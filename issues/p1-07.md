TITLE: [Warehouse] P1-07 · Lots, serials and LPNs as entities, and genealogy recorded at the moment of transformation
LABELS: task,warehouse,phase-p1,warehouse-base
---
Part of __P1__ · Module **`warehouse-base`** · Migrations **V500018, V500035** · Screens **WS-036 Lots**, **WS-037 Serials**, **WS-038 LPNs**, **WS-039 Genealogy Explorer**

## ★ **`V500018` must land before `PNR-1` (`V500030`, `P0-02`)**

`whb_stock_movement_lines.lot_id`, `.serial_id` and `.lpn_id` all FK into these tables
(`IRREVERSIBLE.md` §3.4, `IRR-13`, `IRR-14`, `IRR-15`).

**`V500018` is also `PNR-4` for the serial key.** `uk(owner_id, item_id, serial_number)` — and the
argument is sharper than the item's: **the rows a global unique would have *rejected* were never
recorded, so there is nothing to migrate them from** (`DATA-MODEL.md` `WHB-18`,
`IRREVERSIBLE.md` §3.2). **`DATA-MODEL.md` §2.1 is the authority on all four column sets and must be
re-read immediately before the migrations are written**; `whb_lots` carries `IRR-53`, `whb_lpns`
carries `IRR-15`'s `received_at`.

## Scope

> **Round-2 addition — `Y-006`.** **LPN split, merge and loss are three sentences and one seed,
> and the set has none of them.** A **split** is a movement whose lines move quantity from one LPN
> to a newly created LPN at the same location, conserving under `L-1`, permitted only while the
> source LPN is `OPEN`. A **merge** is the same in reverse, and sets `is_mixed_item`,
> `is_mixed_lot` and `is_mixed_owner` from the resulting contents. A **lost** LPN is a
> reason-coded write-off against the shrinkage virtual location that closes the LPN. Two
> `INTERNAL` movement types join `V500003`'s seed. The **execution** half — performing the split
> or merge as one movement with stored expanded lines — belongs to `P3-15`, which already owns
> that shape.

- **`V500018`** — `whb_lots`, `whb_serials`, `whb_lpns`.
- **`V500035`** — `whb_transformations`, `whb_transformation_inputs`, `whb_transformation_outputs`.
- **A lot is an entity, keyed `uk(owner_id, item_id, lot_code)`, with codes normalised on write**
  (`FR-094`). *On a `VARCHAR` you cannot hold a lot across every location at once.*
- **Best-before and use-by are separate columns with different despatch rules** (`FR-095`) — never one
  "expiry". Plus `manufacture_date`, `expiry_date`, `retest_date`, `receipt_date`.
- **A lot has its own status, independent of item and location** (`FR-096`), so **a whole lot is held
  everywhere at once without moving anything**.
- **A serial is an entity** with current location, status, owner, LPN and lot, keyed
  `uk(owner_id, item_id, serial_number)` — **never globally unique** (`FR-097`), because two owners
  legitimately hold the same manufacturer serial.
- **Serial control and lot control are *modes* on the item, not a mode string read in isolation**
  (`FR-098`): serial ∈ `NONE`/`RECEIPT`/`SHIP`/`FULL`, lot ∈ `NONE`/`OPTIONAL`/`REQUIRED`, expiry a
  policy.
- **The LPN is a ledger object** (`FR-100`): code, **`sscc`**, type, **`parent_lpn_id`**, current
  location, status, mixed-item/lot/owner flags, gross weight, dimensions, and **`received_at`**.
- **Duplicate serial / LPN / lot detection at receipt** (`FR-106`), with a **site-level duplicate
  warning where the scope permits the duplicate**.
- **Genealogy recorded at the moment of transformation** (`FR-105`) — a kit, repack, decant, split or
  merge writes its inputs and outputs — **answerable in both directions** and **never reconstructed**.
- **MRP, net content, country of origin and pack month/year live on the lot**, with item defaults
  (`FR-320`, `P1-03`). **This is where `OD-10`'s recommended answer lands.**

## `whb_lpns.received_at` is `PNR-3`, and it is the anchor for money

★ **3PL anniversary storage billing is defined over `received_at` and cannot be computed for a past
the ledger never recorded** (`IRREVERSIBLE.md` §4.6, `IRR-15`). The feature is v2; the column is v1.

## Screens

| | WS-036 · Lots | WS-037 · Serials | WS-038 · LPNs | WS-039 · Genealogy Explorer |
|---|---|---|---|---|
| Route | `/warehouse/masters/lots` | `/warehouse/masters/serials` | `/warehouse/masters/lpns` | `/warehouse/ledger/genealogy` |
| Reference | **Customer** | **Customer** | **Customer** | grid + two-pane detail |
| `gridIdentifier` | `whb_lots` | `whb_serials` | `whb_lpns` | `whb_transformations` |
| Filter scope | `WAREHOUSE_LOT` | `WAREHOUSE_SERIAL` | `WAREHOUSE_LPN` | `WAREHOUSE_TRANSFORMATION` |
| Caches | `dropdown.whbLot` | — | — | — |
| Key columns | `lotCode`, `itemCode`, `ownerName`, `supplierLot`, `counterpartyName`, `manufactureDate`, **`expiryDate`**, **`bestBeforeDate`**, **`useByDate`**, `retestDate`, `receiptDate`, `countryOfOrigin`, **`mrp`**, `netContent` + `netContentUomCode`, `packMonthYear`, `parentLotCode`, `lotStatus`, `onHandTotal` | `serialNumber`, `secondarySerial`, `itemCode`, `ownerName`, `lotCode`, `currentLocationCode`, `currentStatusCode`, `currentLpnCode`, `soldToCounterpartyName`, `warrantyStartDate`/`warrantyEndDate`, `lastMovementAt`, `linkedCoreSerial` | `code`, **`sscc`**, `lpnType`, `parentLpnCode`, `ownerName`, `currentLocationCode`, `status`, **`receivedAt`**, `isMixedItem`/`isMixedLot`/`isMixedOwner`, `grossWeightKg`, dims, `contentLineCount` | `transformationType`, `occurredAt`, `warehouseName`, `performedByName`, `inputCount`, `outputCount`, `movementId` |
| Filters | `itemId` typeahead → `ownerId` → `warehouseId` · `lotCode`/`supplierLot` text · **`expiryFrom`/`expiryTo` `dateOnly` pair** · `manufactureFrom`/`To` `dateOnly` pair · `lotStatus` · `countryOfOrigin` · **`expiringWithinDays` select (30/60/90)** | `itemId` → `ownerId` → `warehouseId` → `currentLocationId` · `serialNumber` text · `currentStatusCode` · `soldToCounterpartyId` typeahead · `warrantyEndFrom`/`To` `dateOnly` pair · `inWarranty` boolean (backend-computed) | `warehouseId` → `currentLocationId` · `ownerId` · `code`/`sscc` text · `status` · `lpnType` · `receivedFrom`/`receivedTo` (`date` pair) · `isMixedOwner` | by type, site, date, and by input or output item |
| Actions | View · **Hold lot** / **Release lot** (own modals, mandatory reason) · **Split / Merge** (posts a `whb_transformations` row) · Print lot label · Trace | View (the three post-sale answers) | View (contents) · **Move LPN** · Close · Print LPN label (GS1-128) | two-pane inputs ← → outputs |
| Mobile | `screens/whbLot` — list, detail, hold/release; **`expiringWithinDays` is the dropdown that replaces the date range** | `screens/whbSerial` — scan-to-serial, read-only | `screens/whbLpn` — scan-to-LPN, contents, **move** (a genuine RF flow, shares the engine with WS-231) | `none` |

**WS-037 answers the three post-sale questions on one screen** — *where is it now, who did we sell it
to, is it in warranty* (`FR-099`) — and lists the shipment that carried it.

## Requirements closed
`FR-094` `FR-095` `FR-097` `FR-098` `FR-100` `FR-105` `FR-106`

`FR-100` and `FR-105` are multi-phase (plan §8.2): the LPN **object** here, LPN **move expansion** in
`P3-15`; the transformation **tables** here, genealogy **through assembly** in `P5-19`.

## Scenarios closed
`WH-SC-029` `WH-SC-080` `WH-SC-135` `WH-SC-136` `WH-SC-142` `WH-SC-143`

## Closes
`C-028` · `P-034` · `T-003` · `S-045` · `Y-006`


*Added 2026-09-02 (`GAP-REGISTER.md` §5.2). These were `COVERED-uncited` **BLOCKER**s — owned only through an FRD row's `Closes` column, so a builder reading this file alone never met them:*
`E-014` `T-006`

## Traps

- **A globally unique serial is the defect, and it is unrecoverable in a direction people miss.** Two
  owners legitimately hold the same manufacturer serial; a global unique **rejects** the second
  receipt, so **the rows are never written and there is nothing to migrate from later**
  (`FR-097`, `IRREVERSIBLE.md` §3.4). The modal's duplicate check must be **scoped**, and a
  site-level duplicate **warning** is shown where the scope permits the duplicate (`FR-106`).
- **Lots and serials in `accessories` are `VARCHAR` columns on the balance row**, part of a composite
  index — no lot entity, no serial entity, no genealogy, no uniqueness (`V30130:17-19`, `:39-46`,
  `C-028`). There is no prior art to port.
- **Three expiry columns, not one.** `expiry_date`, `best_before_date` and `use_by_date` have
  **different despatch rules** (`FR-095`); collapsing them means a best-before item is despatched
  under a use-by rule or blocked under one it does not have.
- **Genealogy is recorded at the moment of transformation, never reconstructed** (`FR-105`). A split
  that writes only the resulting lots leaves "what went into this unit" unanswerable, and no
  after-the-fact query recovers it. `whb_transformation_inputs`/`_outputs` are written in the same
  transaction as the movement.
- **`received_at` on the LPN is money.** Anniversary storage billing has **no other anchor**
  (`IRR-15`). It is a v1 column with a v2 consumer.
- **`expiryFrom`/`expiryTo` are `dateOnly`** — `expiry_date` is a SQL `DATE` (`FR-327`) and
  `filterUtils.ts:49-56` documents that `date` moves the lower bound back a calendar day east of UTC.
  An expiry filter that silently includes the previous day is a recall that misses a batch.
- **`expiringWithinDays` is a *dropdown* on purpose**, so the mobile screen can carry the same filter
  within `ListHeader`'s dropdown-only constraint (`mobile/…/ListHeader.tsx:210-218`, `C-044`).
- **`whb_lots.counterparty_id` FKs to `whb_counterparties` (`V500011`, `P1-08`)** — that ordering is
  fine (`V500018` > `V500011`), unlike `whb_owners`' (see `DEFECTS-FOUND.md`).
- **`owner_id` is `NOT NULL` on all three tables** (`FR-107`) and participates in the lot and serial
  unique keys.
- **`Move LPN` expands to one movement whose lines the service derives from the LPN's current
  contents and *stores*** — never leaves them implicit (`FR-101`, expansion in `P3-15`). An implicit
  move is a movement whose lines cannot be reconstructed.
- **Four grids × two platform files**; `dropdown.whbLot` in `CacheConfiguration.java`.

## Acceptance
- [ ] `V500018` runs **before** `V500030`
- [ ] Two owners hold the same manufacturer serial for the same item, and both receipts post
      (`WH-SC-142`, `WH-SC-143`)
- [ ] A duplicate serial within one owner+item is **detected at receipt** and refused; a site-level
      duplicate the scope permits produces a **warning**, not a refusal (`WH-SC-080`)
- [ ] A lot hold blocks allocation **everywhere at once, with nothing moved** (`FR-096`)
- [ ] `best_before_date` and `use_by_date` drive different despatch rules and are separately
      filterable
- [ ] A lot split writes a `whb_transformations` row with its inputs and outputs, and genealogy is
      answerable **forward and backward** (`WH-SC-029`, `WH-SC-135`, `WH-SC-136`)
- [ ] `whb_lpns.received_at` is populated on every LPN from the first receipt
- [ ] WS-037 answers *where is it now*, *who did we sell it to* and *is it in warranty* on one screen
- [ ] MRP, net content, country of origin and pack month/year are stored **on the lot**, defaulted
      from the item — and **MRP is not in the position key** (`OD-10`)
- [ ] Four grids, four filter scopes, `dateOnly` on every statutory date, `expiringWithinDays` as a
      dropdown so mobile carries it
- [ ] **Scenarios** `WH-SC-029` · `WH-SC-080` · `WH-SC-135` · `WH-SC-136` · `WH-SC-142` · `WH-SC-143`
