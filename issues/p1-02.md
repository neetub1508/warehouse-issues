TITLE: [Warehouse] P1-02 · UoM and identity — one scan-resolution service, and a barcode that resolves to a packaging level
LABELS: task,warehouse,phase-p1,warehouse-base
issue: 20
---
Part of __P1__ · Module **`warehouse-base`** · Migrations **V500009, V500016** · **V500076** (v2 increment — `whb_uom_scheme_codes`, folded from former `P5-24`) · Screens **WS-007 UoM Classes**, **WS-024 Item Identifiers & Barcodes**, **WS-025 Item Packaging Levels**, **WS-026 Item UoM Conversions**, **WS-034 Units of Measure**, **WS-071 Scan Enquiry Console**

## ★ **`V500009` must land before `PNR-1` (`V500030`, `P0-02`)**

`whb_stock_movement_lines.uom_code` and `.base_uom_code` are both **`NOT NULL`** and FK to
`whb_uoms(code)` (`DATA-MODEL.md` §2.1, `IRR-34`). **`DATA-MODEL.md` §2.1 is the authority on the
column set of `whb_uoms`, `whb_uom_classes`, `whb_item_identifiers`, `whb_item_packaging_levels`,
`whb_item_uom_conversions` and `whb_item_attribute_values`, and must be re-read immediately before
the migration is written.** `whb_item_identifiers` is `PNR-3` — its deadline is **the first scan**
(`IRR-43`).

## Scope

> **Round-2 addition — `Z-004`.** **`conversion_factor` is `Z-004`'s worst case and this task owns
> it.** `L-7`/`IRR-34` freeze `conversion_factor_used` **on the movement line** and left the
> **master** wide open, so a parts manager correcting a case from 12 to 6 is accepted by `WS-026`
> today and makes last year's stock report *internally consistent and wrong* — `IRR-34`'s own word
> for it is **undetectable**. The rule is in `BUILD-SPEC-SCREENS.md` §0.12: the factor is **frozen
> once a ledger row exists for that from/to pair**, enforced by a **trigger in `I-9`'s shape**
> rather than a service check, because the importer and the port write this table too. A supplier
> that changes its pack quantity gets a **new row with a new effective date**, never an edit of
> the old one.

> **Round-4 addition — `RG-008` · `RL-005` · `RG-007` · `RL-013`.** (1) **One fixed pick face, one
> table.** `whb_item_location_settings` moves from `V500061` into **`V500016`**, gaining `is_fixed` and
> `effective_from`/`effective_to` per R22 §1.3; `whb_locations.fixed_item_id` and its `ALTER` in
> `V500016` are dropped, so `P1-05`'s locations and `P1-15`'s `FIXED_LOCATION` putaway read the current
> `is_fixed` rows, and `V500061` becomes a hole (`RG-008`). (2) **The identifier key matches `FR-059`.**
> `whb_item_identifiers` gains `owner_id`, denormalised from the item; the key becomes
> `uk(identifier_type, normalised_value, owner_id, counterparty_id, channel_id) NULLS NOT DISTINCT`,
> with `FR-059`'s sentence as its comment; `identifier_type` is a value of the `IDENTIFIER_TYPE` code
> list (`whb_code_lists`, `P0-05`'s `V500010`, `RL-006`), not a registry. The resolver disambiguates
> **session owner → counterparty context → refuse with `AMBIGUOUS_IDENTIFIER` listing the candidates**
> (`RL-005`). (3) **A serial resolves by any of its identifiers**: the scan-resolution service reads
> `whb_serial_identifiers` (`P1-07`'s `V500018`) beside `serial_number`, scoped by owner, so either IMEI
> of a dual-SIM handset finds the unit (`RG-007`). (4) **Every code-keyed FK this task declares (into
> `whb_uoms`, `whb_uom_classes`) is `ON UPDATE RESTRICT`**, and a UoM `code` is `Frozen when` = *once any
> row references it*; a wrong code is retired and re-seeded (`Z-005`), never renamed (`RL-013`).

- **`V500009`** — `whb_uom_classes`, `whb_uoms` + seed, every row carrying **`unece_rec20_code`** and
  **`gst_uqc_code`** (`FR-056`). Two columns, without which **every compliance payload and every EDI
  mapping is hand-mapped and the IRP rejects invoices.**
- **`V500016`** — `whb_item_identifiers`, `whb_item_packaging_levels`, `whb_item_uom_conversions`,
  `whb_item_attribute_values`, and `whb_item_location_settings` (moved from `V500061`, `RG-008`).
- **Base stocking UoM immutable once a ledger row exists** (`FR-054`) — enforced by a **database
  trigger** (`I-9`, `V500036`, written by `P0-02` because it needs both `whb_items` and the ledger)
  and **not trusted to the service layer**. This task owns the behaviour and the read-only rendering.
- **Conversion lives on the item, not on the UoM master** (`FR-055`). *A case of oil filters is 12; a
  case of wiper blades is 6.* A global factor is correct only for physical units.
- **A barcode resolves to a packaging level, not to an item** (`FR-057`). The registry carries the
  normalised GTIN-14, a **packaging reference**, a barcode type and the pack quantity — so scanning a
  case reads 12, not 1.
- **Supplier-specific packaging with a deterministic resolution order** (`FR-058`):
  supplier-specific → supplier-agnostic default → loose/each, with `priority` as the tie-break.
- **One alias table** (`FR-059`) for owner SKU, GTIN-13/14, UPC/EAN, marketplace codes, OEM part
  number, supplier code, customer part number and legacy code — **one table, `identifier_type` a
  value of the `IDENTIFIER_TYPE` code list** (`RL-005`), not eight columns and not eight tables.
- **`sscc` on the LPN and `gln` on the warehouse, the location and the party as v1 columns**
  (`FR-064`). They are **printed on physical labels and exchanged with trading partners**;
  `whb_lpns.sscc` rides in `P1-07` and the `gln` columns in `P1-05`/`P1-08` — named here because this
  task owns the identifier model they belong to.
- **The one scan-resolution service** (`FR-062`): it resolves **any** scanned string against
  barcodes, location codes, lot codes, serial numbers and document numbers and returns a **typed**
  object. **Nothing parses barcodes inline anywhere in the product, and no scanner SDK enters the
  codebase** — the engine consumes a plain string, so a keyboard-wedge device works. **Every scan is
  logged, resolved or not.**

> **Round-3 fold — `RE-005` (`GAP-REGISTER-R3.md` §2.5).** **The UoM seed is `IRREVERSIBLE.md` §5
> row 7's v1 seed column, corrected:** classes `MASS`, `VOLUME`, `LENGTH`, `COUNT`, `AREA`, `TIME`; units
> **`EA`, `BOX`, `CASE`, `PALLET`, `BAG`, `CARTON`, `KG`, `L`, `M`, `CFT`** (`BAG` and `CARTON` added),
> **each row carrying its `unece_rec20_code` and `gst_uqc_code` literal in the migration**. A code that appears in a requirement, a scenario fixture or another task and not in the seed is a merge blocker — the rule `FR-165` applies to threshold columns.

## Screens

| | Table · scope | Ref | Key columns | Filters (first is the cascade root) |
|---|---|---|---|---|
| **WS-007** UoM Classes | `whb_uom_classes` · `WAREHOUSE_UOM_CLASS` | D | `code`, `name`, `base_uom_code` + registry commons | `code`/`name` text · `isActive` |
| **WS-034** Units of Measure | `whb_uoms` · `WAREHOUSE_UOM` | D | `code`, `name`, `uomClassCode`, `isBaseForClass`, `decimalPlaces`, **`uneceRec20Code`**, **`gstUqcCode`**, `isActive` | `uomClassCode` select · `isBaseForClass` · `code`/`name` · `isActive` |
| **WS-024** Item Identifiers & Barcodes | `whb_item_identifiers` · `WAREHOUSE_ITEM_IDENTIFIER` | C | `itemCode`, `itemName`, `identifierType`, `identifierValue`, **`normalisedValue`**, `uomCode`, `packQuantity`, **`packagingLevelCode`**, `counterpartyName`, `isPrimary`, `isActive` | `itemId` async typeahead → `identifierType` → `counterpartyId` · `identifierValue` text · `isPrimary` |
| **WS-025** Item Packaging Levels | `whb_item_packaging_levels` · `WAREHOUSE_ITEM_PACKAGING_LEVEL` | C | `itemCode`, `levelCode`, `parentLevelCode`, `quantityInParent`, `baseQuantity`, `uomCode`, `counterpartyName`, **`priority`**, `isDefault`, dims, `tareWeightKg` | `itemId` → `counterpartyId` → `levelCode` · `isDefault` |
| **WS-026** Item UoM Conversions | `whb_item_uom_conversions` · `WAREHOUSE_ITEM_UOM_CONVERSION` | C | `itemCode`, `fromUomCode`, `toUomCode`, `conversionFactor`, `isActive` | `itemId` → `fromUomCode` → `toUomCode` |
| **WS-071** Scan Enquiry Console | **no grid** · permission `whb_stock_positions:view` | — | one input, one **typed** answer | — |

- Caches: `dropdown.whbUom`, `dropdown.whbUomClass`. Statistics `—` (filter-aware, `FR-395`).
- **WS-071 renders by resolved type**: item → on-hand by location · location → contents · lot →
  positions + expiry + genealogy link · serial → current custody, sold-to, warranty (`FR-099`) ·
  LPN → contents · document → its detail route.
- **Mobile:** `screens/whbUom` and `screens/whbItemCategory`-style read-only picker lists for WS-007
  and WS-034; **WS-071's primary surface is the RF screen WS-236 (`P3`)**, and this web page is the
  desk equivalent — stated as a decision, not left blank (`FR-218`). WS-024/025/026 are `none` —
  identifier maintenance is a desk task.

## Requirements closed
`FR-054` `FR-055` `FR-056` `FR-057` `FR-058` `FR-059` `FR-062` `FR-064` `FR-078`

## Scenarios closed
`WH-SC-020` `WH-SC-261` `WH-SC-262` `WH-SC-263` `WH-SC-264` `WH-SC-265`

`WH-SC-266` (scan resolution on the handheld) is `v1.1·P3`; the engine it calls is built here.

## Closes
`C-026` · `P-034` `P-035` `P-036` · `Z-004` · `RG-008` `RL-005` · `RG-020` · `RE-005`


*Added 2026-09-02 (`GAP-REGISTER.md` §5.2). These were `COVERED-uncited` **BLOCKER**s — owned only through an FRD row's `Closes` column, so a builder reading this file alone never met them:*
`E-047` `S-001` `S-004`

## Traps

- **UoM conversion is never applied anywhere in the existing codebase.** `conversion_factor` appears
  only in an entity, a DTO, a mapper and an export — **five display-only files**;
  `accessory_stock_levels` has **no UoM column at all** and
  `accessory_inventory_transactions.uom_id` is nullable and unused in arithmetic (`C-026`,
  `V30013:13-14`, `V30130`). There is **no prior art**; retrofitting a stocking unit is a data
  migration, which is why this is v1.
- **A barcode that resolves to an item is the defect.** Scanning a case then adds 1 instead of 12.
  `FR-057` is explicit and `P-034` records the prior art's own conclusion: **quantity derived from
  the packaging level, never stored on the scan.**
- **The resolution order must be deterministic and written down**, or two suppliers' packaging for one
  item resolves differently between two calls: supplier-specific → supplier-agnostic default →
  loose/each, `priority` breaking ties (`FR-058`, `P-035`).
- **`normalised_value` is what the index is on**, not `identifier_value`. Codes are normalised on
  write (GTIN-14 padding, case, separators); resolving against the raw value means a scan that works
  on one label fails on another.
- **No scanner SDK, ever.** The engine takes a plain string so a keyboard-wedge device works
  (`FR-062`, `P-036`). An SDK in the codebase is a hardware dependency that outlives the hardware.
- **`unece_rec20_code` and `gst_uqc_code` are not optional and not India-only.** `D-8` keeps the core
  country-neutral, and these are exactly the *hooks* the core carries because they cannot be added
  later without re-mapping every historical document line (`FR-056`, `FR-319`).
- **The base-UoM trigger is a database trigger, not a service check** (`FR-054`). A service guard is
  bypassed by an import, a support script or a second writer — and this column silently restates
  every historical quantity if it changes.
- **`conversion_factor_used` is frozen on the ledger line** (`P0-02`, `IRR-34`, `L-7`). This task
  supplies the factor; the ledger stores the one it used. **A ledger that re-derives from today's
  factor silently restates last year, and both numbers are internally consistent, so the restatement
  is undetectable.**
- **Six grids × two platform files.** Six `COMMON_FILTER_CONFIGS` scopes and the `dropdown.*` names.
  An unregistered cache name throws on the **first call** (`CacheConfiguration.java:375-377`).
- **`mobile/src/schemas/common.schemas.ts` is a third copy of every dropdown vocabulary** — a UoM code
  the backend opened but the zod schema closed **fails the save with no message**. `identifier_type`
  (a code-list value, `RL-005`) and `level_code` (registry-backed) must both be fetched, never
  enumerated (`OD-5`,
  `FR-380`, `FR-382`).

## Acceptance
- [ ] `V500009` runs before `V500030`
- [ ] Changing an item's base stocking UoM after **one** ledger row exists is refused **by the
      database**, not only by the service (`WH-SC-020`)
- [ ] Every seeded UoM carries `unece_rec20_code` and `gst_uqc_code` (`WH-SC-261`)
- [ ] Scanning a **case** barcode resolves to the case packaging level and yields the pack quantity,
      not 1 (`WH-SC-262`)
- [ ] Two suppliers' packaging for one item resolve deterministically in the stated order, and the
      order is written into the code as a single documented resolver (`WH-SC-263`)
- [ ] One alias table carries all eight identifier kinds; there is no second alias table anywhere
      (`WH-SC-264`)
- [ ] The scan-resolution service returns a **typed** object for a barcode, a location code, a lot
      code, a serial number and a document number — and **logs every scan, resolved or not**
      (`WH-SC-265`)
- [ ] No barcode parsing exists outside that service, proved by grep; no scanner SDK is a dependency
- [ ] Six grids, six filter scopes, `en`/`fr`/`hi` labels, and the mobile decision recorded per screen
- [ ] Editing `conversion_factor` on a pair that any `whb_stock_movement_lines` row has used is
      refused **by the database**, with `CONVERSION_IN_USE`, the count of lines and a link to them;
      the modal renders the field read-only with the reason (`Z-004`, §0.12)
- [ ] A changed pack quantity is entered as a **new** conversion row with its own effective date,
      and the old row stays readable — no path in the UI or the importer edits a used factor
      (`Z-004`)
- [ ] Every field on `WS-024`–`WS-026` carries a `Frozen when` value in the screen block; `never` is
      the default and the common answer (`Z-004`)
- [ ] `whb_locations` has no `fixed_item_id`; `whb_item_location_settings` is created in `V500016`
      with `is_fixed` and dates, and no task claims `V500061` (`RG-008`)
- [ ] Two owners register the same EAN and both save; one OEM part number from two counterparties
      saves; a scan matching two candidates with no session owner or counterparty context is refused
      with `AMBIGUOUS_IDENTIFIER` listing both (`RL-005`)
- [ ] Scanning either IMEI of a dual-SIM serial resolves the same `whb_serials` row (`RG-007`)
- [ ] Every code-keyed FK in `V500009`/`V500016` is `ON UPDATE RESTRICT`, and renaming a referenced
      UoM code is refused (`RL-013`)
- [ ] **Scenarios** `WH-SC-020` · `WH-SC-261` · `WH-SC-262` · `WH-SC-263` · `WH-SC-264` · `WH-SC-265`
- [ ] `V500009` seeds exactly §5 row 7's six classes and the ten units above, each with a literal
      `unece_rec20_code` and `gst_uqc_code` (`RE-005`)

## Owed to `P0-17` — two deferred cost-layer foreign keys

`V500021` (`P0-17`) ships `whb_cost_layers.lot_id` and `whb_cost_layers.serial_id` as bare `UUID`s;
their tables are `whb_lots`/`whb_serials` (`P1-07`'s `V500018`), so whichever of this task and
`P1-07` lands second owes those two `ALTER TABLE`s in **`V500023`** — the first free number in the
reserved gap (`V500022` is consumed by `P0-07`). The exact statements are in `V500021`'s header block
and are not restated here.

## Round-4 additions (folded from former P5-24, #157) — UoM scheme codes

**v2 increment of this task — design the schema now, build in the v2 wave.** Former `P5-24` (`#157`)
gathered every v2 junction a round-4 lens had hung on a v1 task. On 2026-09-10 it was closed as a
duplicate and split by parent table. This task owns `whb_uoms` and its `gst_uqc_code` and
`unece_rec20_code` columns, so the UoM's scheme codes come here. `FR-468` is owned by `P1-05`; this
increment is its UoM part.

**Scope (`RG-020`, `RL-008`).** **`whb_uom_scheme_codes`** — `uom_code`, `scheme`, `code`;
`GST_UQC` and `UNECE_REC20` become rows. `whb_uoms.gst_uqc_code` **stays** in v1 with the stated rule
that a second scheme goes to this side table (`RL-008`). A sub-grid on `WS-034`; no new screen id.

**Migration `V500076`** — allocated at the fold from the post-v1 base gap. Former `P5-24`'s single
`V500070` could not be claimed by the seven base hosts it was split across; `GAP-REGISTER-R4.md` §4.6.

**Closes** `RG-020` (the UoM part; the item and reason-code parts are `P1-01`'s and `P0-04`'s).

**Traps (v2 increment)**

- **`whb_uom_scheme_codes` is deliberately undated.** A UoM's code in a scheme is an identity fact, like a
  variant value (R22 §1.3 rule 6, `RG-009`). A changed code is a correction by retire-and-reseed
  (`RL-013`), not a history row. It is the one table former `P5-24` left undated, and it stays so here.
- **Backfill in the same migration**: a `GST_UQC` row per non-null `whb_uoms.gst_uqc_code` and a
  `UNECE_REC20` row per non-null `unece_rec20_code`.
- **The scalars stay for v1 readers, and nothing new reads them.** The challan (`P2-IN-03`) keeps
  reading its frozen UQC; code written in this increment reads the side table.

**Acceptance (v2 increment)**
- [ ] `V500076` creates `whb_uom_scheme_codes` with `uk(uom_code, scheme)` and no date columns, and backfills one row per non-null scheme column
- [ ] A UoM with codes in two schemes resolves each by scheme; a posted document keeps the code it froze (`RG-020`)
- [ ] **Mobile:** `none`, with the reason stated. Scheme codes are back-office reference data (`D-13`)
