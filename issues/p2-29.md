TITLE: [Warehouse] P2-29 · Grid configuration wave 3 — export follows the visible columns, and filter-aware statistics carry no cache name
LABELS: task,warehouse,phase-p2,warehouse,platform
issue: 154
---
Part of __P2__ · Modules **`warehouse-base`** + **`warehouse`** + **`platform`** (`filterUtils.ts`, `CacheConfiguration.java`) · Migrations **V501073–V501099, V511060–V511139** · Screens **none of its own** — `V501070`–`V501099` are the `warehouse-base` grids (`V501070` and `V501072` claimed by `P2-07`, `V501071` by `P2-22`, 2026-09-18) and `V511060`–`V511139` the `warehouse` grids

> **Out-of-order dependency — read before claiming a number in either band** (recorded 2026-09-18,
> round-3 review F7; `DATA-MODEL.md` §7.1 rule 7). Both bands sort **below** versions that are already
> released: `WHB-75`'s `V501100` and `WH-204`'s `V511200`. Every grid migration claimed inside them is
> therefore an **out-of-order insert** on an install that has already run the earlier waves.
> **Nothing is renumbered** — the bands sit below the config migrations on purpose (DDL before config,
> and a task uses its own block). What follows from it is operational:
> **`spring.flyway.out-of-order` must be `true` on any upgraded install**; with it `false` Flyway
> refuses to boot with *"Detected resolved migration not applied to database"* and the backend
> crash-loops, which this repository's local stack has already done once from a `.env` value that
> overrode `application.yml`. A fresh install is unaffected.

## Scope

> **Round-2 addition — `Q-001` · `Q-005`.** **Two gates this task takes on beside the grids.** (1)
> **The P2 verb-permission and dependency bands.** `WH-206` (`DATA-MODEL.md` §7) is the new
> allocation: its first half is the verb-permission band, its second half the matching dependency
> band. Each task that ships a transition claims **one number from each**, seeds its own verb
> permission with its `role_permissions` back-fill and its `→ :view` `permission_dependencies` row,
> and records both numbers **in its own header**. **`WH-200`'s `V511000` is released and is never
> edited** — the two *"seed it in `P1-20`'s `V511000` block"* instructions in `p2-03.md` and
> `p2-13.md` are deleted. The band is carved from the reserved post-v1 block, **not** from
> `WH-203`: grid configuration and permissions are different allocations, and `WH-203` is already
> sub-allocated down to its last free twenty numbers. (2)
> **The i18n key set is asserted, not assumed.** Every P2 screen's modal titles, action labels,
> validation messages and menu leaves exist in `en`, `fr` and `hi` with **identical key sets**,
> asserted by the same key-diff test `P1-19` ships, extended to the P2 key space — and `P1-19`'s
> own citation is corrected to name the scenario that actually asserts the key-diff, or
> `WH-SC-247`'s expected-outcome cell gains the assertion. (3) For every grid,
> `BUILD-SPEC-SCREENS.md`'s screen row is amended to the `| key | label | type | sort | vis |
> source |` form **before** its migration is authored; a grid whose spec row is still a bare comma
> list is not ready to build. That makes this task the **gate**, not the author — the same way it
> already handles migration numbers.

Every P2 grid's `grid_column_definitions`, `filter_definitions` and `grid_preferences`, plus the two
rules that decide whether a grid works at all.

**How the migration numbers are used, because one task owning eighty files would be a lie.**
`IMPLEMENTATION-PLAN.md` §3.6 states the sequencing rule directly: *"config work is the classic thing
deferred to the end of a phase; a grid without its `grid_preferences` row and its `filterUtils` scope
**looks built and is not usable**, so it is scheduled with its screens, not after them"*.
**So: this task owns the two bands, the two rules, the streaming export path and the ratchets.
Each grid's own migration is authored by the task that creates its table, claiming one number inside
`V501070`–`V501099` or `V511060`–`V511139` and recording it in that task's header.** This task's
completion criterion is that **every P2 grid has a number, a row, a scope and a translation**, and
that the two rules hold everywhere. Filed in `docs/DESIGN-SET-DEFECTS.md` as the reading taken — `X-057` and `X-058`. (`DEFECTS-FOUND.md` was deleted on the 2026-09-02 merge into that file; `X-058` records the stale citation.)

### Rule 1 · Export follows the visible columns and is a superset of them (`FR-400`)

> **How the rule became checkable — 2026-09-18, round-3 review (F1 · F2 · F4).** As first built, the
> rule was two declarations that never met: an export service declared `getExportColumnKeys()`, a
> migration declared `grid_column_definitions`, and **nothing joined them**, so "export ⊇ the visible
> grid columns" was enforced by a reviewer's eye and by nothing else. It failed on its first outing:
> `WhbImportBatchExportService` declared `isLedgerStyle() = true` — "my grid shows no created-by" —
> while its own WS-065 grid (`V500046`) defines `createdByName`, and the contract test could not see
> the contradiction because its audit rule asked only whether BOTH audit columns were present.
> Three corrections, all shipped:
> 1. **A module-level export base, `ai.warehousebase.service.export.WhbExportService`**, adds
>    `protected abstract String gridIdentifier()`. It is in **warehouse-base, never platform**
>    (platform's base stays unedited but for the agreed `isLedgerStyle()` hook), and `warehouse`
>    extends it too — the allowed direction. **All 68 concrete export services declare their grid.**
> 2. **Each module's `ExportServiceContractTest` reads the grid from its own migrations** and asserts
>    `getExportColumnKeys()` ⊇ that grid's `default_visible` columns minus `actions`. The probe runs
>    as a caller holding `warehouse:cost:view`, because `WhbCostVisibility` narrows the declared set
>    for a caller who may not see cost — and narrows the grid the same way.
> 3. **The audit rule is derived from the grid, not guessed.** `isLedgerStyle() = true` now means the
>    grid defines neither audit column; `false` means the export carries **exactly** the audit columns
>    its grid defines — which may be both, **one**, or none. WS-065 is the one-column case:
>    `whb_import_batches` is a **document**, its grid carries `createdByName` and no `updatedByName`,
>    and the export matches it. `isLedgerStyle()` there is now `false`.

- Carrying **created-by and updated-by wherever the grid shows them** — and **not** where it does
  not. *The rule is grid↔export parity, not a fixed column list.* Log- and register-style grids —
  `wh_insufficient_stock_log`, `wh_blocked_movements`, `wh_demand_history`,
  `wh_reconciliation_exceptions`, and every report grid in §7 — deliberately have **no** audit
  columns, so their exports have none either; adding export-only audit columns there emits a column
  no reader can select.
  `ExportServiceContractTest.WITHOUT_AUDIT_COLUMNS` freezes the current set as a **ratchet — it may
  shrink, never grow.**
- **A 100,000-row export does not hold a transaction open**, and that needs a **streaming** path.
  `PP-6`/`PD-D3`: `BaseExportService.DEFAULT_MAX_EXPORT_ROWS = 10_000`
  (`platform/…/service/BaseExportService.java:48`) against a 100,000-row target, and **raising the
  constant solves only the first of three problems** — the transaction is held open for the
  duration, and a per-cell style blows the 64k Excel cell-style limit **at scale, not in dev**.
  **Add a streaming path (`Stream<T>` with a fetch size, written straight to the output) in
  `warehouse-base`** rather than editing `BaseExportService`, and **propose the platform extraction
  separately so the two are not coupled.**
  **Which export streams and which stays capped is stated, not assumed** (F8, 2026-09-18):
  `WhbStreamingExportService` ships with **no production subclass**; **WS-209's movement register is
  the first**, and the **WS-040 `whb_stock_movements` GRID export stays on `BaseExportService`, capped
  at 10,000**. A grid is a paged screen with a filter strip and its export is what the operator is
  looking at. Recorded in `BUILD-SPEC-SCREENS.md` §7.
- **CSV keeps the UTF-8 BOM** (`BaseExportService:59`, with the reason at `:51-58`).
- **Return the raw `Boolean` from `extractRowData`.** `BaseExportService` renders Yes/No on every
  path — CSV, the Excel fallback and `ExcelExportHandler` — and nulls as blank; it does **not**
  expose `formatBoolean(Boolean)` (that is on `BaseController`), so calling it is a **compile
  error** (`T-17`). Do not write a `formatYesNo` replacement.

> **Round-3 fold — `RB-005` · `RB-006` · `RC-004` · `RC-005` (`GAP-REGISTER-R3.md` §2.2, §2.3).**
> - **The ratchet cannot see warehouse today** (`RB-005`).
>   - `ExportServiceContractTest.java:136` scans `ai.platform` only, so it discovers no warehouse
>     export service. **Corrected 2026-09-17 while building this task (`X-057`): the scan CANNOT be
>     widened from platform.** A package list only finds classes on the classpath it runs with, and
>     every module depends on `platform-backend` while platform depends on no module — so adding
>     `ai.warehousebase` to platform's scan returns an empty set and passes vacuously. **Each module
>     carries its own copy of the contract test scanning its own package**, which is what
>     `classic-issues#990` settled for the other twelve modules on 2026-09-11 and what the accounting
>     modules did for `ArchitectureInvariantsTest`. Built as
>     `warehouse-base/backend/src/test/java/ai/warehousebase/service/ExportServiceContractTest.java`
>     and `warehouse/backend/src/test/java/ai/warehouse/service/ExportServiceContractTest.java`.
>   - Warehouse's ledger and report grids never enter `WITHOUT_AUDIT_COLUMNS` (`:80`). They declare
>     themselves through a **`boolean isLedgerStyle()` hook on `BaseExportService`**, which the test
>     reads instead of a name list, so the frozen set keeps shrinking. **Built: the set is EMPTY in
>     both warehouse copies and the audit rule reads the declaration.** Nineteen `warehouse-base`
>     export services override the hook; all nine `warehouse` services carry the audit columns and
>     inherit `false`.
>   - That hook is **the one stated exception** to *"`BaseExportService` is unedited"*, and it goes to
>     platform as its own proposal.
>   - `whb_transformations` joins §0.5's audit-columnless list.
> - **Every grid states its empty state** (`RB-006`).
>   - Each block names an `emptyMessage` i18n key.
>   - The six workflow-gating grids state three distinguishable messages: `empty`, `out of scope` and
>     `never populated`. A master grid may state `never`, the platform default.
>   - Each screen states its statistics tiles, or `none`.
> - **Report totals are statistics strips** (`RC-004`). This wave seeds the tiles `BUILD-SPEC-SCREENS.md`
>   §7 states for each report: WS-210's closing quantity and value, WS-219's five terms and the
>   difference, and WS-211's total value and method. All are filter-aware, so none carries a cache name.
> - **A required report parameter is refused server-side** (`RC-005`).
>   - The report controller reads its grid's `filter_definitions.is_required` rows through the existing
>     `FilterDefinitionRepository` query, which has no caller today.
>   - It returns a **field-level `400`** naming the missing parameter. A report that silently defaults
>     to today is a defect.
>   - The parameter renders above the filter strip, and the grid does not fetch until it is set. That
>     is a page rule, not a `BaseFilter` change.

### Rule 2 · Filter-aware statistics carry no cache name (`FR-395`)

**The platform's cache registry already records that exact mistake with its issue numbers.**
`CacheConfiguration.java:190-196` — issues `neetub1508/classic#790`, `#791`: statistics computed from
the same search + filters as the rows *"are uncacheable under any key short of the full filter set"*,
and **registering a name for them caches nothing while reading as though it did.**
**Most warehouse strips are filter-aware, so most get no name.** A `statistics.*` name appears
**only** where the number is provably independent of the filter strip; **no v1 warehouse screen
qualifies.** `dropdown.<entityCamelCase>` names are fine and **must** be registered — an unregistered
name throws `IllegalArgumentException` on the **first call**, not at startup (`:375-377`).
`PP-8`'s fallback, if a strip is too slow: **rollup tables maintained by the same job that proves
`L-4`** — decided *with* the reconciliation design, not after it. **Never a `statistics.*` name.**

### The registry edits, and the honest statement about them

`D-10`'s corollary: **the loose-coupling ratchet can only ever be *"zero commits to
`warehouse-base`"* — never *"zero commits to `platform`"*.** `COMMON_FILTER_CONFIGS` is a TypeScript
`const` at `platform/frontend/src/utils/filterUtils.ts:346` and `CacheConfiguration.java` is platform
Java. **Every new grid edits both.** *A contract that claims otherwise is false on day one and,
being false, gets ignored — taking the true invariants with it.*

Counts, computed 2026-09-01 (**re-run before quoting; the tree moves under you**):
```
awk '/COMMON_FILTER_CONFIGS/,0' platform/frontend/src/utils/filterUtils.ts \
  | grep -cE "^  [A-Z0-9_]+: \{"                                        # → 213 scopes
awk 'NR>101 && /^\s*\)\);/{exit} NR>101' \
  platform/backend/src/main/java/ai/platform/config/CacheConfiguration.java \
  | grep -cE '^[[:space:]]*"[a-zA-Z0-9._-]+",?$'                        # → 234 cache names
```

**Mobile.** Every scope registered here has a mobile counterpart decision recorded on its screen's
task. `mobile/src/components/common/ListHeader.tsx:210-218` supports
`type?: 'dropdown' | 'text'` and **not dates** (`C-044`, `PP-3`) — so every web grid whose default
strip is date-driven ships the mobile equivalent as a **dropdown of named periods**, and the block
says so.

## Requirements closed
`FR-395` · `FR-400`

## Scenarios closed
- `WH-SC-256` — the export **follows the visible columns and is a superset of them**, carrying created-by and updated-by **because the grid shows them**, and a 100,000-row export does not hold a transaction open
- `WH-SC-247` — the grid migration writes column definitions, filter definitions **and both `default_columns` and `default_filters`**, and the grid is usable on a fresh install
- `WH-SC-298` — statistics are computed from the ledger, and a pre-aggregated table is added only for what is provably too slow

## Closes
`C-043` `G-047` (`FR-395`) · `C-036` `S-084` (`FR-400`) · `Q-001` `Q-005` · `RB-005` `RB-006` ·
`RC-004` `RC-005`


## Traps

- **`T-8` — `grid_preferences.default_filters` AND `default_columns` must BOTH be populated**, as
  `'[…]'::jsonb`. `filter_definitions.default_visible = true` **alone builds nothing**:
  `useGridPreferences` reads `defaultFilters` from `grid_preferences.default_filters`, not from
  `filter_definitions` (`V18:12`, `V229:50-51`, example `V229:53-55`).
- **`CM-4` — the table is `filter_definitions`, created in `V229`.** There is **no**
  `grid_filter_definitions` table — 0 hits over all migrations — and inserting into that name
  **fails at Flyway and crash-loops the backend.**
- **`CM-3` — "NO JSONB" is overstated and the repo's own ratchet says so.**
  `platform/…/architecture/ArchitectureInvariantsTest.java:225-239` freezes a **baseline** rather
  than banning it, and `grid_preferences.default_columns` **is** `JSONB NOT NULL DEFAULT '[]'`.
  **Every warehouse grid migration must emit `'[…]'::jsonb`.** The rule that holds is *no JSONB on
  new `wh_` business tables*.
- **`T-6` — a field not in `COMMON_FILTER_CONFIGS.{SCOPE}` is silently dropped by
  `convertFiltersForApi()`** before the API call, and the filter UI appears to do nothing.
  **Both halves of every date pair** must be listed; there is **no range type** — the eight are
  `text | select | enum | multiselect | boolean | date | dateOnly | number` (`filterUtils.ts:30`).
- **`dateOnly` versus `date` is load-bearing here, not cosmetic.** `filterUtils.ts:49-56` documents
  that `date` shifts to UTC day boundaries for `TIMESTAMP` columns, *"which moves the 'from' bound to
  the previous calendar day for users east of UTC"*. `FR-327` makes every statutory, expiry,
  manufacture and count date a SQL `DATE`; **those filters are `dateOnly`.** The DB side is a
  **different registry** — `filter_definitions.filter_type` (`V229:13`, default `'text'`) takes
  `text | select | date | number | boolean | multiselect`, and **a `dateOnly` scope entry pairs with
  a `date` row there.**
- **There is a third filter surface and it is easy to forget: the API service.** An enumerating
  `getForManagement` silently drops unknown keys, so a scope entry plus a `filter_definitions` row
  plus an unforwarded parameter is still a filter that does nothing. **Three-way parity**
  (`BUILD-SPEC-SCREENS.md` §0.5). **Legs 2 and 3 are now ratcheted** (F3, 2026-09-18) by one Jest
  suite per warehouse frontend — `warehouse-base/frontend/src/__tests__/whbGridFilterParity.test.ts`
  and `warehouse/frontend/src/__tests__/whGridFilterParity.test.ts`. They read the module's
  migrations, bind each grid to its scope through the page that names both, and assert the scope entry,
  the forwarded parameter and the `DATE` → `dateOnly` pairing. They **read** `filterUtils.ts` and never
  write it. Until they existed, `WhbGridConfigContractTest`'s javadoc said the Jest suites asserted the
  frontend legs and §9.6 said the Java test did — **both were false and each made the other look
  verified**; both citations are corrected.
- **`T-9`** — `WHERE NOT EXISTS` on every menu insert; `menus` has no unique constraint on
  `(name, parent_id, menu_level)` and `ON CONFLICT DO NOTHING` does not stop a Flyway-retry
  duplicate.
- **`T-12` — module migrations are flattened into one directory at build time**
  (`Dockerfile.backend:140-181`). Two tasks claiming one number is a **startup failure**. Each grid's
  number is claimed in its own task header and `tools/check-design-set.py` fails on
  double-ownership.
- **`T-11` — the frontend merge is last-write-wins** across the whole `src/` tree
  (`Dockerfile.frontend:80-177`); every locale, component, service and type file is `wh`-prefixed.
- **`T-5` — `SqlSortBuilder` whitelist or no sorting**, always with a default sort. A sortable
  column needs four gates aligned: the column definition, the field mapping, the API sort parameter
  and the entity path.
- **Export ignores filters and export ignores grid sort are known live defects elsewhere in this
  repo.** Assert both: the export of a filtered, sorted grid reproduces **that** filter and **that**
  sort.
- **Excel mangles text columns.** Phone-like and code-like columns need the text pin; a `VARCHAR`
  part number of digits becomes a number and loses its leading zero.

## Acceptance
- [ ] Every P2 grid has: a claimed migration number inside the two bands, a `grid_column_definitions` set, a `filter_definitions` set, a `grid_preferences` row with **both** `default_columns` and `default_filters`, a `COMMON_FILTER_CONFIGS` scope, an API-service parameter for every filter key, and en/fr/hi translations
- [ ] `grep` finds **no** `grid_filter_definitions` in any warehouse migration
- [ ] Every export is a superset of its grid's visible columns, **asserted from the migrations** through the service's `gridIdentifier()` declaration on `WhbExportService`; grids that show created-by/updated-by export them and grids that do not, do not — and a grid whose audit set is exactly `{createdByName}` is a valid one (F1 · F2 · F4)
- [ ] A 100,000-row export completes through the **streaming** path without holding a transaction open, and `BaseExportService` is **unedited** except for the `isLedgerStyle()` hook (`RB-005`)
- [ ] The export of a filtered, sorted grid reproduces that filter and that sort — **re-homed 2026-09-18 (F5)** into `p2-18`, `p2-20`, `p2-21` and every grid-owning P2 task, because this task owns no grid of its own and an acceptance row that lives only here is never executed. The ratchet is a source scan in each module's `ExportServiceContractTest`: `getEntitiesForExport` must consume its filters map and a sort key. **31 `warehouse-base` services read no sort key today** — a FROZEN baseline, `IGNORING_THE_GRID_SORT`, which may shrink and never grow; `warehouse`'s copy is empty and must stay empty
- [ ] `extractRowData` returns raw `Boolean`s; there is no `formatYesNo` and no call to `formatBoolean` from an export service
- [ ] CSV exports carry the UTF-8 BOM
- [ ] **No `statistics.*` cache name is registered for any warehouse screen**; every `dropdown.*` name used is registered
- [ ] Every `DATE` column's filter is `dateOnly` in the scope and `date` in `filter_definitions`
- [ ] ~~Every mobile counterpart of a date-driven strip ships a named-period dropdown, and its screen's task states so~~ — **SUPERSEDED** by the no-mobile decision — *"warehouse has no mobile app"* (user scope rule 2026-09-11, `MPR-OPEN-13`; `BUILD-SPEC-SCREENS.md` §2.6 blocks state it screen by screen). There is no counterpart to ship. The `ListHeader` date-filter constraint (`C-044`, `PP-3`, §0.7) stands as a note for whenever mobile is taken up, and **nothing is owed here**
- [ ] `ExportServiceContractTest` discovers every warehouse export service through its package list, and none is added to `WITHOUT_AUDIT_COLUMNS` — each audit-columnless one returns `true` from `isLedgerStyle()`, **and that declaration is checked against the grid's own `grid_column_definitions` rather than taken on trust** (`RB-005`, F2)
- [ ] Every warehouse grid block names an `emptyMessage` key, and the six workflow-gating grids render distinct `empty` / `out of scope` / `never populated` messages (`RB-006`). **Gated by `tools/check-design-set.py` check 13** (F9): §1's `G` column is the list, §2.1's fourteen catalogues are the worked example, and the **207** blocks written before the rule are named in `BUILD-SPEC-SCREENS.md` §9.6.1's shrink-only register — each grid-owning task converts its own block on the way past
- [ ] Each §7 report ships the statistics tiles its row states, uncached (`RC-004`) — **unticked; `P2-20` owns the report screens and ships them**, this task owns the rule and the §9.6 statement of it
- [ ] A report request missing a required parameter is refused with a field-level `400` naming it (**server half: shipped — `WhbReportParameterGuard`, a `ValidationException` whose field map reaches the browser as `validation_errors`**), and the report page does not fetch until the parameter is set (**frontend half: unticked — `P2-20` / `P2-21` own the report pages**) (`RC-005`)
- [ ] Three-way filter parity legs **2 and 3** are asserted by a Jest suite per warehouse frontend, which reads `filterUtils.ts` and never writes it (F3)
- [ ] Every `grid_preferences` **VALUES row** — not merely every statement — carries `default_columns` AND `default_filters` as a **non-empty** `'[…]'::jsonb` (F11)
- [ ] The streaming test tells streaming from collecting (rows produced when the FIRST row is written ≤ the fetch size) and proves the transaction opens inside the `StreamingResponseBody` and commits with the last row (F6)
- [ ] **Scenarios** `WH-SC-256` · `WH-SC-247` · `WH-SC-298`
