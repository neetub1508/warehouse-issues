TITLE: [Warehouse] P5-27 · Value-banded approval levels — ordered typed rows, not a workflow engine
LABELS: task,warehouse,phase-p5,warehouse
issue: 160
---
Part of __P5__ · Module **`warehouse`** · Migrations **V510222** (`wh_approval_levels`) · **V511180** (the `WS-241` grid configuration) · **V511210** · **V511240** (the resource's permissions, dependency rows and menu) · Screens **`WS-241`** Approval Levels

## Scope — one bounded table the existing approve actions read (`RK-008`)

Every approval in v1 is one step: `FR-408` keeps approver and actor apart, and adjustments, counts, POs
and transfers carry a value threshold. The market's approval is a value-banded chain: *"POs over ₹5 lakh
need the parts manager, then the general manager"* (`RK-008`). This task adds that chain as **ordered,
typed rows**. It stays inside refusal #10 (FRD §10 row 11): it is not a workflow engine (`FR-466`).

1. **`wh_approval_levels`** (`V510222`) — `company_id`, `document_type_code` (the document-type registry,
   `P0-04`), `sequence_no`, `min_value` `DECIMAL(19,4)` (`OD-7`), `required_permission` (a §10.2 approve
   string), `effective_from`, `effective_to`; `uk(company_id, document_type_code, sequence_no)` among
   current rows. The value band is the range from one level's `min_value` to the next.
2. **The rule.** A document requires every current level of its type whose `min_value` is at or below
   the document's value, in `sequence_no` order. With no rows for a type, the v1 single-step threshold
   behaves exactly as before.
3. **The existing approve actions read it.** The approve actions that ship in v1 — PO (`P1-12`),
   adjustment (`P2-01`), count variance (`P2-04`) and transfer (`P2-02`, `FR-462`) — ask for the next
   level instead of approving outright. The document stays in its approval state until the last level
   approves. The existing `approved_by` column holds the last approver.
4. **Each level's approval is an audit event** (`whb_audit_events`, `P0-13`) naming the document, the
   level's `sequence_no` and the approver. The next level and the no-twice rule read those events. There
   is no second approval table.
5. **`WS-241` Approval Levels** — a Department-shaped master: grid (`V511180`), add/edit and view
   modals, company and document-type filters.

## Requirements closed
`FR-466`

## Scenarios closed
`WH-SC-323` — a PO over the second value band needs the parts manager, then the general manager; the
same user cannot approve twice.

## Closes
`RK-008`

## Traps

- **Not a workflow engine** (`RK-008`, refusal #10). No conditions beyond document type and value, no
  branching, no parallel levels, no expressions. A request for any of those is a new refusal row, not a
  column on this table.
- **The levels are resolved once, at submission.** An edit to `wh_approval_levels` closes the old row and
  opens a new one (`D-14`). A document already submitted keeps the set it resolved, recorded on its first
  audit event. Re-routing a document in flight is how an approval chain loses its audit trail.
- **`FR-408` applies at every level, and one user approves a document at most once.** Holding two levels'
  permissions does not let one user clear both. The check reads the document's approval events, not the
  user's roles.
- **A level whose permission nobody holds blocks forever, so refuse it on save.** Saving a level whose
  `required_permission` is granted to no active user in the company is a validation error with that
  message, not a silent dead end.
- **The value is the document's value in the company's base currency**, computed by the document, not by
  this table. A PO in a foreign currency converts at the rate it already carries.

## Acceptance
- [ ] `wh_approval_levels` exists at `V510222` with the R22 §1.3 date columns, the `uk` among current rows, and an FK to the document-type registry
- [ ] `WH-SC-323`: a PO above the second band's `min_value` is `APPROVED` only after two different users holding the two levels' permissions approve in sequence; the second approval by the first user is refused
- [ ] A document type with no level rows approves in one step, exactly as in v1
- [ ] Editing a level while a document is mid-chain does not change that document's remaining levels
- [ ] Each level's approval writes one `whb_audit_events` row with the document, the level and the approver
- [ ] Saving a level whose permission no active user holds is refused with a field-level error
- [ ] `WS-241` ships its grid (`V511180`) with both `default_columns` and `default_filters`, and `V511210`/`V511240` seed the resource, dependencies and menu
- [ ] **Mobile:** the level chain is shown read-only on any mobile approve action that already exists, and the approve button asks for the next level exactly as on the web; `WS-241` itself is back-office with no mobile surface, stated (`D-13`)
- [ ] **Scenarios** `WH-SC-323`
