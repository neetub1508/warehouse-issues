TITLE: [Warehouse] P5-21 · The four remaining owner- and transport-shaped v2 items — per-owner print templates, fitments and equipment, the gate, and returnable packaging
LABELS: task,warehouse,phase-p5,warehouse,warehouse-base
issue: 114
---
Part of __P5__ · Modules **`warehouse`** + **`warehouse-base`** · Migrations **V500065** (`WHB-65` — `whb_packaging_balances`, from §7.2's post-v1 base DDL gap) · **V510223** (`wh_print_template_scopes`, folded from former `P5-24`) · Screens **none new**

## Scope — four items that share one property: each is a column or a rule, not a feature

1. **`owner_id` on the print template (`FR-226`).** *"Client-specific label layouts are a real 3PL
   requirement and that column is the whole of it."* **The column already exists** —
   `wh_print_templates.owner_id` (nullable) shipped at `V510060` in v1 (`WH-50`), with a partial unique
   index giving one default per kind × owner × warehouse. **This task builds the resolution**: rendering a
   label for a shipment resolves owner-specific → warehouse-specific → global, most-specific wins, and the
   resolved template **version** is recorded on the print job so a reprint of last month's label
   reproduces last month's layout.
2. **Fitments and equipment as reservations against warehouse stock (`FR-338`).** A tyre fitted to an axle
   and a tarpaulin issued to a trip are **stock movements posted through the port** —
   `ISSUE_TO_ASSET` / `RETURN_FROM_ASSET` / `EQUIPMENT_ISSUE` / `EQUIPMENT_RETURN` — **and the fitment
   record itself belongs to `logistics`** (`P6-08`). Warehouse ships the movement types, the item types
   (`TYRE`, `RETURNABLE_EQUIPMENT`) and the reservation holder quad; it ships **no fitment table**.
3. **The gate belongs to neither module (`FR-340`).** *A gate event answers neither how much of what nor
   which journey — it answers which vehicle crossed a line at what time, and both modules want it.* It is
   **published to a handler list, exactly as the existing boom-barrier webhook does**
   (`automotive/…/controller/BoomBarrierWebhookController.java:44-48`); **warehouse claims it to stamp a
   dock appointment**, and `logistics` will claim it to stamp a trip departure.
4. **Returnable packaging carries a per-counterparty balance (`FR-343`).** Pallets, crates, cylinders and
   totes exchanged at a stop are stock (`item_type = PACKAGING`) **with a deposit and a balance per
   party** — *and nobody currently has one.*

## Requirements closed
`FR-226` · `FR-338` · `FR-340` · `FR-343`

## Scenarios closed

<!-- check-design-set: scenario-citations begin WH-SC-306 — the SCENARIO-CATALOGUE.md §5 rule 3 allocation marker — the next free scenario id, which by definition has no row yet. Named here so a parallel task does not silently take it twice; it is never a citation of a scenario that exists -->
`WH-SC-296` (`FR-340`'s half) — the yard trailer: *"the trailer is a warehouse location (type `TRAILER`),
or that stock is off the books for eleven days while it sits there… and the weighbridge measures the
stock, not the yard."*

`FR-226`, `FR-338` and `FR-343` have **no scenario** and are on §4.2's unproven list under 6.12 and 6.19.
Author at minimum: a `CL-NOVA` shipment printing that client's carton label while a `HOUSE` shipment
prints the global one, from the same code path; a tarpaulin issued to a trip and returned, posted through
the port with the reservation released; and a pallet exchange at a stop moving a counterparty's balance
from +40 to +28 with the deposit unchanged. Take the next free `WH-SC-` ids from `WH-SC-306` and claim
them in the catalogue in one commit before writing code.

<!-- check-design-set: scenario-citations end -->

## Closes
`F-062` (per-owner print templates) · `G-002` · `G-003` (tyres and equipment as stock with logistics
fitments) · `G-016` (the gate belongs to neither) · `G-024` · `F-060` (returnable packaging with a
per-counterparty balance) · `RG-018` (multi-scope print templates, folded from former `P5-24`).

## Traps

<!-- check-design-set: table-names begin wh_tyre_fitments — the counter-example this trap forbids: FR-338 puts the fitment record in logistics, and building it here is precisely how the boundary rots. Named so it is not built, never cited as a design object -->

- **✅ PARTLY CLOSED 2026-09-02 — `FR-343` had no table and this task had no migration**
  (`IMPLEMENTATION-PLAN.md` §2.7 defect). A **per-counterparty balance** for returnable packaging
  appeared nowhere in `DATA-MODEL.md`; `G-024` marks it explicitly as *"new"* relative to `F-060`,
  which covers packaging **as stock** only. A balance per party with a deposit is not a stock position
  — it is an open-item balance against a counterparty. `whb_packaging_balances` is now a
  `DATA-MODEL.md` §2.1.4 row allocated to **`WHB-65`, `V500065`**, and this task's header claims it.
  **Still open:** the *movement-linked entry table* this task's acceptance implies (*"the balance
  reconciles to the sum of its entries"*) is **not named and not allocated** — naming it is this
  task's first act, and the row goes into §2.1.4 and §7.2's `WHB-65` in the same PR.
- **⚠ Verify `FR-338`'s four movement types are among `V500003`'s seeded fourteen** (`WHB-03`). The
  fourteen are not enumerated in `DATA-MODEL.md` §7.2's row. `ISSUE_TO_ASSET`, `RETURN_FROM_ASSET`,
  `EQUIPMENT_ISSUE` and `EQUIPMENT_RETURN` are named as logistics-posted types in
  `PORT-AND-ADAPTER-CONTRACT.md` §9.7 and as v1-irreversible in R7 §6 item 7 — *"if the type is an enum,
  every one of them is a base release."* They are **catalogue rows with no `CHECK`**, so a missing one is
  a seed `INSERT`, never an `ALTER` — but **this task has no migration to carry it**, so take the next
  free number in the same `V500064`–`V500199` gap and record it.
- **Warehouse ships no fitment table and no trip table.** *"The fitment record itself belongs to
  logistics"* (`FR-338`). Building `wh_tyre_fitments` here is precisely how the boundary rots and how
  `tms_equipment_inventory` became a private ledger (R7 §2.2). If a fitment must be visible before
  `P6-08` ships, it is a **query over movements**, not a table.
- **The gate is a handler list, not a warehouse table.** Follow
  `BoomBarrierWebhookController.java:44-48`'s `List<T>` bean-collection registry — the settled
  multi-implementation port shape in this repo (R1 `C-039`) — **not** `@Primary`, which admits only one
  handler and would give the gate to whichever module registered first. Warehouse's handler does one
  thing: `PATCH` the dock appointment's `arrived_at` / `released_at`, which starts the **detention
  clock** — *"the charge is logistics', the clock is ours"* (R7 §2.2, `PC-`/§4.4's
  `dock.appointment.detention_started`).
- **A trailer holding stock is a `whb_locations` row of type `TRAILER`** (`WH-SC-296`). Yard **slot**
  management is logistics; a yard slot holding a **stock-bearing** trailer is not.
- **The print-template resolution must record the template *version* on the job**, not the template id —
  `wh_print_template_versions` exists because *"a label layout is never edited in place"*.
- **Returnable packaging is `item_type = PACKAGING`**, one of the four seed rows R7 §6 item 20 names as
  *"what stops `tms_equipment_inventory` and `tms_tyres` being built as private ledgers"*. Verify the row
  exists before building the balance.
- Deposit amounts are `DECIMAL(19,4)`; a balance is a quantity, `DECIMAL(18,4)` (`OD-7`).

<!-- check-design-set: table-names end -->

## Acceptance
- [ ] A shipment for a client with a client-specific carton template prints **that** layout; a house shipment prints the global one — one code path, most-specific-wins, and the resolved **version** is on the print job
- [ ] A reprint of a label printed last month reproduces last month's layout
- [ ] A tarpaulin issued to a trip and returned posts `EQUIPMENT_ISSUE` / `EQUIPMENT_RETURN` through `POST /api/warehouse/movements`, and the reservation is released by holder quad
- [ ] **No fitment table and no trip table exists under `warehouse`/`warehouse-base`** — asserted by grep in the PR
- [ ] The four fitment movement types exist as catalogue **rows** with no `CHECK`; a missing one is added by seed `INSERT`, never `ALTER`
- [ ] A gate event reaches a handler list; warehouse's handler stamps the dock appointment and starts the detention clock, and a second registered handler also receives it (proving it is not `@Primary`)
- [ ] `whb_packaging_balances` exists at a number claimed inside `V500064`–`V500199` and recorded in `DATA-MODEL.md` — **and the plan defect is recorded in `DEFECTS-FOUND.md`**
- [ ] A pallet exchange at a stop moves the counterparty balance and leaves the deposit unchanged; the balance reconciles to the sum of its entries
- [ ] **Mobile:** equipment issue/return and pallet exchange are captured on the RF Move / Ship screens — an exchange recorded at a desk the next morning is not an exchange; print-template resolution is server-side and needs no mobile surface (`D-13`)
- [ ] **Scenarios** `WH-SC-296` · plus the ids this task authors into `SCENARIO-CATALOGUE.md`

## Round-4 additions (folded from former P5-24, #157) — print-template scopes

**v2, in this task's own wave.** Former `P5-24` (`#157`) gathered every v2 junction a round-4 lens had
hung on a v1 task. On 2026-09-10 it was closed as a duplicate and split by parent table. This task is
the v2 owner of per-owner print templates (`owner_id` on the template, `F-062`), so the multi-scope
table comes here. `P2-14` owns the v1 template itself. `FR-468` is owned by `P1-05`; this increment is
its print part.

**Scope (`RG-018`).** **`wh_print_template_scopes`** — one layout for a client group or several sites,
dated per R22 §1.3, resolved most-specific-first in the `whb_allocation_rules` shape. A template with no
scope row keeps its single-scalar reading (`owner_id`, `warehouse_id`). A sub-grid on `WS-131`; no new
screen id.

**Migration `V510223`** — allocated at the fold from the app correction reserve, next after
`V510220`–`V510222`. Former `P5-24`'s `V510220` stays with `P1-14`'s inspection-plan assignments;
`GAP-REGISTER-R4.md` §4.6.

**Closes** `RG-018` (the print part; the other configuration scopes are `P0-11`'s, `P1-14`'s and
`P2-04`'s).

**Traps**

- **The resolver is data, not `if`s.** Owner group, owner and site are ranked nullable scope columns; the
  per-owner `owner_id` this task adds is the single-owner case of the same resolution, never a second path.
- **Resolution is server-side**, exactly like the rest of this task's print-template resolution; the
  mobile print action needs no change.

**Acceptance**
- [ ] `V510223` creates `wh_print_template_scopes`, dated with an `EXCLUDE` per scope
- [ ] A template scoped to two owners of one client group prints for both and not for a third; a `HOUSE` shipment still prints the global template (`RG-018`)
- [ ] **Mobile:** `none`, with the reason stated. Resolution is server-side (`D-13`)
