TITLE: [Warehouse] P1-14 · Quality inspection as a header over lines, with the rollup stated to the value
LABELS: task,warehouse,phase-p1,warehouse
---
Part of __P1__ · Module **`warehouse`** · Migrations **V510015, V510016** · Screens **WS-079 Inspection Plans**, **WS-080 Quality Inspections**

## Scope

> **Round-2 addition — `Q-006`.** This task's acceptance was **entirely happy-path** until round 2
> measured it. `WH-SC-301` is the guard: **an inspection cannot be completed while a mandatory
> criterion on any line has no result** — `422` `INSPECTION_INCOMPLETE`, naming each unrecorded
> criterion **by line number and criterion code** — and **a completed inspection cannot be
> re-opened**; a corrected verdict is a **re-inspection with its own number** against the same
> GRN, so the first verdict stays on the record. Without the first guard the mixed-result rollup
> can silently mean *"we did not look"*; without the second, a supervisor can overwrite a `FAIL`
> and leave no trace that it happened.

- **`V510015`** — `wh_inspection_plans`, `wh_inspection_plan_criteria`.
- **`V510016`** — `wh_quality_inspections`, `wh_quality_inspection_lines`,
  `wh_quality_inspection_results`.
- **A header over lines — one inspection number per GRN**, not N documents (`FR-133`, `P-029`).
- **The rollup rules stated to the value**: **all pass → `PASS`, all fail → `FAIL`, anything mixed →
  `PARTIAL`.** Written into the service, the test and the user documentation, because "mostly passed"
  is the ambiguity that produces two different answers in two screens.
- **Inspection is optional; verification always happens** (`FR-127`, owned by `P1-13`). This screen
  is the optional act.

**`DATA-MODEL.md` §2.2 (`WH-06`, `WH-07`) is the authority on all five column sets and must be
re-read immediately before the migrations are written.**

## Screens

| | WS-079 · Inspection Plans | WS-080 · Quality Inspections |
|---|---|---|
| Route | `/warehouse/inbound/inspection-plans` | `/warehouse/inbound/quality-inspections` |
| Reference | **Customer** | **Service Vehicle** |
| `gridIdentifier` | `wh_inspection_plans` | `wh_quality_inspections` |
| Filter scope | `WAREHOUSE_INSPECTION_PLAN` | `WAREHOUSE_QUALITY_INSPECTION` |
| Permission | `wh_inspection_plans:*` | `wh_quality_inspections:*` **+ `:disposition`** |
| Columns | `code`, `name`, `inspectionType` (FULL/SAMPLING/SKIP_LOT), `samplingPlan`, **`sampleSizeFormula`** (**whitelisted**), `aql`, `criterionCount`, `isActive`, audit | `inspectionNumber`, `grnNumber`, `planName`, `inspectorName`, `startedAt`, `completedAt`, **`result`** (PASS/FAIL/PARTIAL), `dispositionCode`, `inspectedQuantity`, `passedQuantity`, `failedQuantity`, audit |
| Filters | `inspectionType` select · `isActive` · `code`/`name` text | `warehouseId` → `planId` → `result` select · `inspectorId` typeahead · `dispositionCode` select · `startedFrom`/`To` `date` pair |
| Modals | Add/Edit with a **child editor over `wh_inspection_plan_criteria` — rows, not `inspection_criteria JSONB`** | Start · **Record results** (child editors over `wh_quality_inspection_lines` and the typed `wh_quality_inspection_results`) · **Disposition** (release / reject / return to supplier / scrap — **QA-role-gated**, `wh_quality_inspections:disposition`) · Complete |
| Mobile | `none` — plan configuration is a desk task (`FR-218`) | `screens/whQualityInspection` — **record results: a genuine floor task** |

**The disposition drives a stock-status movement**, through `P0-05`'s `whb_dispositions` registry and
`P0-03`'s single writer service — a status change is a **balanced two-line movement at the same
location** with a mandatory reason (`FR-103`). It never mutates a position.

## Requirements closed
`FR-133`

## Scenarios closed
`WH-SC-070`

`WH-SC-071` (a QC failure routed to a quarantine status and then dispositioned) is `v1·P2` and is
owned by `P2-01`; the inspection half is built here.

`WH-SC-301` — the completion guard and the no-silent-re-open rule (round 2, `Q-006`). It is this
task's only non-happy scenario and it is not optional: `FR-133`'s rollup is only meaningful over a
complete result set.

## Closes
`P-007` `P-029` · `Q-006`


## Traps
- **The mixed-result rollup value is stated twice in this set, differently, and this task carries one
  of the two.** `FR-133` and `WH-SC-070` say **`CONDITIONAL`**; `DATA-MODEL.md:979`,
  `BUILD-SPEC-SCREENS.md:1505` (WS-080's `result` column) and **this file, in three places** say
  **`PARTIAL`**. It is the value stored in `wh_quality_inspections.result`, so a half-applied choice
  is a data migration on a populated column. **Filed as `X-055`; it is a naming decision for the
  product owner and is not settled by this task.** Whichever word wins, all five sites change in one
  commit.

- **One inspection number per GRN, not one per GRN line.** `P-029` is explicit and the prior art's
  72-table design got it wrong. N documents per receipt means N dispositions, N approvals and a
  rollup nobody can compute.
- **The rollup must be stated to the value.** `all pass → PASS`, `all fail → FAIL`, **anything mixed
  → `PARTIAL`**. Leaving it to the implementer produces "PASS if >90%" in one place and "PARTIAL if
  any fail" in another, and both look reasonable in review.
- **`inspection_criteria JSONB` and `sampling_plan JSONB` are the prior art's shape and are
  forbidden** (`FR-383`, `P-007`, `WMS_DATABASE_DESIGN.md:99` made JSONB a convention on eleven
  business tables). Criteria are **rows** in `wh_inspection_plan_criteria`; results are **typed rows**
  in `wh_quality_inspection_results`.
- **`sample_size_formula` is whitelisted, never a free expression.** `FR-041` and
  `PORT-AND-ADAPTER-CONTRACT.md` §2.9 forbid an expression, a script or a rule DSL anywhere in this
  product — *"the fifth rule engine always introduces one."*
- **Disposition is QA-role-gated with its own verb permission.** A transition with no verb permission
  is a transition anybody with `:edit` can perform, which is the failure `FR-408` names. Register
  `wh_quality_inspections:disposition` in `P1-20`'s `V511000` with its `→ :view` dependency row.
- **The disposition posts a movement; it does not update a position.** Rejected stock moves to a
  quarantine status by a **balanced two-line movement at the same location** (`FR-103`), through the
  single writer service (`FR-436`).
- **The inspector may not be the approver where the disposition needs approval** (`FR-408`); the
  reverse dependency row (`:disposition → :view` only, never `:view → :disposition`) must not be
  created.
- **`whb_item_categories.default_inspection_plan_id` is a *generic reference*, not an FK** — the plan
  lives in `warehouse` and **base may not FK into app** (`DATA-MODEL.md` §3.3, `P1-01`'s WS-022
  note). Resolve it in the service, not in the schema.
- **Two grids × two platform files**, and `result` and `dispositionCode` are registry-backed
  selects that must **fetch** their values, never a TypeScript string union (`OD-5`, `FR-380`).

## Acceptance
- [ ] One GRN produces **one** inspection with **one** inspection number, over N lines (`WH-SC-070`)
- [ ] The rollup is exactly `all pass → PASS`, `all fail → FAIL`, **mixed → `PARTIAL`**, asserted by
      three tests
- [ ] Criteria and results are **rows**; there is **no JSONB column** on any table in this task
- [ ] `sample_size_formula` accepts only whitelisted values
- [ ] **Disposition** is gated on `wh_quality_inspections:disposition`, and rejected quantity moves to
      its target stock status as a **balanced two-line movement with a reason code**
- [ ] The disposition writes through the single writer service — asserted by the architecture test
- [ ] Verification without inspection remains possible for an item whose category has no plan
- [ ] The mobile inspection screen records results; the plan screen is `none` **with its reason
      stated**
- [ ] **Scenarios** `WH-SC-070`
