TITLE: [Warehouse] P1-05 · The facility model — warehouses, the location hierarchy and the virtual-location seed
LABELS: task,warehouse,phase-p1,warehouse-base
issue: 54
---
Part of __P1__ · Module **`warehouse-base`** · Migrations **V500012, V500013** · **V500070** (v2 increment — `whb_location_owner_dedications`, folded from former `P5-24`; `whb_warehouse_companies` moved to `P1-22`, v1) · Screens **WS-005 Location Types**, **WS-016 Warehouses (sites)**, **WS-017 Locations**

## ★ **Both migrations must land before `PNR-1` (`V500030`, `P0-02`)**

`whb_stock_movement_lines.location_id` is **`NOT NULL`** and `whb_stock_movements.warehouse_id` is
**`NOT NULL`** (`IRREVERSIBLE.md` §3.4). `DATA-MODEL.md` `WHB-13` is explicit:

> **★ `V500013` must precede `V500030`** — `IRR-05` requires **virtual locations to exist before the
> first movement can balance**, and `whb_stock_movement_lines.location_id` is `NOT NULL`. ★

**`location_id NOT NULL` *is* `L-1`.** A receipt's counter-side is a virtual location, never an
absent row — without the seed, the very first movement cannot be written. **`DATA-MODEL.md` §2.1 is
the authority on the column sets of `whb_warehouses`, `whb_locations` and
`whb_location_external_refs` and must be re-read immediately before the migrations are written**;
the `IRR` rows they carry are `IRR-05`, `IRR-16`, `IRR-17`, `IRR-29`, `IRR-30`, `IRR-44`, `IRR-57`, `IRR-58`, `IRR-26`,
and — round 4 — `IRR-64` (the dated `REGISTERED` history, `RG-001`) and `IRR-65` (location custody history,
`RG-004`). **`V500012` now creates three tables, not one** — `whb_warehouse_branch_roles`,
`whb_warehouses` and `whb_warehouse_branches` — and every one of them precedes `PNR-1` (`RG-001`).

## Scope

### Adopted solution and configuration contract — 2026-09-11

Apply this task’s relevant behavior from [Global settings and resolved behaviour](https://github.com/neetub1508/warehouse-issues/blob/main/docs/GLOBAL-SETTINGS-DECISIONS.md), adopted by DECISIONS. This resolves earlier open-question/blocked-on wording for the decisions it names; external capability availability remains a testable gate. Preserve this task's existing module, migration and requirement ownership. P0-15/P1-20 own the shared settings implementation; other tasks consume it. Do not build unrelated settings controls or later-version capabilities here.

**OD-19 solution:** Changing registered branch across GSTINs is allowed only with zero on-hand and no open reservations, in-flight transfers, counts or unposted warehouse documents tied to the old registration. Otherwise refuse with actionable dependency links; use the existing transfer/settlement workflow. Maker-checker and effective history are mandatory; there is no bypass switch.


> **Round-2 addition — `Z-004` · `Z-006`.** **Two round-2 additions, both about what happens on
> day 400 rather than day 1.**
>
> **`Z-004` — the facility fields that stop being editable.** `BUILD-SPEC-SCREENS.md` §0.12 is the
> rule; this task owns the location half. `whb_locations.code` is **frozen once a movement in the
> current period exists** — `FR-034`'s own trap is *"a scan gun syncing 400 movements after a
> shift must not lose 399 because one bin was renamed"*, so the design already assumes renames
> happen and has to say what one does — and `location_type_code` with the whole capacity block is
> **frozen once stock is on hand**, because lowering capacity below current contents, or flipping
> `is_stock_holding` under stock, has no defined outcome otherwise. Both are service-enforced,
> both render read-only with a tooltip naming the reason.
>
> **`Z-006` — deactivating a site is a screen, not a queue of refusals.** Deactivating a
> `whb_warehouses` row runs a **closure pre-check** that returns **every** blocking class at once
> — on-hand stock, in-transit inbound, open receipts and orders, open tasks and reservations, an
> active number series, warehouse-scoped user grants, assigned devices — each with a count and a
> link. A site is closed over weeks by a team working the list down; discovering the seven
> blockers one refusal at a time, each after clearing the last, is the failure this replaces.

> **Round-2 addition — `Y-007` · `O-005`.** **Two corrections to this task's premises.** (1)
> **Deactivation is guarded by stock, not by convention.** Modelled on `FR-051` verbatim so the
> message shape matches: *deactivating a location, an owner or a warehouse that holds non-zero
> on-hand across any item, status or lot is blocked, naming the item, quantity and lot; the
> offered alternative is `FR-091`'s block status. Merging two locations requires the source to be
> empty.* Recorded as a **service guard, not a trigger**, for `FR-051`'s stated reason, with a
> companion scenario in the `WH-SC-259` shape. (2) **`branches.owner_type` no longer exists** —
> platform `V160:13,16` dropped `owner_type`/`owner_id` and `:19` made `branch_code` **globally
> unique** (`uk_branches_code`). `branch_type`'s `CHECK` still admits `'WAREHOUSE'` and
> `'DISTRIBUTION_CENTER'` (`V149:61`), so the site *type* is free, but there is no owner dimension
> left to scope on and a warehouse site's branch code competes with every dealer showroom's:
> validate uniqueness against **all** branches, not only warehouse ones (`PD-1`, `MI-4`). The same
> correction is folded into `P0-06`.

> **Round-4 addition — `RG-001` · `RH-001` · `RH-003` · `RH-004` · `RH-005` · `RJ-001` — the
> branch↔warehouse junction (`D-14`, `FR-460`).** **This reverses `FR-079`'s *"belongs to exactly one
> branch"* by user decision.** A site is **linked** to platform branches through a dated M:N junction
> with a role, and has **exactly one `REGISTERED` branch at every instant** — the only source of its
> GSTIN, its branch-scoped series and its tax attribution (`RG-001`, `RH-005`). `RG-001` is canonical;
> `RH-001`, `RH-005` and `RJ-001` merge into it (`GAP-REGISTER-R4.md` §3.1).
>
> - **Registry 16 `whb_warehouse_branch_roles`** (`V500012`, §2.1.1 shape): behaviour columns
>   `is_registration`, `grants_branch_visibility`, `allows_issue_to_branch`, `allows_returns_from_branch`,
>   `is_fulfilment_source`, `max_current_per_warehouse` (1 for `REGISTERED`, null = many). Seed
>   `REGISTERED` (`is_system`, `is_registration`), `SERVING`, `FULFILMENT`, `RETURNS`. **The `REGISTERED`
>   code is immutable** — the partial index and the exclusion predicate carry the literal, so the FK from
>   the junction is declared **without** `ON UPDATE CASCADE` (guard 4, `RG-001`, `RL-013`).
> - **`whb_warehouse_branches`** (`V500012`): `warehouse_id → whb_warehouses ON DELETE RESTRICT`,
>   `branch_id ↓platform branches(id)` (crossing FK `B1` moves here), `relationship_role_code →
>   whb_warehouse_branch_roles(code)`, `effective_from TIMESTAMPTZ NOT NULL`, `effective_to TIMESTAMPTZ`
>   (half-open `[from, to)`), `is_primary` (the **branch-side** default for `FULFILMENT`/`RETURNS`),
>   `priority`, `change_reason_code_id → whb_reason_codes`, `decision_note`. **No `is_active`** — R22 §1.3
>   rule 3 (`RG-001`). Never deleted once a movement stands in its range; a change closes one row and opens
>   the next.
> - **Constraints, in `V500012` beside `whb_warehouses`** (R22 §1.2.2): `CREATE EXTENSION IF NOT EXISTS
>   btree_gist` **and a `DO` block that raises if it is absent**; `uk_whb_warehouse_branches_one_registered`
>   (partial, `relationship_role_code = 'REGISTERED' AND effective_to IS NULL`);
>   `chk_whb_warehouse_branches_range`; `EXCLUDE` — no two `REGISTERED` ranges overlap per warehouse
>   (*one at every instant*, not only now); `EXCLUDE` — no two ranges overlap per `(warehouse, branch,
>   role)`; partial uk one current primary per `(branch, role)`; idx(`branch_id`,`warehouse_id`) `WHERE
>   effective_to IS NULL`; idx(`warehouse_id`,`effective_from`) (`RG-001`).
> - **The guards this task owns** (`RG-001`): **guard 1** — *exactly*, not *at most*: a deferred constraint
>   trigger on `whb_warehouses` and `whb_warehouse_branches` asserts every active warehouse ends the
>   transaction with a current `REGISTERED` row; the create service writes the site and its link in one
>   transaction. *Superseded by `P1-22` (`D-14` item 8g): no at-least-one trigger and no link at create —
>   a site with no current `REGISTERED` row is refused when used.* **Guard 3's append-only trigger reads the ledger, so it is `I-23` in `P0-02`'s
>   `V500037`**; guard 2 (a movement at an unregistered instant) is `I-22` in `V500030`. **Guard 4** — the
>   immutable role code, above.
> - **Jurisdiction stays out of base (`D-8`).** Base exposes a `List<WarehouseBranchLinkValidator>` bean
>   collection (the `IBoomBarrierHandler` pattern); `warehouse-india` contributes three validators in
>   `P2-IN-01` — GSTIN state = `state_code`, profile belongs to the company, the change rule (`RG-001`).
> - **Company anchor (`RH-004`).** Saving a `REGISTERED` link to a branch not in `whb_company_branches`
>   (`P0-07`) for any current company of the site (`whb_warehouse_companies`, `P1-22`) is refused with **`422 WAREHOUSE_BRANCH_COMPANY_MISMATCH`**.
>   `WS-016`'s branch options come from a **warehouse endpoint** returning *my-branches ∩ the company's
>   branches*, intersected in the backend — never a frontend `.filter()`.
> - **Change registration — the one hard workflow** (R22 §1.2.6, `RG-001`). Verb
>   `warehouse:warehouses:change_registration` (seeded by `P0-15`'s `V501000`), maker–checker (`FR-408`).
>   The service refuses an `effective_from` in a `CLOSED` period or earlier than the site's latest posted
>   `occurred_at`; **refuses while the site holds non-zero on-hand if the old and new GSTINs differ** —
>   the `OD-19` safe default (see Blocked on); closes the current row and opens the next **at the same
>   instant**; writes a `whb_audit_events` row and prompts for the new branch-scoped series (`P1-09`).
> - **Classification reads the link at `occurred_at`; access reads today's links** (R22 §1.3 rule 5,
>   `RJ-001` with §3.7(b) — never `posting_date`). A non-physical site (`FR-081`) still carries a
>   `REGISTERED` row before it is used, because a site with none is refused (`RG-001`, `D-14` item 8g).
>
> **Link, never mirror (`RH-003`, `D-14` item 5).** `whb_warehouses.code` is its own namespace — never
> derived from, copied to or validated against `branch_code` (R14's refusal stands). **One exception:**
> when a site is itself a GST place of business that **no existing branch carries**, the administrator
> first creates an ordinary branch (type `WAREHOUSE` or `DISTRIBUTION_CENTER`, carrying `gst_number`) and
> makes it the site's `REGISTERED` link. That branch is the tax anchor; the warehouse stays the stock
> anchor.

- **`V500012`** — `whb_warehouses`, **plus `whb_warehouse_branch_roles` and `whb_warehouse_branches`
  above**. `whb_warehouses` **no longer carries `branch_id`, `tax_registration_id` or
  `legal_entity_id`**, and `idx(branch_id)` goes (`RG-001`): the branch and the GSTIN are read through
  the `REGISTERED` link, never stored on the site. Structured address (line 1/2, city, `state_code` — the
  site's own address fact, no longer derived from a GSTIN — pincode, country, latitude, longitude),
  **`gln`**, **`timezone`** (`FR-080`, `FR-439`). **`timezone` is validated against platform
  `timezones`** (`PM/V55:31`) on save — a mistyped `Asia/Kolkatta` would bucket the site's local day in
  UTC (`RH-011`). **Keys: `uk(company_id, code)` only** — the install-wide `uk(code)` is dropped
  (`RL-004`). *Superseded by `P1-22`:* `uk(code)` install-wide, and `whb_warehouses.company_id` dropped
  (`D-14` item 8c–d).
- **`is_physical`** (`FR-081`) — *"the single most important column for the logistics seam"*: it
  permits a **warehouse row that maps to no building** — a carrier, a trip, a transit pool.
- **`V500013`** — `whb_locations`, `whb_location_external_refs`, **and the virtual-location seed**.
- **The self-referencing hierarchy** (`FR-082`): site → building → zone → aisle → rack → level →
  position, with `location_level` and a materialised path.
- **`location_type` as a registry with behaviour flags** (`FR-083`) — the table is `P0-05`'s
  `V500006`; this task seeds the **locations**.
- **The virtual-location seed, per install and per site** (`FR-084`, `RE-002`): `SUPPLIER`, `CUSTOMER`,
  `ADJUSTMENT`, `SCRAP`, `PRODUCTION`, `IN_TRANSIT`, `COUNT_VARIANCE`, `OPENING_BALANCE`, `CONSUMED`,
  `JOB_WORKER` — `FR-084`'s ten — plus **`VALUE_OFFSET`** (`OD-11`, adopted — see `P0-02`) and
  **`COST_OF_SALES`** (`RF-003`), each coded `VIRT-<PURPOSE>-<SITE CODE>`. **Creating a `whb_warehouses`
  row creates its virtual-location set in the same transaction**, in the warehouse service, and
  `WS-016`'s create modal states it (`RE-002`).
- **Transit locations per reference, never one global bucket** (`FR-085`): per transfer, per trip, so
  two consignments on the road are **separately countable, adjustable, ageable and attributable**.
  **A transfer's transit location is a child of the *source* warehouse**, location type `IN_TRANSIT`,
  created per transfer at dispatch (`FR-147`, `FR-148`, `RJ-002`). **There is no `TRANSIT-WH` site** —
  this task seeds the `IN_TRANSIT` location *type* only, which is all `RE-002`'s transit-site seeding
  shrinks to. In-transit value stays in the sender's grain (`FR-236`) and resolves to the source site's
  `REGISTERED` link (`GAP-REGISTER-R4.md` §3.4).
- **Capacity and constraint columns in v1** (`FR-086`): weight, volume, height, LPN count, unit count,
  mixed-item, mixed-lot, mixed-owner, temperature zone.
- **`commingle_policy`** ∈ {`FREE`, `SINGLE_OWNER`, `SINGLE_ITEM`, `SINGLE_LOT`, `SINGLE_LPN`} **and
  `dedicated_owner_id`** (`FR-087`); the port rejects a violating move.
- **Custody through `whb_location_user_assignments`, with `MOBILE` and `VEHICLE` types** (`FR-088`) —
  **van stock is the same object as a delivery vehicle's load.** Without it, van stock is a second
  inventory model. **`whb_locations.assigned_user_id` is dropped** (`RG-004`): a reassigned scalar says
  only who holds the van now, not who held it when last Tuesday's shortage left. `V500013` creates
  `whb_location_user_assignments` — `location_id`, `user_id ↓platform users(id)` (crossing FK `B2` moves
  here), `assignment_role` from the **`CUSTODY_ROLE` code list** (`CUSTODIAN`/`DRIVER`/`HELPER`, `P0-05`,
  `RL-006`), `effective_from`/`effective_to` — with **one current `CUSTODIAN` per location** (partial uk)
  and an `EXCLUDE` on the `CUSTODIAN` range. `whb_location_types.requires_assigned_user` now means
  *requires a current `CUSTODIAN`*. `P3-20`'s `whaf_van_stock_assignments` FKs down to the base row
  (`RG-004`).
- **`whb_location_external_refs`** (`FR-090`) — the **dual identity** of a vehicle: a location row in
  the ledger and an asset row in its own module, **neither the other's master**.
- **Location status and blocking** (`FR-091`): available / blocked / counting / damaged / frozen, with
  a mandatory reason, so maintenance and counting do not require moving stock. **The `status` ladder,
  with its setters** (`RJ-006`): *Block*/*Unblock* toggle `AVAILABLE ↔ BLOCKED` (and `DAMAGED`); a count
  **Freeze** sets `COUNTING` and records the prior status; the count's **Post or Cancel restores the
  prior status** (`P2-04`); **`FROZEN` is set only by the stocktake window** (`FR-157`) and cleared when
  it closes. No state is left without a way out — a cancelled count must never strand bins unpickable
  (`WH-SC-192`).
- **Codes are unique within the site** (`RL-004`, option (b)): `whb_locations` keeps
  **`uk(warehouse_id, code)` only** — the install-wide `uk(code)` is dropped, so the second site can run
  the same bin mask (`P1-06`). Scan resolution is scoped by the session's or device's site, and a
  cross-site scan returns **`AMBIGUOUS_LOCATION`** (`P1-02`). `RE-002`'s `VIRT-<PURPOSE>-<SITE CODE>`
  convention stands as naming, no longer a key workaround.
- **No `fixed_item_id` on `whb_locations`** (`RG-008`): the fixed pick face is
  `whb_item_location_settings.is_fixed`, dated, in `P1-02`'s `V500016`.
- **Occupancy derived at read time** (`FR-093`), **never a maintained counter**; exclusivity by
  **partial unique index**, not application logic.

> **Round-3 fold — `RF-003` · `RB-002` · `RA-001` · `RB-007` (`GAP-REGISTER-R3.md` §2.6, §2.2, §2.1,
> §4.2).**
> (1) **`V500013` seeds a cost-of-sales virtual location per site** (`RF-003`) — the one seed row
> `P2-17`'s header amendment puts in this migration. Without it, `WH-SC-153`'s v1 happy path posts
> ₹4,500 into stock while its own second sentence puts ₹1,800 in COGS. With it, the landed-cost movement
> is `+2,700` at the receipt location, `+1,800` at `VIRT-COST_OF_SALES-<SITE CODE>` and `−4,500` value
> offset — three lines, quantity and value both conserved. It travels with `RE-002`'s set. The split
> columns on `wh_landed_cost_allocations` are `P2-17`'s.
> (2) **`WS-017`'s three fields with no column** (`RB-002`, taking the register's recommended answers
> as the documented default): **`check_digit` is added to `whb_locations` in `V500013`** — a real GS1
> location-label need, and after `V500030` a schema change on a table the ledger FKs into — and the
> **`isPickable` and `isReceivable` filters are deleted**: pickability at a location is `status`, and
> receivability is an item fact. `WS-017`'s six renames and `commingePolicy` are
> `BUILD-SPEC-SCREENS.md`'s edit.
> (3) **The closure pre-check's *warehouse-scoped user grants* class is `whb_warehouse_grants`**,
> linking to **`WS-238` Warehouse Grants** (`RA-001`, `P1-18`, `V500047`).
> (4) **`WS-016` and `WS-017` carry the Department reference's `/validate/{code|name}` pair**
> (`RB-007`), debounced at ≥300 ms on blur. Location and warehouse `code` is unique within its scope,
> not install-wide (`RL-004`), so the call is scoped the same way; `WS-015`'s partial-unique
> `is_default` is `P0-07`'s.

## Screens

| | WS-005 · Location Types | WS-016 · Warehouses | WS-017 · Locations |
|---|---|---|---|
| Route | `/warehouse/catalogues/location-types` | `/warehouse/masters/warehouses` | `/warehouse/masters/locations` |
| Reference | **Department** | **Customer** (company → branch cascade) | **Customer** |
| `gridIdentifier` | `whb_location_types` | `whb_warehouses` | `whb_locations` |
| Filter scope | `WAREHOUSE_LOCATION_TYPE` | `WAREHOUSE_WAREHOUSE` | `WAREHOUSE_LOCATION` |
| Caches | `dropdown.whbLocationType` | `dropdown.whbWarehouse` | `dropdown.whbLocation` |
| Key columns | `is_physical`, `is_stock_holding`, `is_virtual_counterparty`, `is_mobile`, `is_transit`, `allows_mixed_owner`, `requires_assigned_user`, `default_location_level` | `code`, `name`, `companyName`, **`registeredBranchName`** (**`branches.branch_name` of the current `REGISTERED` link**, `RG-001`), **`linkedBranchCount`** (*Linked branches*, `RH-001`), `warehouseType`, **`isPhysical`**, `city`, `stateCode`, **`gstin` read through the `REGISTERED` link**, `timezone` (select from platform `timezones`, `RH-011`), `gln`, `orderCutoffTime`, `hasPicking`, `isActive` | `code`, `name`, `warehouseName`, `parentLocationCode`, `locationLevel`, `locationTypeCode`, capacity block, `commingePolicy`, `dedicatedOwnerName`, **`custodianName`** (the current `CUSTODIAN`, `RG-004`), `status`, `barcode`, `checkDigit`, `gln`, `countsAsOnHand` |
| Filters | `is_physical` · `is_transit` | `companyId` → **`branchId`** (*linked to*, not *registered under*; options from the warehouse endpoint — my-branches ∩ the company's branches, `RH-004`) → **`relationshipRoleCode`** (in the `WAREHOUSE_WAREHOUSE` allowlist, `RG-001`) · `warehouseType` · `isPhysical` · `stateCode` · `isActive` · `code`/`name` | `warehouseId` → `parentLocationId` (subtree picker) → `locationLevel` · `locationTypeCode` · `status` · `commingePolicy` · `dedicatedOwnerId` · `isPickable` · `isReceivable` · `code` text · `barcode` text |
| Actions | Add/Edit/Deactivate | View · Edit · Deactivate (**refused while any position for the site is non-zero**) · **Generate locations** (opens WS-018, `P1-06`) · **End link** · **Change registration** (`warehouse:warehouses:change_registration`, maker–checker, `RG-001`) | View · Edit · **Block** / **Unblock** (own modals, **mandatory reason**, `whb_locations:block`) · **Print location label** |
| Export | — | the grid columns plus **`registeredBranchName`** and **`linkedBranchNames`** (`RG-001`) | the grid columns |
| Mobile | `none` | `screens/whbWarehouse` — **read-only list + detail**; every RF screen needs a site picker and the picker reads this. The picker is `useScopedBranchOptions` → branch → `GET /warehouses/dropdown?branchId=`, returning sites with a **current link of a visibility role**, `REGISTERED` sites first with a role badge; `additionalFilters.branchId` means *linked to* (R22 §1.2.4 row 15, `RG-001`) | `screens/whbLocation` — read + **Block/Unblock only**; the scan input resolves a scanned code straight to the detail, **scoped by the device's site** (`RL-004`) |

**WS-016's modal is multi-tab** (8 groups > 6): *Identity* · *Company & branch* · *Address* · *Geo* ·
*Tax identity* (**GSTIN read-only through the `REGISTERED` link**) · *Operations* · *Status* ·
**_Branches_** — a sub-grid of `whb_warehouse_branches` (branch, role, from, to, primary) with *End link*
and *Change registration* (`RG-001`). **A site cannot be saved without a `REGISTERED` row** (guard 1).
*Superseded by `P1-22` (`D-14` items 8b, 8g, 8h): the Branches sub-grid and the Company & branch tab leave
Add/Edit, a site saves with no link, and links come from row-action popups copied from their twins.*
**WS-017's** is multi-tab too: *Identity* · *Hierarchy* · *Type & behaviour* · *Capacity* ·
*Commingle & ownership* · *Scanning* · *Status & blocking*, with a child `DataTable` of
`whb_location_external_refs` (**no grid of its own**), a **_Custody_** sub-grid of
`whb_location_user_assignments` (user, role, from, to, `RG-004`) and a live on-hand summary.
*Superseded by `P1-22` (`D-14` item 8b):* WS-016's *Branches* tab and WS-017's *Custody* sub-grid become
**Branches** and **Custody** row actions with assignment modals, and WS-016 gains a **Companies** row
action; the View modals keep the read-only link history.

## Requirements closed
`FR-079` `FR-080` `FR-081` `FR-082` `FR-083` `FR-084` `FR-085` `FR-086` `FR-087` `FR-088` `FR-090`
`FR-091` `FR-093` `FR-460` `FR-468`

`FR-460` is round 4's junction requirement (`RG-001`, `D-14`); `FR-079` is rewritten by it, and still
closed here.

`FR-086` is multi-phase (`P1·P3`): schema here, capacity **enforcement** in `P3-02` (plan §8.2).

## Scenarios closed
`WH-SC-045` `WH-SC-076` `WH-SC-125` `WH-SC-271` `WH-SC-272` `WH-SC-273` `WH-SC-306`

## Closes
`C-016` `C-029` `C-030` `C-033` · `P-011` · `G-050` · `Y-007` `O-005` · `Z-004` `Z-006` ·
`RG-001` `RG-004` `RG-025` · `RH-003` `RH-011` · `RJ-002` · `RG-012` `RG-014` · `RE-002` `RF-003` `RB-002` `RA-001` `RB-007`

*Round 4 (`GAP-REGISTER-R4.md` §2): `C-016`/`C-030`'s advice — one branch, no junction — is **reversed by
`D-14`**, not re-closed; they stay listed because this file is where the reversal is carried. `E-048` and
`S-022` stay closed: the site is still bound to a branch that carries the GSTIN, now with history.*



*Added 2026-09-02 (`GAP-REGISTER.md` §5.2). These were `COVERED-uncited` **BLOCKER**s — owned only through an FRD row's `Closes` column, so a builder reading this file alone never met them:*
`E-048` `G-017`

## Traps

- **`branches.branch_name`, not `branches.name`.** Every join from a warehouse screen onto the
  platform branch table uses `branch_name` (`V149:18`, R1 §8 trap `T-18`, CLAUDE.md). This bites
  WS-016's grid, its export and its filter dropdown.
- **A warehouse is linked to branches, never mirrored as one** (`RH-003`). `branch_type` still admits
  `'WAREHOUSE'` and `'DISTRIBUTION_CENTER'` (`V149:61`, `:63`), but **do not create a branch row per
  site**: `branch_code` is one global namespace (`PM/V160:19`), 154 of 254 `useUserBranches(...)` calls
  pass no `branchType` and would fill every showroom picker with godowns, and a branch row drags in
  `branch_staff`, holiday and working-hours configuration. The **only** exception is a site that is itself
  a GST place of business no branch carries (Scope, `D-14` item 5). `PLATFORM-DEPENDENCIES.md` §1.1's
  *"a site **is** a branch"* is wrong under any cardinality.
- **The junction is the model — but do not copy accessories' version of it** (`RH-001`, `RG-001`,
  replacing round 1's *"do not repeat the accessories junction"*). Accessories' junction
  (`accessory_warehouse_branch`, `V30017:8-31`, `V30018`) failed for having **no role and no dating** and
  `ON DELETE CASCADE` on both FKs, not for being M:N. Two things of it are refused **by name**:
  its `is_primary` means *"the primary warehouse **for the branch**"* (`V30018:35`) — the opposite axis
  to *"the `REGISTERED` branch **of the warehouse**"*; and its guard's *"unassigned warehouses stay
  shared"* arm (`AccessoryStockTransferAccessGuard.java:186-187`) — **an unlinked site is visible to no
  branch-scoped user** (`P1-18`).
- **A partial unique index gives *at most one*, not *exactly one*** (`RG-001`). The *at least one* half
  is guard 1's deferred constraint trigger; forgetting it lets a site exist with no registration, and
  `I-22` then refuses its first movement with no screen that explains why. *Superseded by `P1-22`
  (`D-14` item 8g): there is no at-least-one trigger; the use-time field-level `422` is that screen.*
- **`btree_gist` is not guaranteed in a standalone install** (`D-7`): accounting-base and assets are the
  only modules that create it (`V600030:72`, `V60634:29`). `V500012` declares it itself and **fails
  loudly** if it is absent, as `V60634:427-429` does (`RG-001`).
- **Physical containment stays scalar** (`RG-025`, KEEP-SCALAR). `whb_locations.warehouse_id` and
  `parent_location_id` are containment: a location in two warehouses would make `I-4`'s per-warehouse
  gapless `sequence_no`, the denormalised `whb_stock_positions.warehouse_id` and `IRR-17`'s one-warehouse
  movement header all ambiguous. Cross-cutting zones are `whb_location_zone_memberships` (`RG-015`,
  `P3-06`), never a second parent.
- **Bins in `accessories` are flat free-text.** `zone`/`aisle`/`rack`/`level` are VARCHARs on
  `accessory_storage_bins` with no zone entity, and `max_weight`/`max_volume`/`capacity` are stored
  and **never enforced** (`V30033:16-24`, `V30017:19`, `C-029`). This task builds the real hierarchy;
  `P3-02` enforces the capacity.
- **Occupancy is derived, never cached.** The repo's best model is
  `pdi_storage_slot_assignments` — append-only, released rows retained, exclusivity by **partial
  unique index**, occupancy computed at read time and **deliberately not cached**
  (`dealer/V20735:8`, `:43-52`, `:57-69`; `PdiYardStorageLocationService.java:67`) (`C-033`).
  Copy that shape. `pdi_yard_storage_locations.current_occupancy` is the counter-example: a column
  left unmaintained.
- **A partial unique index cannot be `DEFERRABLE` in PostgreSQL** — recorded in four files
  (`accounting-base/V600001:150`, `V600004:111`, `V600120:90`, `insurance-360/V120175:35`) — **so the
  write path owes an explicit `flush()`.**
- **One global `IN_TRANSIT` bucket is the failure `FR-085` names.** Two consignments on the road merge
  into one number that nobody can age, count or attribute. Transit locations are **per reference**.
- **`is_physical` is what makes the logistics seam possible.** A vehicle is a location row **and** an
  asset row, and neither is the other's master (`FR-339`, `P1-11`). `OD-2` — whether dealer's vehicle
  inventory migrates onto this ledger — is **v3 planning, not before**; model it as a documented
  future adapter and **prove the ledger on parts first** (`G-050`).
- **`useUserBranches` from `@platform/hooks/useUserBranches`** — never an ad-hoc branch fetch
  (CLAUDE.md Filter Rules). **Except `WS-016`'s company → branch cascade**, which has nothing to cascade
  on in platform: `branches` has no `company_id` and `/branches/my-branches` takes no company parameter
  (`BranchesController.java:578-593`). Its options come from the warehouse endpoint that intersects
  my-branches with `whb_company_branches` **in the backend** (`RH-004`); a frontend `.filter()` is the
  CLAUDE.md rule 8 violation.
- **Deactivating a site must check every position row for it**, including non-`OWN` owners and
  non-`AVAILABLE` statuses.
- **Three grids × two platform files**, and `WAREHOUSE_LOCATION` carries a subtree picker whose
  cascade root is `warehouseId` — the child keys must be in the allowlist too or the cascade silently
  does nothing.


## Acceptance

- [ ] `CONFIG-CASE-01` in the adopted contract passes for this task's owned workflow only, including its refusal/retry behavior; reuse shared infrastructure tests and record evidence in the implementation PR.
- [ ] `CONFIG-CASE-11` in the adopted contract passes for this task's owned workflow only, including its refusal/retry behavior; reuse shared infrastructure tests and record evidence in the implementation PR.
- [ ] No setting rewrites posted history, bypasses permissions/period locks or silently enables an unavailable capability.
- [ ] `V500012` and `V500013` both run **before** `V500030`, and the virtual-location seed exists
      before the first movement
- [ ] `whb_warehouses` has **no** `branch_id`, `tax_registration_id` or `legal_entity_id`; the site's
      branch and **GSTIN are read through its current `REGISTERED` link**, never stored (`RG-001`,
      `WH-SC-045`, `WH-SC-271`)
- [ ] `whb_warehouse_branch_roles` (registry 16) is seeded `REGISTERED` · `SERVING` · `FULFILMENT` ·
      `RETURNS`; the `REGISTERED` code refuses an edit, and the junction FK carries no `ON UPDATE
      CASCADE` (`RG-001`)
- [ ] **Exactly one `REGISTERED` link at every instant**: a second current one is refused by the partial
      index and a `SERVING` link is offered instead (`WH-SC-045`); overlapping `REGISTERED` ranges are
      refused by the `EXCLUDE`; a transaction that leaves an active site with none is refused by the
      deferred trigger; `WS-016` cannot save a site without one (`RG-001`). *Superseded by `P1-22` (`D-14`
      item 8g): no deferred trigger (`V500080` drops it), a site saves with no link, and the `REGISTERED`
      link is set and changed only by *Change registration*, never from the **Branches** popup*
- [ ] `V500012` creates `btree_gist` if absent and **raises** when it cannot (`RG-001`)
- [ ] *Change registration* is maker–checker; it is **refused while the site holds stock and the GSTINs
      differ**, accepted once the site is emptied by transfer, and closes the old row and opens the next
      **at one instant**, with an audit row (`RG-001`, `OD-19` default, `WH-SC-306`)
- [ ] A `REGISTERED` link to a branch not in `whb_company_branches` for any current company of the site (`P1-22`) returns
      **`422 WAREHOUSE_BRANCH_COMPANY_MISMATCH`**, and `WS-016`'s branch options come from the backend
      intersection (`RH-004`)
- [ ] `warehouse-india`'s `WarehouseBranchLinkValidator`s run on every link save through the base bean
      collection; base contains no GSTIN rule (`RG-001`, `D-8`)
- [ ] No warehouse creates a platform `branches` row, and `whb_warehouses.code` is never validated
      against `branch_code` (`RH-003`)
- [ ] `timezone` is stored per site and drives local-day bucketing — "the 30th" is a different day at
      two sites (`FR-439`); a value absent from platform `timezones` is refused (`RH-011`)
- [ ] `is_physical = false` permits a warehouse row that maps to no building (`WH-SC-273`)
- [ ] A transit location is created **per transfer reference**; two consignments on the road are two
      separately countable rows (`WH-SC-125`)
- [ ] That transit location is an `IN_TRANSIT`-type child **of the source warehouse**; no `TRANSIT-WH`
      site is seeded or referenced (`RJ-002`)
- [ ] A move violating `commingle_policy` or `dedicated_owner_id` is **rejected by the port**
      (`WH-SC-272`)
- [ ] A blocked location refuses putaway and pick, with the reason visible, and unblocking is a
      separate permissioned action (`WH-SC-076`)
- [ ] Occupancy is computed at read time; there is **no maintained occupancy counter column**
- [ ] A current `CUSTODIAN` in `whb_location_user_assignments` + a `VEHICLE` location type make van
      stock the same object as a delivery load, with no second model; a reassignment closes one row and
      opens the next, so *who held it on a past date* is answerable; a second current `CUSTODIAN` is
      refused; `whb_locations` has no `assigned_user_id` (`RG-004`)
- [ ] The location `status` ladder holds: Freeze sets `COUNTING`, the count's Post or Cancel restores the
      prior status, `FROZEN` is set only by the stocktake window — no state without an inbound transition,
      no non-terminal state without an outbound one (`RJ-006`)
- [ ] `whb_locations` and `whb_warehouses` carry no install-wide `uk(code)`: the same bin mask runs at a
      second site, and a scanned code resolves within the session's site or returns
      `AMBIGUOUS_LOCATION` (`RL-004`)
- [ ] Three grids, three filter scopes, `useUserBranches` for branch dropdowns other than `WS-016`'s
      company cascade, `branch_name` in every join; `relationshipRoleCode` is in the
      `WAREHOUSE_WAREHOUSE` allowlist and the export carries `registeredBranchName` and
      `linkedBranchNames` (`RG-001`)
- [ ] `whb_locations.code` refuses an edit once a movement exists in the current period, and
      `location_type_code` plus the capacity block refuse one once stock is on hand — each
      read-only in the modal with a tooltip naming the reason (`Z-004`, §0.12)
- [ ] Every field on `WS-016` and `WS-017` carries a `Frozen when` value in the screen block
      (`Z-004`)
- [ ] Deactivating a warehouse returns **all** blocking classes in one response — on-hand stock,
      in-transit inbound, open receipts and orders, open tasks and reservations, active number
      series, warehouse-scoped user grants, assigned devices — with a count and a link per class,
      rendered as a closure pre-check screen rather than one refusal at a time (`Z-006`)
- [ ] **Scenarios** `WH-SC-045` · `WH-SC-076` · `WH-SC-125` · `WH-SC-271` · `WH-SC-272` · `WH-SC-273` · `WH-SC-306`
- [ ] `V500013` seeds, per site, `FR-084`'s ten purposes plus `VALUE_OFFSET` and `COST_OF_SALES`, each
      coded `VIRT-<PURPOSE>-<SITE CODE>`, and creating a warehouse creates the same set in the same
      transaction (`RE-002`, `RF-003`)
- [ ] `whb_locations` carries `check_digit`, and `WS-017` has no `isPickable` or `isReceivable` filter
      (`RB-002`)
- [ ] The closure pre-check's grants class counts `whb_warehouse_grants` rows and links to `WS-238`
      (`RA-001`)
- [ ] `WS-016` and `WS-017` validate `code`/`name` debounced on blur, scoped as the key is (`RB-007`,
      `RL-004`)

## Blocked on
**`OD-11`** — do value-only movements conserve value? The recommendation adds a **`VALUE_OFFSET`
virtual location**, and the virtual-location seed is **this task's `V500013`**. If `OD-11` resolves
after `V500013` has run, the row can still be inserted (locations are mutable master data, `PNR-3`
not `PNR-2`) — but the invariant that reads it is a **trigger on the ledger**, and that is `PNR-2`.
**Answer `OD-11` before `P0-02`, and seed the location here.**

**Also `OD-13`** — the offset location's *code*. `OD-11`/`P2-28` say `VALUE_OFFSET`, `PC-12` says
`LANDED_COST_OFFSET`, and `FR-084`'s seeded list contains neither. `OD-13` recommends `VALUE_OFFSET`
and its deadline **is this task's `V500013`**, because this is the migration that seeds the row.

**Also `OD-19`** (round 4, `RG-001`) — the statutory treatment of on-hand stock when a site's
`REGISTERED` branch moves to a branch under a different GSTIN. **It gates only the *Change registration*
action on `WS-016`, not `V500012`**: the history table is correct under every answer. Until it is
answered the **safe default ships**: the change is refused while the site holds any stock under a
different GSTIN, and the operator empties the site by transfer first. If an adviser rules otherwise,
that stock refusal in the change service is relaxed with no schema change.

## Owed to `P0-07` — the `whb_stock_periods.warehouse_id` foreign key

**`V500019` (`P0-07`) deliberately ships `whb_stock_periods.warehouse_id` as a bare `UUID` with no
foreign key**, because `whb_warehouses` is created by **this task's `V500012`** and `V500012` sorts
*below* `V500019`: a constraint written in `V500019` would reference a table Flyway has not created
yet and the backend would crash-loop on first start. Deferring it was the only way `P0-07` could run
at all.

**So this task owes the constraint, and it cannot go in `V500012` or `V500013` either** — both also
sort below `V500019`, so at the moment they run the periods table does not exist. It needs **a
migration of this task's own, numbered ABOVE `V500019`**:

```sql
ALTER TABLE whb_stock_periods
    ADD CONSTRAINT fk_whb_stock_periods_warehouse
    FOREIGN KEY (warehouse_id) REFERENCES whb_warehouses (id);
```

`NULL` stays legal and is not an omission — it is the **all-sites** scope (`RG-026`), and a foreign
key does not constrain it. Until this lands, *Generate periods* trusts the `warehouseId` it is given
and a period can be created against a site that does not exist; that is recorded on `P0-07` as
carried work, not as a defect of this task.

## Owed to `P0-17` — two deferred cost-layer foreign keys

Distinct from the `P0-07` debt above. `V500021` (`P0-17`) ships
`whb_valuation_policies.warehouse_id` and `whb_cost_layers.warehouse_id` as bare `UUID`s because
`V500012` had not landed, so **this task owes those two `ALTER TABLE`s in `V500024`** — the first
free number in the reserved gap (`V500022` is consumed by `P0-07`, `V500023` by `P0-17`'s own
correction; five slots remain before `PNR-2` at `V500030`). The exact statements are in `V500021`'s
header block and are not restated here.

## Round-4 additions (folded from former P5-24, #157) — warehouse companies and owner-set cages

**v2 increment of this task — design the schema now, build in the v2 wave.** Former `P5-24` (`#157`)
gathered every v2 junction a round-4 lens had hung on a v1 task. On 2026-09-10 it was closed as a
duplicate and split by parent table: each junction now lives in the task that owns its parent. This task
owns `whb_warehouses` and `whb_locations`, so their two junctions come here, together with migration
**`V500070`** and **`FR-468`**. *Since `D-14` item 8 (2026-09-14), the warehouse's company junction is
`P1-22`'s, at v1; `V500070` keeps only the location's owner dedications.*

**`FR-468` is owned here** because this task carries `D-14`'s canonical junction (`RG-001`). The other
parts of `FR-468` are v2 increments of their parent-table owners: `P0-04` (`whb_reason_code_tax_treatments`,
`V500072`), `P0-06` (`whb_owner_companies`, `V500073` — moved to `P1-22` at v1), `P0-11` (`whb_outbox_subscription_owners`,
`V500074`), `P1-01` (`whb_item_uom_defaults`, `whb_item_tax_classifications`, `V500075`), `P1-02`
(`whb_uom_scheme_codes`, `V500076`), `P1-03` (site-scoped supplier sources, `V500077`), `P1-14`
(`wh_inspection_plan_assignments`, `V510220`), `P5-21` (`wh_print_template_scopes`, `V510223`) and
`P2-04` (a `WAREHOUSE` count-programme scope). `FR-468` is met when all ten land — the warehouse and
owner parts at v1 in `P1-22` (`D-14` item 8c), the rest in the v2 wave.

Every table follows R22 §1.3: `effective_from` and `effective_to` as `TIMESTAMPTZ`, half-open; one current
row per key by `EXCLUDE`; no `is_active`.

1. **`whb_warehouse_companies`** (`RG-012`) — **moved to `P1-22` (v1, `V500078`, `D-14` item 8c).**
   Role `OPERATOR` or `STOCK_HOLDER`, dates, one current `OPERATOR`. The writer's assertion reads a
   `STOCK_HOLDER` link at `occurred_at`, and `whb_warehouses.company_id` is dropped once backfilled.
2. **`whb_location_owner_dedications`** (`RG-014`) — the set of owners a cage is dedicated to, dated,
   and a commingle policy value `OWNER_SET` that the port evaluates (`FR-087`). `dedicated_owner_id`
   stays the v1 single-owner case.

The dedications are maintained from a **Dedicated owners** row action on `WS-017`, per `D-14` item 8b —
not a sub-grid; the site's **Companies** row action on `WS-016` is `P1-22`'s. No new screen id.

**Closes** `RG-012` (built by `P1-22`) · `RG-014`.

**Traps (v2 increment)**

- **Backfill from the v1 scalar, in the same migration, or the resolver sees nothing.** `V500070` writes
  one dedication row per non-null `dedicated_owner_id`. `effective_from` is the parent row's `created_at`,
  so a resolver reading history before v2 still finds a link. (The site's company pair is backfilled by
  `P1-22`'s `V500078`, `RG-012`.)
- **The scalar stays until nothing reads it, and nothing new reads it.** `dedicated_owner_id` is kept for
  v1 readers. Code written in this increment reads the junction, never the scalar, or the two drift the
  way `FR-079`'s three GSTIN copies did.
- **`btree_gist` is declared by the migration that uses it** (R22 §1.3 rule 2). `P1-08` declared it
  first, but `V500070` must not assume a v1 migration ran in this database's order.
- **Every new vocabulary is a code list or a closed system value, never a `CHECK` on an open set**
  (`RL-006`). `OWNER_SET` is a commingle-policy value the port already switches on (the two company roles
  are `P1-22`'s code-list rows).

**Acceptance (v2 increment)**
- [ ] `V500070` creates `whb_location_owner_dedications` with the R22 §1.3 columns, an `EXCLUDE` for the one-current rule and no `is_active` (`whb_warehouse_companies` is `P1-22`'s `V500078`, `RG-012`)
- [ ] The backfill leaves every dedicated location with exactly one open link, asserted by count against the scalar it came from
- [ ] A location dedicated to two owners of one client group accepts both and refuses a third, through the port (`RG-014`, `FR-087`)
- [ ] **Mobile:** `none`, with the reason stated. The dedications row action is back-office maintenance; the RF screens see the result through the port's commingle check (`D-13`)
