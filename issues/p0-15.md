TITLE: [Warehouse] P0-15 · Base permissions, verb permissions, menus, the first grid block and admin settings
LABELS: task,warehouse,phase-p0,warehouse-base
issue: 117
---
Part of __P0__ · Module **`warehouse-base`** · Migrations **V501000, V501001, V501002, V501010, V501020–V501049, V501100** · Screens **none of its own — it configures the P0 base grids**

## Do not leave this to the end of the phase

**A grid without its `grid_preferences` row and its `filterUtils` scope looks built and is not
usable.** Config work is the classic thing deferred to the end of a phase; it is scheduled *with* the
screens (plan §3.6).

## Scope

### Adopted solution and configuration contract — 2026-09-11

Apply this task’s relevant behavior from [Global settings and resolved behaviour](https://github.com/neetub1508/warehouse-issues/blob/main/docs/GLOBAL-SETTINGS-DECISIONS.md), adopted by DECISIONS. This resolves earlier open-question/blocked-on wording for the decisions it names; external capability availability remains a testable gate. Preserve this task's existing module, migration and requirement ownership. P0-15/P1-20 own the shared settings implementation; other tasks consume it. Do not build unrelated settings controls or later-version capabilities here.

- `warehouse.negative_stock.default_mode`: default **BLOCK**; allowed BLOCK / WARN / ALLOW. WARN requires explicit acknowledgement and override permission; ALLOW still requires an explicit matching policy and audit. Never auto-reserve shortage.
- `warehouse.reservation.default_ttl_minutes`: default **30**; allowed integer 1–10080. Only new reservations inherit it; extensions are explicit, authorized and retain history.
- `warehouse.outbox.max_attempts`: default **10**; allowed integer 1–100. Subscription-specific value wins. Exhausted work enters retained failed queue; retry keeps original event key.
- `warehouse.port.rejected_queue_threshold`: default **10**; allowed integer 1–10000. Alert at count >= threshold; alert once per breach episode, resolve below threshold; no automatic discard.
- `warehouse.position.max_contention_retries`: default **3**; allowed integer 0–10. Bounded transaction retries only; exhaustion returns retryable conflict with the same idempotency key.
- `warehouse.outbox.max_cursor_lag`: default **1000**; allowed integer 1–1000000 events. Measures pending event sequence distance; alert on breach, recovery below threshold. Never advance a cursor to silence an alert.
- `warehouse.period.soft_close_requires_approval`: default **true**; allowed locked true. Display the rule read-only. No setting permits a hard-close write or maker self-approval.

Replace the settings-seed ellipsis with the full Base-owned catalogue in the contract. Seed `whb_settings:edit` with its dependency; changing settings requires both that grant and `admin_settings:edit`. Soft-close approval is a locked rule, not a bypass checkbox.


> **Round-2 addition — `Q-004` · `Z-010`.** **Four seeded role bundles, and a grid-spec gate.**
> (1) `V501002` seeds four warehouse roles as **starting bundles**, documented as a
> customer-editable starting point and **not** as a security boundary: **Storekeeper** (every
> `:view`, receipt/putaway/pick/count execution, no `:approve`, no `:reverse`, no `:close`, no
> `:override`), **Supervisor** (Storekeeper plus `whb_stock_movements:approve`,
> `whb_locations:block`, task reassignment and count-variance approval — never approval of their
> own count, per `FR-408`), **Stock Controller** (masters `:create`/`:edit`,
> `whb_stock_periods:close`/`:reopen`, valuation and adjustment approval,
> `whb_stock_positions:rebuild`), and **3PL Client** (`:view` filtered to their own `owner_id`,
> plus ASN and outbound-order create — the `FR-284` portal bundle). Every insert is guarded `WHERE
> NOT EXISTS`; the grants are `role_permissions` rows, never new permissions. Without this,
> `D-7`'s standalone reference configuration ships with no way to give anyone less than ADMIN. (2)
> For every P0 grid this task owns, `BUILD-SPEC-SCREENS.md`'s screen row is amended to the `| key
> | label | type | sort | vis | source |` form **before** its migration is authored; a grid whose
> spec row is still a bare comma list is not ready to build.

> **Round-4 addition — `RH-002`.** **Reuse platform's scope tier; do not re-specify it.** For every
> base management resource, `V501000` seeds **`<resource>:view:all`** and **`<resource>:view:branch`**
> beside `:view`, and `V501001` adds each tier's dependency row (tier → its `:view`). Grants follow
> `platform/…/V685:20-47`, `:72-85`: **ADMIN and AUDITOR get `:view:all`**; **Branch Admin gets
> `:view:branch` and never `:view:all`**; the operational bundles in `V501002` (Storekeeper,
> Supervisor, Stock Controller) get `:view:branch`. *(C3 fix pass 3, 2026-09-15: "never `:view:all`" is the
> branch axis. The operational bundles and Branch Admin also hold `whb_owners:view:all`, the owner-axis flag,
> via `V501020`.)* The three-mode predicate **is**
> `branchScopeService.resolveStrict(principal, VIEW_ALL, VIEW_BRANCH)` (`BranchScopeService.java:143`,
> fails closed), wrapped by `P1-18`'s warehouse-set resolver. An `ArchitectureInvariantsTest` rule
> refuses any `ai.warehouse*` class that injects `BranchStaffRepository` or re-implements branch
> resolution. **`PP-13`/`PD-D11` are withdrawn** — the extraction they asked platform for has
> shipped. `V501000` also seeds two round-4 verbs: **`warehouse:warehouses:change_registration`**
> (maker–checker, `RG-001`) and **`whb_accounting_handovers:void`** (`RJ-011`).

- **`V501000`** — `permissions` rows for **every base resource** (`view`/`create`/`edit`/`delete`/
  `export`, plus the `:view:all`/`:view:branch` tier pair, `RH-002`) plus the **verb permissions**,
  with ADMIN and `ADMIN_GROUP` grants; **AUDITOR gets every
  `:view` and every `:view:all`, never an `:export`, never a `:create`/`:edit`/`:delete` and never a verb
  permission** (user decision 2026-09-15, platform `V698` policy);
  and the **reserved `logistics:*` namespace** with its dependency rows.
- **`V501001`** — `permission_dependencies`, **`INSERT` only, never `CREATE TABLE`**.
- **`V501002`, after its bundle grants** — the `V727`-style source-page revoke for the dashboard
  tier (`RH-006`, moved here from `P0-01`): once `V501000`/`V501001` have created
  `whb_stock_positions:view:branch`, revoke `widget:warehouse:view:branch` from every role that does
  not hold `whb_stock_positions:view:branch`, leaving `dashboard:admin` holders alone. `P0-01`'s
  `V500200` seeds the tier but cannot carry this row: its guard permission does not exist yet when
  `V500200` runs, and a versioned migration never re-runs.
- **`V501010`** — the menu tree: L2 nodes under the L1 `warehouse` node created by `P0-01`'s
  `V500000`, L3 leaves for the base screens, `menu_translations` in **`en` + `fr` + `hi`**, and
  `menu_permissions`. **Every insert guarded with `WHERE NOT EXISTS`.**
- **`V501020`–`V501049`** — grid configuration, **one migration per grid**, for the P0 base grids.
- **`V501100`** — `admin_settings` seed, category `WAREHOUSE`, base-owned keys:
  `warehouse.negative_stock.default_mode`, `warehouse.period.soft_close_requires_approval`,
  `warehouse.outbox.max_attempts`, `warehouse.reservation.default_ttl_minutes`,
  `warehouse.port.rejected_queue_threshold`, …
- **The three-mode view predicate** (`FR-404`): view-all, view-branch, no view — keyed on the
  `:view:all`/`:view:branch` pair and resolved by platform's `BranchScopeService.resolveStrict`, not
  re-implemented (`RH-002`). It is applied as a **record-level guard in the `WHERE` clause of every
  management query**, never as a UI filter. `P1-18` extends the same predicate with warehouse scope.

> **Round-3 fold — `RA-003` · `RA-006` · `RA-007` · `RE-003` · `RE-008` (`GAP-REGISTER-R3.md` §2.1,
> §2.5).**
> (1) **The `Z-010` bundles become allow-lists** (`RA-003`) — this amends the round-2 block above; it
> does not add a second. A four-verb deny-list over a sixty-verb space lets Storekeeper hold the QC
> release gate, the blocked-movement force, the hold release and the reservation release. **Each
> `V501002` bundle names its verb strings**, and **two more bundles** join it — **Warehouse Manager**
> and **Finance Controller**, with the verb lists `docs/reviews/R16` `RA-003` specifies — plus a **QA**
> bundle, since `wh_quality_inspections:disposition` and `wh_return_receipts:disposition` belong to it
> and nothing else; the FRD §4 actor table gains the QA actor row. Narrowing an over-granted bundle
> after a customer clones it is a revocation across every clone, so this lands in `V501002`.
> (2) **§10.2 is the seed authority** (`RA-007`). `V501000` is written from `BUILD-SPEC-SCREENS.md`
> §10.2, which follows §10.1's table-name rule; `PORT-AND-ADAPTER-CONTRACT.md` `PC-31` is rewritten to
> §10.2's strings (that document's edit). §10.2 carries the two gates the port needs and no screen
> has — **`whb_stock_movements:post_backdated`** (posting into a `SOFT_CLOSED` period, `mgr1`'s
> `WH-SC-022` authority, distinct from `whb_stock_periods:override`) and **`whb_outbox:view`** — and
> `V501000` also seeds **`whb_stock_movements:verify`** (`RC-008`, `P0-13`). `WH-SC-022` and
> `WH-SC-241` are re-pointed at the seeded strings. Permission names are `IRR-63`: forward-only.
> (3) **`V501100`'s key list is closed** (`RE-003`): the adopted catalogue above replaces the
> ellipsis, and every key is seeded with `setting_value`, `default_value`, `setting_type`,
> `description` and a `validation_rules` bound. **Every key states its scope in `description`, and
> `admin_settings` expresses install scope only** — a key that needs company × site scope is a table,
> not a key. The warehouse settings tab, the third platform file, is `P1-20`'s.
> (4) **The emergency-replenish auto-count switch** (`RA-006`) needs a home if `P2-09` builds it:
> `warehouse.short_pick.auto_count` is **not in the adopted catalogue**, so it is added there before
> `V501100` seeds it — install scope only, per (3). Whether v1 offers emergency replenish at all is
> `P2-09`'s build-or-withdraw decision (`GAP-REGISTER-R3.md` §5.8).
> (5) **Menus of modules that are not installed are inert** (`RE-008`): menu rows this task seeds for
> modules other than `warehouse-base`/`warehouse` are seeded `is_active = false`, and each module's
> own migration enables them when it runs — the Doc OCR AI tab's frontend gating
> (`admin-settings/page.tsx:145-152`) applied at the data layer. Disabling a module after enabling it
> still leaves its `menus`, `permissions` and `admin_settings` rows live (`MODULE-INTEGRATION.md` §15).

## The verb permissions — the ones `view/create/edit/delete/export` does not cover

**A transition with no verb permission is a transition anybody with `:edit` can perform**, which is
the failure `FR-408` names.

| Permission | Gates | Screen |
|---|---|---|
| `warehouse:movements:post` | `POST /api/warehouse/movements`, **and `whb_source_systems.post_permission`** — `P0-04`'s `V500002` seeds this exact string on eight of its nine rows, so until `V501000` creates it the column names a permission nothing grants (`MPR-OPEN-01` is still open on the namespace; if it ratifies `whb_stock_movements:post`, **this row and `V500002`'s seed change together**) | the port (`P0-08`) |
| `warehouse:movements:reverse` | `POST /movements/{id}/reverse` | WS-040 |
| `warehouse:movements:simulate` | `POST /movements/simulate` | WS-040 toolbar |
| `warehouse:movements:view` | `GET /movements`, incl. the lineage query | WS-040 |
| `whb_stock_movements:approve` | approval of `requires_approval` movement types | WS-040 |
| `whb_stock_periods:close` · `:reopen` · `:override` | soft close, close, reopen, soft-close override | WS-045, WS-046 |
| `whb_reservations:release` | release by holder | WS-047 |
| `whb_locations:block` | block / unblock a location | WS-017 (`P1-05`) |
| `whb_stock_positions:rebuild` | the `L-4` rebuild check | WS-042 |
| `whb_inbound_messages:reprocess` | reprocess a failed inbound message | WS-053 |
| `whb_outbox:replay` | replay from cursor | WS-056 |
| `whb_accounting_handovers:retry` | retry a rejected handover | WS-052 |
| `whb_job_runs:trigger` | Run now | WS-064 |
| `warehouse:warehouses:change_registration` | change a site's `REGISTERED` branch, maker–checker (`RG-001`) | WS-016 (`P1-05`) |
| `whb_accounting_handovers:void` | `REJECTED → VOIDED`, approver ≠ requester (`RJ-011`) | WS-052 |
| `whb_stock_movements:post_backdated` | posting into a `SOFT_CLOSED` period, distinct from `whb_stock_periods:override` (`RA-007`) | the port (`P0-08`) |
| `whb_outbox:view` | reading the outbox and its deliveries (`RA-007`) | WS-056 |
| `whb_stock_movements:verify` | the ledger-chain verifier (`RC-008`, `P0-13`) | WS-040 |
| `warehouse:stock:view` | `GET /warehouse/stock/as-at`, the as-at query (`FR-013`, `MPR-T1-13`). **Already enforced** by `P0-03`'s `WhbStockAsAtController`, so the endpoint is granted to nobody until `V501000` seeds it | API (`P0-03`) |

**Hand-off from `P0-03` (C2 fix pass 1, 2026-09-15).** `warehouse:stock:view` above is this issue's to
seed in `V501000` (BUILD-SPEC §10.2). `whb_position_drift_findings:assign` · `:resolve` are **not** —
`P0-03`'s `V500045` seeds them with WS-043, so do not seed them twice. `whb_stock_positions:rebuild`,
`whb_stock_movements:post_backdated` and `whb_negative_stock_policies:override` stay here.

**Hand-off from `P0-12` (C2 fix pass 1, 2026-09-15).** `P0-12`'s `V500042` already seeds
`whb_accounting_handovers:void` (and `:retry`) with `ON CONFLICT DO NOTHING`, so the `V501000` seed of
the rows above is a harmless double seed. Keep it, with the **same name and description** as
`V500042`'s.

**Defaults settled for fix 1A (C3 fix pass 1, 2026-09-15).** The branch-tier `WHERE` predicate (`WH-SC-242`)
applies these: (1) a row with no site (`warehouse_id` NULL) is visible to any holder of either tier; (2) the
items on-hand total is narrowed to the caller's scope for display only, and the `FR-051` guards stay global;
(3) the 3PL Client holds `:view:all` on its owner-scoped stock reads (positions, movements, lots, serials,
LPNs, snapshots), narrowed by `WhbOwnerScopeResolver` — it is not a branch role, and `whb_owners:view` is not
in its bundle because the owner reads are not owner-scoped.

**Hand-offs to `P1-20` (C3 fix pass 1, 2026-09-15).** The app-verb bundle grants (`RA-003`, `Z-010`), the
settings API refusals `CONFIG-CASE-02` needs, and the threshold's decimal scale are recorded on
`issues/p1-20.md` under *Hand-off from `P0-15`*.

**Settled in C3 fix pass 1C (2026-09-15).** (1) Task reassignment is `whb_tasks:reassign`, the Supervisor-only
addition this task's Round-2 bundle names (lines 35-36): `V501000` seeds it, `V501001` makes it require
`whb_tasks:view`, `V501002` grants it to Supervisor and Manager and never Storekeeper, `/reassign` is gated on it,
and §10.2 carries the row. (2) The `whb_stock_positions:view` → `whb_items:view` / `whb_locations:view` /
`whb_owners:view` dependency rows (line 195, §10.3) and the 3PL Client bundle **intentionally differ**: the rows
are a permission-matrix implication with no runtime effect, and `Z-010` limits the 3PL Client to owner-filtered
reads, which none of those three masters is — so the bundle holds none of them. (3) Branch Admin holds the base
`:view` and `:view:branch` on every read fix 1A narrowed, plus the masters those screens read, and never a branch-axis
`:view:all` (it does hold the owner-axis `whb_owners:view:all`, below).

**Settled in C3 fix pass 3 (2026-09-15) — the owner tier for the internal bundles.** The runtime tester found that
tasks, stock movements, stock positions, the outbox, lots, serials, LPNs and accounting handovers apply the branch
tier **and** `WhbOwnerScopeResolver`'s owner tier, which unscopes only `ROLE_ADMIN` or `whb_owners:view:all` holders
and scopes everyone else to their `whb_owner_grants` — the empty set, deliberately (`P0-06`). So every internal role
read 0 rows inside its own branch, and a Supervisor's `/reassign` answered `403 OWNER_NOT_PERMITTED`. Per `WH-SC-242`
and `IRR-60` (`DATA-MODEL.md`, owner segregation is for owner/3PL clients): **the six operational bundles
(Storekeeper, Supervisor, Stock Controller, Warehouse Manager, Finance Controller, QA) and Branch Admin hold
`whb_owners:view:all`** — owner axis unscoped, branch axis still narrowed by `:view:branch`. **The 3PL Client stays
owner-scoped.** `V501020` carries the grant and asserts all three (the seven hold it, the 3PL Client does not, no
branch-scoped role holds `:view:all` on any of the nineteen branch-narrowed reads); `V501000`–`V501002` are applied
and unchanged, and the resolver is unchanged. `whb_owners:view` is not granted with it, so Branch Admin still has no
Owners screen or menu; `V500007`'s `whb_owners:view:all → whb_owners:view` dependency row is unmet for Branch Admin,
intentionally, as with the 3PL Client's rows in (2).

## The role decisions that must be **re-taken**, not inherited (`FR-403`)

The deleted prior module's auditor and branch-admin exclusions were **one-shot migrations that have
already run**. They grant a new module nothing, so the recorded product decisions are simply
**absent** and must be taken again, explicitly, here:

- `platform/…/V528__Create_auditor_role_with_view_permissions.sql:5` — *"all view permissions from
  all modules **EXCEPT warehouse** (`wms_*`, `logistics_*`)"*; `:37` `AND p.name NOT LIKE 'wms_%'`;
  `:57` `AND m.name NOT IN ('warehouse', 'warehouse-setup', 'warehouse-catalog', 'warehouse-config')`;
  `:59`, `:67`, `:71` extend it to L3 children and grandchildren.
- `platform/…/V663__Grant_branch_admin_branch_safe_permissions.sql:17-19` — Branch Admin deliberately
  **not** granted *"pages with NO branch filtering (warehouse `wms_*`, …) — granting these would leak
  all-branch data"*.

**The decision this set takes:** AUDITOR **is** read-only across everything **including the ledger**
— it receives every `:view` and no writer permission and no verb permission (FRD §4) — and `:view:all`
(`RH-002`). **It receives no `:export`** (user decision 2026-09-15, platform `V698` policy: AUDITOR reads,
it does not extract; this supersedes the earlier *"every `:export`"* wording). Branch Admin holds **the branch tier only** — `:view:branch`,
never `:view:all` (`FR-404`, `RH-002`) — enforced by the record-level predicate, not by a menu filter. That is the
branch axis; on the owner axis Branch Admin holds `whb_owners:view:all` (`V501020`, C3 fix pass 3).

## Requirements closed
`FR-346` `FR-401` `FR-402` `FR-403` `FR-404` `FR-407` `FR-410`

## Scenarios closed
`WH-SC-241` `WH-SC-242` `WH-SC-245` `WH-SC-246` · `WH-SC-044` (partial: menu reachability —
handed off by `P0-01`, which closes the Mode F boot; the coupling assertion is `P0-14`'s)

## Closes
`C-017` · `P-014` · `T-070` · `Q-004` `Z-010` · `RH-002` · `RA-003` `RA-006` `RA-007` `RE-003` `RE-008`


## Traps

- **`permission_dependencies` is a PLATFORM table** created by `V248`, with `uq_permission_dependency`
  and `chk_no_self_reference` (`V248:17-30`). Creating it defensively from a module migration is a
  **documented false premise** — `dealer/V20501:10` is the vestigial second creator (`C-017`,
  R1 §8 trap `T-10`). **Insert rows only.**
- **The column is `dependent_permission_id`, not `depends_on_permission_id`** (`V248:18-22`), and the
  semantics are *"granting `permission_id` also grants `dependent_permission_id`"*. Inspect before
  authoring:
  `SELECT p.name, d.name FROM permission_dependencies pd JOIN permissions p ON pd.permission_id=p.id JOIN permissions d ON pd.dependent_permission_id=d.id LIMIT 5;`
- **The dependency rows to write:** every non-view action → its own `:view`; every verb permission →
  its resource's `:view`; every document screen → the masters it renders
  (`whb_stock_positions:view → whb_items:view, whb_locations:view, whb_owners:view`). **The approval
  reverse row must not exist** — `FR-408` separates approval from execution and the approver may not
  be the actor.
- **Menu inserts are guarded with `WHERE NOT EXISTS`, never `ON CONFLICT DO NOTHING`.** `menus` has no
  unique constraint on `(name, parent_id, menu_level)`, so a Flyway retry duplicates the row —
  `assets/…/V60171:154-155` spells it out (`FR-410`, R1 §8 trap `T-9`).
- **`grid_filter_definitions` does not exist.** The table is **`filter_definitions`**, created by
  `platform/…/V229__Add_filter_definitions_table.sql:8`. Inserting into the wrong name fails at
  Flyway and **crash-loops the backend** — and the prior art names the wrong table in six documents,
  fourteen times (`P-006`).
- **`grid_preferences.default_columns` AND `default_filters`, both, as `'[…]'::jsonb`.**
  `default_columns` has been on the table since `V18:12`; `default_filters` was added later at
  `V229:51` and is **nullable**, which is exactly why it gets forgotten. Setting
  `filter_definitions.default_visible = true` alone does **not** populate the strip (R1 §8 trap
  `T-8`). Never patch these arrays later with `REPLACE` on a text literal — a known whitespace trap.
- **A required column not in `default_columns` bricks every grid-preference save**, silently, for
  that grid only. Mark `is_required` on **at most one identity column per grid** and include
  BUILD-SPEC-SCREENS §9.4's `DO $$ … RAISE EXCEPTION` assertion in the same migration.
- **`filter_definitions.position` has no uniqueness** and defaults to 0, so colliding positions render
  in an arbitrary order that changes between queries. Number from 1, contiguously, in reading order.
- **Never name a warehouse permission `wms_*` or `scc_*`** (`D-3`, `FR-401`, `P-014`).
- **AUDITOR must never receive a verb permission, nor an `:export`** (user decision 2026-09-15, platform
  `V698` policy). `:approve`, `:reverse`, `:release`,
  `:close`, `:override`, `:retry`, `:trigger`, `:void` and `:change_registration` never (`RH-002`,
  `RJ-011`, `RG-001`).
- **Do not write a warehouse branch-scope service** (`RH-002`). `BranchScopeService` exists because
  copies had accumulated. Without the tier pair, `WH-SC-242`'s branch-scoped user has no grant that
  makes them one, and role-name checks are the only fallback, which `P1-18`'s `hasAnyRole` trap
  forbids.
- **The `logistics:*` reservation is rows, `is_active = false`** — `logistics:trips:view|create|edit|
  dispatch`, `logistics:epod:view|capture`, `logistics:freight:view|settle`, each with its `→ :view`
  dependency (`FR-407`, `FR-346`).
- **`COMMON_FILTER_CONFIGS` and `CacheConfiguration.java` are not migrations** and must be edited in
  the same PR. An unlisted filter field is silently dropped by `convertFiltersForApi()`; an
  unregistered cache name throws on the **first call**, not at startup
  (`filterUtils.ts:346`, `CacheConfiguration.java:375-377`).

## Acceptance

- [ ] `CONFIG-CASE-01` in the adopted contract passes for this task's owned workflow only, including its refusal/retry behavior; reuse shared infrastructure tests and record evidence in the implementation PR.
- [ ] `CONFIG-CASE-02` in the adopted contract passes for this task's owned workflow only, including its refusal/retry behavior; reuse shared infrastructure tests and record evidence in the implementation PR.
- [ ] `CONFIG-CASE-03` in the adopted contract passes for this task's owned workflow only, including its refusal/retry behavior; reuse shared infrastructure tests and record evidence in the implementation PR.
- [ ] No setting rewrites posted history, bypasses permissions/period locks or silently enables an unavailable capability.
- [ ] Every base controller method carries `@PreAuthorize` in `resource:action` form; a method
      without one **fails the architecture test** (`WH-SC-241`)
- [ ] Export is `hasAuthority('<table>:export') or hasRole('ADMIN')` on every export endpoint
- [ ] AUDITOR holds every `:view` and `:view:all`, **no** `:export` (user decision 2026-09-15, platform
      `V698` policy) and **no** writer or verb permission, asserted by a query over `role_permissions`
      (`WH-SC-245`, `RH-002`)
- [ ] Every base management resource has `:view:all` and `:view:branch` with dependency rows; Branch
      Admin holds `:view:branch` and **no** `:view:all` on a branch-narrowed read, asserted by query (`RH-002`)
- [ ] The six operational bundles and Branch Admin hold `whb_owners:view:all` and the 3PL Client does not,
      asserted by query in `V501020` (`WH-SC-242`, `IRR-60`)
- [ ] The architecture test fails an `ai.warehouse*` class that injects `BranchStaffRepository`
      (`RH-002`)
- [ ] `warehouse:warehouses:change_registration` and `whb_accounting_handovers:void` are seeded in
      `V501000` with `→ :view` dependencies (`RG-001`, `RJ-011`)
- [ ] Branch Admin sees only its branch's rows, enforced in the `WHERE` clause — **a menu filter is
      not a guard**, and the negative test is per endpoint (`WH-SC-242`)
- [ ] `permission_dependencies` rows are `INSERT`ed and **no migration creates the table**
- [ ] The `logistics:*` namespace, the `V524000`–`V524999` band and the `log_` prefix are reserved,
      with **no `logistics` migration** (`WH-SC-246`)
- [ ] Every P0 base grid has `grid_column_definitions`, `filter_definitions` **and**
      `grid_preferences` with **both** JSONB arrays, and the required-column assertion passes
- [ ] Every P0 base filter scope is in `COMMON_FILTER_CONFIGS`; every `dropdown.*` cache name is in
      `CacheConfiguration.java`; **no `statistics.*` name is registered for a filter-aware strip**
      (`FR-395`)
- [ ] Menus are guarded `WHERE NOT EXISTS` and carry `en`/`fr`/`hi` translations
- [ ] Every §10.2 verb appears in at least one seeded bundle or in an explicit *granted to no seeded
      bundle* list, asserted by a query that fails when §10.2 grows; Warehouse Manager, Finance
      Controller and QA bundles exist, and Storekeeper holds no `:approve`, release, `:disposition` or
      blocked-movement force (`RA-003`)
- [ ] `V501000` seeds `whb_stock_movements:post_backdated`, `whb_outbox:view` and
      `whb_stock_movements:verify`; every seeded permission string is in `BUILD-SPEC-SCREENS.md` §10.2,
      and `WH-SC-022`/`WH-SC-241` assert on seeded strings (`RA-007`)
- [ ] **`V501000` creates `warehouse:movements:post`**, the string `P0-04`'s `V500002` already seeds
      into `whb_source_systems.post_permission`; a query joining every non-null `post_permission` to
      `permissions.name` returns **no unmatched row**. Without it the column points at nothing and the
      port's authorisation check can never pass (`MPR-OPEN-01` provisional, `RA-007`)
- [ ] Every `V501100` key carries `setting_value`, `default_value`, `setting_type`, a scope-stating
      `description` and a `validation_rules` bound, and the seed has no ellipsis (`RE-003`)
- [ ] Menu rows for a module that is not installed are `is_active = false` until that module's
      migration enables them (`RE-008`)
- [ ] `V501002` revokes `widget:warehouse:view:branch` from every role lacking
      `whb_stock_positions:view:branch` (except `dashboard:admin` holders), and a query over
      `role_permissions` after a fresh ascending migrate finds no role holding the widget `:branch`
      tier without the source page's `:view:branch` (`RH-006`, moved from `P0-01`)
- [ ] **Scenarios** `WH-SC-241` · `WH-SC-242` · `WH-SC-245` · `WH-SC-246` · `WH-SC-044` (partial:
      menu reachability)
