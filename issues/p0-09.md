TITLE: [Warehouse] P0-09 · Reservations as an open-item ledger, never a counter
LABELS: task,warehouse,phase-p0,warehouse-base
issue: 77
---
Part of __P0__ · Module **`warehouse-base`** · Migrations **V500033** · Screens **WS-047 Reservations**

## Scope

### Adopted solution and configuration contract — 2026-09-11

Apply this task’s relevant behavior from [Global settings and resolved behaviour](https://github.com/neetub1508/warehouse-issues/blob/main/docs/GLOBAL-SETTINGS-DECISIONS.md), adopted by DECISIONS. This resolves earlier open-question/blocked-on wording for the decisions it names; external capability availability remains a testable gate. Preserve this task's existing module, migration and requirement ownership. P0-15/P1-20 own the shared settings implementation; other tasks consume it. Do not build unrelated settings controls or later-version capabilities here.

- `warehouse.reservation.default_ttl_minutes`: default **30**; allowed integer 1–10080. Only new reservations inherit it; extensions are explicit, authorized and retain history.

**Stock solution:** apply the contract's signed-balance/ATP distinction; never reserve shortage, issue a serial twice or consume another holder's reservation. BLOCK/WARN/ALLOW govern eligible physical issues, not allocation permission.


> **Round-4 addition — `RJ-007` · `RJ-015` · `RL-010`.** (1) **`expires_at` applies to `SOFT`
> reservations only** (`RJ-007`). A `HARD` row ends only by consume, cancel or explicit release; the
> expiry job never touches one, so a hold cannot lapse mid-pick or while staged. Ageing shows stale
> `HARD` rows separately (`P2-07`, `WH-SC-317`). (2) **Re-attach at pick is release-and-recreate**
> (`RJ-015`): in the pick's writer transaction the source reservation is released (`released_at`,
> reason `PICKED`) and a new row is created at the staging position with the same holder quad, so
> `I-6`'s per-position `CHECK` never sees a pick debit a position its reservation still points at
> (`P2-09`). (3) **`whb_allocation_strategies`, `whb_allocation_strategy_rules` and
> `whb_allocation_rules` are versioned under `I-24`** in `V500033` (`RL-010`): a row referenced by any
> reservation is immutable, enforced by a trigger in `I-9`'s shape; an edit is copy-on-write — a new
> row with `version_no` and `supersedes_id`, the old one deactivated — so the recorded
> `strategyCode`/`ruleSequence` always resolves to the rules that actually ran.

- **`V500033`** — `whb_reservations` + **`I-12`**, plus `whb_allocation_strategies`,
  `whb_allocation_strategy_rules` and `whb_allocation_rules`, all three versioned under **`I-24`**
  (`RL-010`).
- **The holder quad is the whole point** (`FR-167`, `L-10`):
  `(holder_system, holder_document_type, holder_document_id, holder_line_no)` **plus `expires_at`**
  (on `SOFT` rows only, `RJ-007`).
  With a counter you cannot say **who** holds a reservation, cannot release one order's holds, and
  cannot expire a stale one. `DELETE /reservations?holder=…` answers *"release everything trip X
  held"*, and that question is unanswerable against a `quantity_reserved` column.
- **Availability is computed, never stored** (`FR-168`):
  `available = Σ on_hand where status.is_allocatable − Σ open reservations`. The computation lives in
  `P0-03`'s writer service; this task supplies the open-item side of the subtraction.
- **`owner_id` is `NOT NULL` on the reservation** (`FR-107`) and participates in its uniqueness.
- **Allocation strategy as a configured row, not an `if`** — `whb_allocation_strategies`
  (`scope_type` ∈ GLOBAL/WAREHOUSE/ITEM_CATEGORY/OWNER/CHANNEL, `scope_ref`, `priority`) over
  `whb_allocation_strategy_rules` with a **whitelisted** `ordering_key`
  (FIFO/FEFO/LIFO/LOT_SPECIFIED/NEAREST_LOCATION/FEWEST_PICKS/ZONE_PRIORITY/HIGHEST_QUANTITY),
  `direction`, and whitelisted `filter_column`/`filter_operator`/`filter_value`. **Never a free
  expression language** — the fifth rule engine always introduces one.

## `whb_reservations` carries a `PNR-1`-class deadline of its own

`IRREVERSIBLE.md` §3.4: *"`whb_reservations` — **PNR-1** — the holder quad must exist before the
first reservation."* A reservation written before the quad exists cannot be attributed to a holder
afterwards, and the release-by-holder path has nothing to match on. **`DATA-MODEL.md` §2.1 is the
authority on the column set and must be re-read immediately before `V500033` is written**;
`IRR-45` is the row that binds it.

## Screen — WS-047 · Reservations

`/warehouse/ledger/reservations` · **Service Vehicle** · `gridIdentifier` `whb_reservations` ·
filter scope `WAREHOUSE_RESERVATION` · `whb_reservations:view|export` + **`:release`** ·
statistics `—` (filter-aware, `FR-395`).

- **Columns:** `ownerName`, `itemCode`, `locationCode`, `lotCode`, `serialNumber`, `lpnCode`,
  `stockStatusCode`, `quantity`, `baseQuantity`, **`holderSystem`**, **`holderDocumentType`**,
  **`holderDocumentId`**, **`holderLineNo`**, `reservationType` (SOFT/HARD), `priority`,
  **`expiresAt`**, `status`, **`strategyCode`**, **`ruleSequence`**.
- **Filters:** `warehouseId` → `itemId` · `ownerId` · `holderSystem` select → `holderDocumentType`
  select · `holderDocumentId` text · `reservationType` select · `status` select ·
  `expiresFrom`/`expiresTo` `date` pair · `expiredOnly` boolean.
- **Actions:** **Release** (its own modal, reason-coded — **Release exists in v1 even though the
  allocation wave does not**, `FR-169`) · **Extend expiry** · View holder document, routed through
  the bean-collection display resolver of `FR-357`.
- **The strategy and rule that chose the stock are recorded on the reservation and shown on its
  detail view** — otherwise "why did it pick that lot" is unanswerable.
- **Mobile:** `screens/whbReservation`, **read-only** — an operator needs to see who holds the stock
  they cannot pick. *Amended 2026-09-16 (C4): not built — there is no warehouse mobile app in v1 (user direction
  "no mobile changes for warehouse"; the reservations contract and menu carry no mobile screen). Revisit with the
  warehouse mobile app.*

> **BUILD-SPEC-SCREENS §1 marks WS-047 `v1 · P2`;** `IMPLEMENTATION-PLAN.md` §2.1 assigns it to this
> P0 task. **Followed the plan** — the table is `V500033` here, and a reservation grid is the only
> way to see why availability is below on-hand. Recorded in `DEFECTS-FOUND.md`.

## Requirements closed
`FR-166` `FR-167` `FR-168`

## Scenarios closed
`WH-SC-025` `WH-SC-182` `WH-SC-183` `WH-SC-185` `WH-SC-188`

`WH-SC-053` (allocation strategy chooses the FEFO lot end to end) is marked `v1·P2` and is proved by
`P2-07`; the strategy rows it reads are built here.

## Closes
`C-022` · `T-004` · `F-028` `F-037` `F-090`

## Traps

- **`StockStatusChangeCommand.callerAuthorities` MUST come from the authenticated principal, never
  from caller-supplied data** (`FR-448`, `Y-002`, handed over by `P0-05`). This resource owns the
  permission the check names — `whb_reservations:release`, seeded by `P0-05` in `V500005` because the
  planner that reads it ships there — and `P0-03`'s writer service is what fills the field. Recorded
  here too because **this task owns the reservation release itself**: any release path this task adds
  answers to the same rule, and a `List<String>` bound from a request body is a caller granting
  themselves the permission by typing it into the payload. Read it from the `SecurityContext`.
- **`whb_reservations:view|create|edit|delete|export` and the `whb_reservations:release` →
  `:view` dependency row belong to THIS task's migration.** `P0-05` seeds `:release` alone and
  deliberately inserts no `permission_dependencies` row, because the dependent `:view` permission
  does not exist yet. Seeding the rest without the dependency leaves `:release` grantable to a role
  that cannot see a reservation.
- **There is no prior art. Reservations in `accessories` are schema-only.** `quantity_reserved` is
  read in **7** places and written in **0**; `StockLevel.reserveQuantity()` and
  `releaseReservedQuantity()` (`StockLevel.java:189-206`) have **zero call sites** (`C-022`). Budget
  this as net-new and do not let a reviewer point at the accessories column as a pattern.
- **A stored `quantity_reserved` column is the failure mode.** It drifts, and its drift is invisible
  until a picker finds nothing on the shelf. `FR-168` is explicit: availability is computed. The
  reconciliation job of `P0-03` writes `RESERVATION_DRIFT` and `ORPHANED_RESERVATION` findings for
  exactly this.
- **`expires_at` is a threshold column, so it needs its job in the same merge** (`FR-165`): a dated
  obligation with no actor is a defect **at the moment it is merged**, not when it is noticed. The
  reservation-expiry job is registered in `P1-20` and appears on **WS-064 Job Runs** (`P0-13`); the
  ageing report and the holder notification are `P2-07`. State the ownership here so it is not lost.
- **A partial unique index cannot be declared `DEFERRABLE` in PostgreSQL** — four files in this repo
  record it (`accounting-base/V600001:150`, `V600004:111`, `V600120:90`,
  `insurance-360/V120175:35`, `V120180:41`) and each names the consequence: **the write path owes an
  explicit `flush()`**. Any "one active reservation per holder line" index inherits that obligation.
- **The rule columns are whitelisted, not free text.** `ordering_key`, `filter_column` and
  `filter_operator` are bounded vocabularies; `filter_value` is a scalar. `FR-041` and
  `PORT-AND-ADAPTER-CONTRACT.md` §2.9 both forbid an expression, a script or a rule DSL anywhere in
  this product.
- **`WAREHOUSE_RESERVATION` must be added to `COMMON_FILTER_CONFIGS`**, and `expiresFrom`/`expiresTo`
  are **two `date` keys** — there is no `daterange` type (`filterUtils.ts:30`).
- **`holderDocumentId` is a `VARCHAR`, not a UUID and not an FK.** The holder may be a document in a
  module that is not installed; the display resolver falls back (`FR-357`).

## Acceptance

- [ ] `CONFIG-CASE-04` in the adopted contract passes for this task's owned workflow only, including its refusal/retry behavior; reuse shared infrastructure tests and record evidence in the implementation PR.
- [ ] `CONFIG-CASE-05` in the adopted contract passes for this task's owned workflow only, including its refusal/retry behavior; reuse shared infrastructure tests and record evidence in the implementation PR.
- [ ] No setting rewrites posted history, bypasses permissions/period locks or silently enables an unavailable capability.
- [ ] A reservation is a **row** with the full holder quad and, if `SOFT`, an `expires_at`
      (`RJ-007`); there is no `quantity_reserved` counter column anywhere (`WH-SC-025`)
- [ ] The expiry job releases a `SOFT` row past `expires_at` and leaves a `HARD` row untouched, even
      mid-pick (`RJ-007`)
- [ ] A pick releases the source reservation with reason `PICKED` and creates one at staging with the
      same holder quad in the same transaction, and `I-6` does not fire (`RJ-015`)
- [ ] Editing a referenced allocation strategy or rule is refused by `I-24`; the edit lands as a new
      version with `supersedes_id`, and the older reservation still resolves to the old row
      (`RL-010`)
- [ ] `DELETE /reservations?holder_system=…&holder_document_id=…` releases exactly that document's
      holds and nothing else (`WH-SC-182`, `WH-SC-183`)
- [ ] `available` falls by the reserved quantity and **on-hand does not move** — a reservation is not
      a movement (`WH-SC-188`)
- [ ] A reservation against a non-allocatable stock status is refused (`WH-SC-188`)
- [ ] A reservation on a blocked location behaves per `FR-093`'s rule and is visible as such
      (`WH-SC-185`)
- [ ] The **Release** action ships in v1 with its reason code, and the reservation records the
      `strategyCode` and `ruleSequence` that chose the stock
- [ ] The reservation-expiry job is registered with `expires_at` in the same merge, and a
      `whb_job_runs` row proves it ran
- [ ] No release path anywhere reads its authorities from a request body, a DTO field or a query
      parameter — asserted by grep (`FR-448`)
- [ ] `whb_reservations:view|create|edit|delete|export` are seeded here, and
      `whb_reservations:release` (seeded by `P0-05`) gains its `REQUIRED` dependency on `:view`
- [ ] **Scenarios** `WH-SC-025` · `WH-SC-182` · `WH-SC-183` · `WH-SC-185` · `WH-SC-188`
