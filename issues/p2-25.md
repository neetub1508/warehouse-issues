TITLE: [Warehouse] P2-25 · warehouse-adapter-dealer — the first port proof, with a keyboard-first counter sale
LABELS: task,warehouse,phase-p2,warehouse-adapter-dealer
issue: 150
---
Part of __P2__ · Module **`warehouse-adapter-dealer`** (`ai.warehouseadapterdealer`, prefix `whad_`) · Migrations **V520000, V520011, V520012, V520100–V520149** · Screens **WS-194 Counter Sale**

## Scope

**The first proof that the port is a port.** `D-11`: an adapter **may** depend on `warehouse-base`
and its own vertical, post movements through the port, register its own permissions, menus, grids
and filter scopes, own its own `whad_` tables and read the vertical's tables. It **must not** be
depended upon by base or app, write a `whb_`/`wh_` table directly, add a column to a base table, or
extend a base vocabulary by `ALTER`.

**The ratchet is a command, not an intention:**
```
git log --oneline -- warehouse-base/ | wc -l     # unchanged at the merge commit
```
recorded in the PR description (`FR-352`, `BUILD-SPEC-SCREENS.md` §6).

**Migrations.**
- **`V520000`** — bootstrap **plus** the adapter's **source-system** and **movement-type** seed
  rows, **idempotently** (`FR-356`). *Any Flyway failure in this codebase takes the whole backend
  down*, and idempotent registration is what makes an adapter addable without a base release.
- **`V520011`** — `whad_counter_sales`, `whad_counter_sale_lines`.
- **`V520012`** — `whad_oem_orders`, `whad_oem_order_lines`. *(The OEM **workflow** — transmit,
  acknowledge, match — is `WS-196`, **v1.1**; v1 ships the order object.)*
- **`V520100`–`V520149`** — permissions, `permission_dependencies` (**inserted, never created**),
  menus (`WHERE NOT EXISTS`-guarded), grid configuration with **both** `default_columns` and
  `default_filters`, and i18n **en / fr / hi**.
- `whad_vehicle_fitments` is **`V520010`** and belongs to **`P2-24`**. `whad_price_files` (`V520013`)
  is v1.1 and `whad_core_exchanges` (`V520014`) is v2. Do not write them here.

**Counter sale is keyboard-first** (`FR-359`): **scan or part number → quantity → price level →
print → next.** *A sub-ten-second bill*, with **trade price levels** and a cash ticket.
**One focused input, no mouse.** *The bar is set by the Indian retail incumbents, not by an ERP* —
and `COMPETITOR-BENCHMARK.md` lists Marg, GoFrugal and Busy as the incumbents whose muscle memory
this must not fight.

**Three trade-counter behaviours ride on it**, and each is a different task's substance surfacing
here:
- **supersession substitution** — `is_superseded_substitute`, `original_item_id` on the line
  (`P2-24`);
- **the interchange offer** — *"not in stock — 2 available as `<interchange>`"* with availability
  **across every branch** (`FR-073`, `P2-24`);
- **lost-sale capture straight into `wh_insufficient_stock_log`** when the guard refuses
  (`FR-257`, `P2-01`/`P2-15`).

**The adapter reserves and issues through the *public* API**, with `holder_system = ADAPTER_DEALER`
and its own document type — **not through an internal service call and not with a new identifier for
the holder** (`WH-SC-217`, `L-10`).

**Core return** is part of `FR-358`'s v1 sentence; the **core-exchange object with the core bank,
deposit, deadline and grading is `WS-198`, v2**. v1 ships the movement path for a core coming back
as an ordinary return (`P2-12`), and says so.

**WS-194** · `whad_counter_sales` · `WAREHOUSE_DEALER_COUNTER_SALE` · **not a management grid page** —
a keyboard-first single-screen flow, with a Service-Vehicle-shaped **list** beneath it.
Filters `branchId` → `warehouseId` · `customerCounterpartyId` typeahead · `paymentMode` select ·
`saleDateFrom`/`saleDateTo` **`date`** pair.

**Mobile.** `mobile/src/screens/whadCounterSale` — **the counter is often a tablet**; the
keyboard-first flow degrades to a **scan-first** flow and the behaviour is identical. That is the
`D-13` divergence, stated and honoured within the constraint, not skipped.

## Requirements closed
`FR-352` · `FR-358` · `FR-359`

## Scenarios closed
- `WH-SC-216` — the whole bill is keyboard-first and **under ten seconds**: scan or part number, quantity, price level, print, next
- `WH-SC-217` — the adapter reserves then issues through the **public** reservation API with the holder quad, and its movements carry its own source system
- `WH-SC-223` — both v1 adapters ship with **zero commits to `warehouse-base`**, and each posts through `POST /api/warehouse/movements`
- `WH-SC-164` — the adapter posts the full envelope with its own idempotency key and receives `201` with the movement id and its assigned `sequence_no`
- `WH-SC-292` — the adapter's reference data registers **by migration, in its own sub-band, idempotently** — running it twice produces no duplicate movement types

## Closes
`E-006` `T-079` (`FR-352`) · `T-021` `C-034` (`FR-358`) · `E-065` `S-063` (`FR-359`)

## Traps

- **The package is a *sibling*, never `ai.warehouse.adapter.*`.** `@ComponentScan("ai.warehouse")`
  would load `ai.warehouse.adapter.dealer` **unconditionally, in every install, including ones where
  dealer is not built** (`D-1`). This is the exact trap the accounting round-4 review found and
  corrected. The package is **`ai.warehouseadapterdealer`**.
- **`T-16` — controllers under `ai.warehouseadapterdealer.controller…`** or
  `UserActivityTrackingAspect`'s pointcut (`:59`, `within(ai..controller..*)`) cannot see them.
  Module resolution takes **package segment 2, uppercased** (`:167-174`), so the activity log will
  render this module's name accordingly — check what it renders before shipping.
- **`ModuleImportSelector.java` is the gate that fails *silently*.** A module JAR on the classpath
  with no entry there loads **nothing** — the build succeeds and the module is inert
  (`R1 §2` row 8, `:52-55` constants, `:154-186` blocks). Verify with
  `docker logs platform-backend | grep "Module configuration found"`.
- **`FR-356` — idempotent seed or a redeploy takes the backend down.** `ON CONFLICT DO NOTHING`
  needs a real unique key to conflict on; a seed row with a `NULL` in the key **never conflicts**
  and duplicates on every re-run (`P-043`'s `T-M11` is exactly this defect, shipped).
- **`T-9` — menus.** `menus` has **no** unique constraint on `(name, parent_id, menu_level)`, so
  `ON CONFLICT DO NOTHING` does not stop a duplicate on a Flyway retry. `WHERE NOT EXISTS`, per
  `assets/…/V60171__…:154-155`.
- **`T-10` — `permission_dependencies` is a PLATFORM table.** Insert rows; **never** `CREATE TABLE`.
  Columns are `permission_id` and **`dependent_permission_id`**.
- **`D-3` — do not name anything `wms_*` or `scc_*`.** `platform/…/V528:37,57,59` and `V663:17-19`
  still carry hardcoded `wms_*`/`scc_*`/`warehouse-*` **permission exclusions** from a deleted
  earlier module. Those migrations have already run, so they grant a new module nothing — but a new
  `wms_`-prefixed permission would inherit a decision nobody took.
- **`T-12` — every module's migrations are flattened into one directory at build time**
  (`Dockerfile.backend:140-181`). A number outside `V520000`–`V520999` collides with another
  module and is a **startup failure**, not a merge conflict.
- **`T-11` — the frontend merge is last-write-wins across the whole `src/` tree.** `whad`-prefix
  every file; `/warehouse/dealer/*` is this module's exclusive route root.
- **`AF-2` — ship en / fr / hi.** *The newest module ships all three*
  (`accounting-base/frontend/src/i18n/locales/{en,fr,hi}/`); CLAUDE.md's *"every module ships `en`
  only"* is stale. Register in the adapter's own SafeTranslation file, and **never** import `t()`
  from `@platform/hooks/useSafeTranslation`.
- **The counter's sub-ten-second target is a performance requirement with a test.** The dominant
  cost is the availability call and the reservation; measure them, do not assume.
- **`R1 §2` — a new module is 16 files and ~40 edits across 2 build systems.** `P0-14` did the
  scaffold and the reference adapter; this task must not re-do it, and must not skip
  `startX2.sh` **silently** — accounting skipped it and `AF-4` records that as
  *optional-but-name-it*.

## Acceptance
- [ ] `git log --oneline -- warehouse-base/` is **unchanged** at this task's merge commit, and the count is in the PR description
- [ ] A build-time architecture test fails on any `whb_`/`wh_` write, any base-table `ALTER`, and any base vocabulary widened by `ALTER`
- [ ] A counter sale completes in under ten seconds from first keystroke to printed ticket, measured, with no mouse interaction
- [ ] The sale reserves and issues through `POST /api/warehouse/reservations` and `POST /api/warehouse/movements` with `holder_system = ADAPTER_DEALER` — `grep` finds no internal service call into `warehouse-base`
- [ ] Running `V520000` twice registers each movement type once
- [ ] A refused line writes a lost sale into `wh_insufficient_stock_log`, and an interchange offer shows availability across branches
- [ ] `ModuleImportSelector` carries the module and `docker logs platform-backend` confirms it loaded
- [ ] i18n ships **en / fr / hi**, registered in the adapter's own SafeTranslation file
- [ ] `mobile/src/screens/whadCounterSale` degrades to scan-first with identical behaviour
- [ ] **Scenarios** `WH-SC-216` · `WH-SC-217` · `WH-SC-223` · `WH-SC-164` · `WH-SC-292`
