TITLE: [Warehouse] P0-04 · The open-catalogue framework and the four the ledger cannot exist without
LABELS: task,warehouse,phase-p0,warehouse-base
issue: 43
---
Part of __P0__ · Module **`warehouse-base`** · Migrations **V500002–V500004** · Screens **WS-001 Movement Types**, **WS-002 Document & Reference Types**, **WS-003 Source Systems**, **WS-006 Reason Codes**

## Getting this wrong breaks the adapter model permanently

Nothing fails on the day. What fails is `D-11`: an adapter that needs `PDI_CONSUME` or
`TRANSFER_DEPART` then needs a **core release** — which is precisely what the adapter contract exists
to prevent. **A base release per adapter, forever** (plan §4, risk R3).

Two failure modes, and the second is the likely one:

1. Someone adds `CHECK (direction IN ('IN','OUT'))` because it feels safe.
2. **The frontend re-closes what the backend opened**, because CLAUDE.md TYPESCRIPT RULE #6 mandates
   string unions over enums. R2 records this as *a documented recurring defect in this codebase*.
   `types/whbMovementType.ts` must **not** contain
   `type MovementTypeCode = 'RECEIPT' | 'ISSUE' | …`.

The precedent that works is live and self-documenting: `acc_reason_codes.context` carries no `CHECK`
**deliberately** — *"It is a CATALOGUE, not an enum"*
(`accounting-base/…/V600002__Create_acc_reason_codes.sql:22-26`). The counter-example is equally
live: `widget_definitions.chk_module` has been dropped and rebuilt **three times** (`V234:36` →
`V276:10` → `V557:18`) and still admits neither `warehouse` nor `logistics`. And the prior warehouse
product broke on exactly this: `location_type` / `zone_type` `CHECK`s *"dropped and recreated with
completely different enum value sets"* **36 versions after creation**.

## Scope

> **Round-2 addition — `Z-005`.** **Deactivation is a retirement, not a delete.** `is_active =
> false` means *the code may no longer be selected on a new document*; it does **not** mean the
> row is unreferenced. Deactivation is therefore **always permitted** and never returns
> `REGISTRY_ROW_IN_USE`. Historic rows continue to render the code's `name` and `badge_variant`
> exactly as before (`FR-381`), and the grid, filter and export continue to offer the retired code
> as a filter value so a five-year-old document is still findable. The dropdown endpoint returns
> active rows only, **plus the currently-selected value when editing an existing row**, so an old
> document does not silently lose its code on save. `REGISTRY_ROW_IN_USE` is retained for
> **Delete** only — `:delete`, `is_system = false`, zero references — where it is correct, and its
> message names the referencing table and the count. Each of `WS-001`, `WS-002`, `WS-003` and
> `WS-006` gains an `isActive` filter defaulting to active.

**`COEXISTENCE.md` `M4` lands here.** The UoM, reason-code, movement-type and warehouse-code
catalogues this task seeds are the *one list on both sides* that `M4` requires, because `D-9` keeps
`accessories` permanently separate and `P2-27`'s coexistence reports can only union two inventories
whose vocabularies agree. Seed from the accessories vocabulary where one already exists, and where
it does not, record which codes have no counterpart — `P2-27` reads that list to footnote its
reports rather than silently mis-adding (`X-052`, R3 `E-085`).

**The registry shape** (`DATA-MODEL.md` §1.8) is one table shape used fourteen times: `code`, `name`,
`description`, **`owning_module`** (an **opaque string, never an FK** — `FR-376`), `is_system`,
`is_active`, plus behaviour flags. **No `CHECK (x IN (…))`, no Java enum, no TypeScript string
union.** `WarehouseBaseCouplingTest` enumerates the fourteen `table.column` pairs and asserts none
carries a `CHECK` (`I-18`).

- **`V500002`** — `whb_source_systems`, `whb_document_types` + seed, **including the `ACCESSORIES`
  source-system row seeded `is_reserved = true`, `is_claimable = false`** with a comment naming
  `D-9`/`FR-367`. `accessories` is never an adapter and its code must not be claimable by one.
- **`V500003`** — `whb_movement_types` + the seed of the fourteen types **and their reversal
  counterparts**, including `FR-046`'s four **value-only** types: `COST_ADJUSTMENT`, `REVALUATION`,
  `LANDED_COST_APPLY`, `WRITE_DOWN`. Behaviour flags: `direction`, `is_financial`,
  `cost_basis_default`, **`balance_rule`** ∈ {`MUST_BALANCE_PER_OWNER_ITEM`, `MUST_BALANCE_PER_ITEM`,
  …}, `requires_approval`, `requires_reason`, `affects_availability`, `is_stock_bearing`,
  `is_billable_event`, `reversal_type_code`.
- **`V500004`** — `whb_reason_codes` + the seed of the **eleven contexts**. `context` carries **no
  `CHECK`**. Each row carries an **`itc_treatment`** and a **`statutory_category`**, because the
  reason code *is* a tax classification: the input-tax reversal on a write-off is computable only if
  the reason says so (`FR-315`).
- **Document display resolvers as a bean-collection registry** with a **base fallback** that renders
  the document type and id when no bean is registered (`FR-357`). The shape is settled —
  `ExportService.java:31`, `BoomBarrierWebhookController.java:44-48`,
  `AccImportHandlerRegistry.java:36-44` — and it is **not** `@Primary`, which admits only one
  implementation (`C-039`).

## Screens — WS-001, WS-002, WS-003, WS-006

All four are the **Department** shape: modal CRUD, no lifecycle, no cascade. Build one, copy it.

| | Table | `gridIdentifier` | Filter scope | Behaviour columns | Triage filters |
|---|---|---|---|---|---|
| WS-001 Movement Types | `whb_movement_types` | `whb_movement_types` | `WAREHOUSE_MOVEMENT_TYPE` | `direction`, `is_financial`, `cost_basis_default`, `balance_rule`, `requires_approval`, `requires_reason`, `affects_availability`, `is_stock_bearing`, `is_billable_event`, `reversal_type_code` | `direction` select · `is_financial` · `requires_approval` |
| WS-002 Document & Reference Types | `whb_document_types` | `whb_document_types` | `WAREHOUSE_DOCUMENT_TYPE` | `is_stock_bearing`, `is_external`, `display_resolver_bean` | `is_external` |
| WS-003 Source Systems | `whb_source_systems` | `whb_source_systems` | `WAREHOUSE_SOURCE_SYSTEM` | `module`, `is_reserved`, `is_claimable`, `post_permission` | `is_reserved` · `is_claimable` |
| WS-006 Reason Codes | `whb_reason_codes` | `whb_reason_codes` | `WAREHOUSE_REASON_CODE` | `context` (**no `CHECK`**), `requires_note`, `blocks_posting`, `affects_demand_history`, `itc_treatment`, `statutory_category` | `context` select · `affects_demand_history` |

- Permission `<table>:view|create|edit|delete|export`; export is
  `hasAuthority('<table>:export') or hasRole('ADMIN')`.
- Caches: `dropdown.<entityCamelCase>` — every catalogue backs at least one dropdown. Statistics
  **`—` (filter-aware, `FR-395`)**.
- **`code` is immutable after create** and read-only on the edit modal: every ledger row FKs to it by
  code (`whb_stock_movements.movement_type_code`, `…_lines.stock_status_code`).
- Row actions: View · Edit (`:edit` **and** `is_system = false`) · Activate/Deactivate (deactivation
  refused where a live row references the code — the service returns `REGISTRY_ROW_IN_USE`) · Delete
  (`:delete` **and** `is_system = false` **and** zero references).
- **Mobile: `none` for all four** — a catalogue is administered from a desk and no RF flow of
  `FR-217` edits one. **But `FR-382`/`OD-5` still applies:** wherever a mobile screen shows one of
  these vocabularies it must **fetch** it, and `mobile/src/schemas/common.schemas.ts` must not
  re-enumerate it as a zod enum. That file is a third copy of every dropdown vocabulary, and a value
  the backend opened but the schema closed fails the save **with no message**.

## Requirements closed
`FR-003` `FR-019` `FR-046` `FR-315` `FR-357` `FR-367` `FR-375` `FR-376` `FR-380`

`FR-315` is multi-phase (`P0·P4`): this task owns the reason code's ITC treatment and statutory
category; `P4-04` owns the reversal computation (plan §8.2).

## Scenarios closed
`WH-SC-035` `WH-SC-173` `WH-SC-179` `WH-SC-215` `WH-SC-226` `WH-SC-290`

## Closes
`C-039` · `P-014` · `G-064` · `T-005` · `Z-005`


*Added 2026-09-02 (`GAP-REGISTER.md` §5.2). These were `COVERED-uncited` **BLOCKER**s — owned only through an FRD row's `Closes` column, so a builder reading this file alone never met them:*
`P-003` `S-028`

## Traps

- **`OD-5` must be answered before the first warehouse page is written — and these are the first
  warehouse pages.** The recommendation: catalogue-backed dropdowns **fetch** their values; a string
  union is permitted only for a **closed system** vocabulary (`posting_status`, an import row's
  `map_status`, an HTTP outcome). `FR-380` is the rule; `OD-5` is the ruling that makes it
  enforceable.
- **`whb_document_types.display_resolver_bean` names a bean, and base must ship the fallback.**
  Without the fallback, a document type registered by a module that is not installed renders nothing
  and the movement register's "View source document" action dead-ends (`FR-357`, `WH-SC-226`).
- **`owning_module` is an opaque string and never an FK.** The previous attempt at this seam died of
  exactly the opposite: 84 FK references from warehouse into `scc_*`, several to tables that never
  existed (`P-003`, `OD-4`).
- **The `ACCESSORIES` source-system row must be unclaimable, with the reason in the migration
  comment.** `D-9` is a user decision; the row is what stops an engineer in two years wiring
  accessories in as an adapter and creating a second stock truth with a shared code (`FR-367`,
  `WH-SC-179`).
- **`filter_definitions.position` collisions.** `position` is `NOT NULL DEFAULT 0` with **no
  uniqueness**, so two filters at the same position render in an arbitrary order that changes between
  queries. Number every grid's filters from 1, contiguously, in reading order.
- **`grid_column_definitions.is_required = true` bricks the save** if the required column is not in
  `grid_preferences.default_columns` — silently, for that grid only. Mark `is_required` on **at most
  one identity column** (`code`) and assert its presence in the same migration
  (BUILD-SPEC-SCREENS §9.4).
- **Menu inserts are guarded with `WHERE NOT EXISTS`, never `ON CONFLICT DO NOTHING`** — `menus` has
  no unique constraint on `(name, parent_id, menu_level)` and a Flyway retry duplicates the row
  (`FR-410`, R1 §8 trap `T-9`).


## Acceptance
- [ ] `WarehouseBaseCouplingTest` asserts that **none of the fourteen registry columns** carries a
      `CHECK (… IN (…))`, and the test fails when one is deliberately added (`WH-SC-290`)
- [ ] A consumer adds a movement type by **idempotent migration in its own sub-band** and posts with
      it, with **zero commits to `warehouse-base`** (`WH-SC-173`)
- [ ] A movement with a free-text reason instead of a `reason_code_id` is refused (`WH-SC-035`)
- [ ] A write-off reason code carries an ITC treatment and a statutory category, and the reversal is
      computable from it (`WH-SC-215`)
- [ ] An adapter attempting to claim the `ACCESSORIES` source system is refused (`WH-SC-179`)
- [ ] A document type with no registered resolver bean renders through base's fallback rather than
      breaking the register (`WH-SC-226`)
- [ ] No `whb*` TypeScript type file enumerates a registry vocabulary as a string union, and
      `mobile/src/schemas/common.schemas.ts` does not re-close one as a zod enum
- [ ] Four grids ship `grid_column_definitions`, `filter_definitions` **and** `grid_preferences` with
      **both** JSONB arrays; four scopes are in `COMMON_FILTER_CONFIGS`; four `dropdown.*` names are
      in `CacheConfiguration.java`
- [ ] **Scenarios** `WH-SC-035` · `WH-SC-173` · `WH-SC-179` · `WH-SC-215` · `WH-SC-226` · `WH-SC-290`

## Blocked on
**`OD-5`** — does the frontend re-close the vocabularies the backend opens? Referred to the standards
owner, and it must be answered **before the first warehouse page**, which is this task's own screens.
