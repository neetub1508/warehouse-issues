TITLE: [Warehouse] P5-09 · Channel accounts, idempotent order import, publish rules as the oversell control, tracking links and cross-dock
LABELS: task,warehouse,phase-p5,warehouse
issue: 52
---
Part of __P5__ · Module **`warehouse`** · Migrations **V510200** · **V510208–V510209** · Screens **Cross-dock Plans (WS-087)** · **Channel Accounts (WS-123)** · **Channel Order Imports (WS-124)** · **Channel Publish Rules (WS-125)** · **Tracking Links (WS-126)**

## Scope

### Adopted solution and configuration contract — 2026-09-11

Implement [Global settings and resolved behaviour](https://github.com/neetub1508/warehouse-issues/blob/main/docs/GLOBAL-SETTINGS-DECISIONS.md), adopted by DECISIONS. This resolves earlier open-question/blocked-on wording for the decisions it names; external capability availability remains a testable gate. Preserve this task's existing module, migration and requirement ownership.

- `warehouse.dropship.enabled`: default **false**; allowed boolean; v2 capability required. Disable blocks new drop-ship orders; linked in-flight confirmations, reversals and reconciliation remain available.

**OD-18 solution:** v2 drop shipment uses a SUPPLIER-to-CUSTOMER virtual movement with purchase and demand line references and required lot/serial evidence, without changing physical on-hand. Default disabled; refuse in v1 or when the adapter is unavailable. Receipt/dispatch confirmations are idempotent and reversal follows the original links.


- **`V510208` — `wh_channel_accounts`, `wh_channel_order_imports`, `wh_channel_publish_rules`** (`WH-108`),
  over `whb_channels` (`WHB-51`, `V500051`, already v1).
- **`wh_channel_account_warehouses`** (`V510208`, built with this task) — the sites a channel account
  fulfils from, with a priority and a role `FULFIL`/`RETURNS`, as an **effective-dated junction**, not
  the scalar `wh_channel_accounts.warehouse_id` — a marketplace account fulfils from several sites
  (`RG-017`, `D-14`). The shape is R22 §1.3: `effective_from`/`effective_to`, an `EXCLUDE` refusing two
  overlapping rows for one account, site and role, and no `is_active` (`RG-017`).
- **Drop-shipment (`FR-467`) — gated on `OD-18`** (`RK-005`). A drop-shipment posts as `OD-18` decides;
  the recommended shape is one movement, `−q` at `SUPPLIER` and `+q` at `CUSTOMER`, with both documents in
  the source quad, serial and lot capture mandatory where the item is tracked, and no stock position ever
  created (`RK-005`). It needs no new column: both virtual locations are already seeded (`RK-005`).
- **Order import is idempotent on `(channel account, external order id)`**, with an **external version**
  so a stale re-poll is discarded, and an **import decision log** recording created / updated /
  ignored-stale / rejected **with the reason**. *"The order never came through" is the commonest support
  call in this category and must be answerable in one screen* (`F-027`).
- **Publish rules are the oversell control**: a basis (`AVAILABLE` · `ON_HAND` · `AVAILABLE_MINUS_BUFFER`
  · `FIXED` · `PERCENT`), a buffer, a maximum, a **minimum threshold below which zero is published**,
  included stock statuses and warehouses, and a **publish log recording what was pushed and what the
  channel acknowledged**.
- **`V510209` — `wh_tracking_links`** (`WH-109`): a **signed, expiring public tracking link** per shipment,
  and consumer notification rules on dispatch, out-for-delivery, NDR and delivery — **reusing the
  platform's existing providers rather than adding a second**.
- **`V510200` — `wh_cross_dock_plans`** (`WH-100`): GRN line × demand order line with a cross-dock type.
  The **nullable `cross_dock_reference` column on the v1 receipt line** (`P1-13`, `FR-137`) is what makes
  *"which receipts were cross-docked"* answerable for the period before this feature shipped.

## Requirements closed
`FR-208` · `FR-209` · `FR-210` · `FR-467`

**`FR-467` is gated on `OD-18`** — it is built only once that row is answered (`RK-005`); see *Blocked on*.

**Follow-on ownership:** `FR-137`'s v1 half is `P1-13`'s (the nullable column); **this task builds the
cross-dock behaviour over it** (`IMPLEMENTATION-PLAN.md` §8.2).

## Scenarios closed
`WH-SC-326` — an OEM ships a serialised part straight to a fleet customer: one movement
`SUPPLIER → CUSTOMER` with the serial captured, so the later return has an origin; gated on `OD-18`
(`RK-005`).

<!-- check-design-set: scenario-citations begin WH-SC-306 — the SCENARIO-CATALOGUE.md §5 rule 3 allocation marker — the next free scenario id, which by definition has no row yet. Named here so a parallel task does not silently take it twice; it is never a citation of a scenario that exists -->
**None for `FR-208`–`FR-210` in `SCENARIO-CATALOGUE.md`** — `WH-SC-326` above is `FR-467`'s alone (`RK-005`). `FR-208`–`FR-210` are on §4.2's unproven list (the carrier, channel
and parcel surface). **`FR-207`'s v1 half is separately flagged there as *"the one genuinely v1 gap"* and
needs a scenario before `P1` closes — this task depends on that column existing.**
Author at minimum: the same `(channel account, external order id)` imported twice producing **one**
demand order (this is a named clause of the **v2 exit criterion** in `IMPLEMENTATION-PLAN.md` §1.7); a
re-poll carrying a lower external version being **ignored as stale** with the decision logged; and a
publish run pushing zero when available falls below the floor. Take the next free `WH-SC-` ids from
`WH-SC-306` and claim them in the catalogue in one commit before writing code.

<!-- check-design-set: scenario-citations end -->

## Closes
`F-027` (import idempotent on the channel pair, with a decision log) · `F-035` (publish rules as the
oversell control) · `F-049` (signed expiring public tracking link) · `T-039` (the cross-dock flag as a v1
column) · `RK-005` (drop-ship placed in v2 as `FR-467`, gated on `OD-18`).

## Traps

- **⚠ `V510126` is not this task's number.** An early draft of this row cited `V510126` for
  `wh_tracking_links`; that number is not allocated by `DATA-MODEL.md` §7.3 and the table is `WH-109` at
  **`V510209`**. Recorded in `IMPLEMENTATION-PLAN.md` §2.9 because *"an invented migration number that
  looks plausible is precisely the failure mode `DECISIONS.md` §7 rule 3 exists to prevent."*
- **Idempotency is on the pair plus a version, not on the pair alone.** Marketplaces re-poll and send
  *updates*; a design that treats every repeat as a duplicate loses genuine address and quantity changes,
  and a design with no version applies an old payload over a new one.
- **A rejected import is a row, not a log line.** It must be visible, filterable and re-runnable from
  WS-124; that screen is the answer to the support call.
- **The publish log records the channel's acknowledgement, not just our push.** An oversell almost always
  traces to a push the channel silently dropped.
- **`AVAILABLE` means `on_hand − Σ open reservations`, computed, never stored** (`L-6`, `P0-03`).
  Publishing from a stored counter re-introduces exactly the drift `C-022` records in accessories, where
  `quantity_reserved` is read in 7 places and written in 0.
- **The tracking link is signed and expiring and is a public endpoint.** It is the only unauthenticated
  surface in the warehouse product. Never leak owner, cost, client name or another shipment through it;
  the token must not be enumerable.
- **Reuse the platform's notification providers.** The repo already carries an AiSensy WhatsApp provider
  and the platform email/template registry; a second provider stack is a maintenance surface nobody asked
  for.
- **Every warehouse frontend filename must be globally unique** — `Dockerfile.frontend:80-177` merges
  module `src/` last-write-wins (R1 `C-012`). `ChannelAccountModal.tsx` is a collision waiting to happen;
  `WhChannelAccountModal.tsx` is not.
- **The drop-ship movement type is not seeded in v1** (`RK-005`). Movement types live in `V500003`, not
  `V500013` as R25 wrote, and `D-10` lets any module seed the type once `OD-18` is answered — no v1
  migration is edited for it (`RK-005`).
- Register five filter scopes in `COMMON_FILTER_CONFIGS` (`filterUtils.ts:346`) or every one of these
  filters renders and does nothing.

## Acceptance

- [ ] `CONFIG-CASE-12` in the adopted contract passes, including refusal/retry behavior; record evidence in this task's implementation PR.
- [ ] No setting rewrites posted history, bypasses permissions/period locks or silently enables an unavailable capability.
- [ ] Importing the same `(channel account, external order id)` twice produces **one** demand order — a named clause of the v2 exit criterion
- [ ] A re-poll with a lower external version is **ignored as stale** and the decision log says so with a reason
- [ ] A rejected import is visible and re-runnable from WS-124, and names the field that failed
- [ ] Publish computes from `available` derived at read time, honours buffer, maximum and floor, publishes **zero** below the floor, and logs the channel's acknowledgement
- [ ] A signed tracking link expires, and an expired or tampered token returns a neutral 404 that leaks nothing
- [ ] Consumer notifications fire on dispatch, out-for-delivery, NDR and delivery through the platform's existing providers
- [ ] A cross-dock plan stamps `cross_dock_reference` on the receipt line, and the KPI is answerable for receipts predating this feature (they read null, not missing)
- [ ] **Mobile:** cross-dock confirmation on the RF Receive screen (`screens/whRfReceive`) or a stated `none` with a reason; channel accounts, publish rules and tracking links are `none` — desk masters (`D-13`)
- [ ] A channel account fulfilling from two sites is two `wh_channel_account_warehouses` rows; `wh_channel_accounts` carries no `warehouse_id`, and an `EXCLUDE` refuses an overlapping duplicate (`RG-017`)
- [ ] Once `OD-18` is answered, a drop-shipped serialised part posts as `OD-18` records, never touches a physical location and creates no stock position (`RK-005`)
- [ ] **Scenarios** `WH-SC-326` (`RK-005`, gated on `OD-18`) · plus the ids this task authors into `SCENARIO-CATALOGUE.md`

## Blocked on
**`OD-18`** — what the product records for a drop-shipment (`RK-005`). `FR-467` and `WH-SC-326` are
gated on it; the rest of this task is not (`RK-005`). In v1 a drop-shipment is simply not recorded by
warehouse, and **the drop-ship movement type is not seeded in v1** — any module seeds it under `D-10`
once `OD-18` is answered (`RK-005`). The row is escalated because it decides what the product claims to
track for goods it never touches (`OD-18`, `RK-005`).
