TITLE: [Warehouse] P2-13 · Supplier returns, quarantine disposition, and inbound reconciliation as a decision centre that never moves stock
LABELS: task,warehouse,phase-p2,warehouse
issue: 93
---
Part of __P2__ · Module **`warehouse`** · Migrations **V510019, V510020, V511206, V511236** · Screens **WS-083 Inbound Reconciliation Cases**, **WS-084 Supplier Returns**

## Scope

**`V510019` — WH-10 — `wh_reconciliation_cases`, `wh_reconciliation_case_events`.**
**`V510020` — WH-11 — `wh_supplier_returns`, `wh_supplier_return_lines`.**

Both numbers sit in the **inbound** block (`V510010`–`V510020`) even though the task is P2, because
they belong beside receiving in the migration order. That is deliberate and it is not a collision:
`P1-12`…`P1-16` own `V510011`–`V510018`.

**A supplier return is not an RMA** (`FR-139`). It is **its own document with its own state ladder**
— `DRAFT` → `APPROVED` → `PICKED` → `DISPATCHED` → `CLOSED` / `CANCELLED` — and **inventory is
reduced only at dispatch**. *The prior system's bug was a QC-fail auto-RMA stuffing the supplier's
name into a customer field.* The outbound movement shape is `P2-12`'s `FR-275`; the document, the
ladder and the state gates are here. Columns: `return_number` (gapless), `supplier_counterparty_id`,
`warehouse_id`, `owner_id`, `origin_grn_id`, `origin_lot_id`, `reason_code_id`, `status`,
`dispatched_at`, `total_value`, and `demand_order_id`, which `P2-08`'s `V510040` adds by `ALTER`.

**It runs through the one demand model, not beside it** (`RJ-003`).
- *Approve* creates **one** `wh_demand_orders` row of demand type `VENDOR_RETURN`, a `DEMAND_TYPE`
  code-list row (`RL-006`). **Its stock is reserved from that moment.**
- Reservation, pick and staging are `P2-07` and `P2-09`. The dispatch is `wh_shipments:dispatch`
  (`P2-10`), relieving to the `SUPPLIER` virtual location.
- **`PICKED` and `DISPATCHED` are effects of the demand path**, not actions on WS-084.
- Before round 4, WS-084 had its own *Pick* and *Dispatch* with no pick-task mechanism behind them,
  and nothing reserved a supplier return's stock. `FR-275`'s *"return to vendor is an ordinary
  outbound"* is now literally true.

**The header's origin columns are nullable and derived** (`RG-019`). `origin_grn_id` and
`origin_lot_id` hold the only origin, when there is only one. The lines carry their own origin, so a
return spanning two GRNs does not contradict its header. *"Returns against GRN X"* reads through the
lines.

**Which layer a return to vendor relieves** (`RJ-010`, `P2-16`). A line carrying an origin GRN
relieves **that receipt's layer**, by specific identification. A line with no origin relieves by the
site's valuation method, as any issue does.

**Quarantine disposition is a workflow with a QA-role gate** (`FR-134`) — release / reject / return
to supplier / scrap — and **regulated item classes are quarantined by default on receipt rather than
flagged**. The default chain is item → supplier → `AVAILABLE` (`FR-129`, `P1-13`); a regulated class
overrides it to `QUARANTINE` **on the first transaction**, and the stock is never `AVAILABLE` for an
instant. Release posts a **balanced status-change movement at the same location**
(`QUARANTINE → AVAILABLE`) with a **mandatory reason** — quarantine never requires a physical move.

**Inbound reconciliation is a decision centre that never moves stock itself** (`FR-138`). A case
carries a **type** (quantity / over-receipt / invoice / ASN / inventory variance / receipt reversal /
supplier return), a **severity**, a **status**, **nullable document links**, a **root cause** and —
the load-bearing column — a **resolution-document pointer**. Cases are **auto-created at variance
points**, and **each of the five resolutions creates a document**. The case itself posts nothing;
its resolution names the document that does.

> *`P-028` places inbound reconciliation at v1.1; `DECISIONS.md` §5 makes v1.1 = P3 execution and
> puts adjustments in the v1 exit criterion, so it lands v1/P2.* That reasoning is `FR-138`'s own —
> do not re-defer it.

**The two ladders** (`RJ-004`), in §0.11's columns. The supplier return's is the ladder this task
always carried, now with its demand-path effects. The case's is derived from WS-083's actions.

| Table | From | To | Verb | Actor (permission) | Guard | Terminal? |
|---|---|---|---|---|---|---|
| `wh_supplier_returns` | — | `DRAFT` | Add | `wh_supplier_returns:create` | — | no |
| `wh_supplier_returns` | `DRAFT` | `APPROVED` | Approve | `wh_supplier_returns:approve` | approver ≠ creator (`FR-408`); creates the `VENDOR_RETURN` demand order | no |
| `wh_supplier_returns` | `APPROVED` | `PICKED` | *(effect of the demand order's pick)* | `wh_pick_tasks:edit` | never set directly | no |
| `wh_supplier_returns` | `PICKED` | `DISPATCHED` | *(effect of the shipment's dispatch)* | `wh_shipments:dispatch` | inventory is reduced here and only here | no |
| `wh_supplier_returns` | `DISPATCHED` | `CLOSED` | Close | `wh_supplier_returns:close` | — | **yes** |
| `wh_supplier_returns` | `DRAFT` · `APPROVED` | `CANCELLED` | Cancel | `wh_supplier_returns:cancel` | cancels the demand order and releases its reservation | **yes** |
| `wh_supplier_returns` | `PICKED` | `CANCELLED` | Cancel | `wh_supplier_returns:cancel` | **refused with `409 STAGED_STOCK`** until the staged stock is de-staged (`RJ-006`, `FR-449`) | **yes** |
| `wh_reconciliation_cases` | — | `OPEN` | *(auto-created at a variance point)* | the posting service | — | no |
| `wh_reconciliation_cases` | `OPEN` · `ASSIGNED` | `ASSIGNED` | Assign | `wh_reconciliation_cases:edit` | — | no |
| `wh_reconciliation_cases` | `OPEN` · `ASSIGNED` | `RESOLVED` | Resolve | `wh_reconciliation_cases:resolve` | names the resulting document; the case posts nothing | **yes** |

**A supplier return picked and then cancelled is not a hole** (`RJ-006`). Before round 4 that was the
staged-stock state `FR-449` had closed for orders only. The cancel either de-stages the stock by a
real movement, or it is refused with `409 STAGED_STOCK`, naming the staging location and the quantity.

**The verbs** (`RJ-005`). Before round 4 `wh_supplier_returns` had no §10.2 row at all.
- `V511206` seeds `wh_supplier_returns:approve`, `:close` and `:cancel`, and
  `wh_reconciliation_cases:resolve`. The pick and the dispatch are the demand path's verbs.
- `V511236` inserts their `permission_dependencies` rows.

**Screens.** WS-083 `wh_reconciliation_cases` · `WAREHOUSE_RECONCILIATION_CASE` · Service Vehicle —
Assign · Add event (the child timeline) · **Resolve** (*the modal names the resulting document; the
case never moves stock itself*). WS-084 `wh_supplier_returns` · `WAREHOUSE_SUPPLIER_RETURN` ·
Service Vehicle — Approve · Close · Cancel · Print. **The pick and the dispatch are shown, not
offered**: they belong to the demand order and the shipment (`RJ-003`), and a status column links to
each.

**Mobile.** `mobile/src/screens/whSupplierReturn` lists supplier returns and their status. Picking
and dispatch are floor tasks, done on `whPickTask` (`P2-09`) and `whShipment` (`P2-10`), like every
other outbound. `none` for WS-083 (a controller decision centre), stated.

**WS-073's Returns and Exceptions tabs are this task's child grids (hand-off from `P1-12`, 2026-09-15).**
`P1-12` ships each tab as a count (`WhPurchaseOrderQueryService.RETURN_COUNT` / `EXCEPTION_COUNT`) or
*not available* with the reason "Supplier returns / Reconciliation cases arrive with P2-13". Replace
them with child grids — the supplier returns of the PO's GRNs, and the reconciliation cases whose
subject is the PO or one of its GRNs — with no `gridIdentifier` of their own (BUILD-SPEC WS-073).

**`FR-422`'s concurrent-counting number is asserted HERE** (`K-003`, `R13 K-003` disposition:
*"three targets need no fixture and become ordinary tests in `P0-03` (concurrent allocation),
`P2-13` (10 concurrent counters) and `P2-29` (100k streaming export); name them there"*). It is
named here, in the same four-cell shape as every other `FR-422` number, so it is testable on the
day this task lands:

| | |
|---|---|
| **Number** | a **5,000-line count** entered by **10 counters concurrently** |
| **What is measured** | ten concurrent sessions entering count lines against **one** count document; the assertion is that **no line is lost, no line is double-applied, and the variance total equals the sum of the per-line variances** |
| **Fixture** | **none needed — an ordinary test.** This is concurrency, not volume; it does not run on `LedgerVolumeFixtureTest` and is not `@Tag("perf")` |
| **Asserted by** | **`P2-13`** (ten concurrent counters) |

> ⚠ **The count document itself is built by `P2-04`** (*Counting — a document that proposes an
> adjustment and never writes on-hand*); this task owns supplier returns, quarantine disposition and
> inbound reconciliation. `R13`'s `K-003` disposition nonetheless routes the number to `P2-13` by
> name, and `NonFunctionalFoundationsTest.FR_422_TARGETS` records that routing, so it is recorded
> here rather than silently re-homed. **A person should ratify whether the assertion runs here or
> moves to `P2-04`**; whichever is chosen, both this file and `FR_422_TARGETS` must say the same
> thing, because `shouldRouteTheFixtureFreeNumbersToTheirOwners` fails if the number loses its owner.

The same four cells are recorded in
`warehouse-base/backend/src/test/java/ai/warehousebase/nonfunctional/NonFunctionalFoundationsTest.java`
(`FR_422_TARGETS`). `K-003`: a number with neither a measurement method nor an owning task is
**deleted**, not left undefended.

## Requirements closed
`FR-134` · `FR-138` · `FR-139`

## Scenarios closed
- `WH-SC-071` — a QA-role holder releases 228 and rejects 12; release posts a balanced status-change movement **at the same location** with a mandatory reason
- `WH-SC-072` — a supplier return is raised and dispatched as **its own document with its own ladder**, and the supplier's name never reaches a customer field
- `WH-SC-073` — a regulated class lands in `QUARANTINE` **on the first transaction** and is never `AVAILABLE`
- `WH-SC-084` — each variance point auto-creates a case with a type, a severity, a status, nullable document links, a root cause and a **resolution-document pointer**
- `WH-SC-067` — a 12 % over-receipt against a 5 % tolerance is refused and raises a case

## Closes
`S-037` `E-029` (`FR-134`) · `P-028` `T-038` (`FR-138`) · `F-056` (`FR-139`)

## Traps

- **The case never posts a movement.** `wh_reconciliation_cases` has **no** `movement_id` column.
  It has `resolution_document_type` + `resolution_document_id`. The moment a case can move stock, it
  becomes a second adjustment path with no reason-code discipline.
- **"Quarantined by default" is a receipt-time default, not a post-receipt status change.** If the
  stock is `AVAILABLE` for one transaction and then moved to `QUARANTINE`, the allocator could have
  taken it in between. `FR-129`'s defaulting chain resolves **before** the first movement is built.
- **A status-change movement is two lines at the same location**, differing only in
  `stock_status_code` (`FR-103`). It is not an adjustment and it does not change quantity.
- **`C-025`** — `AccessoryIssuanceService.java:277-279` swallows non-`BusinessException` failures
  and continues (line reference **refined from R1's `:275-278`** by `COEXISTENCE.md` §8.1); the
  `BusinessException` branch immediately above at `:270-276` correctly re-throws. Do not copy the
  general `catch (Exception e)`.
- **`V510019`/`V510020` are in the inbound block but this is a P2 task.** Do not "tidy" them into
  `V510036`–`V510039`; the numbers are pre-allocated in `DATA-MODEL.md` §7.3 as WH-10/WH-11 and
  `tools/check-design-set.py` fails on a renumber.
- **The QA role is a permission, not a job title.** Seed it in `P1-20`'s `V511000` block and gate
  release on it; `permission_dependencies` rows are **inserted, never created** (`T-10`) — the
  columns are `permission_id` and **`dependent_permission_id`**.
- **`T-9`** — `WHERE NOT EXISTS` on the menu inserts.
- **`T-6`** — two scopes; `openedFrom`/`openedTo` and `createdFrom`/`createdTo` are `date` pairs and
  **all four keys** are listed.

## Acceptance
- [ ] A supplier return reduces inventory **only** at `DISPATCHED`; approving and picking move nothing
- [ ] The supplier return's ladder is distinct from the RMA's, and `wh_supplier_returns` has no customer column
- [ ] A regulated item class lands in `QUARANTINE` on its first transaction; there is no window in which it is `AVAILABLE`
- [ ] Quarantine release is refused without the QA permission, and posts a two-line same-location status-change movement with a mandatory reason
- [ ] `wh_reconciliation_cases` has **no** movement column; each of the five resolutions produces a named document
- [ ] Cases are auto-created at each variance point, not raised by hand
- [ ] Approving a supplier return creates one `VENDOR_RETURN` demand order and reserves its stock; WS-084 offers no *Pick* or *Dispatch*, and `grep` finds no supplier-return-side pick or dispatch path (`RJ-003`)
- [ ] Cancelling a `PICKED` supplier return de-stages the stock or returns `409 STAGED_STOCK` naming the location and quantity (`RJ-006`)
- [ ] A line with an origin GRN relieves that receipt's layer; the header's `origin_grn_id` is set only when every line shares one origin (`RJ-010`, `RG-019`)
- [ ] *Approve*, *Close*, *Cancel* and case *Resolve* are each refused to a user holding only `:edit` (`RJ-005`); both ladders have no state without an inbound transition and no non-terminal state without an outbound one
- [ ] `FR-422`'s **5,000-line count entered by 10 concurrent counters** is an ordinary test — no
      fixture, no `@Tag("perf")` — asserting **no lost line, no double-applied line, and a variance
      total equal to the sum of the per-line variances** (`K-003`)
- [ ] **Scenarios** `WH-SC-071` · `WH-SC-072` · `WH-SC-073` · `WH-SC-084` · `WH-SC-067`
