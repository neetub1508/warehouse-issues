# Functional Contract — Movement post / reverse (the stock ledger's single writer)

> **What this is.** The functional truth for posting a stock movement, re-posting it idempotently,
> reversing it, and what positions, periods, availability, the as-at query and the `L-4` rebuild
> observably do as a result. Every row cites its source id, so a later gate can check a diff against
> the row rather than re-derive it.
>
> **What this is not.** It does not restate `DECISIONS.md`, `DATA-MODEL.md` or the port contract. A row
> points at them. It does not cover coding standards. Where two sources disagree, the row says so and
> points at §9. It never picks one source silently.
>
> **Status.** Derived from the design set on 2026-09-12 and **ratified** at `57bd7df` (W0-2a). **No code
> exists yet.** Every §9 row was resolved on 2026-09-14 (WAVE C0). The user decided `MPR-OPEN-05`,
> `MPR-OPEN-07` and G5; the rest follow the design set's documented defaults and the closest `classic`
> precedent (§7). `TBD` marks a cell no source answers. An `OPEN-nn` or `G` citation points at its
> resolution in §9.

---

## 0. Identity

| | |
|---|---|
| **Workflow** | Movement post / idempotent re-post / reverse, with the position cache, period gating, availability refusal, the as-at query and the `L-4` rebuild |
| **Module** | `warehouse-base` (package `ai.warehousebase`, `D-1`) |
| **Domain baseline** | `docs/DECISIONS.md` (`D-4`, `D-15`, `L-1`…`L-15`) · `docs/DATA-MODEL.md` §1.9, §2.1.8, §2.1.9, §6.3–§6.5 · `docs/PORT-AND-ADAPTER-CONTRACT.md` §3.5–§3.10 · `docs/IRREVERSIBLE.md` §4.1–§4.3 · `docs/BUILD-SPEC-SCREENS.md` §2.6, §10.2 · `docs/SCENARIO-CATALOGUE.md` §3.1, §3.9, §3.11 · `docs/GLOBAL-SETTINGS-DECISIONS.md` |
| **Owning task bodies** | `issues/p0-02.md` (ledger, WS-040/041/042) · `issues/p0-03.md` (writer, availability, as-at, rebuild) · `issues/p0-08.md` (port, idempotency ladder) · `issues/p0-07.md` (period ladder, override) |
| **Primary table(s)** | `whb_stock_movements`, `whb_stock_movement_lines`, `whb_movement_line_attributes` (`V500030`); `whb_stock_positions` (`V500031`); read: `whb_stock_periods`, `whb_stock_period_overrides` (`V500019`); `whb_position_drift_findings` (`V500045`); `whb_inbound_messages`, `whb_movement_batches`, `whb_movement_batch_results` (`V500041`); `whb_movement_idempotency_keys` (`V500030`, `I-11`'s guard) |
| **Status column** | **There is no header `status` column, and no DB `CHECK`** (`RL-006`, `D-10`, `MPR-OPEN-05`). Lifecycle is carried by `approval_status` (`CLOSED-SYSTEM`, writer-enforced): `NULL`/`APPROVED` = ledger effect; `PENDING` = no ledger effect and the only header `I-2` lets change; `REJECTED`/`WITHDRAWN` = terminal. Also `is_reversed` + `reversed_by_movement_id` + `reversal_of_movement_id` |
| **Permission resource** | Port: `warehouse:movements:post` · `:reverse` · `:simulate` · `:view`. Screens: `whb_stock_movements:view` · `:export` · `:approve` · `:post_backdated` · `:verify`; `whb_stock_positions:view` · `:export` · `:rebuild` · `:change_status`; `whb_stock_periods:override`; `whb_negative_stock_policies:override`. `BUILD-SPEC-SCREENS.md` §10.2 is the sole seed authority (`RA-007`; `MPR-OPEN-01`, `-02`, G2, G3) |
| **Routes** | web: `/api/warehouse/movements` (+ `/batch`, `/{id}/reverse`, `/{id}/withdraw`, `/simulate`, `/{id}`, lineage `GET`), `GET /stock/as-at`, `/warehouse/ledger/movements` (WS-040), `/warehouse/ledger/movements/[id]` (WS-041), `/warehouse/ledger/positions` (WS-042) · **mobile: none.** Scope rule set by the user on 2026-09-11: warehouse gets **no mobile app**, so this screen contract is web only. The sources that still say otherwise are listed at **OPEN-13** (resolved) |
| **Requirement source** | `issues/p0-02.md`, `issues/p0-03.md`, `issues/p0-08.md`, `issues/p0-07.md` at `warehouse-issues` `main` `7ab6717` |
| **Contract status** | `ratified` at `57bd7df` (W0-2a); amended 2026-09-14 (WAVE C0) — every §9 row resolved; amended 2026-09-15 (C1 fix cycle, P0-02) — see §7 |
| **Contract version** | `v1 — 2026-09-14` |

### Acceptance checks

| # | The requester expects… | Delivered by |
|---|---|---|
| A1 | A receipt writes two balanced lines against a virtual location, and a one-sided movement is refused before any row is written (`WH-SC-001`, `WH-SC-002`) | `MPR-T1-01`, `MPR-GRD-01` |
| A2 | A retried identical request returns the original movement with `200` and posts nothing; a reused key with a different body is `409` naming the original (`WH-SC-023`, `WH-SC-165`) | `MPR-T1-03`, `MPR-GRD-03`, `MPR-GRD-04` |
| A3 | A correction is a mirrored, linked reversal with a mandatory reason; a reversal cannot be reversed; nothing is ever edited or deleted (`WH-SC-006`…`WH-SC-013`) | `MPR-T1-08`, `MPR-UNR-01`…`MPR-UNR-05` |
| A4 | A `CLOSED` period refuses every movement including a reversal; a `SOFT_CLOSED` one admits only a recorded, authorised override (`WH-SC-021`, `WH-SC-022`) | `MPR-GRD-09`, `MPR-GRD-10`, `MPR-T4-06` |
| A5 | A status change is a balanced two-line movement at one location with a reason (`FR-103`) | `MPR-T1-10`, `MPR-GRD-18` |
| A6 | An issue beyond available stock is refused with the arithmetic in the message; contention is a different code from shortage (`WH-SC-017`, `WH-SC-018`, `WH-SC-190`) | `MPR-GRD-15`, `MPR-GRD-16`, `MPR-GRD-17` |
| A7 | The as-at query answers from the ledger, and a full rebuild reproduces every position row exactly (`WH-SC-040`, `WH-SC-014`, `WH-SC-061`) | `MPR-T1-13`, `MPR-T3-01`, `MPR-T4-09` |
| A8 | The deliverables of `CONFIG-CASE-05`, `CONFIG-CASE-06` and `CONFIG-CASE-10` hold for this workflow (`GLOBAL-SETTINGS-DECISIONS.md`) | `MPR-GRD-02`, `MPR-GRD-16`, `MPR-T1-01` |

---

## 1. T1 — State machine

A movement is a header plus two or more signed lines (`D-4`, `FR-001`). There is no draft screen. A
movement arrives through the port or a document screen and posts in one transaction, or it is refused
and nothing is written (`FR-040`, `FR-029`).

### 1.1 Status inventory

| Status | Represented by | Reachable via | Exits via | Kind |
|---|---|---|---|---|
| `POSTED_LIVE` | header with ledger effect (`approval_status IS NULL` or `APPROVED`), `is_reversed = false`, `reversal_of_movement_id IS NULL` | `MPR-T1-01`, `-02`, `-05`, `-10`, `-11` | `MPR-T1-08` | active |
| `POSTED_REVERSED` | `is_reversed = true`, `reversed_by_movement_id` set (only through `I-2`'s allowlist, `I-3`) | `MPR-T1-08` when the reversal's type needs no approval; `MPR-T1-05` when a PENDING reversal is approved. A PENDING, REJECTED or WITHDRAWN reversal leaves its original `POSTED_LIVE`. | none | **terminal** |
| `REVERSAL` | posted header with `reversal_of_movement_id` set, `movement_type_code` = original's `reversal_type_code` | `MPR-T1-08` | none. `CANNOT_REVERSE_A_REVERSAL` (`PC-20`) | **terminal** |
| `PENDING_APPROVAL` | `approval_status = PENDING`: a ledger row with **no ledger effect** (`WH-SC-039`). It is out of positions, the as-at query, `I-1`'s assertion and the `I-7` rebuild, and it is the only header `I-2` still lets change (`MPR-OPEN-05`) | `MPR-T1-04` | `MPR-T1-05`, `-06`, `-07` | active |
| `REJECTED` | `approval_status = REJECTED`, row kept, no ledger effect (`L-2`) | `MPR-T1-06` | none — resubmit as a new movement with a new key (G4) | **terminal** |
| `WITHDRAWN` | `approval_status = WITHDRAWN`, row kept, no ledger effect (`RA-004`) | `MPR-T1-07` | none | **terminal** (`RA-004`) |

Not statuses, stated so they are not mistaken for them: `posting_status` (`NOT_APPLICABLE`/`PENDING`/
`POSTED`/`REJECTED`/`BLOCKED`/`DISCARDED`) belongs to the accounting handover (`IRR-41`, P0-12's workflow) and appears here
only in T4. A **simulated** movement is not a state, because it writes nothing (`PC-27`).

### 1.2 Transitions

| ID | From | Event / action | To | Guard (service, first) | Actor (permission) | Side effects | Comment/reason required | UI entry point |
|---|---|---|---|---|---|---|---|---|
| MPR-T1-01 | — | Post one movement. `POST /api/warehouse/movements`, one transaction, all or nothing (`FR-032`, `FR-040`) | `POSTED_LIVE` | `MPR-GRD-01`…`-09`, `-11`, `-15`…`-17`, `-21`…`-24`, `-26` | `warehouse:movements:post` (`PC-31`); owner grants checked by the single resolver, `403 OWNER_NOT_PERMITTED` (p0-08 Traps) | `MPR-T4-01`, `-02`, `-03`, `-11`, `-12`, `-14`. Returns `201` with `movement_id` + `sequence_no` (p0-08 idempotency table) | only if the type has `requires_reason` (`FR-019`, `WH-SC-035`) | **no Add on WS-040** (`FR-436`). Posted by the port or by a document screen |
| MPR-T1-02 | — | Batch post. `POST /movements/batch`: N movements, each with its own key, **each in its own transaction**, processed in array order (`FR-034`, `WH-SC-167`, `WH-SC-168`) | `POSTED_LIVE` per entry | per movement, as `MPR-T1-01`; plus `MPR-GRD-23` for the batch | `warehouse:movements:post` | per-movement result array (`CREATED`/`DUPLICATE`/`CONFLICT`/`REJECTED`), with `whb_movement_batch_results` rows | as T1-01 | port only (WS-055 shows the results) |
| MPR-T1-03 | any posted status | Idempotent re-post: same `(source_system, idempotency_key)`, identical `payload_hash` | unchanged | `MPR-GRD-04` | as the original call | **nothing written**. `200` with the **original** `movement_id` and `sequence_no`, read from its `whb_movement_idempotency_keys` row (`WH-SC-023`, `WH-SC-180`). Holds across a month boundary because that registry is not partitioned (`RL-011`, `MPR-OPEN-07`). A repeated `batch_reference` returns `200` with the stored result array (`RD-004`) | no | port only |
| MPR-T1-04 | — | Post a movement whose type has `requires_approval` (`FR-027`) | `PENDING_APPROVAL` | as T1-01, except that positions do not move, so availability (`MPR-GRD-15`, `-16`) is **not** checked at submission — it is checked at approval (`MPR-T1-05`) | `warehouse:movements:post` | the header (`approval_status = PENDING`), its lines and its `whb_movement_idempotency_keys` row are written; `sequence_no` and `posted_at` are assigned at acceptance; **no position change**, no handover and no outbox event until approval (`MPR-T4-07`, `-08`), and the row is out of the as-at query and the rebuild (`WH-SC-039`, `MPR-OPEN-05`). For a reversal, the original stays `POSTED_LIVE` (`is_reversed = false`, `reversed_by_movement_id` NULL) and reversible. I-3 marks it at INSERT and the writer takes the mark back in the same transaction. | type-dependent | port / document screen |
| MPR-T1-05 | `PENDING_APPROVAL` | Approve. Held by `WhbStockLedgerWriter.approve(movementId, approverId, acknowledgement)` (`P0-03`); the endpoint and the actions that call it are **`P0-08`'s** (`issues/p0-08.md`, hand-off) | `POSTED_LIVE` | `MPR-GRD-28` (row is `PENDING`); approver ≠ actor (`MPR-GRD-19`); every `WhbMovementApprovalGuard` bean (`MPR-GRD-27`); the period of `posting_date` has not closed since submission, else `MPR-GRD-20`; availability `MPR-GRD-15`, `-16` checked **here**, against the approver's `WARN` acknowledgement and the approver's authorities read from the security context (`FR-448`) | `whb_stock_movements:approve` (BUILD-SPEC §10.2) | `approval_status` → `APPROVED`; stamps `approved_by`, `approved_at`; restamps `posted_at`; `I-1` asserts conservation; positions move in the same transaction (`WH-SC-039`); then the handover and the outbox event, last (`MPR-T4-08`, `-07`). For a reversal, the original becomes `POSTED_REVERSED` in the same transaction, through I-3's conditional update, after the position locks. Zero rows gives `409 ALREADY_REVERSED` (`MPR-GRD-13`). | no — the Approve modal carries none (BUILD-SPEC WS-040 *Modals*) | WS-040 row action **Approve** (`approval_status = PENDING`); WS-041 action — both `P0-08` |
| MPR-T1-06 | `PENDING_APPROVAL` | Reject. Held by `WhbStockLedgerWriter.reject(movementId, approverId)` (`P0-03`); the endpoint and the action are **`P0-08`'s** | `REJECTED` | `MPR-GRD-28` (row is `PENDING`); approver ≠ actor (`MPR-GRD-19`); every `WhbMovementApprovalGuard` bean (`MPR-GRD-27`) | `whb_stock_movements:approve` (WS-040 Actions) | `approval_status` → `REJECTED`; the decider is stamped in `approved_by`, `approved_at` (the header has no separate rejected-by pair); no position change, no handover, no outbox event; row kept (`L-2`). For a reversal, the original stays `POSTED_LIVE` and reversible. | `TBD` — the writer carries none | WS-040 row action **Reject** — `P0-08` |
| MPR-T1-07 | `PENDING_APPROVAL` | Withdraw (the only correct act after `MPR-GRD-20`). `POST /api/warehouse/movements/{id}/withdraw` with **its own** idempotency key. Held by `WhbStockLedgerWriter.withdraw(WhbMovementWithdrawCommand)` (`P0-03`); the endpoint and the actions are **`P0-08`'s** | `WITHDRAWN` | `MPR-GRD-03`, `-04` (its own key); `MPR-GRD-28` (row is `PENDING`, caller is the submitter) | `warehouse:movements:post`, **maker only** (G5, decided 2026-09-14) | `approval_status` → `WITHDRAWN`; `approved_by`, `approved_at` stay empty; row kept, `L-2` untouched, nothing moves, no handover, no outbox event (`RA-004`); `MPR-T4-14`. For a reversal, the original stays `POSTED_LIVE` and reversible. | `TBD` | WS-040 row action **Withdraw** and WS-041 action **Withdraw**, shown while `approval_status = PENDING` and the viewer is the submitter — `P0-08` |
| MPR-T1-08 | `POSTED_LIVE` | Reverse. `POST /movements/{id}/reverse` (`FR-035`, `PC-20`) | original → `POSTED_REVERSED`; new movement → `REVERSAL` | `MPR-GRD-29`, `-30`, `-34` (port `/reverse` only) on the original; `MPR-GRD-03`, `-04` (its own key), `-05`…`-10` on the reversal's **own** dates, `-11` (`REVERSAL` context), `-12`, `-13`, `-14`, and `-15`/`-16`/`-17` on the mirror's outbound lines (G7). The request may carry `occurred_at`/`posting_date`; when it omits them the reversal is **current-dated** — the injected `Clock`'s now and the current date (`MPR-OPEN-17`) | `warehouse:movements:reverse` | `MPR-T4-04`, `-05`, `-14`; the mirror has its own `movement_id` and `sequence_no` (`WH-SC-171`); outbox and handover per `MPR-T4-07`, `-08` | **yes**, `reason_code_id` from the closed catalogue. No free-text field exists (`WH-SC-013`) | WS-040 row action **Reverse**; WS-041 action **Reverse** |
| MPR-T1-09 | `REVERSAL` (already posted) | Re-send the same reverse request with the same reversal key | unchanged | `MPR-GRD-04` against the **reversal's own** key in `whb_movement_idempotency_keys`, not the original's | `warehouse:movements:reverse` | `200` with the same reversal; no second reversal (`WH-SC-171`) | — | port |
| MPR-T1-10 | — | Status change: a balanced **two-line movement at the same location**, differing only in `stock_status_code` (`FR-103`, p0-02 Traps) | `POSTED_LIVE` | `MPR-GRD-01`, `-09`, `-11`, `-18` | `whb_stock_positions:change_status` (G3) | as T1-01. Quarantine never needs a physical move | **yes** (`FR-103`) | WS-042 row action **Change status** (its own modal) |
| MPR-T1-11 | — | Value-only movement: every line `quantity = 0`, value explicit, balanced against the `VALUE_OFFSET` virtual location (`L-15`, `OD-11`, `OD-13`, `FR-010`) | `POSTED_LIVE` | `MPR-GRD-02`, `-09`, `-11` | `warehouse:movements:post` | on-hand unchanged; the cost effect is P0-17/P2-16's (`D-15`) | type-dependent | port / document screen (`WH-SC-034`; offset location `VALUE_OFFSET`, `OD-13`) |
| MPR-T1-12 | — | Simulate. `POST /movements/simulate` (`FR-037`, `PC-27`) | — (nothing) | the **whole** guard chain; returns the **complete** error list and the balance deltas | `warehouse:movements:simulate` | **writes nothing**: no movement, no inbound-message row, no outbox event, no `whb_movement_idempotency_keys` row (`WH-SC-172`) | — | WS-040 toolbar **Simulate** |
| MPR-T1-13 | — | As-at query. `GET /warehouse/stock/as-at?at=…&warehouseId=…` (`FR-013`, `PC-25`): `warehouseId` required, `itemId`, `ownerId`, `locationId` optional (`WhbStockAsAtController`) | — (read) | none | `warehouse:stock:view` (`PC-31`), seeded by `P0-15` in `V501000` (BUILD-SPEC §10.2) | computed **from the ledger**, never from a snapshot or the position cache. The response carries the `sequence_no` it was computed at. A later backdated post legitimately changes a past answer (`WH-SC-040`). Owner-scoped through `WhbOwnerScopeResolver`; virtual-counterparty locations are left out | — | API only, no screen — reports and the reconciliation tooling call it |

### 1.3 Guards — the predicate, the refusal, and where it lands

Every guard has a **service pre-check that rejects first, inside the transaction, with a field-level
error** in the house envelope `{ "error", "details": { "errors": { "<field path>": "<message>" } } }`
(`PC-28`, `DECISIONS.md` §4). The DB guard is only the backstop. A deferred constraint trigger raises at
`COMMIT`, outside every `@Transactional`, so its message can never reach a form field. **A trigger that
fires in production is an incident, not a validation** (`DATA-MODEL.md` §6.0). Each scenario cited
asserts the service message **and** that the trigger did not fire.

| ID | Predicate (must hold to proceed) | Refusal: HTTP · code · field | Message shape | DB backstop | Source |
|---|---|---|---|---|---|
| MPR-GRD-01 | Lines sum to zero in base UoM per `(company, owner, item, lot, serial, duty_status)` under the type's `balance_rule`. Mixed owners only where the type's `is_ownership_transfer` is set and its `balance_rule` is not per owner (`OWNER_CHANGE` and its reversal are seeded so; an admin sets it on any non-system type on WS-001, which refuses saving it on a `MUST_BALANCE_PER_OWNER_ITEM` type, create or update, with `422 OWNERSHIP_TRANSFER_BALANCES_PER_OWNER` on `isOwnershipTransfer`), and such a movement still balances under that `balance_rule`; under `MUST_BALANCE_PER_MOVEMENT`, quantity never nets across items unless the type transfers ownership | `422 MOVEMENT_UNBALANCED` · `lines` (or `lines[n].lot_id` / `.duty_status`); `422 MIXED_OWNER_NOT_ALLOWED` | names the owner/item and the **net difference**, e.g. *"net +120.0000 EA … must balance against a virtual location"* | `I-1` deferred trigger (memoised, §6.2); `I-14` in the same deferred assertion reads `whb_movement_types.is_ownership_transfer` (`V500049`) | `L-1`, `FR-001`, `FR-002`, `WH-SC-002`, `WH-SC-003`, `WH-SC-141`, `WH-SC-177` |
| MPR-GRD-02 | Value-only: all quantities zero and signed extended value balances per currency. A movement mixing zero- and non-zero-quantity lines is refused (it is two movements). A type with `is_stock_bearing = false` carries no non-zero quantity at all | `422 VALUE_UNBALANCED` · `TBD` field; the code for the mixed case is `TBD` | `TBD` | `I-21`; since `V500049` it also refuses any non-zero quantity on a type with `is_stock_bearing = false` | `L-15`, `OD-14`, `CONFIG-CASE-10`, `WH-SC-034` |
| MPR-GRD-03 | `idempotency_key` present and non-blank. **Never server-generated** — except the in-process natural key (`MPR-GRD-03a`) | `422 IDEMPOTENCY_KEY_REQUIRED` · `idempotency_key` (`MPR-OPEN-09`: `MISSING_IDEMPOTENCY_KEY` is retired) | *"an idempotency key is required and is never generated by the server"* | `I-11`: `idempotency_key NOT NULL` | `L-9`, `FR-017`, `IRR-04`, `WH-SC-024` |
| MPR-GRD-03a | **In-process natural key (exception to `MPR-GRD-03` and `MPR-UNR-09`).** A caller that posts through `WhbStockLedgerWriter` inside its own transaction, with no HTTP request to carry a client key, supplies a key the server builds **deterministically from one business fact's immutable identifiers** — today only the master merge's `MASTER_MERGE:ITEM:<losingId>:<warehouseId>` (`WS-245`, `WhbMasterMergeStockPlanner.idempotencyKey`). `uk(entity_type, losing_id)` lets that key name one movement only, so a retry of the same fact collides on `whb_movement_idempotency_keys` exactly as a client retry would. Never random, never a timestamp or counter, never on the HTTP port: a request to `POST /movements` without a key is still refused by `MPR-GRD-03` | — (the caller's key is non-blank by construction) | — | `I-11`: `idempotency_key NOT NULL` | `L-9`, `FR-017`, `FR-451`, `WS-245` |
| MPR-GRD-04 | For a seen key, the server-computed `payload_hash` (`PC-17` canonical form, over the **received** body) equals the stored one. A client-supplied hash is rejected | same hash → `200` replay (not an error); different → `409 IDEMPOTENCY_KEY_REUSED` | the body **names the original movement id** | `I-11`: `whb_movement_idempotency_keys` `PRIMARY KEY (source_system, idempotency_key)`. It is not partitioned, so it holds across months; a concurrent duplicate waits on the key, fails, and is answered from the registry row. Backstop `uk_whb_stock_movements_idempotency (…, occurred_at)` | `FR-017`, `FR-033`, `WH-SC-165`, `WH-SC-166` |
| MPR-GRD-05 | `occurred_at` ≤ server now (injected UTC `Clock`, `RL-016`) | `422 OCCURRED_AT_IN_FUTURE` · `occurred_at` | *"business time is in the future; the producer's clock is wrong"* | — | `FR-008`, `PC-24`, `WH-SC-031` |
| MPR-GRD-06 | `occurred_at` ≥ the oldest **live** partition. The floor is read from the live partition set, never from a constant | `422 OCCURRED_AT_BEFORE_RETENTION` · `occurred_at` | `TBD` | partition absence → opaque 500 without the pre-check | `Y-008` (p0-02 Scope) |
| MPR-GRD-07 | The site has a `REGISTERED` link covering `occurred_at` | `422 UNREGISTERED_INSTANT` · `occurredAt` | names the site, the instant and the first registration start | `I-22` (`BEFORE INSERT`). `I-23` (`V500037`) then freezes the `REGISTERED` link under every movement standing on it, and counts a `PENDING` movement as standing on it | `RG-001`, `WH-SC-312` |
| MPR-GRD-08 | Header `company_id` = the site's company, and every line's location/owner lies inside it | `422 COMPANY_MISMATCH` · `lines[n].location_id`, also for a line location in another site (confirmed 2026-09-15) | `TBD` | `I-22` trigger also asserts company (§2.1.8) | `RG-012`, `FR-025`, `WH-SC-038` |
| MPR-GRD-09 | The period of `posting_date` is `OPEN`; or `SOFT_CLOSED` and `MPR-GRD-10` holds; **`CLOSED` admits nothing, including a reversal** | `409 PERIOD_CLOSED` · `posting_date` (`MPR-OPEN-04`) | *"stock period 2026-08 is CLOSED and admits nothing, including a reversal"*; the correct act is a **current-dated** reversal | `I-10` `BEFORE INSERT`. Since `V500049` it reads the period of `posting_date` itself: the named `period_id` must contain `posting_date`, belong to the movement's company, be its site's or all sites', and be the only period covering that date (`WhbPeriodResolver`'s KEEP-SCALAR rule, `RG-026`) | `L-8`, `FR-020`, `PC-26`, `WH-SC-021`; a reversal is checked on its own dates (`MPR-OPEN-17`) |
| MPR-GRD-10 | `SOFT_CLOSED`: the poster holds `whb_stock_movements:post_backdated` (port gate, `RA-007`) **and** `whb_stock_periods:override`, and a `whb_stock_period_overrides` row exists with a reason code, a justification and `approved_by` ≠ `requested_by`. Only then is the transaction-local GUC `whb.period_override_id` set. Approval cannot be configured off | `403 PERIOD_OVERRIDE_FORBIDDEN` · `posting_date`; `422 PERIOD_OVERRIDE_SELF_APPROVED` · `posting_date` | *"Stock period 2026-08 is soft closed. Posting into it requires whb_stock_periods:override and an approved, recorded override."* | `I-10` reads `whb.period_override_id` (`MPR-OPEN-03`); `chk_whb_stock_period_overrides_approver` | `FR-020`, `WH-SC-022`, `I-10`; `GLOBAL-SETTINGS-DECISIONS.md` `warehouse.period.soft_close_requires_approval` (locked true); built `WhbPeriodOverrideRecorder` (`V500019`); `MPR-OPEN-02` |
| MPR-GRD-11 | A reason code is present where the type has `requires_reason`, and **always** on a reversal; it belongs to the right context — `REVERSAL` on a reversal, otherwise the type's `reason_context` when set (`NULL` accepts any active reason code) | `422 REASON_CODE_REQUIRED` / `422 UNKNOWN_REASON_CODE` · `reason_code_id` | *"a reason code from the REVERSAL context is mandatory on every reversal"*; the wrong-context case names the code's context and the context needed (*"… belongs to the DAMAGE context; movement type SCRAP needs one from the SCRAP context"*) | `I-3` `chk_whb_movements_reversal_shape`. The `REVERSAL` context is seeded by `P0-02` in `V500030` with a seed `INSERT` (`context` has no `CHECK`, `V500004`). `whb_movement_types.reason_context` (`V500084`, no `CHECK`, no FK) is seeded `ADJUST_UP`/`ADJUST_DOWN` → `ADJUSTMENT`, `COUNT_ADJUST` → `COUNT_VARIANCE`, `MASTER_MERGE` → `MASTER_MERGE`, `RETURN_RECEIPT` → `RETURN`, `RTO_RECEIPT` → `RTO`, `SCRAP` → `SCRAP`, `TRANSIT_LOSS` → `TRANSIT_LOSS`, and those types' reversals plus the reversals of `COST_ADJUSTMENT`, `OWNER_CHANGE`, `REVALUATION`, `STATUS_CHANGE`, `TRANSFER_RETURN`, `WRITE_DOWN` → `REVERSAL`; those six stay `NULL`. Set on WS-001; the registry refuses a context no reason code has (`422 UNKNOWN_REASON_CODE`) | `L-3`, `FR-005`, `FR-019`, `WH-SC-013`, `WH-SC-035` |
| MPR-GRD-12 | The target of `/reverse` is not itself a reversal | `409 CANNOT_REVERSE_A_REVERSAL` · `movement_id` | *"m-A2 is a reversal of m-A1"* | service only (`I-3` has no DB rule for this) | `PC-20`, `FR-005`, `FR-035`, `WH-SC-012` |
| MPR-GRD-13 | The target is not already reversed | `409 ALREADY_REVERSED` · header | `TBD` | `I-3` trigger: the conditional `UPDATE … SET is_reversed = true … WHERE is_reversed = false` on the original raises on 0 rows, serialised by the row lock; backstop `uk_whb_stock_movements_one_reversal (reversal_of_movement_id, occurred_at)`. I-3 applies at INSERT only to a reversal with ledger effect. For a PENDING reversal the writer takes I-3's mark back, and approval runs the conditional update. Several PENDING reversals of one original may coexist; the first one approved wins (`MPR-OPEN-18`) | `PC-20`, `WH-SC-012` |
| MPR-GRD-14 | The reversal's lines mirror the original **line for line**: negated quantities, same lot/serial/LPN/status/owner/duty status, **same frozen conversion factor**, type = the original's `reversal_type_code` | `TBD` (the service constructs the mirror, so no caller input can break it) | — | service only, by design (`DATA-MODEL.md` §6.5) | `L-3`, `PC-20`, `WH-SC-011` |
| MPR-GRD-15 | An issue does not consume reserved stock: quantity ≤ ATP = `max(0, allocatable on-hand − open reservations)`, evaluated **at post time, not as at `occurred_at`** — and at approval for a `PENDING` movement (`MPR-T1-05`) | `409 INSUFFICIENT_STOCK` · `lines[n].quantity`, **only when the issue would consume reserved stock**. A shortage with nothing reserved to consume is `MPR-GRD-16`'s (user decision 2026-09-15; `WH-SC-190` amended) | shows the arithmetic: *"available 2.0000 EA at … (on hand 20.0000 less 18.0000 reserved); requested 5.0000"* | `I-6` `CHECK (quantity_available >= 0)` | `L-6`, `FR-014`, `FR-168`, `PC-23`, `WH-SC-017`, `WH-SC-170`, `WH-SC-190` |
| MPR-GRD-16 | Negative physical on-hand follows the effective policy (most-specific-first, default `BLOCK`). `WARN` needs an acknowledgement, a reason and an override permission. `ALLOW` needs a matching policy. **Neither** consumes another holder's reservation, and a serial is never issued twice | `409 NEGATIVE_STOCK_NOT_ALLOWED` · `lines[n].quantity` — also the refusal for a shortage that consumes no reserved stock (`WH-SC-190`); `409 SERIAL_ALREADY_ISSUED` | `TBD`. A WARN acknowledgement without `whb_negative_stock_policies:override` is `403 NEGATIVE_STOCK_OVERRIDE_FORBIDDEN` naming it (G2); at approval the acknowledgement and the authorities are the approver's | `I-6` (`V500031`) refuses only an on-hand that becomes more negative under a `BLOCK` policy, and never at a virtual counterparty location, whose balance is negative by construction; `I-24` freezes a referenced policy row | `L-6`, `FR-014`, `FR-015`, `CONFIG-CASE-05`, `WH-SC-018` |
| MPR-GRD-17 | The optimistic `@Version` conflict on a position resolves within `warehouse.position.max_contention_retries` (default 3, range 0–10), with jittered backoff and re-read. An advisory lock serialises a key that has no row yet | `409 POSITION_CONTENTION` — **a different code from `INSUFFICIENT_STOCK`**. The key is consumed exactly once whatever the retry count | `TBD` | `I-6` + lock ordering (`FR-016`) | `J-001`, `K-004`, `WH-SC-196`, `WH-SC-198` |
| MPR-GRD-18 | Status change: the two lines share location, owner, item, lot, serial, LPN and duty status; the from→to status pair is allowed by the type's allowed from/to statuses and by the statuses' `requires_reason_to_enter`/`_to_leave` flags. The flags act on `STATUS_CHANGE` only (§7 2026-09-16 C8 fix 7b), and `STATUS_CHANGE` is seeded `requires_reason = true`, so under the seed the flags are **informational**; they stay meaningful if an operator turns `requires_reason` off on `STATUS_CHANGE` (§7 2026-09-16 P1-14/P1-15 C8 fix 8) | `422 STATUS_TRANSITION_NOT_ALLOWED` · `lines[n]` | `TBD` | `I-1` (conservation holds per status pair) | `FR-003`, `FR-102`, `FR-103` |
| MPR-GRD-19 | The approver, on Approve or Reject, is not the actor | `403 MOVEMENT_SELF_APPROVED` · header (ratified 2026-09-15; folded into `PORT-AND-ADAPTER-CONTRACT.md` §3.9.2 by `RD-008`, 2026-09-16) | `TBD` | service only (`DATA-MODEL.md` §6.5) | `FR-408`, `WH-SC-039` |
| MPR-GRD-20 | Approval is refused when the period of `posting_date` has closed **since submission**, i.e. it is no longer `OPEN`. A submission admitted into a `SOFT_CLOSED` period by an approved `whb_stock_period_overrides` row for this movement and period stays approvable while that period is still `SOFT_CLOSED` | `409 PERIOD_CLOSED_SINCE_SUBMISSION` · `posting_date` (G1) | *"withdraw and resubmit current-dated"* | `I-10` | `RA-004` (p0-02 Scope round-3 fold) |
| MPR-GRD-21 | Every registry code on the line resolves (`duty_status`, `stock_status_code`, movement type, document type, source system) | `422` `UNKNOWN_*` · the field, e.g. `lines[0].duty_status` | *"'Bonded' is not a duty status code"* | FKs `ON UPDATE RESTRICT` (`RL-013`) | `RL-001`, `WH-SC-327`, `WH-SC-173`, `WH-SC-028` |
| MPR-GRD-22 | The payload carries no carrier/AWB/trip, sales price, customer, tax, charge code, channel field, free-text reference or JSONB | rejected as an unknown field (not ignored) | names the field | none in the payload. The header's `channel_id` (`V500059`) is stamped by the writer from the source document, never read from the payload (`MPR-T4-15`) | `FR-041`, `WH-SC-178` |
| MPR-GRD-23 | Batch size ≤ `warehouse.port.max_batch_size` | `422 BATCH_TOO_LARGE`, **before any movement posts** | `TBD` | — | `RD-004` |
| MPR-GRD-24 | Every line attribute's key is an active `MOVEMENT_LINE` key, and its value sets exactly the one typed column of the key's `value_type`. The writer writes the `whb_movement_line_attributes` rows with the lines, in the same transaction | `422 UNKNOWN_ATTRIBUTE_KEY` / `422 ATTRIBUTE_VALUE_TYPE_MISMATCH` · `lines[n].attributes.<key>` (folded into `PORT-AND-ADAPTER-CONTRACT.md` §3.9.2 by `RD-008`, 2026-09-16) | names the unregistered key, or the key's value type | `whb_movement_line_attributes` FK and its key / value-type trigger | `RL-007`, `WH-SC-042` |
| MPR-GRD-25 | Every request is persisted to `whb_inbound_messages` **before** validation | — (a parser crash still leaves a row) | — | — | `FR-044`, `WH-SC-180` |
| MPR-GRD-26 | A non-zero entered quantity never yields a zero base quantity: round **away from zero**; the counter line negates the primary line's base quantity rather than converting again | — (the service constructs it: where a balance grain holds one counter-side line among two or more lines, that line's base quantity becomes the negated sum of the others' when its own conversion differs from that only by rounding — less than one base-scale step per line. A real imbalance is left to `MPR-GRD-01`) | — | `I-8` | `L-7`, `FR-009`, `WH-SC-004`, `WH-SC-005`, `WH-SC-019` |
| MPR-GRD-27 | Approving a `PENDING` movement whose source document is a QC inspection (`wh_quality_inspections`) additionally requires `wh_quality_inspections:approve`, and the approver is not the user who recorded the disposition | `403` naming `wh_quality_inspections:approve`; `403` naming the maker-checker rule when approver = disposer · header | names the missing permission, or the disposer | service only, through the `WhbMovementApprovalGuard` hook the writer calls on Approve, Reject **and Withdraw** (no bean, no check — the `WhbInsufficientStockRecorder` precedent); the QC-inspection bean is `P1-14`'s (`DATA-MODEL.md` §6.5, `FR-408`). The QC-inspection bean has landed: `WhQualityInspectionScrapApprovalGuard` (P1-14 C8 fix 1). On both APPROVE and REJECT of a `QCINSP` movement it requires `wh_quality_inspections:approve` and refuses the disposer. APPROVE of a SCRAP also needs the GRN POSTED. REJECT of a SCRAP gives the quantity back to the inspection line in the same transaction. The guard port is called on APPROVE, REJECT **and WITHDRAW**. On WITHDRAW the decider is the submitter (`MPR-GRD-28`), so no maker-checker or approve-permission rule applies; a guard only releases what its document counted — WITHDRAW of a SCRAP gives the quantity back exactly as REJECT does (§7 2026-09-16 P1-14 C8 fix 5a) | `FR-164`, `FR-408`, `BUILD-SPEC-SCREENS.md` §10.2 round 4; receipt contract `RQP-OPEN-13` (decided 2026-09-14) |
| MPR-GRD-28 | Approve, Reject and Withdraw act only on a row at `approval_status = PENDING`; Withdraw only for the submitting actor (`actor_user_id` = caller). **A replay does not bypass the submitter test**: it runs before the idempotency key is read, so a non-submitter resending the submitter's key and body is refused `403`, never answered `200` | not `PENDING`: `409 MOVEMENT_NOT_PENDING` · `movement_id`; not the submitter: `403 MOVEMENT_NOT_SUBMITTER` · header (both ratified 2026-09-15; folded into `PORT-AND-ADAPTER-CONTRACT.md` §3.9.2 by `RD-008`, 2026-09-16) | names the current `approval_status`, or the submitter | `I-2` lets a header change only while it is `PENDING` | `RA-004`; G5 (decided 2026-09-14) |
| MPR-GRD-29 | The target of `/reverse` has ledger effect (`approval_status IS NULL` or `APPROVED`): a `PENDING`, `REJECTED` or `WITHDRAWN` movement moved nothing, so there is nothing to reverse | `409 APPROVAL_REQUIRED` · `movement_id` | names the movement and its `approval_status` | service only | `PC-20`, `MPR-OPEN-05`; recorded from code 2026-09-15 |
| MPR-GRD-30 | The original's movement type declares a `reversal_type_code` | `422 UNKNOWN_MOVEMENT_TYPE` · `movement_id` | names the type that declares no reversal type | service only | `PC-20`, `L-3`; recorded from code 2026-09-15 |
| MPR-GRD-31 | The port's `source_system` is claimable: a registered, active row with `is_claimable = true` (a reserved row such as `ACCESSORIES` is not), and on a post the caller holds the row's `post_permission` where one is set | `403 SOURCE_SYSTEM_NOT_CLAIMABLE` · `source_system` | names the source system, or the permission posting as it needs | — | `PORT-AND-ADAPTER-CONTRACT.md` §3.9.2, §7.2 row 3, `G-052` |
| MPR-GRD-32 | A producer-supplied `lines[n].owner_type_code` names the owner's actual type — the type is derived, never accepted as authority | `422 OWNER_TYPE_MISMATCH` · `lines[n].owner_type_code` | names the owner's type | — | `PORT-AND-ADAPTER-CONTRACT.md` §2.5, §3.9.2 |
| MPR-GRD-33 | Port post, every batch member and simulate: the header's `warehouse_id` is on the caller's `whb_stock_movements` site tier (`WhbWarehouseScopeService`). The one verb-carried exception (`RJ-002`): a caller holding `wh_transfer_orders:receive` whose **every** line is at the `IN_TRANSIT` location of that site whose `transit_reference_id` is the movement's `source_document_id` — that transfer's own transit location, and no other location at the source site. Reverse and withdraw are site-checked through the original's read; a reprocess runs every check as the reprocessor | `403`, no code — a permission error, never an empty result; a batch member answers its own `403` and the batch goes on; simulate collects it on `warehouse_id` | "You do not have access to stock movements at this warehouse" | — | `P1-18` (`issues/p1-18.md` RJ-002 and its acceptance rows), `WH-SC-242` |
| MPR-GRD-34 | Port `POST /movements/{id}/reverse` (and its reprocess) refuses a movement whose `source_document_type = 'GRN'` names an existing `wh_goods_receipts` row. The check is narrowed to the caller's movement sites; out-of-scope movements fall through to the poster's `403`/`404`. Reason: a port reversal would leave the GRN `POSTED`, the PO line counted and the putaway tasks open. WS-078 reverses all of them in one transaction (`RQP-GRD-27`) | `409 MOVEMENT_REVERSED_THROUGH_RECEIPT_REVERSAL` · header; refusal recorded on the inbound-message row (`FR-044`) | names the GRN and WS-078 | service only | receipt contract `RQP-GRD-27`, `FR-044`; §7 2026-09-16 P1-16 C8 fix 3 |

> **Note on `MPR-GRD-33` (2026-09-16) — the `RJ-002` transit exception is inert.** It needs the caller to hold
> `wh_transfer_orders:receive` **and** every line to be at the `IN_TRANSIT` location whose `transit_reference_id`
> equals the movement's `source_document_id`. That permission is not seeded yet, so no caller meets the exception.
> Confirm it when the transfer task lands (`P1-17` / `P2-02`).

> **Note on `MPR-GRD-33` (2026-09-16, settled by default, `MPR-OPEN-20`) — batch replay does not apply the exception.**
> The batch replay site check (`WhbPortReferenceResolver`) does not apply the `RJ-002` transit exception, so a
> submitter whose batch member posted through that exception would get `403` on retry. It has no effect today:
> `wh_transfer_orders:receive` is not seeded anywhere. Hand-off, as an Acceptance checkbox in `P2-02`: the batch replay
> site check must honour the exception before that permission is seeded.

### 1.4 Transitions that must be unreachable

| ID | Forbidden | What makes it unreachable | Source |
|---|---|---|---|
| MPR-UNR-01 | Editing any posted header column (a header whose `approval_status` is not `PENDING`) outside `I-2`'s seven-column allowlist, or any line column | writer has **no update method**; the `I-2` `to_jsonb`-diff trigger (line allowlist `'{}'`, kept empty by `D-15`); no `PUT`/`PATCH` on any `/movements/{id}` path other than `/reverse`, asserted by a controller-enumeration test | `L-2`, `FR-004`, `WH-SC-006`, `WH-SC-010`; the allowlist is seven columns (`MPR-OPEN-06`) |
| MPR-UNR-02 | Deleting a posted header or line, by any actor including `ADMIN` | `I-2` `DELETE` branch; header→line FK `ON DELETE RESTRICT`, **no cascade**; mapping `cascade = {PERSIST, MERGE}` with no `orphanRemoval` | `L-2`, `WH-SC-007`, `WH-SC-008` |
| MPR-UNR-03 | Inserting a line into a posted movement | `I-2` `INSERT` branch: a line is admitted only into a `PENDING` header, or into a header whose id the header's `AFTER INSERT` trigger appended to the transaction-local setting `whb.tx_inserted_movements` in this transaction (`V500030`). Like `I-10`'s `whb.period_override_id`, that setting is a **writer-only backstop**, not a boundary against a session that calls `set_config` by hand (D4, 2026-09-15) | `WH-SC-009` |
| MPR-UNR-04 | Reversing a `REVERSAL` | `MPR-GRD-12` | `PC-20`, `WH-SC-012` |
| MPR-UNR-05 | Reversing a `POSTED_REVERSED` movement a second time | `MPR-GRD-13` + `I-3`'s conditional update | `I-3`, `WH-SC-012` |
| MPR-UNR-06 | Any movement, including a reversal, into a `CLOSED` period | `MPR-GRD-09` + `I-10` | `L-8`, `WH-SC-021` |
| MPR-UNR-07 | `WITHDRAWN` or `REJECTED` → anything | `I-2` treats every non-`PENDING` header as posted, so the row is immutable; `MPR-GRD-28` refuses first | `RA-004`, `MPR-OPEN-05` |
| MPR-UNR-08 | A position row written by anything but the writer, or an owner changed by `UPDATE` | architecture test (`FR-436`); owner changes only by a movement whose type has `is_ownership_transfer` (`OWNER_CHANGE`), and `I-14` refuses any other (`V500049`) | `FR-436`, `L-11`, `WH-SC-027` |
| MPR-UNR-09 | A server-generated idempotency key — other than an in-process natural key under `MPR-GRD-03a` | `MPR-GRD-03`; asserted by a test that submits without one | `PC-16`, p0-08 Acceptance |
| MPR-UNR-10 | Restating `moving_average_after` or re-drawing an existing FIFO consumption on a backdated post or a reversal | `D-15`; line allowlist `'{}'` | `D-15`, `WH-SC-151` |
| MPR-UNR-11 | Two accepted requests on one `(source_system, idempotency_key)`, even with `occurred_at` in different months | `whb_movement_idempotency_keys` `PRIMARY KEY` (not partitioned) | `L-9`, `RL-011`, `MPR-OPEN-07` |

---

## 2. T2 — Action × role × state

Columns are the §1.1 statuses. `✓` = offered/allowed, `—` = not offered, `n/a` = not applicable.

| ID | Action | Permission | (none) | PENDING_APPROVAL | POSTED_LIVE | POSTED_REVERSED | REVERSAL | REJECTED / WITHDRAWN | Extra condition |
|---|---|---|---|---|---|---|---|---|---|
| MPR-T2-01 | Post / batch post | `warehouse:movements:post` | ✓ | n/a | n/a | n/a | n/a | n/a | owner grant (`PC-32`); the header's site on the caller's `whb_stock_movements` tier, else `403` (`MPR-GRD-33`, `P1-18`), with `RJ-002`'s transit-location exception; a batch member answers its own `403`; a SOFT_CLOSED date adds `MPR-GRD-10` |
| MPR-T2-02 | Simulate | `warehouse:movements:simulate` | ✓ | n/a | n/a | n/a | n/a | n/a | writes nothing; the `MPR-GRD-33` site check is collected as an error on `warehouse_id`, so no balance at a site outside the tier is returned |
| MPR-T2-03 | View register / detail / lineage query | `whb_stock_movements:view` (WS-040/041); `warehouse:movements:view` (port `GET /movements/{id}` and lineage) | n/a | ✓ | ✓ | ✓ | ✓ | ✓ | site scope via `BranchScopeService` (`D-14`) — **not yet enforced on WS-040/041/042**: warehouse grids are not branch-scoped until `P1-18` (batch note; C1 finding F1). Owner scope via `WhbOwnerScopeResolver` holds on the grid, the statistics, the export and the detail. WS-041 answers `403` when the viewer may read none of the movement's lines |
| MPR-T2-04 | Reverse | `warehouse:movements:reverse` | n/a | — | ✓ | — | — | — | the reversal's period is `OPEN`, or `SOFT_CLOSED` with an approved override (WS-040 Actions). That is the reversal's own period — the current one unless the request supplies dates (`MPR-OPEN-17`) |
| MPR-T2-05 | Approve | `whb_stock_movements:approve` | n/a | ✓ | — | — | — | — | approver ≠ actor; `MPR-GRD-20`; `MPR-GRD-15`/`-16` at approval; `MPR-GRD-27` for a QC-inspection source. Endpoint and actions: `P0-08` |
| MPR-T2-06 | Reject | `whb_stock_movements:approve` | n/a | ✓ | — | — | — | — | approver ≠ actor (`MPR-GRD-19`); row is `PENDING` (`MPR-GRD-28`); `MPR-GRD-27` hook. Endpoint and action: `P0-08` |
| MPR-T2-07 | Withdraw | `warehouse:movements:post` | n/a | ✓ | — | — | — | — | the caller is the submitter (`MPR-GRD-28`); its own idempotency key (G5). Endpoint and actions: `P0-08` |
| MPR-T2-08 | Change status (WS-042) | `whb_stock_positions:change_status` (G3) | ✓ (a new movement) | n/a | n/a | n/a | n/a | n/a | reason mandatory |
| MPR-T2-09 | Post into a SOFT_CLOSED period | `whb_stock_movements:post_backdated` + `whb_stock_periods:override` (`MPR-OPEN-02`) | ✓ | — | — | — | — | — | an approved override row, approver ≠ poster |
| MPR-T2-10 | Rebuild check (WS-042 toolbar) | `whb_stock_positions:rebuild` | n/a | n/a | n/a | n/a | n/a | n/a | writes findings only, never positions (`MPR-T4-09`); covers the selected location and its child locations; raises no alert (`MPR-T3-01`) |
| MPR-T2-11 | As-at query | `warehouse:stock:view` | n/a | n/a | n/a | n/a | n/a | n/a | owner scope via `WhbOwnerScopeResolver`; the permission is seeded by `P0-15` (`V501000`) |
| MPR-T2-12 | Edit / Delete a movement | **none exists** | — | — | — | — | — | — | `MPR-UNR-01`, `-02` |
| MPR-T2-13 | Export register (line grain) | `whb_stock_movements:export` or `ADMIN` | n/a | ✓ | ✓ | ✓ | ✓ | ✓ | — |
| MPR-T2-14 | Any write, as an auditor | — | — | — | — | — | — | — | auditor is read-only across the ledger; every write is `403` (`WH-SC-245`) |
| MPR-T2-15 | Assign a drift finding (WS-043) | `whb_position_drift_findings:assign` (seeded by `P0-03` in `V500045`) | n/a | n/a | n/a | n/a | n/a | n/a | the finding is unresolved (`409 FINDING_ALREADY_RESOLVED`); the owner is an active user (`422 FINDING_OWNER_USER_INACTIVE` · `ownerUserId`) |
| MPR-T2-16 | Resolve a drift finding (WS-043) | `whb_position_drift_findings:resolve` (seeded by `P0-03` in `V500045`) | n/a | n/a | n/a | n/a | n/a | n/a | the finding is unresolved (`409 FINDING_ALREADY_RESOLVED`); a note is mandatory (`422 FINDING_RESOLUTION_NOTE_REQUIRED` · `resolutionNote`, raised by the service so the contract's code is what the caller receives) |
| MPR-T2-17 | Verify chain (WS-040 toolbar) | `whb_stock_movements:verify` | n/a | n/a | n/a | n/a | n/a | n/a | `GET /warehouse/ledger/movements/verify-chain` recomputes `prev_payload_hash` in SQL over the filtered warehouse and occurred-at range, **within the caller's sites**, and returns verified/broken with the two `sequence_no` values either side of each break (`RA-005`, `RC-008`); reads only, writes no `wh_reconciliation_exceptions` row — that is the nightly `WHB_LEDGER_CHAIN_VERIFY` job's, via `P2-06`. In code, recorded 2026-09-17 (`P0-13` C9 fix) |

---

## 3. T3 — Time-driven rules & notifications

| ID | Rule | Trigger (clock / date field) | What it changes | Who is notified | Channel | Job class | Notify-once? |
|---|---|---|---|---|---|---|---|
| MPR-T3-01 | Nightly `L-4` rebuild. A full rebuild from `whb_stock_movements` (rows with ledger effect: `approval_status IS NULL` or `APPROVED`) is diffed against `whb_stock_positions`, row for row at the nine-member grain. `POSITION_DRIFT` also covers a cached `quantity_available` that differs from the rebuilt ATP projection. `NEGATIVE_AVAILABLE` is raised only when reservations exceed on-hand — a negative on-hand admitted by a `WARN`/`ALLOW` policy is no finding | nightly, injected UTC `Clock` (`RL-016`) | writes `whb_position_drift_findings` (`POSITION_DRIFT`/`RESERVATION_DRIFT`/`ORPHANED_RESERVATION`/`NEGATIVE_AVAILABLE`); **never rewrites a position** | nobody by name: the rebuild writes a finding with no owner, and WS-043 *Assign* (`MPR-T2-15`) names one. The alert is an `error` log line with the `security` marker `WHB_POSITION_DRIFT` plus the `FAILED` run row — **nightly only**: the WS-042 on-demand *Rebuild check* writes findings and raises no alert | the log marker; delivery to a person is the watcher set's, with no in-app channel (the `AccBalanceDriftAlertService` precedent) | `WhbPositionRebuildJob` (job key `WHB_POSITION_REBUILD`, on WS-064) + `WhbPositionDriftAlertService` (G8) | **one alert per run** that finds drift |
| MPR-T3-02 | Partition-creation job: the partitions of both ledger tables exist ahead of need | at application start-up, then daily at `warehouse.ledger.partitions.cron` (default `0 0 2 * * *`); every missing month through the injected UTC `Clock`'s today plus `warehouse.ledger.partitions.months-ahead` (default 3) | creates partitions through `whb_extend_monthly_partitions` (`V500030`), forward-only and idempotent; there is never a `DEFAULT` partition | none | — | `WhbLedgerPartitionScheduler` + `WhbLedgerPartitionService` | — |
| MPR-T3-03 | Period generation. `period_id` is `NOT NULL`, so a date with no period cannot post | `MONTHLY`, missed-run `CATCH_UP` (`RE-001`) | creates `whb_stock_periods` rows (P0-07 owns them) | `TBD` | — | registered on WS-064 | — |
| MPR-T3-04 | Daily position snapshot (non-zero positions at physical locations only). Does **not** feed the as-at query (`MPR-T1-13` reads the ledger). A re-run for the same site and date supersedes that site's rows for the date — no quantity is updated — and links each to its replacement. The anniversary anchor is the LPN's receipt date on the site's calendar, else the lot's receipt date, else the oldest open cost layer | once a day **per site, for the site's local calendar date** in `whb_warehouses.timezone` (`RH-011`): `warehouse.position.snapshot.cron` runs every quarter hour (default `0 0/15 * * * *`) and takes each site in the last quarter hour of its local day; injected `Clock` (`RL-016`) | writes `whb_stock_position_snapshots` (P0-03) | none | — | `WhbPositionSnapshotScheduler` + `WhbPositionSnapshotService`; its WS-064 registration is `P0-13`'s | — |
| MPR-T3-05 | The server clock is the reference for `recorded_at` and for the future-date refusal | every post | stamps `recorded_at` | — | — | the writer | — |
| MPR-T3-06 | Interrupted port requests: stale means **no progress for the threshold**, never "received before it", so a live request is never recovered — with one bounded exception, a single post that outlives the threshold (below). A `whb_inbound_messages` row still `RECEIVED` whose current attempt has made no progress for the threshold (`attempt_started_at`: stamped on receipt, restamped when Reprocess begins an attempt, and by a batch before each movement and on completion) becomes `FAILED` with an "Interrupted" `error_detail` and `http_status` 500, so Reprocess and Discard reach it. A `whb_movement_batches` row still open (`completed_at` NULL) with no progress for the threshold (`last_progress_at`: stamped at the claim, with each stored answer, and in the same transaction as its envelope's heartbeat) is closed: `succeeded_count` is its stored results below 300, and every movement it never answered counts in `failed_count`. **Guarded late writes:** a request's `processed` or `failed` answer, and its batch's completion, land only while the row is `RECEIVED` on the attempt that writes them (`attempt_count`); a per-movement result lands only on a batch still open. A zero-row write means recovery, a discard or a later reprocess won: it is logged, never a 500, and the row keeps the winning answer. A batch that loses mid-run posts nothing more and answers its stored results, as a repeated `batch_reference` replays them (RD-004). **The bounded exception (settled by default, 2026-09-16, `MPR-OPEN-19`):** the heartbeat fires before each batch member and when the batch completes; a single post — a plain `POST /movements`, or one batch member — is stamped once, when it is received, with no mid-post heartbeat, because a post is one transaction. A post running longer than `warehouse.port.stale-after-minutes` can therefore be failed "Interrupted" although its movement commits; its `processed()` is then a guarded no-op, and its batch counts that member failed. Accepted: the threshold must be set above the longest expected single-post duration, and the fix is Reprocess, which replays idempotently — the key is already posted, so it answers `DUPLICATE` and the row becomes `PROCESSED`, with no second movement | at application start-up, then on `warehouse.port.recovery.cron` (default `0 */5 * * * *`); threshold `warehouse.port.stale-after-minutes` (default 15); injected `Clock` | the row's `status`, `http_status`, `error_detail`, `processed_at`; the batch's counts and `completed_at` | nobody by name: a failed row joins WS-054's queue and its alert (`warehouse.port.rejected_queue_threshold`) | the audit log line `WHB_INBOUND_MESSAGE_INTERRUPTED` | `WhbInboundMessageRecoveryScheduler` + `WhbInboundMessageService.recoverInterrupted` (the `WhbImportRecoveryScheduler` idiom) | — |

Scan for untracked dated obligations: a `PENDING_APPROVAL` movement has **no expiry and no automatic
action**, and the only exit forced by time is `MPR-GRD-20`. Escalation is
`warehouse.approval.pending_alert_hours` (default 24, one alert per pending episode, never auto-approve).
`P1-20`/`P2-23` own it, not this workflow (G6).

---

## 4. T4 — Cross-entity effects

| ID | When this happens | Then this other record must change | Enforced in |
|---|---|---|---|
| MPR-T4-01 | A movement posts (`MPR-T1-01/02/05/10/11`) | the `whb_stock_positions` row for each line's nine-member key is created or updated **in the same transaction**: signed `quantity_on_hand` and the maintained ATP projection (computed authority, stored projection — `MPR-OPEN-12`). A `PENDING` movement moves nothing until it is approved. An advisory lock covers a key with no row (`WH-SC-198`) | writer service (`FR-436`) |
| MPR-T4-02 | A movement posts | `sequence_no` is the next gapless number **per warehouse** in server acceptance order, and `prev_payload_hash` chains to the predecessor (`WH-SC-033`, `WH-SC-169`) | writer + `I-4`'s locked counter row (the guard); backstop `uk_whb_stock_movements_sequence (warehouse_id, sequence_no, occurred_at)`. A `PENDING` movement takes its number at acceptance |
| MPR-T4-03 | Any port request arrives | a `whb_inbound_messages` row is written **before** processing, with the status ladder `RECEIVED`/`PROCESSED`/`FAILED`/`DISCARDED` | port (`FR-044`) |
| MPR-T4-04 | A reversal posts | original header: `is_reversed = true`, `reversed_by_movement_id` = the reversal. **This is the only post-hoc mutation of a posted header this workflow makes**, made through `I-2`'s allowlist | `I-3` trigger + reversal service |
| MPR-T4-05 | A reversal posts | the mirror lines move positions back. Cost consequences follow `D-15` and are not restated here | writer |
| MPR-T4-06 | A post into a `SOFT_CLOSED` period | a `whb_stock_period_overrides` row (`period_id`, `movement_id`, requester, approver, reason code, justification) exists **before** the GUC is set, and appears on WS-046 | writer + `I-10` (`whb.period_override_id`; `MPR-OPEN-02`, `-03`) |
| MPR-T4-07 | A movement posts with ledger effect (`MPR-T1-01`, `-02`, `-10`, `-11`), is approved (`MPR-T1-05`) or is reversed (`MPR-T1-08`) | outbox events at each code's catalogue grain (`PC-42`), appended through `WhbOutboxAppender.append` once per event **after** the handover (`MPR-T4-08`), as the last writes of the writer's transaction, each with `recorded_at` from the injected `Clock`. (1) **movement grain**, one event: `stock.movement.posted`, or `stock.movement.reversed` for a reversal (seeded by `P0-11` in `V500040`); its `owner_id` is the movement's owner, single by `MPR-GRD-01`/`I-14`, and on an ownership-transfer type it is line 1's. (2) **line grain**, only where the type's `is_ownership_transfer` is set (`OWNER_CHANGE`, its reversal, or an install's marked type): one `owner.changed` per movement line, in `line_no` order after (1), each carrying that line's own `owner_id`, `item_id`, `lot_id`, `serial_id`, `lpn_id`, `location_id`, `stock_status_code`, signed base `quantity`, base `uom_code`, `source_line_ref`, `reason_code_id` (the line's, else the header's) and `movement_line_id`. A replay (`MPR-T1-03`, `-09`), a `PENDING` submission, a reject and a withdraw append nothing. A line-grain code raised by a document stays with that document's task: `receipt.line.confirmed` is `RQP-T4-07`'s, `count.variance.posted` is `P2-04`'s. `PC-38` at the writer: every event carries owner, warehouse, company and the three timestamps; a line-grain event also carries its line's item, lot, serial, LPN, location, stock status, signed base quantity, UoM and `movement_line_id`; a movement-grain event carries the movement's single owner and leaves the line dimensions NULL. A replay (`MPR-T1-03`, `-09`), a `PENDING` submission, a reject and a withdraw append nothing (WS-041 *Outbox* tab; `WH-SC-294` names "movement posted and reversed") | the writer, calling `P0-11`'s appender |
| MPR-T4-08 | Same triggers as `MPR-T4-07` | the handover envelope is emitted **first**, through `WhbAccountingHandoverEmitter.emit(movementId, occurredAt, actorId)`, then the outbox events (`MPR-T4-07`); a replay emits nothing. `posting_status` follows P0-12's contract. **A downstream rejection never reverses the stock movement** (`WH-SC-158`). Reversing a movement whose envelope never posted makes it eligible for *Void* on WS-052 (`RJ-011`) | the writer, calling `P0-12`'s emitter |
| MPR-T4-09 | The rebuild check (WS-042) or the nightly job runs | `whb_position_drift_findings` rows only. A finding already open at the same type and grain is updated in place, not duplicated (the partial unique index `WHERE resolved_at IS NULL`, `V500045`). The WS-042 check covers the selected location and its child locations. The cache is **not** corrected in place by this workflow | P0-03 |
| MPR-T4-10 | `WARN`/`ALLOW` lets on-hand go negative | an insufficient-stock log row (item, warehouse, requested, available, source, user, time) is written | writer (`FR-015`, `WH-SC-018`) |
| MPR-T4-11 | A movement is approved late, rejected or withdrawn | nothing else moves; the row stays (`L-2`) | writer |
| MPR-T4-12 | A stock period's close pre-check runs | it lists every `PENDING_APPROVAL` movement dated in the period (P0-07 owns the close) | P0-07 (`RA-004`) |
| MPR-T4-13 | Simulate (`MPR-T1-12`) | **nothing** — no movement, inbound message, outbox event or `whb_movement_idempotency_keys` row | port (`PC-27`) |
| MPR-T4-14 | A movement, a reversal or a withdraw is accepted | a `whb_movement_idempotency_keys` row (`source_system`, `idempotency_key`, `movement_id`, `occurred_at`, `payload_hash`, `created_at`) is inserted **in the same transaction**; for a withdraw, `movement_id` is the withdrawn movement | writer + the registry `PRIMARY KEY` (`I-11`, `MPR-OPEN-07`) |
| MPR-T4-15 | A movement posts from a source document that carries a channel | `whb_stock_movements.channel_id` (`V500059`) is stamped by the writer, **derived from the source document** — never a payload field (`MPR-GRD-22` stands). It needs no term of its own in `payload_hash`: it follows from the source-document fields already in it. `I-2` seals it on post, and it is `NULL` on every movement posted before `V500059` (D3, 2026-09-15) | writer (`P0-03` / `P0-08`) |
| MPR-T4-16 | A reservation is admitted through the writer (the `P0-09` consumer) | the reservation is refused `409 INSUFFICIENT_STOCK` when the grain's ATP does not cover it, and nothing is reserved | writer (`WhbStockPositionWriter`) |

---

## 5. T5 — Screen contract (web only — see §0)

### 5.1 `/warehouse/ledger/movements` — WS-040 Stock Movement Register

| | |
|---|---|
| **Type** | list + transition modals (reference: **Service Vehicle**); **no add, no edit** |
| **Grid identifier** | `whb_stock_movements` · filter scope `WAREHOUSE_STOCK_MOVEMENT` |
| **Default sort** | `e.occurred_at DESC, e.sequence_no DESC` (G8), kept as the secondary order behind any sort the reader picks (`WhbStockMovementQueryService`) |
| **Statistics tiles** | filter-aware, uncached: movements today · pending approval · pending handover · rejected handovers · reversed today (BUILD-SPEC WS-040) |
| **Columns** | `sequenceNo`, `movementTypeCode`, `warehouseName`, `companyName`, `occurredAt`, `recordedAt`, `postingDate`, `sourceSystem`, `sourceDocumentType`, `sourceDocumentNo`, `sourceDocumentId`, `reasonCodeName`, `actorType`, `actorUserName`, `deviceId`, `periodCode`, `lineCount`, `totalBaseQuantity`, `postingStatus`, `approvalStatus`, `isReversed`, `reversalOfSequenceNo` |
| **Sortable columns** | all except `sourceDocumentId`, `lineCount`, `totalBaseQuantity`, `reversalOfSequenceNo` |
| **Filters** | `warehouseId` → `movementTypeCode` → `reasonCodeId`; `sourceSystem` → `sourceDocumentType`; `sourceDocumentNo`; `itemId`; `lotId`; `serialNumber`; `locationId` (the line's location exactly); `ownerId`; `actorUserId`; `actorType`; `occurredFrom`/`occurredTo` (`date`); `postingDateFrom`/`postingDateTo` (`dateOnly`); `postingStatus`; `approvalStatus` (`Y-001`); `isReversed`; `periodId`. **`sourceSystem` + `sourceDocumentType` + `sourceDocumentId` must all be in the scope allowlist** (`FR-036`) |
| **Row actions** | View → always (routes to WS-041) · **Reverse** → `MPR-T2-04` (reverse permission **and** `is_reversed = false` **and** not itself a reversal **and** the period admits it — shown from the response flag `isReversalPeriodOpen`: exactly one period covers today at the movement's site and it is `OPEN`, the resolver's rule for the current-dated reversal the screens send; display only, the writer still refuses) · **Approve / Reject** → `MPR-T2-05/06` (`approval_status = PENDING`; a QC-inspection source also needs `wh_quality_inspections:approve`, `MPR-GRD-27`) · **Withdraw** → `MPR-T2-07` (`approval_status = PENDING` **and** the viewer is the submitter) · View source document → via the display resolver (`FR-357`) · View handover → where `handover_id` is set |
| **Toolbar** | Export · Grid config · Help · **Simulate** (`MPR-T2-02`). **No Add button** (`FR-436`) |
| **Export columns** | **line grain**, one row per line: the header columns plus `lineNo`, `ownerName`, `itemCode`, `locationCode`, `lotCode`, `serialNumber`, `lpnCode`, `stockStatusCode`, `conditionCode`, `dutyStatus`, `quantity`, `uomCode`, `baseQuantity`, `baseUomCode`, `conversionFactorUsed`, `unitCost`, `extendedCost`, `costBasis`, `movingAverageAfter`, `sourceLineRef`, `isCounterSide`, `taxClassificationCode`, `taxClassificationScheme` (`MPR-OPEN-14`). **Ledger-style: no `createdByName`/`updatedByName` in the grid or the export**, deliberately |
| **Add / Edit** | none. Write modals: **Reverse** (`REVERSAL`-context reason code from `whb_reason_codes`, its own idempotency key; no date fields, so it is current-dated), **Approve**, **Reject** and **Withdraw** (its own idempotency key) |
| **View** | a route: WS-041 |
| **Mobile counterpart** | none (§0) |

### 5.2 `/warehouse/ledger/movements/[id]` — WS-041 Movement Detail

| | |
|---|---|
| **Type** | full-page tabs, no grid |
| **Grid identifier** | — |
| **Header panel** | every WS-040 column plus `idempotency_key`, `payload_hash`, `prev_payload_hash`, `occurred_at_tz_offset`, `handover_id`, `notes` |
| **Tabs** | **Lines** (every line column; typed attributes inline, registered keys only) · **Reversal** (the mirror, or the movement this one reverses) · **Handover** · **Outbox** · **Cost** |
| **Actions** | Reverse (`MPR-T2-04`) · Approve (`MPR-T2-05`) · Withdraw (`MPR-T2-07` — the submitter, while `PENDING`) · Print movement document (template kind `MOVEMENT_DOCUMENT`) |
| **Permission** | `whb_stock_movements:view` |
| **Mobile counterpart** | none (§0) |

### 5.3 `/warehouse/ledger/positions` — WS-042 Stock Position Enquiry

| | |
|---|---|
| **Type** | list, two tabs on one grid identifier: *By item* and *By location* (reference: **Customer**). Virtual-counterparty locations are left out of the grid, the statistics and the export |
| **Grid identifier** | `whb_stock_positions` · filter scope `WAREHOUSE_STOCK_POSITION` |
| **Default sort** | *By location* `i.code ASC, w.name ASC, l.code ASC`; *By item* `i.code ASC, w.name ASC` (G8); each kept as the secondary order behind any sort the reader picks (`WhbStockPositionQueryService`) |
| **Statistics tiles** | filter-aware, uncached: distinct items · total on hand · total available · negative signed-balance rows (visible shortage) · negative ATP rows (**must be 0**) · rows drifted at last rebuild (the unresolved `POSITION_DRIFT` findings over the same criteria, `MPR-T3-01`) |
| **Columns** | `itemCode`/`itemName`, `ownerName`, `warehouseName`, `locationCode`, `lotCode`, `expiryDate`, `serialNumber`, `lpnCode`, `stockStatusCode` (badge variant from the registry, `FR-102`), `dutyStatus`, `quantityOnHand`, `quantityReserved`, `quantityAvailable`, `baseUomCode`, `unitCost`/`value` (**suppressed where the owner's type has `posts_to_our_gl = false`**, `L-14`), `lastMovementAt`, `lastOutwardMovementAt`, `lastCountDate` |
| **Filters** | `warehouseId` → `locationId` (the location **and its child locations**) → `itemId`; `ownerId`; `lotId`; `serialNumber`; `lpnCode`; `stockStatusCode` (multiselect); `dutyStatus`; `itemCategoryId`; **`nonZeroOnly` default true**; `hasAvailable`; `expiryFrom`/`expiryTo` (`dateOnly`); `expiringWithinDays`; `agedOverDays` |
| **Row actions** | View movements for this grain → WS-040 pre-filtered · **Change status** → `MPR-T2-08` (`whb_stock_positions:change_status`) · Adjust → WS-089 pre-filled · Reserve → `whb_reservations:create` · Trace → WS-218 |
| **Toolbar** | Export · Grid config · **Rebuild check** → `MPR-T2-10` (the selected location and its child locations; no alert) |
| **Export columns** | the nine-member grain plus quantities, UoM, cost/value where permitted, the three timestamps. **Ledger-style: no audit-name columns** |
| **Add / Edit** | none. The position is a cache (`L-4`) and is never edited |
| **Mobile counterpart** | none (§0) |

### 5.4 `/warehouse/platform/port-monitor` · `/warehouse/platform/port-rejected` — WS-053 / WS-054, the port's inbound-message verbs (`P0-08`)

| | |
|---|---|
| **Reprocess** | `whb_inbound_messages:reprocess` on a `FAILED` row, **plus the stored endpoint's own permission** — `warehouse:movements:reverse` for `/reverse`, `warehouse:movements:post` for every other endpoint (`PC-31`) — checked before the attempt begins, else `403` with the row untouched. The re-run is site- and owner-scoped as the reprocessor (`MPR-GRD-33`) |
| **Bulk reprocess** | the `FAILED` rows of the filtered set within the caller's sites, oldest first, **capped at `warehouse.port.max_batch_size`** (default 400, the batch endpoint's own ceiling); a row whose endpoint permission the caller lacks is left `FAILED` and counted as not processed |
| **Discard with reason** | `whb_inbound_messages:reprocess`; `FAILED` only; reason 5–1000 characters |
| **Refusals** | not `FAILED` — including a Reprocess or Discard that lost a race to another Reprocess or a Discard — `409 INBOUND_MESSAGE_NOT_FAILED` · `status`. A screen refusal, not a §3.9 port code (the WS-043 `FINDING_*` precedent) |
| **Time-driven exit** | a row left `RECEIVED` by an interrupted request is failed by `MPR-T3-06` |

Screens this workflow feeds but does not own: WS-043 (drift findings, P0-03), WS-045/WS-046 (periods and
overrides, P0-07), WS-052 (handover queue), WS-053/WS-054/WS-055 (port monitor, rejected queue, batches,
P0-08).

WS-043 and WS-044 are `P0-03`'s and list what this workflow writes. WS-043 *Assign* and *Resolve* are
`MPR-T2-15` and `-16`. Its `FINDING_*` codes stay in `WhbPositionDriftFindingService`, **not** in
`WhbLedgerErrorCodes`: that artefact is the port's §3.9 vocabulary (`WH-SC-041`), and a screen's
refusal is not a port code (decided 2026-09-15). WS-044 (position snapshots) filters `isSuperseded`
to **No** by default, so the rows a re-run replaced stay hidden until the reader asks for them.

---

## 6. Out of scope (explicit decisions)

| Baseline item | Decision | Reason | Revisit |
|---|---|---|---|
| Mobile screens for WS-040/041/042 | **not built** | user scope rule of 2026-09-11 (§0); `MPR-OPEN-13`, resolved | only if the user reverses the rule |
| Period ladder transitions (open → soft close → close → reopen) | owned by P0-07's own contract | this contract reads the period status only | — |
| Handover lifecycle and `posting_status` | P0-12 | T4 cross-reference only | — |
| Reservation lifecycle | P0-09 | the ATP input only | — |
| Cost layers, AVCO/FIFO arithmetic | P0-17 / P2-16 under `D-15` | cited, never restated | — |
| Blocked-move queue for refused physical moves | P2 (`FR-028`) | refusals here are final to the caller. **Note (§7 2026-09-16 P1-14/P1-15 C8 fix 8):** forcing stock out of `PENDING_RESOLUTION` (`P2-12`) must carry its own reason rule — leaving by `ISSUE` or `TRANSFER` no longer inherits one from `requires_reason_to_leave`, which binds `STATUS_CHANGE` only | P2 |
| Receipt reversal as a document (reverse GRN) | P1 (`WH-SC-081`, `WH-SC-082`) | it posts through `MPR-T1-08` | P1 contract |
| Drift-finding resolution workflow | P0-03 / WS-043 | this contract only writes findings | — |

---

## 7. Amendments

| Date | Row IDs affected | Change | Found by |
|---|---|---|---|
| 2026-09-12 | all | v0 derived from the design set at `7ab6717`; unratified | `functional-reviewer`, derive mode (task W0-2a) |
| 2026-09-14 | §0; §1.1; `MPR-T1-01`, `-03`…`-12`; `MPR-GRD-03`, `-04`, `-09`, `-10`, `-11`, `-13`, `-16`, `-20`, new `-27`, `-28`; `MPR-UNR-01`, `-05`, `-07`, new `-11`; `MPR-T2-02`…`-09`; `MPR-T3-01`; `MPR-T4-01`, `-02`, `-06`, `-13`, new `-14`; §5; §9 | **v1. Every §9 row resolved.** The user decided `MPR-OPEN-05` (no `status` column; `approval_status` carries the lifecycle), `MPR-OPEN-07` (non-partitioned `whb_movement_idempotency_keys` registry in `V500030`) and G5 (maker-only `POST /movements/{id}/withdraw`). The rest follow documented defaults or `classic` precedent. Added: the `REVERSAL` reason-context seed in `V500030`; `MPR-GRD-27` (QC-inspection approval needs `wh_quality_inspections:approve`, from the receipt contract's `RQP-OPEN-13`). Carried into `DATA-MODEL.md`, `IRREVERSIBLE.md`, `PORT-AND-ADAPTER-CONTRACT.md`, `BUILD-SPEC-SCREENS.md`, `issues/p0-02.md`, `issues/p0-03.md`, `issues/p0-08.md` | WAVE C0 research + user decisions |
| 2026-09-15 | `MPR-GRD-01`, `-02`, `-07`, `-09`, `-16`, `-22`; `MPR-UNR-03`, `-08`; `MPR-T2-03`; `MPR-T3-02`; new `MPR-T4-15`; §5.1, §5.3 | **C1 fix cycle (P0-02).** `V500049` (forward-only) makes `I-10` read the period of `posting_date` for the movement's company and site (D1), and adds `whb_movement_types.is_ownership_transfer` for `I-14`, with `I-21` refusing quantity on a non-stock-bearing type (D2); `GRD-01`'s `allows_mixed_owner` wording corrected to that flag. `GRD-22` stands, and `MPR-T4-15` records the writer-derived `channel_id` (D3). `UNR-03` records `whb.tx_inserted_movements` as a writer-only backstop (D4). `T3-02`'s job class filled. Code facts the contract lacked are recorded: `I-6`'s virtual-counterparty exemption and more-negative-only refusal, `I-23` counting `PENDING`, WS-041's `403`, WS-040's exact and WS-042's subtree `locationId`, WS-042's virtual-location exclusion and `posts_to_our_gl` cost suppression, and the G8 secondary order. `T2-03`'s site scope is recorded as not yet enforced, owned by `P1-18` (F1) | `functional-reviewer` conformance gate (C1) + driver resolutions |
| 2026-09-15 | `MPR-GRD-01` | **C1 fix pass 2 (P0-02).** `is_ownership_transfer` is configurable on WS-001 like the other movement-type flags (user decision N1), so `GRD-01` names the screen as where a type is marked mixed-owner; a two-owner movement still balances under the type's `balance_rule` (`WH-SC-177`, tested in `WhbLedgerInvariantsIntegrationTest`) | user decision + driver |
| 2026-09-15 | §1.1 note; `MPR-T1-04`…`-08`, `-13`; `MPR-GRD-08`, `-15`, `-16`, `-19`, `-20`, `-24`, `-26`, `-27`, `-28`, new `-29`, `-30`; `MPR-T2-05`…`-07`, `-10`, `-11`, new `-15`, `-16`; `MPR-T3-01`, `-04`; `MPR-T4-07`, `-08`, `-09`, new `-16`; §5.3; the WS-043 / WS-044 note | **C2 fix pass 1 (P0-03).** The writer emits the handover and then the outbox event, last in its transaction, at movement grain; replays emit nothing (F1/F2). It holds Reject and Withdraw (F4). Approve takes the approver's `WARN` acknowledgement and security-context authorities (F8), refuses only a period closed since submission (F9), checks availability at approval (F11) and calls the `WhbMovementApprovalGuard` hook (F10). Endpoints and actions handed to `P0-08` (F5); `warehouse:stock:view` to `P0-15` (F6). The snapshot day is the site's (F3). The rebuild compares `quantity_available` (F7), scopes to child locations (F12), alerts nightly only (F14) and raises `NEGATIVE_AVAILABLE` only for reservations beyond on-hand. User decision: `INSUFFICIENT_STOCK` only when reserved stock would be consumed (`GRD-15`/`-16`, `WH-SC-190`). Codes ratified: `MOVEMENT_SELF_APPROVED`, `MOVEMENT_NOT_PENDING`, `MOVEMENT_NOT_SUBMITTER`, `UNKNOWN_ATTRIBUTE_KEY`, `ATTRIBUTE_VALUE_TYPE_MISMATCH`, and `COMPANY_MISMATCH` for `GRD-08`. WS-043's `FINDING_*` codes stay out of `WhbLedgerErrorCodes`. `posting_status` also carries `BLOCKED`/`DISCARDED`. Rows recorded from code: `GRD-29`, `-30`, `T4-16`, T1-13's filters | `functional-reviewer` + `reviewer` gates (C2) + driver decisions |
| 2026-09-15 | `MPR-T4-07` | **C2 fix pass 2 (P0-03).** Events at catalogue grain — `stock.movement.posted`/`.reversed` stay movement grain; an ownership-transfer type adds line-grain `owner.changed` per line | driver |
| 2026-09-15 | §1.1; `MPR-T1-04`…`-07`; `MPR-GRD-13`; new `MPR-OPEN-18` | **C2 fix pass 4 (P0-03).** A pending reversal no longer marks its original reversed | driver |
| 2026-09-16 | `MPR-GRD-19`, `-24`, `-28`, new `-31`, `-32`, `-33`; `MPR-T2-01`, `-02`; new `MPR-T3-06`; §5.1; new §5.4 | **C4 fix (P0-08).** The port's post, batch and simulate check the header's site on the `whb_stock_movements` tier with `RJ-002`'s transit-location exception (`GRD-33`, F1/F3). Reprocess requires the stored endpoint's permission before the attempt and answers `409 INBOUND_MESSAGE_NOT_FAILED` when it loses a race (§5.4, F4/F9). Interrupted requests and open batches get an exit (`T3-06`, F5; `attempt_started_at` on `V500041`). `RD-008` folds the six approval and attribute codes, with contract G1's five and the two built precedents, into `PORT-AND-ADAPTER-CONTRACT.md` §3.9.2 and `FR-039` — 58 codes (F7). A withdraw replay no longer bypasses the submitter test (`GRD-28`, F10). WS-040/041's Reverse gate reads the period flag (§5.1, F11). Rows recorded from code: `GRD-31`, `-32`, §5.4's bulk-reprocess cap | `functional-reviewer` + `reviewer` gates (P0-08) |
| 2026-09-16 | `MPR-GRD-03`; new `MPR-GRD-03a`; `MPR-UNR-09` | The master merge (`P1-21`) posts through the writer with a server-built natural key; recorded as a bounded exception rather than left as a silent conflict (C4 functional review F12). | WAVE C4 fix P1-21 |
| 2026-09-16 | `MPR-T3-06` | **C4 fix 2X (P0-08, warehouse-issues#71).** Recovery never touches a live request. Stale is "no progress for N minutes": a batch stamps its envelope's `attempt_started_at` and its own new `last_progress_at` (`V500041`, edited in place before it was applied) in one transaction before each movement and on completion. Recovery reads those two columns. `processed`, `failed` and batch completion are guarded on `status = 'RECEIVED' AND attempt_count = :n`, and a result only lands on an open batch. A lost write is logged, never a 500. A batch that loses answers its stored results | `functional-reviewer` re-verify of `4883489c84` (P0-08) |
| 2026-09-16 | `MPR-T3-06`; `MPR-GRD-33` note; new `MPR-OPEN-19`, `-20` | **C4 settle (P0-08), settled by default.** A single post is stamped once when received and has no mid-post heartbeat, so a post longer than `warehouse.port.stale-after-minutes` can be failed "Interrupted" although it commits. `T3-06` states this as the one bounded exception to "a live request is never recovered": set the threshold above the longest single post, and Reprocess replays it as `DUPLICATE` to `PROCESSED` with no second movement (`OPEN-19`). Batch replay's site check does not apply `RJ-002`'s transit exception; inert while `wh_transfer_orders:receive` is unseeded, and handed to `P2-02` as an Acceptance checkbox (`GRD-33` note, `OPEN-20`) | `functional-reviewer` re-verify (P0-08) |
| 2026-09-16 | `MPR-GRD-27`; new `MPR-GRD-34` | **C8 fixes (P1-14 fix 1, warehouse-issues#120; P1-16 fix 3, warehouse-issues#129).** `GRD-27`: the QC-inspection approval bean has landed (`WhQualityInspectionScrapApprovalGuard`) — APPROVE and REJECT of a `QCINSP` movement need `wh_quality_inspections:approve` and refuse the disposer; APPROVE of a SCRAP also needs the GRN POSTED; REJECT of a SCRAP gives the quantity back to the inspection line in the same transaction (receipt contract `RQP-GRD-19`, `RQP-T4-16`). `GRD-34`: the port's `/reverse` (and its reprocess) refuses a movement sourced from an existing GRN with `409 MOVEMENT_REVERSED_THROUGH_RECEIPT_REVERSAL`, narrowed to the caller's movement sites, because only WS-078 reverses the GRN, the PO counter and the putaway tasks together (`RQP-GRD-27`) | WAVE C8 fix agents (driver apply) |
| 2026-09-16 | `MPR-GRD-27` | **C8 fix 5a (P1-14, warehouse-issues#120).** The guard port is called on APPROVE, REJECT **and WITHDRAW** (`Decision.WITHDRAW`). On WITHDRAW the decider is the submitter (`MPR-GRD-28`), so no maker-checker or approve-permission rule applies; a guard only releases what its document counted. WITHDRAW of a PENDING QC scrap gives its quantity back to the inspection line exactly as REJECT does, in the same transaction (receipt contract `RQP-T1-36`, `RQP-T4-16`) | WAVE C8 fix 5a (driver apply) |
| 2026-09-16 | `MPR-GRD-11`, `MPR-GRD-18`; receipt contract `RQP-T1-08`, `-09` | **P0-08/P1-13 C8 fix 7b, user decision.** The writer enforces `MPR-GRD-18`'s `requires_reason_to_enter` / `requires_reason_to_leave` flags on **status changes only** (the `STATUS_CHANGE` type, which `P0-05`'s planner already refuses without a reason, FR-103). It had applied them to every movement type, so a GRN post into `QC_HOLD`, `DAMAGED`, `QUARANTINE` or `REJECTED` - which carries no header reason (`RQP-T1-09`) - was refused `422 REASON_CODE_REQUIRED`. `MPR-GRD-11` is unchanged: a reason is still mandatory where the type has `requires_reason` (every seeded adjustment, count, scrap, transit-loss, owner-change, value-only and master-merge type) and on every reversal. A non-status-change movement into or out of a reason-required status (a receipt, a putaway, a transfer or issue posted through the port) needs no header reason; the GRN line's `condition_code`, `damage_notes` and `rejection_reason_code_id` remain the record. No seed or migration change | WAVE C8 fix 7b (user decision 2026-09-16) |
| 2026-09-16 | `MPR-GRD-18`; §6 `FR-028` row | **P1-14/P1-15 C8 fix 8 (N3), settled by default.** After fix 7b the `requires_reason_to_enter` / `_to_leave` flags act only on `STATUS_CHANGE`, which is seeded `requires_reason = true`, so under the seed the flags are informational; they remain meaningful if an operator turns off `requires_reason` on `STATUS_CHANGE`. `FR-028` note: forcing stock out of `PENDING_RESOLUTION` (`P2-12`) must carry its own reason rule, since leaving by `ISSUE` / `TRANSFER` no longer inherits one. No code change | WAVE C8 P1-14/P1-15 fix 8 |
| 2026-09-17 | new `MPR-T2-17` | **P0-13 C9 fix (warehouse-issues#104), recorded from code.** WS-040's *Verify chain* toolbar and its `GET /verify-chain` were built by `P0-13` gated `whb_stock_movements:verify` with no T2 row; the row is added as built. |
| 2026-09-17 | `MPR-GRD-11` | **CL-1 defect WH-SC-035c, user decision.** The context was enforced only on a reversal, so a `requires_reason` `SCRAP` posted `201` with a `DAMAGE`-context reason. A movement type now names the reason-code context it needs (`whb_movement_types.reason_context`, `V500084`; `NULL` = any active reason); the writer refuses a mismatch `422 UNKNOWN_REASON_CODE` naming both contexts. Reversals keep their `REVERSAL` rule and message; the reversal types are seeded `REVERSAL` so one rule covers a direct post of them too. No new error code. | user decision |

---

## 8. Invariants and scenarios this workflow must not break

| Invariant | Rows that carry it | Scenarios that prove it |
|---|---|---|
| `L-1` conservation (`I-1`) | `MPR-GRD-01`, `-26` | `WH-SC-001`, `WH-SC-002`, `WH-SC-003`, `WH-SC-004`, `WH-SC-005`, `WH-SC-141`, `WH-SC-177` |
| `L-2` append-only (`I-2`, `I-4`) | `MPR-UNR-01`…`-03`, `MPR-T4-02` | `WH-SC-006`, `WH-SC-007`, `WH-SC-008`, `WH-SC-009`, `WH-SC-010`, `WH-SC-033` |
| `L-3` correction is reversal (`I-3`) | `MPR-T1-08`, `-09`, `MPR-GRD-11`…`-14`, `MPR-UNR-04`, `-05` | `WH-SC-011`, `WH-SC-012`, `WH-SC-013`, `WH-SC-171` |
| `L-4` positions are a cache (`I-7`) | `MPR-T3-01`, `MPR-T4-01`, `-09`, `MPR-T1-13` | `WH-SC-014`, `WH-SC-015` (v1·P2), `WH-SC-040`, `WH-SC-061` |
| `L-5` full-grain key (`I-5`) | `MPR-T4-01` | `WH-SC-016`, `WH-SC-141`, `WH-SC-198` |
| `L-6` availability / negative policy (`I-6`, `I-24`) | `MPR-GRD-15`…`-17` | `WH-SC-017`, `WH-SC-018`, `WH-SC-170`, `WH-SC-190`, `WH-SC-191`, `WH-SC-196` |
| `L-7` frozen factor (`I-8`) | `MPR-GRD-14`, `-26` | `WH-SC-011`, `WH-SC-019` |
| `L-8` period-bound (`I-10`) | `MPR-GRD-09`, `-10`, `-20`, `MPR-UNR-06` | `WH-SC-021`, `WH-SC-022`, `WH-SC-036` |
| `L-9` idempotent ingestion (`I-11`) | `MPR-T1-03`, `-09`, `MPR-GRD-03`, `-04`, `MPR-UNR-09`, `-11`, `MPR-T4-14` | `WH-SC-023`, `WH-SC-024`, `WH-SC-165`, `WH-SC-166`, `WH-SC-180`, `WH-SC-195` |
| `L-11` ownership never changes silently (`I-14`) | `MPR-UNR-08` | `WH-SC-026`, `WH-SC-027` |
| `L-12` traceability (`I-15`) | lineage `GET` (`MPR-T2-03`) | `WH-SC-028`, `WH-SC-174` |
| `L-13` three timestamps (`I-13`) | `MPR-GRD-05`, `-06`, `MPR-T3-05` | `WH-SC-030`, `WH-SC-031`, `WH-SC-169` |
| `L-14` non-own never valued (`I-16`) | WS-042 `unitCost`/`value` suppression | `WH-SC-032` |
| `L-15` value conservation (`I-21`) | `MPR-T1-11`, `MPR-GRD-02` | `WH-SC-034` |
| `D-14` registered instant (`I-22`, `I-23`) | `MPR-GRD-07` | `WH-SC-312` |
| `D-15` never restate | `MPR-UNR-10`, `MPR-T4-05` | `WH-SC-151` |
| Other scenarios in scope | port surface, errors, partitions | `WH-SC-037`, `WH-SC-038`, `WH-SC-039`, `WH-SC-041`, `WH-SC-042`, `WH-SC-043`, `WH-SC-164`, `WH-SC-167`, `WH-SC-168`, `WH-SC-172`, `WH-SC-173`, `WH-SC-178`, `WH-SC-327` |

---

## 9. Open rows — contradictions between sources, and holes no source fills (all RESOLVED 2026-09-14)

**Contradictions.** Each was left open for the ratifier with its precedence pointer. Each is now
resolved; the last column records the resolution and its authority.

| ID | Contradiction | Source A | Source B | Rows affected | Precedence pointer → resolution |
|---|---|---|---|---|---|
| MPR-OPEN-01 | Permission names for the movement verbs | `warehouse:movements:post` / `:reverse` / `:simulate` / `:view` — `issues/p0-08.md` endpoint table; `PORT-AND-ADAPTER-CONTRACT.md` `PC-31`; `BUILD-SPEC-SCREENS.md` §10.2 | `whb_stock_movements:reverse` / `:simulate` / `:view` — `BUILD-SPEC-SCREENS.md` WS-040 *Actions*/*Toolbar* and WS-041; `issues/p0-02.md` *Screens* table and bullet ("`whb_stock_movements:simulate`") | T1-01/08/09/12, T2-02/03/04, WS-040, WS-041 | **RESOLVED (DEFAULT, later fold).** Port verbs `warehouse:movements:post` / `:reverse` / `:simulate` / `:view`; the screens use `whb_stock_movements:view` / `:export` / `:approve` / `:post_backdated` / `:verify`. §10.2 is the sole seed authority — `GAP-REGISTER-R3.md` `RA-007`; `BUILD-SPEC-SCREENS.md` §10.2 and WS-040 amended |
| MPR-OPEN-02 | Soft-close override: which permission, and whether approval is needed | `whb_stock_periods:override`, override written to `whb_stock_period_overrides` with reason code and justification — `issues/p0-07.md` Scope + Acceptance, BUILD-SPEC §10.2; an **approved** override with `approved_by` ≠ poster — `DATA-MODEL.md` §2.1.9 and `I-10`; setting `warehouse.period.soft_close_requires_approval` locked true — p0-07 | `warehouse:movements:post:backdated` alone, *"records the override with the overriding user"* — `PC-26`, `PC-31`, `FR-020`; *"recorded **on the movement**"* — `WH-SC-022` (the header has no override column) | GRD-10, T2-09, T4-06 | **RESOLVED (PRECEDENT + DEFAULT).** The poster holds `whb_stock_movements:post_backdated` **and** `whb_stock_periods:override`; an override row exists whose approver is not the requester; approval is locked on. `RA-007`; `GLOBAL-SETTINGS-DECISIONS.md`; the built `WhbPeriodOverrideRecorder` and `V500019` in `classic`; `PORT-AND-ADAPTER-CONTRACT.md` `PC-31` amended |
| MPR-OPEN-03 | The name of the session GUC that `I-10` reads | `whb.period_override_id` — `DATA-MODEL.md` §6.4 `I-10` | `warehouse.soft_close_override` — `issues/p0-07.md` Traps | GRD-10, T4-06 | **RESOLVED (DEFAULT).** `whb.period_override_id` — `DATA-MODEL.md` §6.4 `I-10`; already built (`WhbPeriodOverrideRecorder`) and `issues/p0-07.md` *Traps* already amended |
| MPR-OPEN-04 | The name of the accounting-date field (it decides the refusal's field path) | `posting_date` — `L-8`, `L-13`, `DATA-MODEL.md` §2.1.8, `PC-26`, p0-07 | `effective_date` — `FR-007`, `FR-020`, `WH-SC-021` (`details.errors["effective_date"]`), `WH-SC-022`, `WH-SC-030` | GRD-09, GRD-10 | **RESOLVED (DEFAULT).** `posting_date`, and the refusal's field path is `posting_date` — `DATA-MODEL.md` §2.1.8 |
| MPR-OPEN-05 | Movement lifecycle column | `I-2`'s function reads `whb_stock_movements.status` (`'POSTED'`; "drafts are freely editable"); `WH-SC-008` edits a `DRAFT`; `WH-SC-039` creates a `PENDING` movement "with no ledger effect" that "posts" on approval | `DATA-MODEL.md` §2.1.8 and `IRREVERSIBLE.md` §4.1 list **no `status` column** (only `approval_status`, `posting_status`, `posted_at NOT NULL`), and `approval_status`/`approved_by`/`approved_at` are **not** on `I-2`'s mutable allowlist | §1.1, T1-04…07, UNR-07 | **RESOLVED — user decision, 2026-09-14 (option B).** No `status` column. `approval_status` carries the lifecycle: `NULL`/`APPROVED` has ledger effect; `PENDING` has none and is the only header that can change; `REJECTED`/`WITHDRAWN` are terminal. `I-1`, `I-2` and `I-7` filter on it, and `I-2` admits lines into a header written by the current transaction. `DATA-MODEL.md` §2.1.8, §6.2, §6.4; `IRREVERSIBLE.md` §4.1; `issues/p0-02.md` |
| MPR-OPEN-06 | The size of `I-2`'s header allowlist | *"deliberately five columns"* — `DATA-MODEL.md` §6.4 `I-2` prose | the array literally lists seven: `posting_status, handover_id, is_reversed, reversed_by_movement_id, updated_at, updated_by, version` — same section | UNR-01, T4-04 | **RESOLVED (DEFAULT).** Seven columns; the `I-2` prose now says so — `DATA-MODEL.md` §6.4 |
| MPR-OPEN-07 | Unique keys on the partitioned header | *"PostgreSQL requires the partition key to be a member of every unique or primary key"* — `DATA-MODEL.md` §1.9; the same trap for the port's key — `issues/p0-08.md` Traps (`RL-011`) | `uk(source_system, idempotency_key)`, `uk(warehouse_id, sequence_no)` — §2.1.8 and §6.4 `I-4`/`I-11`; `uk(reversal_of_movement_id)` — §6.4 `I-3`. None includes `occurred_at`. The index names also differ between §2.1.8 (`uk_whb_stock_movements_*`) and §6.4 (`uk_whb_movements_*`) | GRD-03, -04, -13, T4-02, UNR-05 | **RESOLVED — user decision, 2026-09-14 (key registry).** The non-partitioned `whb_movement_idempotency_keys` (`PRIMARY KEY (source_system, idempotency_key)`; bare `movement_id`, `occurred_at`, `payload_hash`, `created_at`), created in `V500030`, is `I-11`'s guard for movements, reversals and withdraws. Sequence: `I-4`'s counter row. Single reversal: `I-3`'s conditional update. The ledger's unique indexes include `occurred_at` as backstops and are named `uk_whb_stock_movements_*`. `DATA-MODEL.md` §1.9, §2.1.8, §6.4 |
| MPR-OPEN-08 | Type of the reversal link | bare UUID, no `REFERENCES`; `L-3` link held by the service + `I-3` — `DATA-MODEL.md` §1.9, §2.1.8 | "UUID FK self" — `IRREVERSIBLE.md` §4.1; `PORT-AND-ADAPTER-CONTRACT.md` field table | T1-08, T4-04 | **RESOLVED (DEFAULT).** Bare UUID, no `REFERENCES` — `DATA-MODEL.md` §1.9; `IRREVERSIBLE.md` §4.1 amended |
| MPR-OPEN-09 | Code for a missing idempotency key | `IDEMPOTENCY_KEY_REQUIRED` — `issues/p0-08.md`, `PC` §3.9.1, `WH-SC-024` | `MISSING_IDEMPOTENCY_KEY` — `DATA-MODEL.md` §6.4 `I-11` prose | GRD-03 | **RESOLVED (DEFAULT).** `IDEMPOTENCY_KEY_REQUIRED`; `MISSING_IDEMPOTENCY_KEY` is retired and removed from `DATA-MODEL.md` `I-11` — `PORT-AND-ADAPTER-CONTRACT.md` §3.9.1, `WH-SC-024` |
| MPR-OPEN-10 | Null discipline on the `L-5` key (it decides whether rebuild equality holds) | `NULLS NOT DISTINCT`, explicitly *not* a sentinel — `DATA-MODEL.md` §2.1.8, `issues/p0-02.md` Traps, `IRREVERSIBLE.md` §4.3 | "nil-UUID sentinel" — `FR-011`, `WH-SC-016` | T4-01, T3-01 | **RESOLVED (DEFAULT).** `NULLS NOT DISTINCT`, no sentinel — `DATA-MODEL.md` §1.10, §2.1.8 |
| MPR-OPEN-11 | Code of the value-offset virtual location | `VALUE_OFFSET` — `OD-13`, `FR-084` | `VIRT-LANDED-COST-OFFSET` — `WH-SC-034` | T1-11 | **RESOLVED (DEFAULT).** `VALUE_OFFSET` — `OD-13`; the built `V500006` refuses any other code |
| MPR-OPEN-12 | Whether availability is read from a stored column | *"`available` is computed, never read from a stored column"* — `WH-SC-017` | *"`quantity_available` is a stored column that the writer maintains … not a computed read"*; the ATP authority is computed and the column is its projection — `DATA-MODEL.md` §2.1.8 note | GRD-15, T4-01 | **RESOLVED (DEFAULT).** The ATP authority is computed; `quantity_available` is a writer-maintained projection covered by the rebuild — `DATA-MODEL.md` §2.1.8 note |
| MPR-OPEN-13 | Mobile scope | user scope rule of 2026-09-11: warehouse gets **no mobile app** | `D-13` (*mobile is not optional*); `BATCH-WAREHOUSE-P0.md` Rule 13; `issues/p0-02.md` *Screens* "Mobile" row and its date-range bullet; `BUILD-SPEC-SCREENS.md` WS-040/041/042 *Mobile* paragraphs; `WH-SC-195` (`base·mobile`) | §0, T5 | **RESOLVED (user rule, 2026-09-11).** Web only. `issues/p0-02.md` *Screens* and `BUILD-SPEC-SCREENS.md` WS-040/041/042 amended; `D-13`, the batch rule and `WH-SC-195` still name mobile and are their owners' to amend |
| MPR-OPEN-14 | Tax column in the WS-040 export | `hsnCode` — `BUILD-SPEC-SCREENS.md` WS-040 *Export* | no `hsn_code` column exists; the pair is `tax_classification_code` + `tax_classification_scheme` — `RL-008`, `issues/p0-02.md`, `DATA-MODEL.md` §2.1.8 | WS-040 export | **RESOLVED (DEFAULT).** Export `taxClassificationCode` + `taxClassificationScheme` — `DATA-MODEL.md` §2.1.8, `RL-008`; `BUILD-SPEC-SCREENS.md` WS-040 amended |
| MPR-OPEN-15 | Scenario cited for the corrupted-cache case | `issues/p0-03.md` Acceptance: *"Deliberately corrupting one position row … (`WH-SC-014`)"* | catalogue: `WH-SC-014` is the **clean** rebuild; the corrupted case is `WH-SC-015` (v1·P2), and p0-03 itself says `WH-SC-015` is proved later | T3-01, §8 | **RESOLVED (DEFAULT).** `WH-SC-014` is the clean rebuild; the corrupted cache is `WH-SC-015` (v1·P2). `issues/p0-03.md` acceptance amended |
| MPR-OPEN-16 | Whether soft close refuses on pending handovers | WS-045 *Actions*: *"Soft close · Close · Reopen — three modals, each refusing while `pendingHandoverCount > 0`"* — `issues/p0-07.md` Screens | *"Soft close warns and lists; hard close refuses"* — `issues/p0-07.md` round-3 fold (`RA-004`) | GRD-09 (period state reachable) | **RESOLVED (DEFAULT, later fold).** Soft close warns and lists; hard close refuses — `issues/p0-07.md` round-3 fold (`RA-004`); the built `WhbStockPeriodTransitionModal` |

**Holes.** No source answered these, so nothing was contradicted. Each is now resolved.

| ID | Hole | Rows | Resolution |
|---|---|---|---|
| MPR-OPEN-17 | **Which dates a reversal carries.** `PC-20` and `WH-SC-021` say the correct act is a *current-dated* reversal, and `PERIOD_CLOSED`'s retry note says *"re-date and re-post"*. No source says whether `/reverse` copies the original's `occurred_at`/`posting_date`, takes them from the caller, or stamps the current date, so which period WS-040's Reverse gate reads is undefined | T1-08, T2-04, GRD-09 | **RESOLVED (DEFAULT).** The request may carry `occurred_at` / `posting_date`; when it omits them the reversal is current-dated. Guards read the reversal's own dates, and the WS-040/041 Reverse modal sends none — `PORT-AND-ADAPTER-CONTRACT.md` `PC-20` (amended), `WH-SC-021`(b) |
| G1 | The `FR-039` code for *approval after close* is promised by `RA-004` ("a named error code") and never allocated. The `FR-039` fold that adds `POSITION_CONTENTION`, `BATCH_TOO_LARGE`, `OCCURRED_AT_BEFORE_RETENTION`, `UNREGISTERED_INSTANT` and `VALUE_UNBALANCED` to the 43 is still pending (`RD-008`) | GRD-20, GRD-02, GRD-06, GRD-17 | **RESOLVED (PRECEDENT).** `409 PERIOD_CLOSED_SINCE_SUBMISSION` on `posting_date`, added to `PORT-AND-ADAPTER-CONTRACT.md` §3.9.2 (the built `PERIOD_OVERRIDE_*` codes are the naming precedent). The `FR-039` fold stays `RD-008`'s, in `P0-08` |
| G2 | The permission a `WARN` negative-stock acknowledgement needs ("override permission") is never named | GRD-16 | **RESOLVED (PRECEDENT).** `whb_negative_stock_policies:override`, seeded by `P0-15` in `V501000` (the `whb_stock_periods:override` shape) — `BUILD-SPEC-SCREENS.md` §10.2 |
| G3 | The permission gating WS-042 *Change status* is never named | T1-10, T2-08 | **RESOLVED (PRECEDENT).** `whb_stock_positions:change_status`, seeded by `P0-15` in `V501000`, depending on `whb_stock_positions:view` — `BUILD-SPEC-SCREENS.md` §10.2, WS-042 |
| G4 | The literal and the exits of the approval *reject* outcome | §1.1, T1-06 | **RESOLVED (PRECEDENT).** `REJECTED`, terminal, no exits; resubmit as a new movement. Guards `MPR-GRD-19`, `-28` — `DATA-MODEL.md` §2.1.8 `approval_status` |
| G5 | Who may withdraw a `PENDING_APPROVAL` movement, and from which screen | T1-07, T2-07 | **RESOLVED — user decision, 2026-09-14.** Maker only: `POST /api/warehouse/movements/{id}/withdraw` with its own key, `warehouse:movements:post`, caller = submitter; a Withdraw action on WS-040/041 while `PENDING` — `PORT-AND-ADAPTER-CONTRACT.md` §2.2, `issues/p0-08.md`, `BUILD-SPEC-SCREENS.md` WS-040/041 |
| G6 | A `PENDING_APPROVAL` movement has no ageing, reminder or expiry rule | T3 | **RESOLVED (DEFAULT).** No expiry; escalation by `warehouse.approval.pending_alert_hours`, owned by `P1-20`/`P2-23` — `GLOBAL-SETTINGS-DECISIONS.md` |
| G7 | Whether availability (`MPR-GRD-15`/`-16`) applies to a reversal's outbound lines, e.g. reversing a receipt whose stock has since been issued. `WH-SC-082` refuses this one level up, for a GRN, but the ledger-level rule is unstated | T1-08 | **RESOLVED (DEFAULT).** Yes: `MPR-GRD-15` / `-16` / `-17` apply to the mirror's outbound lines at post time — `FR-014`, `GLOBAL-SETTINGS-DECISIONS.md` `CONFIG-CASE-05`, `FR-436`; `issues/p0-03.md` amended |
| G8 | Default sort for WS-040 and WS-042; the rebuild job's class, channel and notify-once behaviour | T5, T3-01 | **RESOLVED (PRECEDENT).** WS-040 `e.occurred_at DESC, e.sequence_no DESC`; WS-042 by item code, site, location code. Job `WhbPositionRebuildJob` / `WHB_POSITION_REBUILD`, marker `WHB_POSITION_DRIFT`, one alert per run — `classic` warehouse-base query services and `AccBalanceDriftAlertService`; `issues/p0-03.md`, `BUILD-SPEC-SCREENS.md` WS-040/042 amended |
| MPR-OPEN-18 | **What still counts a reversal that never took ledger effect** (C2 fix pass 4, 2026-09-15). (1) `uk_whb_stock_movements_one_reversal (reversal_of_movement_id, occurred_at)` still counts rejected, withdrawn and pending reversals, so a new reversal sending the same explicit `occurred_at` hits the index instead of a coded `409`. (2) `I-3` still bumps the original's version and `updated_*` when a pending reversal is submitted | T1-04…-08, GRD-13 | **OPEN.** Either could be fixed by narrowing the index and having `I-3` skip `PENDING`. Owner: a future migration, numbered from a `DATA-MODEL.md` §7 claim |
| MPR-OPEN-19 | **A single post that outlives the stale threshold** (C4 functional re-verify, 2026-09-16). The heartbeat fires before each batch member and when the batch completes, never inside a post, so a post running longer than `warehouse.port.stale-after-minutes` (default 15) can be failed "Interrupted" although its movement commits; `processed()` is then a guarded no-op and the batch counts that member failed | T3-06 | **RESOLVED (DEFAULT), 2026-09-16.** Accepted as the one bounded exception to "a live request is never recovered". The threshold must be set above the longest expected single-post duration; the fix is Reprocess, which replays idempotently — the key is already posted, so it answers `DUPLICATE` and the envelope becomes `PROCESSED`, with no second movement |
| MPR-OPEN-20 | **Batch replay does not apply the `RJ-002` transit exception** (C4 functional re-verify, 2026-09-16). The batch replay site check in `WhbPortReferenceResolver` checks the plain tier, so a submitter whose member posted through the exception would get `403` on retry | GRD-33 | **RESOLVED (DEFAULT), 2026-09-16.** No effect today: `wh_transfer_orders:receive` is not seeded anywhere. Hand-off to `P2-02` as an Acceptance checkbox: the batch replay site check must honour the exception before that permission is seeded |

<!-- Ratify with /functional-contract. A derived contract proves nothing until a human has ratified it. -->
