# Functional Contract — Receipt → QC → putaway (and receipt reversal)

> **What this is.** The functional truth for receiving goods at the dock (receiving session and GRN),
> the GRN's effect on the purchase order, optional quality inspection with its QC hold / release /
> reject / scrap dispositions, the putaway task that moves received stock to its bin, and the receipt
> reversal. Every row cites its source id, so a later gate can check a diff against the row rather
> than re-derive it.
>
> **What this is not.** It does not restate the movement writer. Everything this flow posts goes
> through [`movement-post-reverse.contract.md`](movement-post-reverse.contract.md), and a row here cites
> its `MPR-*` id instead of repeating the guard. It does not restate `DECISIONS.md`, `DATA-MODEL.md` or
> the screen spec. It does not cover coding standards.
>
> **Status.** Derived 2026-09-12 (v0); **ratified 2026-09-14** (v1). Every §9 row is now `RESOLVED` or
> `DEFERRED-TO-<task>`; the resolutions were carried into the documents that own them in the same
> change (§7). **No code exists yet** (warehouse is greenfield in `classic`). A remaining `TBD` cell
> names the task that owns it; that task fills it in its own PR and adds a §7 row.

---

## 0. Identity

| | |
|---|---|
| **Workflow** | Receiving session → GRN (blind or against PO) → post → optional quality inspection and disposition → putaway task → completion; receipt reversal; the PO roll-up each of these drives |
| **Module** | `warehouse` (documents, `wh_`); posts through `warehouse-base`'s single writer (`FR-436`). One `warehouse-base` seed correction, `V500058` (`REJECTED` status + `REJECT` disposition), is carried by `P1-14` |
| **Domain baseline** | `docs/DECISIONS.md` (`L-1`, `L-2`, `L-3`, `L-7`, `L-8`, `L-9`, `L-12`, `L-13`, `D-15`) · `docs/DATA-MODEL.md` §2.1.13, §2.2.1, §7.2 · `docs/BUILD-SPEC-SCREENS.md` §0.11, §0.13, §10.2, WS-072…WS-082 · `docs/SCENARIO-CATALOGUE.md` §3.2, §3.3, §3.21 · `docs/IRREVERSIBLE.md` (`IRR-23`, `IRR-34`, §5 rows 4 and 12) · `docs/GLOBAL-SETTINGS-DECISIONS.md` (`warehouse.receiving.over_receipt_percent`, `warehouse.approval.pending_alert_hours`, `CONFIG-CASE-14`) · `docs/PORT-AND-ADAPTER-CONTRACT.md` event vocabulary · `movement-post-reverse.contract.md` (`MPR-OPEN-05` settled 2026-09-14: `approval_status` carries the lifecycle — a pending movement is a ledger row with `approval_status = PENDING` and no stock effect) |
| **Owning task bodies** | `issues/p1-12.md` (PO, cancel cascade, line ladder) · `issues/p1-13.md` (sessions, GRNs, blind receipt) · `issues/p1-14.md` (inspection, disposition, the `V500058` seed correction) · `issues/p1-15.md` (putaway rules and tasks) · `issues/p1-16.md` (receipt reversal). Depends on `issues/p0-05.md` (statuses, dispositions), `issues/p0-10.md` (`whb_tasks`), `issues/p1-02.md` (scan resolution), `issues/p1-03.md` (tolerance ladder), `issues/p1-05.md` (virtual locations), `issues/p1-07.md` (lots, serials, LPNs), `issues/p1-09.md` (gapless numbers), `issues/p1-20.md` (verb seed) |
| **Primary table(s)** | `wh_purchase_orders`, `wh_purchase_order_lines` (`V510011`); `wh_receiving_sessions`, `wh_receiving_session_documents` (`V510013`); `wh_goods_receipts`, `wh_goods_receipt_lines`, `wh_goods_receipt_line_serials` (`V510014`); `wh_inspection_plans`, `wh_inspection_plan_criteria` (`V510015`); `wh_quality_inspections`, `wh_quality_inspection_lines`, `wh_quality_inspection_results` (`V510016`); `wh_putaway_rules`, `wh_putaway_tasks` (`V510017`) over `whb_tasks` (`V500034`); `wh_receipt_reversals`, `wh_receipt_reversal_lines` (`V510018`); seed rows in `whb_stock_statuses` / `whb_dispositions` (`V500058`) |
| **Status columns** | **No DB `CHECK` on any of them, by design** (`D-10`, BUILD-SPEC §0.11 *"why a document and not a `CHECK`"*). The service is the only enforcement point. `V510021` dropped the two PO status `CHECK`s that `V510011` had created; `order_source` is not a status column and keeps its `CHECK` (P1-12, §7 2026-09-15). Ladders: session and GRN (`p1-13`), PO header (BUILD-SPEC §0.11), PO line (`p1-12`), reversal (`p1-16`), task (`whb_tasks.status`, `p0-10`). **`wh_quality_inspections` has no status column** — its state is derived from `started_at`/`completed_at` (§1.1); a pending scrap is the linked `SCRAP` movement's `approval_status` |
| **Permission resources** | `wh_purchase_orders:*` + `:approve` `:cancel` · `wh_receiving_sessions:*` + `:complete` `:cancel` · `wh_goods_receipts:*` + `:post` `:cancel` · `wh_quality_inspections:*` + `:disposition` `:approve` · `wh_inspection_plans:*` · `wh_putaway_rules:*` · `wh_putaway_tasks:*` + `:complete` (Assign and Cancel run under `:edit`) · `wh_receipt_reversals:*` + `:approve` `:post` (seed: `p1-20` `V511000`/`V511001`). **No `wh_goods_receipts:reverse`** (RQP-OPEN-10) |
| **Routes** | web: `/warehouse/inbound/purchase-orders` (WS-072) + `/[id]` (WS-073), `/warehouse/inbound/receiving-sessions` (WS-075), `/warehouse/inbound/goods-receipts` (WS-076) + `/[id]` (WS-077), `/warehouse/inbound/receipt-reversals` (WS-078), `/warehouse/inbound/inspection-plans` (WS-079), `/warehouse/inbound/quality-inspections` (WS-080), `/warehouse/inbound/putaway-rules` (WS-081), `/warehouse/inbound/putaway-tasks` (WS-082) · **mobile: none.** User scope rule of 2026-09-11: warehouse gets **no mobile app**; the RF-gun / scan flows (WS-229 RF Receive, WS-230 RF Putaway, every `screens/wh*` mobile screen) are out of scope for the classic build (`RB-001`, RQP-OPEN-16) |
| **Requirement source** | `issues/p1-12.md` … `issues/p1-16.md` at `warehouse-issues` `main` `57bd7df`, amended 2026-09-14 in the ratifying change (§7) |
| **Contract status** | `ratified 2026-09-14` |
| **Contract version** | `v1 — 2026-09-14` |

### Acceptance checks

| # | The requester expects… | Delivered by |
|---|---|---|
| A1 | One session holds N POs × N GRNs with a nullable supplier; a blind receipt posts with no PO and names a counterparty derived from its owner — a picker only for house-owned stock (`WH-SC-063`, `WH-SC-064`, `WH-SC-086`) | `RQP-T1-01`, `RQP-T1-07`, `RQP-GRD-01` |
| A2 | Post is the only ledger-writing path; it writes a balanced receipt against the counterparty's virtual location with a gapless GRN number, and the PO line counts it (`WH-SC-051`, `WH-SC-194`) | `RQP-T1-09`, `RQP-T4-01`…`-04` |
| A3 | Received status defaults item × site → movement type → **refuse**, never hard-coded (the supplier rung is DEFERRED-TO-P1-03; §7 2026-09-16); tolerance, UoM, serial and identifier guards refuse with the field named (`WH-SC-066`, `-067`, `-073`, `-078`, `-079`, `-175`, `-176`) | `RQP-GRD-02`…`-08` |
| A4 | PO cancel is a cascade with a stock gate that names its blocker; close-short is explicit on WS-072 and creates no backorder; no Reopen (`WH-SC-068`, `WH-SC-083`) | `RQP-T1-14`, `RQP-T1-15`, `RQP-GRD-13`, `RQP-GRD-14` |
| A5 | One inspection series per GRN; completion refuses an incomplete result set; a completed inspection is never re-opened; the rollup is `PASS` / `FAIL` / `PARTIAL` (`WH-SC-070`, `WH-SC-301`) | `RQP-T1-16`…`-19`, `RQP-GRD-15`…`-17` |
| A6 | Disposition is QA-gated and after Complete; release / reject / RTV post a balanced status movement (`RESTOCK_SELLABLE` / `REJECT` → `REJECTED` / `RTV`); scrap posts a `SCRAP` movement `PENDING` that a second user approves or rejects on WS-080; release — and only release — creates the putaway task for QC-held stock (`WH-SC-071`, `WH-SC-039`, `RJ-005`, `RJ-017`) | `RQP-T1-20`…`-23`, `RQP-T1-36`, `RQP-T4-05`, `RQP-T4-08` |
| A7 | Putaway is open work: suggested from rules-as-data, completed by the operator with an override reason where overriding, refusing a policy-forbidden location, posting a conserving move (`WH-SC-052`, `WH-SC-074`, `WH-SC-075`, `WH-SC-076`) | `RQP-T1-24`…`-27`, `RQP-GRD-21`…`-23` |
| A8 | Receipt reversal is request → approve → post over the whole GRN, posts a linked `REVERSAL`, decrements the PO line and regresses the header in one transaction, cancels the GRN's open putaway tasks, and refuses once stock has moved on, naming what moved (`WH-SC-081`, `WH-SC-082`) | `RQP-T1-29`…`-32`, `RQP-T1-35`, `RQP-GRD-24`…`-27` |
| A9 | `CONFIG-CASE-14` holds for this flow: over-receipt zero is respected (`GLOBAL-SETTINGS-DECISIONS.md`) | `RQP-GRD-06` |
| A10 | No edit of a posted GRN exists anywhere; no document writes a position directly | `RQP-UNR-01`, `RQP-UNR-05` |

---

## 1. T1 — State machine

The flow is five ladders that hand work to each other: **session** (the truck) → **GRN** (the legal
receipt) → optional **inspection** (the QC act) → **putaway task** (the move to the bin), with the
**PO** as a roll-up and the **receipt reversal** as the only correction. Verification always happens on
the GRN; inspection is optional and a different screen (`FR-127`).

### 1.1 Status inventory

| Entity | Status | Represented by | Reachable via | Exits via | Kind |
|---|---|---|---|---|---|
| Session | `ARRIVED` | `wh_receiving_sessions.status`; `arrived_at` stamped | `RQP-T1-01` | `-02`, `-06` | initial, active |
| Session | `UNLOADING` | `status`; `docked_at`, `unload_started_at` stamped | `RQP-T1-02` | `-05`, `-06` | active |
| Session | `COMPLETED` | `status`; `unload_completed_at` stamped | `RQP-T1-05` | none | **terminal** |
| Session | `CANCELLED` | `status`; reason recorded | `RQP-T1-06` | none | **terminal** |
| GRN | `DRAFT` | `wh_goods_receipts.status` | `RQP-T1-07` | `-09`, `-10`, `-15` (cascade) | initial, active |
| GRN | `POSTED` | `status`; `receipt_movement_id` on each line | `RQP-T1-09` | `-11` | active |
| GRN | `CANCELLED` | `status` | `RQP-T1-10`, `RQP-T1-15` | none | **terminal** |
| GRN | `REVERSED` | `status` | `RQP-T1-11` (effect only) | none | **terminal** |
| PO header | `DRAFT` · `SUBMITTED` · `APPROVED` | `wh_purchase_orders.status` | BUILD-SPEC §0.11 authoring rows (cited, not restated — §6); `APPROVED` also by `RQP-T1-35` | §0.11 | active |
| PO header | `PARTIALLY_RECEIVED` · `RECEIVED` | `status` | `RQP-T1-12`, `RQP-T1-35` (effects only) | `RQP-T1-14`, `RQP-T1-35` | active |
| PO header | `CLOSED` · `CANCELLED` | `status` | `RQP-T1-14`, `RQP-T1-15` | none — **no Reopen in v1** (RQP-OPEN-07) | **terminal** |
| PO line | `OPEN` · `PARTIALLY_RECEIVED` · `RECEIVED` | `wh_purchase_order_lines.line_status` | `p1-12` Add line; `RQP-T1-12`, `-13` | `RQP-T1-12`…`-15` | active |
| PO line | `CLOSED` · `CANCELLED` | `line_status` | `RQP-T1-14`, `RQP-T1-15` | none | **terminal** |
| Inspection | *in progress* (derived) | `started_at` set, `completed_at` null. **No status column** (DATA-MODEL §2.2.1) | `RQP-T1-16` | `-18` | active |
| Inspection | *completed* (derived) | `completed_at` set, `result` set | `RQP-T1-18` | none; a correction is a **new** inspection (`RQP-T1-19`) | **terminal** (`WH-SC-301`) |
| Scrap disposition | *pending approval* (derived) | the `SCRAP` movement posted by `RQP-T1-22` with `approval_status = PENDING` and no stock effect, found by `source_document_type`/`source_document_id` = the inspection (`MPR-T1-04`; `MPR-OPEN-05` settled) — **no approval column on `wh_quality_inspection_lines`** | `RQP-T1-22` | `RQP-T1-23` (approve), `RQP-T1-36` (reject) | active |
| Putaway task | `CREATED` · `ASSIGNED` | `whb_tasks.status` — `p0-10`'s ladder, with `wh_putaway_tasks` as the 1:1 extension carrying no lifecycle column (`p1-15` Traps) | `RQP-T1-24`, `RQP-T1-25` | `RQP-T1-26`, `-27`, `-28` | active |
| Putaway task | `COMPLETED` · `CANCELLED` | `whb_tasks.status` | `RQP-T1-26`, `RQP-T1-27`, `RQP-T1-32` (cascade) | none | **terminal** |
| Putaway task | `EXCEPTION` | `whb_tasks.status`, `exception_code` | `RQP-T1-28` | WS-059 **Reassign** · **Cancel** · **Complete-with-exception** (`p0-10`; ladder rows in `task.contract.md` `TSK-T1-02`…`-04`, H9). Complete-with-exception on a PUTAWAY task is refused `409 TASK_TRANSITION_INVALID` unless the task is `EXCEPTION`, and the WS-059 row hides the action accordingly (§7 2026-09-16 P1-15 C8 fix 2) | active |
| Putaway task | `STARTED` · `PAUSED` | `whb_tasks.status` | **not reachable in v1** — no Start or Pause action exists; Complete stamps `started_at` itself (RQP-OPEN-08). They arrive with `P3-02` | — | not reachable in v1 |
| Reversal | `REQUESTED` | `wh_receipt_reversals.status` | `RQP-T1-29` | `-30`, `-31` | initial, active |
| Reversal | `APPROVED` | `status`; `approved_by`, `approved_at` | `RQP-T1-30` | `-32`, `-31` (Reject, e.g. when Post is refused — H4) | active |
| Reversal | `REJECTED` · `POSTED` | `status`; `reversal_movement_id` on `POSTED` | `RQP-T1-31`, `RQP-T1-32` | none | **terminal** |

Not statuses, stated so they are not mistaken for them: seals, `departed_at` and the gate pass are
recorded facts (`p1-13`); `match_status` (`MATCHED`/`QTY_OVER`/`QTY_UNDER`/`ITEM_MISMATCH`) is a receipt
outcome on the **GRN header**, not a lifecycle and not a PO-line column (RQP-OPEN-02, -18); `result`
(`PASS`/`FAIL`/`PARTIAL`, RQP-OPEN-01) is a verdict; the ASN (`wh_asns`) is v1.1 and has no status
values in v1 (`RJ-018`).

### 1.2 Transitions

| ID | From | Event / action | To | Guard (service, first) | Actor (permission) | Side effects | Comment/reason required | UI entry point |
|---|---|---|---|---|---|---|---|---|
| RQP-T1-01 | — | Gate-in: a truck arrives; one session for N POs × N ASNs × N GRNs, supplier **nullable** (`FR-124`) | Session `ARRIVED` | — | `wh_receiving_sessions:create` | stamps `arrived_at` (`IRR-23`, cannot be backfilled) | no | WS-075 toolbar **Add** (the Gate-in modal); WS-072 *Receive against* opens it pre-filled |
| RQP-T1-02 | `ARRIVED` | Start | `UNLOADING` | dock door set | `wh_receiving_sessions:edit` | stamps `docked_at`, `unload_started_at` | no | WS-075 row action **Start** |
| RQP-T1-03 | `ARRIVED` · `UNLOADING` | Attach PO — child editor over `wh_receiving_session_documents`. ASN attach is v1.1 and not offered in v1 | unchanged | the PO is attachable (`RQP-GRD-28`); a terminal session changes nothing | `wh_receiving_sessions:edit` | junction row | no | WS-075 **Attach PO/ASN** |
| RQP-T1-04 | `ARRIVED` · `UNLOADING` | Record seals in / out, gate pass (`FR-211`) | unchanged | — | `wh_receiving_sessions:edit` | `seal_number_in` / `_out`, `gate_pass_ref` | no | WS-075 **Record seals** |
| RQP-T1-05 | `UNLOADING` | Complete | `COMPLETED` | `RQP-GRD-10` | `wh_receiving_sessions:complete` — **not** `:edit` (`RJ-005`) | stamps `unload_completed_at` | no | WS-075 **Complete** |
| RQP-T1-06 | `ARRIVED` · `UNLOADING` | Cancel | `CANCELLED` | `RQP-GRD-11` | `wh_receiving_sessions:cancel` — **not** `:edit` (`RJ-005`) | none on stock | **yes** | WS-075 **Cancel** |
| RQP-T1-07 | — | Create GRN — from a session, or a **blind receipt** with no PO (`FR-128`) | GRN `DRAFT` | `RQP-GRD-01`, `RQP-GRD-28`. `session_id` is **nullable**: WS-075 *Create GRN* sets it (PO receipts arrive through WS-072 *Receive against* → WS-075); WS-076 *Blind receipt* leaves it null (H2) | `wh_goods_receipts:create` | gapless GRN number issued **at create** (`RQP-T4-04`) | no | WS-075 **Create GRN**; WS-076 **Blind receipt** modal |
| RQP-T1-08 | `DRAFT` | Receive line — item by id / sku / barcode / `(source_module, external_id)`; quantity, UoM, lot, `expiry_date_override`, serials, LPN, free quantity + `scheme_reference`, received stock status, `condition_code` / `damage_notes`, `LOT`-kind attributes, `cross_dock_reference`. Damage found at the dock is its **own line** in `DAMAGED` (RQP-OPEN-05/-06) | unchanged | `RQP-GRD-02`…`-08` | `wh_goods_receipts:edit` (H17) | line rows; `conversion_factor_used` fixed on the line | no | WS-076 **Receive line**, **Over/short** modals |
| RQP-T1-09 | `DRAFT` | **Post** — the only path that writes the ledger, through `MPR-T1-01` | `POSTED` | `RQP-GRD-09`, `RQP-GRD-28`, then every `MPR-T1-01` guard | `wh_goods_receipts:post` | `RQP-T4-01`…`-07` | no | WS-076 row action **Post** |
| RQP-T1-10 | `DRAFT` | Cancel | `CANCELLED` | `RQP-GRD-12` | `wh_goods_receipts:cancel` — **not** `:edit` (`RJ-005`) | none on stock; the issued GRN number stays in `whb_number_series_issued` | **yes** (BUILD-SPEC §0.11 GRN Cancel row) | WS-076 row action **Cancel** (`DRAFT` only) |
| RQP-T1-11 | `POSTED` | *(effect of `RQP-T1-32`)* | `REVERSED` | never set directly | `wh_receipt_reversals:post` | — | — | none — WS-076 **Reverse** routes to WS-078 |
| RQP-T1-12 | PO header `APPROVED` · `PARTIALLY_RECEIVED`; line `OPEN` · `PARTIALLY_RECEIVED` | *(effect of a GRN post)* | line `PARTIALLY_RECEIVED` when 0 < received and ordered − received > the short-side tolerance; line `RECEIVED` when ordered − received ≤ ordered × the **short-receipt** tolerance (item → warehouse → 0, RQP-OPEN-19). Header `RECEIVED` when every line is `RECEIVED`/`CLOSED`/`CANCELLED`, else `PARTIALLY_RECEIVED`. The recompute itself never closes the header: every line `CLOSED`/`CANCELLED` is reached only by a line Close, and that line Close closes the header (`RQP-T1-14`, user decision 2026-09-15) | never set directly (BUILD-SPEC §0.11); `applyReceipt` re-checks `RQP-GRD-28` under the header lock | `wh_goods_receipts:post` | `received_quantity` / `accepted_quantity` / `rejected_quantity` rolled up; `first_receipt_at` stamped when null, never cleared (H12) | — | none (effect) — triggered by WS-076 **Post** (`RQP-T1-09`); a screen offering it directly is the defect, §0.11 |
| RQP-T1-13 | PO line `PARTIALLY_RECEIVED` · `RECEIVED` | *(effect of a receipt-reversal post)* | line `OPEN` · `PARTIALLY_RECEIVED` | `received_quantity` decremented **in the same transaction** (`p1-16`) | `wh_receipt_reversals:post` | the header is recomputed by the `RQP-T1-12` rule (`RQP-T1-35`) | — | none (effect) — triggered by WS-078 **Post** (`RQP-T1-32`) |
| RQP-T1-14 | header `APPROVED` · `PARTIALLY_RECEIVED` · `RECEIVED`; line `OPEN` · `PARTIALLY_RECEIVED` · `RECEIVED` | Close / **Close short** | `CLOSED` | `RQP-GRD-14` | `wh_purchase_orders:edit` | line remainder → `cancelled_quantity`; **no backorder** (`WH-SC-068`); stamps `closed_at` on a header Close and on the auto-close below. **A header Close closes every line not already terminal; a line Close closes that line only.** When that line Close leaves every line `CLOSED`/`CANCELLED`, the header moves to `CLOSED` as a header Close would: `closed_at` stamped, the same `WH_PURCHASE_ORDER_CLOSED` audit entry, and no header reason, because the lines carry theirs (user decision 2026-09-15). Otherwise the header is recomputed by the `RQP-T1-12` rule, so a mix of `RECEIVED` and `CLOSED` lines leaves it `RECEIVED` (P1-12, §7 2026-09-15) | **yes** where received < ordered | WS-072 **Close short** modal — the only place the action lives (RQP-OPEN-03); it offers the whole order or one line (`PATCH /{id}/close` · `PATCH /{id}/lines/{lineId}/close`) |
| RQP-T1-15 | header `DRAFT` · `SUBMITTED` · `APPROVED`; line `OPEN` | Cancel — a **cascade with a stock gate**, not a status flip (`FR-132`) | `CANCELLED` | `RQP-GRD-13` | `wh_purchase_orders:cancel` | lines → `CANCELLED`; every `DRAFT` GRN holding a line of this PO → `CANCELLED`; **no session transition** — the guard leaves only terminal sessions attached; the junction row is kept (RQP-OPEN-21) | **yes** — reason recorded (the §0.11 cancel pattern of sessions and GRNs) | WS-072 **Cancel** modal |
| RQP-T1-16 | — (or a completed inspection of the same GRN) | Start an inspection. **One series per GRN; a re-inspection takes the next number** (`RJ-014`, `FR-133`) | *in progress* | GRN `POSTED` (only a posted GRN holds QC-held stock); plan resolved from `whb_item_categories.default_inspection_plan_id` in the service, never an FK (`p1-14` Traps); refused `409 INSPECTION_IN_PROGRESS` while an inspection of the GRN is *in progress*, `409 INSPECTION_GRN_NOT_POSTED` when the GRN is not `POSTED` (§7 2026-09-16 P1-14 C8 fix 1) | `wh_quality_inspections:create` (H17) | header + one line per inspected GRN line; the inspected quantity is proposed from `sample_size_formula` applied to the quantity still QC-held (received + free − rejected − already dispositioned) (§7 2026-09-16 P1-14 C8 fix 5a) (`FIXED_QUANTITY` / `PERCENTAGE` propose the whole lot until `wh_inspection_plans` gains a parameter column — **open**) | no | WS-076 **Inspect** → WS-080 **Start** |
| RQP-T1-17 | *in progress* | Record results — per line and per criterion, typed; per-line endpoints for incremental capture (`FR-133`) | unchanged | criterion rows only; no JSONB (`FR-383`) | `wh_quality_inspections:edit` (H17) | `wh_quality_inspection_results` rows | no | WS-080 **Record results** |
| RQP-T1-18 | *in progress* | Complete | *completed* | `RQP-GRD-15`, `RQP-GRD-17` | `wh_quality_inspections:edit` (H17) | `result` rolled up; `started_at` = min of line starts, `completed_at` = max (`FR-133`). **Nothing moves** | no | WS-080 **Complete** |
| RQP-T1-19 | *completed* | Submit again / edit the verdict | refused | `RQP-GRD-16` | any | none — the correction is `RQP-T1-16` with a new number; the first verdict stays | — | none (`RQP-UNR-04`) |
| RQP-T1-20 | QC-held stock of a GRN line, inspection *completed* | **Disposition → release** — `disposition_code = RESTOCK_SELLABLE` | stock in `AVAILABLE` | `RQP-GRD-18`, `RQP-GRD-29` | `wh_quality_inspections:disposition` (QA-role gated) | a `STATUS_CHANGE` through `MPR-T1-10` (balanced, same location, reason mandatory); **then a putaway task for the released quantity** (`RQP-T4-05`, `RJ-017`) | **yes** (`FR-103`) | WS-080 **Disposition** |
| RQP-T1-21 | QC-held stock, inspection *completed* | **Disposition → reject** (`disposition_code = REJECT`, target `REJECTED`, seeded by `V500058`) **/ return to supplier** (`RTV`, target `BLOCKED`) | stock in `REJECTED` / `BLOCKED` | `RQP-GRD-18`, `RQP-GRD-29` | `wh_quality_inspections:disposition` | a `STATUS_CHANGE` through `MPR-T1-10`; **no putaway task** (`RJ-017`); the supplier return document and its despatch `ISSUE` are `p2-13`'s (`WH-SC-072`) | **yes** | WS-080 **Disposition** |
| RQP-T1-22 | QC-held stock, inspection *completed* | **Disposition → scrap** — `disposition_code = SCRAP` | *pending approval* | `RQP-GRD-18`, `RQP-GRD-29` | `wh_quality_inspections:disposition` | posts the `SCRAP` movement (issue to the site's `SCRAP` virtual location, `FR-164`) through `MPR-T1-04`: seeded `SCRAP.requires_approval = true`, so it lands `approval_status = PENDING` with **no stock effect** (`WH-SC-039`); `source_document_type`/`source_document_id` name the inspection | **yes** | WS-080 **Disposition** |
| RQP-T1-23 | *pending approval* | Approve the scrap | stock scrapped | `RQP-GRD-19` | `wh_quality_inspections:approve` — delegating to `MPR-T1-05` | only now does the `SCRAP` movement take effect (`MPR-T1-05`: `approved_by`, `approved_at`, positions move); no putaway task | no — the disposition's reason travels on the movement (`MPR-GRD-11`) | WS-080 row action **Approve scrap** |
| RQP-T1-24 | — | Create the putaway task: **at GRN post** for a line received into a status with `is_allocatable = true`; **at release** for QC-held stock, for the released quantity; **never** for rejected, RTV, scrapped quantity or any other received status (`RJ-017`, H13) | task `CREATED` | the rule chain evaluates in sequence and returns `suggested_location_id` + `rule_id` (`FR-135`). **Not auto-completed** — the task is open work until `RQP-T1-26` (RQP-OPEN-08) | system (effect of `RQP-T1-09` / `-20`) | one `whb_tasks` row + one `wh_putaway_tasks` row per line (`p0-10`); `stock_status_code` records the status the task moves; a serialised line's serials travel into `wh_putaway_task_serials` (`V510022`); the site's `TASK` number series is provisioned at runtime when none exists (H8 RESOLVED, §7 2026-09-16 P1-15 C8 fix 2) | — | none (effect) |
| RQP-T1-25 | `CREATED` | Assign — optional | `ASSIGNED` | explicit assignment only; no `FOR UPDATE SKIP LOCKED` claiming in v1 (`p1-15` Traps, `C-040`) | `wh_putaway_tasks:edit` (RQP-OPEN-11) | stamps `assigned_to`, `assigned_at` | no | WS-082 **Assign** |
| RQP-T1-26 | `CREATED` · `ASSIGNED` | **Complete** — scan the actual location; capture the override reason when actual ≠ suggested | `COMPLETED` | `RQP-GRD-21`, `-22`, `-23`; every `MPR-T1-01` guard | `wh_putaway_tasks:complete` (seeded by `p1-20`; §10.2 row added) | `RQP-T4-11`: a two-line conserving move staging → actual through `MPR-T1-01`; **in this one request** stamps `assigned_to`/`assigned_at` when null and `started_at` = `completed_at` = now() (`p0-10` Traps); `completion_movement_id` | **yes, when overriding** | WS-082 **Complete** modal (WS-076 **Putaway** routes here) |
| RQP-T1-27 | `CREATED` · `ASSIGNED` | Cancel | `CANCELLED` | a catalogue reason code | `wh_putaway_tasks:edit` (RQP-OPEN-11) | **posts nothing** — the stock stays at staging; **a fresh unassigned `CREATED` task is raised for the same stock** (from WS-082 or WS-059), except when the cancel is caused by a receipt reversal post, which raises none (user decision 2026-09-16; H8 RESOLVED, §7 2026-09-16 P1-15 C8 fix 2). A Cancel raises **no** replacement — the cancel itself still succeeds — when the task's goods receipt is not `POSTED`, or when the staged on-hand at the task's grain (owner, item, staging location, status, lot and LPN matched exactly; when serialised, only the task's serials) is zero. When the GRN is `POSTED` and staged on-hand is above zero but below the task quantity, the replacement is raised for `min(staged on-hand, task quantity)`; a serialised task's replacement carries only the task's serials still standing at staging, its quantity their count (replaces fix 5b N1's "no replacement below the task quantity", §7 2026-09-16 P1-14/P1-15/P1-16 C8 fix 6) | **yes** — reason-coded per `p0-10` WS-059 | WS-082 **Cancel** |
| RQP-T1-28 | `ASSIGNED` | Ages past the task type's `stale_after_minutes` | `EXCEPTION` | `p0-10` (`Y-004`) | scheduled job (`RQP-T3-02`) | unassigned; supervisor notified | — | none (scheduled job) |
| RQP-T1-29 | — | Request a receipt reversal — **of the whole GRN** | Reversal `REQUESTED` | `RQP-GRD-24` | `wh_receipt_reversals:create` | reversal number from `p1-09`'s gapless series; one `wh_receipt_reversal_lines` row per GRN line, `quantity` = the line's full received quantity — no partial reversal (RQP-OPEN-20) | **yes** — catalogue reason code | WS-078 **Request**; WS-076 **Reverse** routes here |
| RQP-T1-30 | `REQUESTED` | Approve | `APPROVED` | `RQP-GRD-25` | `wh_receipt_reversals:approve` | stamps `approved_by`, `approved_at` | no | WS-078 **Approve** |
| RQP-T1-31 | `REQUESTED` · `APPROVED` | Reject — including an approved reversal whose Post is refused (H4) | `REJECTED` | reason recorded | `wh_receipt_reversals:approve` | none | **yes** | WS-078 row action **Reject** |
| RQP-T1-32 | `APPROVED` | **Post** | `POSTED`; GRN → `REVERSED` (`RQP-T1-11`); PO line decrement (`RQP-T1-13`); header regress (`RQP-T1-35`) — **one transaction** | `RQP-GRD-26`, `-27`, `-30`; then `MPR-T1-08`'s guards. `RQP-GRD-30` (open inspection, then pending scrap) is evaluated before `RQP-GRD-26`; a receipt failing both is refused with `RECEIPT_INSPECTION_OPEN` / `RECEIPT_SCRAP_PENDING` (§7 2026-09-16 P1-16 C8 fix 5c) | `wh_receipt_reversals:post` — **not** `:approve` (`RJ-005`) | `RQP-T4-12` | the request's reason travels to the `REVERSAL` movement (`MPR-GRD-11`) | WS-078 **Post** |
| RQP-T1-33 | a putaway rule a task references | Edit / Reorder | a new rule version; the old one deactivated | copy-on-write (`version_no`, `supersedes_id`); a direct `UPDATE`/`DELETE` is refused by `I-24` (`RL-010`) | `wh_putaway_rules:edit` | the task's `rule_id` still resolves to the rule that ran | no | WS-081 **Edit** / **Reorder** |
| RQP-T1-34 | — | Test the rule chain: item + quantity + optional status + optional owner (one the caller may read; picker `GET /owners?warehouseId`, filtered to the site) → suggested location and winning rule. Without an owner, dedicated locations and occupied commingle-constrained ones are not offered (§7 2026-09-16 P1-15 C8 fix 5b) | — (nothing) | — | `wh_putaway_rules:view` (H17) | **writes nothing** | — | WS-081 **Test** modal |
| RQP-T1-35 | PO header `RECEIVED` · `PARTIALLY_RECEIVED` | *(effect of `RQP-T1-32`)* — the header is recomputed from its lines in the reversal transaction | recomputed by the `RQP-T1-12` rule: `APPROVED` when no line holds received quantity; `RECEIVED` when every line is `RECEIVED`/`CLOSED`/`CANCELLED` (a short-receipt tolerance can keep a line `RECEIVED` after a reversal); else `PARTIALLY_RECEIVED`. The recompute never closes the header; only the line Close that leaves no line open does (`RQP-T1-14`) | never set directly | `wh_receipt_reversals:post` | `first_receipt_at` and `received_at` are **not** cleared (`IRR-23`) (H6) | — | none (effect) |
| RQP-T1-36 | *pending approval* | Reject the scrap | stock stays QC-held | approver holds `wh_quality_inspections:approve` | `wh_quality_inspections:approve` — delegating to `MPR-T1-06` | the `PENDING` `SCRAP` movement → rejected, no position change (`L-2`); the quantity is given back to the inspection line (`dispositioned_quantity`) in the same transaction, so the QC-held quantity may be dispositioned again (H3; §7 2026-09-16 P1-14 C8 fix 1). **Withdraw** (maker, WS-040 `POST /movements/{id}/withdraw`) of a PENDING QC scrap gives its quantity back to the inspection line exactly as Reject does, in the same transaction (`MPR-GRD-27` `Decision.WITHDRAW`; §7 2026-09-16 P1-14 C8 fix 5a) | **yes** | WS-080 row action **Reject scrap** |

### 1.3 Guards — the predicate, the refusal, and where it lands

Every guard has a **service pre-check that rejects first, inside the transaction, with a field-level
error** in the house envelope `{ "error", "details": { "errors": { "<field path>": "<message>" } } }`
(`DECISIONS.md` §4, BUILD-SPEC §0.13). This flow's documents carry **no DB trigger of their own** except
`I-24` on putaway rules, so the service check is the only enforcement for `RQP-GRD-01`…`-21` and
`-24`…`-30`. Anything the post reaches in the ledger is backstopped by the `MPR-GRD-*` row cited; a
ledger trigger that fires in production is an incident, not a validation (`DATA-MODEL.md` §6.0).

**Refusal codes.** `FR-039` is the port's vocabulary; an application refusal code lives in the block
that raises it (BUILD-SPEC §0.13). A code marked *proposed* follows the `warehouse-base` convention
`<SUBJECT>_<CONDITION>` (`COUNTERPARTY_REQUIRED`, `CONVERSION_IN_USE`) and is DEFERRED to the named task,
which fixes it in its own block (H11). HTTP: `422` for invalid input, `409` for a state conflict.

| ID | Predicate (must hold to proceed) | Refusal: HTTP · code · field | Message shape | DB backstop | Source |
|---|---|---|---|---|---|
| RQP-GRD-01 | Blind receipt: item, quantity, UoM, owner, status and location suffice — **no PO, no placeholder PO number**. The GRN still names a counterparty (`FR-120`): **derived** from `whb_owners.counterparty_id` for a non-house owner; for a house owner (whose `counterparty_id` is null) the modal shows a counterparty picker and the value is required (RQP-OPEN-15) | `422 COUNTERPARTY_REQUIRED` · `supplier_counterparty_id` (the base code, `warehouse-base`) | *"a blind receipt of house-owned stock names its counterparty"* | — | `FR-128`, `FR-120`, `WH-SC-063`, `DATA-MODEL.md` `whb_owners` |
| RQP-GRD-02 | The line's UoM converts to the item's base UoM. **The GRN modal does not offer an unconvertible unit** — the same guard as the PO line editor, built once | `422 UOM_NOT_CONVERTIBLE` · `lines[n].uom_code` | *"LITRE is not convertible to base UoM EA for item OF-1120"* | the ledger's frozen factor (`MPR-GRD-26`) | `FR-143`, `WH-SC-078`, `L-7` |
| RQP-GRD-03 | Tracking follows the item's **control policy** (serial `NONE`/`RECEIPT`/`SHIP`/`FULL`, lot `NONE`/`OPTIONAL`/`REQUIRED`, expiry policy), never a mode string read alone | `422 SERIAL_REQUIRED` · `lines[n].serials` (an application code, H10); `LOT_REQUIRED` for a lot-required item | names the item and its policy | — | `FR-144`, `FR-098`, `WH-SC-079` |
| RQP-GRD-04 | No duplicate serial, LPN or lot in its uniqueness scope. The same serial under a **different owner** is accepted with a site-level warning; lot codes are normalised on write | `409 SERIAL_ALREADY_ISSUED` · `lines[n].serials[m]` | names the serial and its existing owner | `uk(owner, item, serial_number)`; lot `uk(owner_id, item_id, lot_code)` | `FR-106`, `WH-SC-080`, `WH-SC-142` |
| RQP-GRD-05 | The item resolves uniquely from id / sku / barcode / `(source_module, external_id)`; supplying two identifiers that resolve to different items is refused, never settled by precedence. A scanned code resolves by session owner → counterparty context → refuse | `422 UNKNOWN_ITEM`; `422 ITEM_IDENTIFIER_CONFLICT` · `lines[n]`; `409 AMBIGUOUS_IDENTIFIER` **listing the candidates** | names both identifiers and both items | — | `FR-038`, `WH-SC-175`, `WH-SC-176`, `RL-005` |
| RQP-GRD-06 | Received − ordered (free quantity excluded) ≤ the over-receipt tolerance, resolved **PO line → PO header → item → warehouse → global `warehouse.receiving.over_receipt_percent`** (default 0). Explicit zero blocks; NULL inherits; a percentage, not a fraction. Within tolerance: posts with `match_status = QTY_OVER` | `422` · `lines[n].quantity`; code *proposed* `OVER_RECEIPT_TOLERANCE_EXCEEDED` — DEFERRED-TO-P1-13 | *"received 560 exceeds ordered 500 by 12.00%, over which the tolerance is 5.00%"* **and the level that supplied the 5.00%** (PO line / PO header / item / warehouse / global setting); offers raise-the-PO or receive-to-tolerance + an `OVER_RECEIPT` reconciliation case. Nothing posts until one is chosen | — | `FR-130`, `RE-004`, `WH-SC-066`, `WH-SC-067`, `WH-SC-085`, `CONFIG-CASE-14`, `GLOBAL-SETTINGS-DECISIONS.md` |
| RQP-GRD-07 | The received stock status is **resolved line override → item × site (`whb_item_site_settings.default_receipt_status_code`) → movement type (`whb_movement_types.default_stock_status_code`) → refuse** and written on the line; `AVAILABLE` appears in no code path as a literal default. The **supplier** rung of the original chain is not implementable while `whb_item_supplier_sources` carries no status column — DEFERRED-TO-P1-03 (§7 2026-09-16); the last rung is a refusal, not a literal, so the no-`AVAILABLE` predicate still holds | `422 RECEIVED_STATUS_UNRESOLVED` · `lines[n].receivedStatusCode` when nothing is configured; otherwise — (a predicate on the code, asserted by test) | names the item, the site, and what to configure | — | `FR-129`, `WH-SC-073` |
| RQP-GRD-08 | `LOT`-kind attribute keys entered on Receive line exist in `whb_attribute_keys` with `applies_to = LOT`, and are written to `whb_entity_attribute_values` in the post transaction | `422` naming the key; code *proposed* `ATTRIBUTE_KEY_NOT_REGISTERED` — DEFERRED-TO-P1-13 | names the unregistered key | FK to the key registry | `RL-007` |
| RQP-GRD-09 | Post: at least one line; the UoM guard (`RQP-GRD-02`) re-run; `match_status` computed; the post goes through the single writer and nothing else (architecture test) — then every `MPR-T1-01` guard (period `MPR-GRD-09`/`-10`, registered instant `MPR-GRD-07`, company `MPR-GRD-08`, registries `MPR-GRD-21`, attributes `MPR-GRD-24`, rounding `MPR-GRD-26`) | as the cited `MPR-GRD-*` row; the empty GRN `422` · `lines`, code *proposed* `GRN_HAS_NO_LINES` — DEFERRED-TO-P1-13 | as cited; *"a GRN with no lines cannot be posted"* | as cited | BUILD-SPEC §0.11, `p1-13` ladder, `FR-436` |
| RQP-GRD-10 | Session Complete: no GRN in the session is `DRAFT` | `409` · banner; code *proposed* `SESSION_HAS_DRAFT_GRN` — DEFERRED-TO-P1-13 | names the `DRAFT` GRN number(s) | — | `p1-13` ladder |
| RQP-GRD-11 | Session Cancel: no `POSTED` GRN in the session; a reason is recorded | `409` · banner, code *proposed* `SESSION_HAS_POSTED_GRN`; `422` · `reason` — DEFERRED-TO-P1-13 | names the posted GRN number(s) | — | `p1-13` ladder |
| RQP-GRD-12 | GRN Cancel: the GRN is `DRAFT` (nothing posted); a reason is recorded | `409` · banner, code *proposed* `GRN_NOT_DRAFT`; `422` · `reason` — DEFERRED-TO-P1-13 | names the GRN's current status | — | `p1-13` ladder, BUILD-SPEC §0.11 |
| RQP-GRD-13 | PO Cancel: **no GRN line has received stock** against any PO line, **and** no session this PO is attached to through `wh_receiving_session_documents` is `ARRIVED` or `UNLOADING`. **No v1 code path reads `wh_asns`** (the ASN clause activates in `P3-05`). **Serialised on the PO header row:** every PO ladder write (update, submit, approve, return, cancel, close, line close) and the receipt counter write (`applyReceipt`, called by the GRN post and the reversal post) read `wh_purchase_orders` under a row lock before this check, so it runs on committed state | `409` · banner; codes `PO_HAS_RECEIVED_STOCK` / `PO_SESSION_ARRIVED` (settled by P1-12, H11); the modal **names the blocking GRN line**, or **names the session** | *"…because GRN-… line n has received stock"* / *"…because receiving session … has arrived"* | — | `FR-132`, `WH-SC-083`, `RJ-018` |
| RQP-GRD-14 | Close / Close short: a reason is required where received < ordered — over the open lines on a header Close, over that line alone on a line Close; the remainder moves to `cancelled_quantity`; **no backorder is created** | `422` · `reason_code_id`; code `CLOSE_SHORT_REASON_REQUIRED` (settled by P1-12, H11). A line already `CLOSED`/`CANCELLED`: `409 PO_LINE_TRANSITION_INVALID` | *"a short close needs a reason: received 160 of 200"* | — | `FR-130`, `WH-SC-068`, `p1-12` line ladder |
| RQP-GRD-15 | Inspection Complete: **every mandatory criterion on every line has a result**. The rollup is never computed from a partial set. It also refuses a line with neither a criterion result nor passed/failed units (`Q-006`; §7 2026-09-16 P1-14 C8 fix 1) | `422 INSPECTION_INCOMPLETE` · `lines[n].results` | names **each unrecorded mandatory criterion by line number and criterion code** | — | `WH-SC-301`, `Q-006` |
| RQP-GRD-16 | A completed inspection is never re-opened or re-submitted | `409 INSPECTION_ALREADY_COMPLETED` | points to re-inspection under the next number of the GRN's series | — | `WH-SC-301`, `RJ-014` |
| RQP-GRD-17 | Rollup, **stated to the value**: all pass → `PASS`, all fail → `FAIL`, anything mixed or partial → `PARTIAL`. Three tests, one per outcome | — | — | — | `FR-133`, `p1-14` Acceptance, `X-055` (fixed) |
| RQP-GRD-18 | Disposition: the actor holds `wh_quality_inspections:disposition` (QA role); the code is a `whb_dispositions` row fetched from the registry — release `RESTOCK_SELLABLE`, reject `REJECT`, return to supplier `RTV`, scrap `SCRAP` — written to `wh_quality_inspection_lines.disposition_code` with `target_status_code`; a status disposition is a balanced two-line movement at the same location with a mandatory reason (`MPR-T1-10`, `MPR-GRD-18`, `MPR-GRD-11`); it never mutates a position | `403` naming the permission (`WH-SC-071`: `stores1` refused); the ledger refusals as cited | as cited | `MPR-GRD-18`'s backstop | `FR-134`, `FR-103`, `FR-408`, `p0-05` seed rule |
| RQP-GRD-19 | Scrap approval: the approver holds `wh_quality_inspections:approve` **and is not the disposer** (`MPR-GRD-19` approver ≠ actor). A holder of only `whb_stock_movements:approve` cannot approve a scrap sourced from an inspection (the matching guard row in `movement-post-reverse.contract.md`, `MPR-GRD-27`). Both APPROVE and REJECT of a `QCINSP` movement need the permission and refuse the disposer; APPROVE of a `SCRAP` also needs the GRN `POSTED` (`INSPECTION_GRN_NOT_POSTED`) | `403 INSPECTION_APPROVE_PERMISSION_REQUIRED` (settled, §7 2026-09-16 P1-14 C8 fix 1); `403` naming the maker-checker rule | *"the user who dispositioned this scrap may not approve it"* (the `WH-SC-039` shape) | — | `RJ-005`, `FR-164`, `FR-408`, `WH-SC-039` |
| RQP-GRD-20 | `sample_size_formula` and putaway `strategy` accept **whitelisted values only** — no expression, script or rule DSL | `422 VALUE_NOT_WHITELISTED` · the field (settled by P1-14 and P1-15, §7 2026-09-16) | lists the allowed values — for putaway `strategy`, the five | — | `FR-041`, `p1-14`, `p1-15` Traps |
| RQP-GRD-21 | Putaway Complete with actual ≠ suggested: `override_reason_code_id` present. **The reason is captured, never silently discarded.** An override exists only when a suggestion existed: with `suggested_location_id` null no reason is required and `hasOverride` is false (`chk_wh_putaway_tasks_override_reason`; §7 2026-09-16 P1-15 C8 fix 2) | `422 OVERRIDE_REASON_REQUIRED` · `overrideReasonCodeId` (settled, §7 2026-09-16 P1-15 C8 fix 2) | *"putting away to A-04-02-03 instead of the suggested A-04-02-01 needs a reason"* | — | `FR-135`, `WH-SC-052` |
| RQP-GRD-22 | The actual location admits the stock: temperature class matches the zone; `commingle_policy` and `dedicated_owner_id` hold against existing stock. **Refused by the port, and the override path cannot walk past it** — the override is for *"a better location"*, never *"the guard is inconvenient"*. The port (`WhbMovementPostValidationService`) raises it for every incoming line of a new movement at a non-virtual location; the temperature class is not asked of handling location types (`RECEIVING`, `SHIPPING`, `VIRTUAL`, `DOCK`, `STAGING`); reversals and stored-movement approvals are not re-checked (settled by default, §7 2026-09-16 P1-15 C8 fix 2) | `422 TEMPERATURE_ZONE_MISMATCH` · `lines[1].location_id`; `409 LOCATION_POLICY_VIOLATED` · `lines[1].location_id` | names the item's class and the zone; names **the policy and the conflicting stock**. Distinguishable from a rule-engine suggestion | the port (a port refusal in production is an incident) | `FR-068`, `FR-087`, `WH-SC-075`, `WH-SC-076`, `Q-006` |
| RQP-GRD-23 | The putaway move is two lines, conserving: same status, owner, lot, quantity; from the staging location to the actual location (`L-1`, `MPR-GRD-01`) | as `MPR-GRD-01` | as cited | `I-1` | `WH-SC-052`, `p1-15` Traps |
| RQP-GRD-24 | Reversal request: the GRN is `POSTED` and has no reversal in `REQUESTED`/`APPROVED`/`POSTED`; a catalogue reason code is given — active and from the `REVERSAL` context (checked at Request and Reject); `requires_note` makes the justification mandatory | `409` · `grn_id`, `GRN_NOT_POSTED` / `RECEIPT_REVERSAL_EXISTS`; `422` · `reason_code_id`, `REVERSAL_REASON_INVALID` / `REVERSAL_REASON_NOTE_REQUIRED` (settled, §7 2026-09-16 P1-16 C8 fix 3) | names the GRN's status, or the open reversal | `uk_wh_receipt_reversals_open_per_grn` | `p1-16` ladder |
| RQP-GRD-25 | Reversal approve: approver ≠ requester | `403 REVERSAL_SELF_APPROVAL` naming the maker-checker rule (settled, §7 2026-09-16 P1-16 C8 fix 3) | *"the requester may not approve their own reversal"* | — | `FR-408`, `p1-16` ladder |
| RQP-GRD-26 | Reversal post: **the received stock has not moved on**, re-checked at post, per grain the receipt posted into its receiving location (owner, item, location, lot and serial exact, LPN where the receipt carried one — a no-LPN grain never reads an LPN this receipt posted on the same grain — stock status). A completed putaway whose task belongs to one of this receipt's GRN lines (`wh_putaway_tasks.grn_line_id` → `whb_tasks.completion_movement_id`) blocks this receipt, charged in full up to the receipt's quantity, whatever the on-hand. A putaway of another receipt's task is never charged to this receipt. Every other live movement (a non-putaway, or a putaway with no task) is attributed by on-hand: ordered by `sequence_no`, stock that was there before or arrived after is consumed first, and the receipt is refused when the on-hand falls below its quantity once provenance-attributed putaways are accounted for. Where another receipt's putaway takes stock an earlier unlinked outbound had been read as taking, that outbound is charged to this receipt. A later inbound gives units back to the most recent on-hand taker first. Reversed movements and reversal mirrors are excluded. If the grain was negative before the receipt, the part of the receipt that covered it is named as the pre-existing negative balance, never as the receipt's own movement. Only movements that are posted and either need no approval or are `APPROVED` are read; `PENDING` movements have no ledger effect and are excluded (a pending scrap from this receipt's inspection is caught by `RQP-GRD-30` instead). Order is by the per-site `sequence_no` issued when the movement is submitted, never by `occurred_at`, so a back-dated movement sits where it was posted. The ledger read takes no lock; the single writer's reversal refusals (`MPR-GRD-12`, `-13`) and the one-transaction post are the backstop against a movement committed between read and write. **User decision 2026-09-16: a putaway blocks its own receipt** (wording replaced 2026-09-16, P1-16 C8 fix 5c; reconciles `RQP-OPEN-09`). Also: the period admits it (`MPR-GRD-09`, `-10`; which date is `MPR-OPEN-17`); the target is not a reversal and not already reversed (`MPR-GRD-12`, `-13`) | `409 RECEIPT_MOVED_ON` · banner (settled, §7 2026-09-16 P1-16 C8 fix 3); the others as cited | **names the movements that took this receipt's units out of the receiving location** and does not follow stock downstream: *"144 EA of goods receipt GRN-2026-000841 have moved on: 144 EA of OF-1120 left RECV-01 on PUTAWAY movement 10418 (GRN-2026-000841). Reverse receipt is unavailable once the stock has moved on — it would have to un-ship goods that are physically gone. Correct the difference with an adjustment instead."* — never *"stock has moved"*. The refusal leads with the total, then names at most 5 blocking movements in `sequence_no` order (a negative-balance row first) — each movement named once per grain, its taker rows merged by movement and their quantities summed, so a movement reached both on the on-hand and by re-attribution is one clause and one of the 5 slots (§7 2026-09-16 P1-14/P1-15/P1-16 C8 fix 6); beyond 5 it appends *"and further movements beyond the first 5"*. A single unit leads the total only when every named row shares it (§7 2026-09-16 P1-16 C8 fix 5c) | as cited | `FR-131`, `WH-SC-081`, `WH-SC-082`, `p1-16` Traps |
| RQP-GRD-27 | Reversal post: the `REVERSAL` movement, the GRN → `REVERSED`, the PO line decrement, the header recompute and the cancellation of the GRN lines' cancellable (`CREATED`, `ASSIGNED`, `EXCEPTION`) putaway tasks commit **in one transaction**; the reversal movement carries **its own idempotency key** (`MPR-GRD-03`, `-04`); the corrected receipt is later posted under a new key (`WH-SC-081`). The task ids are read unlocked: a task found **no longer cancellable once locked** (cancelled, completed or started concurrently) is **skipped**, never a task-ladder refusal that rolls the approved reversal back; the open tasks are then re-read and any replacement a concurrent cancel raised for the same GRN lines is cancelled the same way, until none is left (§7 2026-09-16 P1-14/P1-15/P1-16 C8 fix 6) | as cited | — | as cited | `p1-16` ladder, `FR-035`, `WH-SC-081` |
| RQP-GRD-28 | A GRN line carrying `po_line_id` needs the PO header in `APPROVED` · `PARTIALLY_RECEIVED` · `RECEIVED` and the line not `CLOSED`/`CANCELLED` — checked at create, at Attach PO and again at Post (H5), and once more by `applyReceipt` for a receipt, under the PO header lock on a refreshed header and line (a reversal is guarded by `RQP-GRD-26` / `-27` instead) | `409` · `lines[n].po_line_id`; code `PO_NOT_RECEIVABLE`, settled by P1-12's `applyReceipt` re-check (§7 2026-09-15). P1-13's create, Attach PO and Post checks use the same literal and carry the field | names the PO number and its status | — | BUILD-SPEC §0.11 PO rows (receipt effects start at `APPROVED`; `CLOSED`/`CANCELLED` terminal) |
| RQP-GRD-29 | Disposition needs the inspection *completed* (`completed_at` set), the GRN `POSTED`, and a quantity ≤ the GRN line's QC-held quantity not yet dispositioned; stock already released is not QC-held and cannot be re-dispositioned (H7). **A line is dispositioned repeatedly until its QC-held quantity is used up** (user decision 2026-09-16): "QC-held" = a GRN line received into `QC_HOLD`, holding (received + free − rejected) in its own unit; the cap is that minus the dispositions of the whole inspection series, tracked in `wh_quality_inspection_lines.dispositioned_quantity` (GRN-line unit, `V510022`) and converted to base with the frozen `conversion_factor_used`; a line's `disposition_code` / `target_status_code` / `rejection_reason_code_id` mean **the latest decision still standing**: after a scrap is rejected or withdrawn they are re-derived from the line's newest QCINSP movement that is not rejected, not withdrawn, not a reversal and **not a reversed original** (`is_reversed`, or a reversal row that is not rejected or withdrawn names it; reversed originals added §7 2026-09-16 P1-14/P1-15/P1-16 C8 fix 6) (null if none), and the header code is recomputed from the lines; a disposition is tied to its line through the idempotency key `QCINSP:<lineId>:<code>:…`, since status-change movements carry no line number (wording replaced §7 2026-09-16 P1-14 C8 fix 5a). Disposition takes the **GRN row lock** (then the inspection lock) before reading the series cap, so two dispositions of one receipt line from different inspections serialise. Lock order (rewritten in place, §7 2026-09-16 P1-14/P1-15/P1-16 C8 fix 6): approve / reject / withdraw of a movement — from WS-040 or WS-080 alike — take the writer's order, **positions → movement → inspection** (the inspection only via the `MPR-GRD-27` guard, on reject and withdraw; on approve the guard only reads); WS-080 pre-locks neither the movement nor the inspection, and a scrap decided by someone else meanwhile is `409 MOVEMENT_NOT_PENDING`. Disposition takes **goods receipt → inspection** (then the writer's positions for its own new movement, an INSERT). **A serialised line is dispositioned in full**, one status change (or scrap grain) per serial. `LINE_ALREADY_DISPOSITIONED` is **withdrawn** (§7 2026-09-16 P1-14 C8 fix 1) | `409 INSPECTION_NOT_COMPLETED` · banner; `409 INSPECTION_GRN_NOT_POSTED`; `409 DISPOSITION_STOCK_NOT_QC_HELD` · `lines[n].lineId`; `422 DISPOSITION_QUANTITY_EXCEEDED` · `lines[n].quantity`; `422 SERIALISED_LINE_PARTIAL_DISPOSITION` · `lines[n].quantity` (settled, §7 2026-09-16) | names the inspection number, or the dispositionable quantity | — | `WH-SC-301` (no verdict from a partial set), `RQP-GRD-15` |
| RQP-GRD-30 | Reversal post: no quality inspection of the GRN is uncompleted, and no `PENDING` `SCRAP` movement sourced from its inspections exists | `409 RECEIPT_INSPECTION_OPEN` / `409 RECEIPT_SCRAP_PENDING` · banner | names the inspection number(s). Exit: complete the inspection, or approve or reject the scrap, in WS-080, or Reject the reversal (H4) | — | §7 2026-09-16 P1-16 C8 fix 3; `RQP-T4-12` |

### 1.4 Transitions that must be unreachable

| ID | Forbidden | What makes it unreachable | Source |
|---|---|---|---|
| RQP-UNR-01 | Editing a posted GRN, from any screen or endpoint | no edit modal for a posted GRN anywhere in the product, **proved by grep**; correction is WS-078 only; ledger immutability is `MPR-UNR-01` | `p1-13`, `p1-16` Acceptance, `L-2`, `L-3` |
| RQP-UNR-02 | Setting a GRN to `REVERSED`, or a PO header/line to `PARTIALLY_RECEIVED` / `RECEIVED`, directly | these are effects only; no screen offers them | BUILD-SPEC §0.11, `p1-12`, `p1-13` |
| RQP-UNR-03 | A `CLOSED` or `CANCELLED` PO, a `COMPLETED`/`CANCELLED` session, a `CANCELLED`/`REVERSED` GRN, or a `REJECTED`/`POSTED` reversal moving anywhere | terminal in every ladder; **no Reopen exists in v1** (RQP-OPEN-07) | §0.11, `p1-12`, `p1-13`, `p1-16` |
| RQP-UNR-04 | Re-opening a completed inspection, or overwriting its verdict | `RQP-GRD-16`; the correction is a new inspection | `WH-SC-301` |
| RQP-UNR-05 | A receipt, disposition or putaway writing `whb_stock_positions` | architecture test; every write goes through `MPR-T1-01` / `-04` / `-10` (`MPR-UNR-08`). The `accessories` failure mode — a document that says `POSTED` and writes no stock — is tested for (`C-023`) | `FR-436`, `p1-13` Traps |
| RQP-UNR-06 | A reversal as an `UPDATE` on the GRN plus a compensating position write | the reversal is a `REVERSAL` movement through `MPR-T1-08`; no `cascade = ALL` from `wh_receipt_reversals` to its lines (`T-1`) | `p1-16`, `L-3` |
| RQP-UNR-07 | A putaway task created at GRN post for QC-held quantity or any status with `is_allocatable = false`, or ever for rejected, RTV or scrapped quantity | `RJ-017` creation rule (`RQP-T1-24`) | `RJ-017` |
| RQP-UNR-08 | An override that places stock in a policy-forbidden location | `RQP-GRD-22` | `Q-006` |
| RQP-UNR-09 | A scrap taking stock effect before a **different** user approves it | the `SCRAP` movement is `PENDING` until `RQP-T1-23`; `RQP-GRD-19` | `RJ-005`, `WH-SC-039` |
| RQP-UNR-10 | `UPDATE` or `DELETE` of a putaway rule a task references | `I-24` trigger (`V510017`) | `RL-010` |
| RQP-UNR-11 | Reversal Post by a user holding only `:approve`, or Approve by one holding only `:post`; a `:view → :approve` dependency row | separate verbs; the reverse dependency row must not exist | `RJ-005`, `p1-16` Acceptance |
| RQP-UNR-12 | Reversing a reversal, or reversing into a `CLOSED` period | `MPR-UNR-04`, `MPR-UNR-06` | `FR-035`, `L-8` |
| RQP-UNR-13 | A JSONB column on any table of this flow (criteria, sampling, serials) | criteria and results are rows; serials are `wh_goods_receipt_line_serials` | `FR-383`, `P-007` |
| RQP-UNR-14 | A partial receipt reversal, or a second reversal of the same GRN | `RQP-T1-29` whole-GRN rule; `RQP-GRD-24`; `MPR-UNR-05` | `L-3`, `MPR-GRD-14` |
| RQP-UNR-15 | Approving a scrap sourced from an inspection without `wh_quality_inspections:approve`, or by its disposer | `RQP-GRD-19` and the movement contract's matching guard row | `RJ-005`, `FR-408` |

---

## 2. T2 — Action × role × state

One row per action; `Allowed from` lists the §1.1 states it is offered in (every other state: not
offered). Every management query and action is further limited to the user's allowed warehouses
(`p1-18`, `D-14`, resolved through `BranchScopeService`), and an auditor is read-only everywhere.

| ID | Action | Permission | Allowed from | Extra condition |
|---|---|---|---|---|
| RQP-T2-01 | Gate-in a session | `wh_receiving_sessions:create` | — | WS-075 toolbar **Add** |
| RQP-T2-02 | Start | `wh_receiving_sessions:edit` | Session `ARRIVED` | dock door set |
| RQP-T2-03 | Attach PO · Record seals | `wh_receiving_sessions:edit` | Session `ARRIVED` · `UNLOADING` | ASN attach is v1.1 and not offered; `RQP-GRD-28` on attach |
| RQP-T2-04 | Complete session | `wh_receiving_sessions:complete` | `UNLOADING` | `RQP-GRD-10`; refused to a user with only `:edit` |
| RQP-T2-05 | Cancel session | `wh_receiving_sessions:cancel` | `ARRIVED` · `UNLOADING` | `RQP-GRD-11`; refused to a user with only `:edit` |
| RQP-T2-06 | Create GRN · Blind receipt | `wh_goods_receipts:create` | — | counterparty derived from the owner; picker only for a house owner (`RQP-GRD-01`) |
| RQP-T2-07 | Receive line · Over/short | `wh_goods_receipts:edit` | GRN `DRAFT` | — |
| RQP-T2-08 | Post GRN | `wh_goods_receipts:post` | GRN `DRAFT` | `RQP-GRD-09`, `RQP-GRD-28` |
| RQP-T2-09 | Cancel GRN | `wh_goods_receipts:cancel` | GRN `DRAFT` | reason; refused to a user with only `:edit` |
| RQP-T2-10 | Edit a GRN | **none exists after post** | — | `RQP-UNR-01` |
| RQP-T2-11 | Close / Close short a PO or PO line | `wh_purchase_orders:edit` | header `APPROVED` · `PARTIALLY_RECEIVED` · `RECEIVED`; for a line, the line not `CLOSED`/`CANCELLED` | WS-072 only — the Close modal offers the whole order or one line; reason where short (`RQP-GRD-14`) |
| RQP-T2-12 | Cancel a PO | `wh_purchase_orders:cancel` | header `DRAFT` · `SUBMITTED` · `APPROVED` | `RQP-GRD-13`; reason |
| RQP-T2-13 | Start inspection · Record results · Complete | `wh_quality_inspections:create` (Start) · `wh_quality_inspections:edit` (Record results, Complete) | GRN `POSTED`; inspection *in progress* | — |
| RQP-T2-14 | Disposition (release / reject / RTV / scrap) | `wh_quality_inspections:disposition` | QC-held stock of a *completed* inspection (`RQP-GRD-29`) | the reverse dependency row (`:view → :disposition`) must not exist |
| RQP-T2-15 | Approve scrap · Reject scrap | `wh_quality_inspections:approve` | *pending approval* (the linked `SCRAP` movement is `PENDING`) | approver ≠ disposer |
| RQP-T2-16 | Assign putaway task | `wh_putaway_tasks:edit` | `CREATED` | explicit, no claiming |
| RQP-T2-17 | Complete putaway task | `wh_putaway_tasks:complete` | `CREATED` · `ASSIGNED` | override reason when overriding |
| RQP-T2-18 | Cancel putaway task | `wh_putaway_tasks:edit` | `CREATED` · `ASSIGNED` | reason-coded |
| RQP-T2-19 | Request reversal | `wh_receipt_reversals:create` | GRN `POSTED` | catalogue reason; whole GRN |
| RQP-T2-20 | Approve / Reject reversal | `wh_receipt_reversals:approve` | Approve: `REQUESTED` · Reject: `REQUESTED` · `APPROVED` | approver ≠ requester; reject needs a reason |
| RQP-T2-21 | Post reversal | `wh_receipt_reversals:post` | Reversal `APPROVED` | `RQP-GRD-26`, `-27`, `-30` |
| RQP-T2-22 | Reverse row action on WS-076 | `wh_receipt_reversals:create` | GRN `POSTED` | routes to WS-078 |
| RQP-T2-23 | Putaway rule Add / Edit / Reorder | `wh_putaway_rules:create` / `:edit` | — | copy-on-write once referenced (`RQP-T1-33`) |
| RQP-T2-23a | Putaway rule Delete | `wh_putaway_rules:delete` | — | refused `409 PUTAWAY_RULE_REFERENCED` when a task or successor references the rule (§7 2026-09-16 P1-15 C8 fix 2) |
| RQP-T2-23b | Putaway rule Activate / Deactivate | `wh_putaway_rules:edit` | — | deactivate always; activate refused `409 PUTAWAY_RULE_SUPERSEDED` on a superseded version, copy-on-write when task-referenced; row flag `canActivate` (§7 2026-09-16 P1-15 C8 fix 2) |
| RQP-T2-24 | Putaway rule Test | `wh_putaway_rules:view` | — | writes nothing |
| RQP-T2-25 | Inspection plan Add / Edit | `wh_inspection_plans:create` / `:edit` | — | criteria as rows; whitelisted formula |
| RQP-T2-26 | View / export any screen of this flow | `<resource>:view` / `:export` (or `ADMIN` for export) | every state | warehouse scope (`p1-18`) |

---

## 3. T3 — Time-driven rules & notifications

| ID | Rule | Trigger (clock / date field) | What it changes | Who is notified | Channel | Job class | Notify-once? |
|---|---|---|---|---|---|---|---|
| RQP-T3-01 | PO **overdue** | `expected_delivery_date < today` in the **site's** timezone (`FR-439`), for a PO still `DRAFT` · `SUBMITTED` · `APPROVED` · `PARTIALLY_RECEIVED` — **`RECEIVED`, `CLOSED` and `CANCELLED` are never overdue** | nothing — a backend-computed filter (`overdueOnly`) and a WS-072 tile | none | — | none — computed on read | — |
| RQP-T3-02 | Putaway task ageing: an `ASSIGNED` task (no `STARTED` in v1) past its type's `stale_after_minutes` → `EXCEPTION`, unassigned | job clock vs `assigned_at` (`Y-004`) | `whb_tasks.status`, `exception_code` | supervisor; also the `warehouse.tasks.blocked_alert_minutes` alert (default 15) | per `task.contract.md` `TSK-X-05` (classic `warehouse-base/docs/contracts/`) | `p0-10`'s task-ageing job — `task.contract.md` `TSK-T3-01` | yes — once per run (`TSK-X-05`) |
| RQP-T3-03 | Pending-approval escalation after `warehouse.approval.pending_alert_hours` (default 24): covers a `REQUESTED` receipt reversal and a *pending* scrap (H15) | elapsed hours since request / since the `SCRAP` movement went `PENDING` | nothing — never auto-approves | `TBD` — DEFERRED-TO-P2-23 | `TBD` — DEFERRED-TO-P2-23 | `p2-23`'s pending-approval alert job | one alert per pending episode |
| RQP-T3-04 | Minimum remaining shelf life at receipt (`min_shelf_life_receipt_pct`) | receipt date vs lot expiry | refusal `SHELF_LIFE_RULE_VIOLATED` (`FR-039`) | — | — | columns ship v1 (`p1-03`); **enforcement is `p2-05`'s** | — |
| RQP-T3-05 | Lifecycle timestamps stamped by the transitions that cause them — session `arrived_at`, `docked_at`, `unload_started_at`, `unload_completed_at`, `departed_at`; GRN `received_at`; PO `submitted_at`, `closed_at` (a header Close, or the line Close that leaves no line open, `RQP-T1-14`); **PO `first_receipt_at`** by the first GRN Post touching the PO when null, never cleared; **PO `received_at`** by the first header recompute that enters `RECEIVED` (`RQP-T1-12`, including the recompute after a line Close), never cleared. The WS-072 *fully received this month* tile counts the POs whose `received_at` falls in the site's month, with no `status = 'RECEIVED'` filter: a PO received and then closed, or regressed by a reversal, still counts (`V510021`, user decisions 2026-09-15); **GRN `dock_to_stock_completed_at`** by the transaction that leaves the GRN with no open putaway task and no undispositioned QC-held quantity (BUILD-SPEC §8 dock-to-stock = arrival → putaway completion) (H12); task `assigned_at`, `started_at`, `completed_at`. **None can be backfilled** | the injected UTC clock at the transition | the stamped column | — | — | the transition's service | — |
| RQP-T3-06 | Business time of the receipt and putaway movements | `occurred_at` / `posting_date` | as `MPR-GRD-05`, `-07`, `-09`, `MPR-T3-05` | — | — | the writer | — |

A session left `ARRIVED`/`UNLOADING`, a `DRAFT` GRN and QC-held stock nobody inspects carry **no ageing
rule in v1** — recorded in §6 (the detention clock is WS-086, v1.1).

---

## 4. T4 — Cross-entity effects

| ID | When this happens | Then this other record must change | Enforced in |
|---|---|---|---|
| RQP-T4-01 | GRN post (`RQP-T1-09`) | **one balanced receipt movement** through `MPR-T1-01`: −quantity at the site virtual location of the GRN's counterparty role (`SUPPLIER` → `VIRT-SUPPLIER-<SITE>`, `CUSTOMER` → `VIRT-CUSTOMER-<SITE>`, `p1-05`), +quantity at the receiving location, in the resolved received status, with lot, serial, LPN, owner and the frozen `conversion_factor_used`; `receipt_movement_id` stored bare on each GRN line; rejected units land in `REJECTED` (user decision 2026-09-16, §7 P1-14 C8 fix 1). Which role applies when a counterparty holds both: DEFERRED-TO-P1-13 | GRN service → writer (`WH-SC-051`, `L-1`, `L-7`) |
| RQP-T4-02 | GRN post | PO line `received_quantity` (and accepted / rejected) move; `line_status` and header `status` follow `RQP-T1-12`; `po_id` on the header is **derived** — set only when every line's `po_line_id` belongs to one PO; a GRN may span POs. *GRNs of PO X* reads through lines (`RG-019`) | GRN service, same transaction |
| RQP-T4-03 | GRN post | lots, serials and LPNs are resolved or created (normalised codes); captured serials land in `wh_goods_receipt_line_serials`; `LOT`-kind attributes land in `whb_entity_attribute_values`; the counterparty is named, so the lot has a backward traceability end (`L-12`) | GRN service (`p1-07`, `RL-007`, `WH-SC-142`) |
| RQP-T4-04 | GRN created (`WH-SC-051`) / reversal requested / inspection started | a **gapless** number from the pessimistically locked counter row, logged in `whb_number_series_issued` — for the GRN **at create**, so a cancelled `DRAFT` keeps its issued number | `p1-09` (`WH-SC-194`) |
| RQP-T4-05 | GRN post, line received into a status with `is_allocatable = true`; **or** release (`RQP-T1-20`) of QC-held stock; **or** putaway Cancel not caused by a receipt reversal (`RQP-T1-27`) | a putaway task for that quantity (`RQP-T1-24`) — on Cancel, a fresh unassigned `CREATED` task for the same stock, its serials carried on (§7 2026-09-16 P1-15 C8 fix 2); **no** replacement — the cancel itself still succeeds — when the task's goods receipt is not `POSTED`, or when the staged on-hand at the task's grain (owner, item, staging location, status, lot and LPN matched exactly; when serialised, only the task's serials) is zero; a partial shortfall on a `POSTED` receipt is replaced for `min(staged on-hand, task quantity)`, a serialised task's replacement carrying only its serials still standing at staging (quantity = their count) (§7 2026-09-16 P1-14/P1-15/P1-16 C8 fix 6, replacing fix 5b N1) | `RJ-017` |
| RQP-T4-06 | GRN post | a cost layer is inserted — a backdated receipt **inserts a layer and restates nothing** (`D-15`); the landed value spreads across billed **plus** free quantity (₹41,250 over 110 → ₹375.00) | `p0-17` / `p2-16` — cited, not restated (`WH-SC-085`) |
| RQP-T4-07 | GRN post | outbox `receipt.line.confirmed` per line, carrying owner, lot, LPN, warehouse and the three timestamps, plus `MPR-T4-07`'s `stock.movement.posted`; the accounting handover per `MPR-T4-08` | outbox (`WH-SC-294`) |
| RQP-T4-08 | Disposition posts | release / reject / RTV: a `STATUS_CHANGE` per `MPR-T1-10` to `AVAILABLE` / `REJECTED` / `BLOCKED`; scrap: a `SCRAP` movement `PENDING` per `MPR-T1-04`, taking effect only at `RQP-T1-23`; `wh_quality_inspection_lines.disposition_code` / `target_status_code` recorded (the latest disposition) and `dispositioned_quantity` incremented (`V510022`); status-change movements carry `qcResultCode`; a serialised line moves one status change (or scrap grain) per serial (§7 2026-09-16 P1-14 C8 fix 1); release adds `RQP-T4-05`, its serials travelling into `wh_putaway_task_serials`. RTV feeds a supplier return document, which is `p2-13`'s | inspection service |
| RQP-T4-09 | Inspection Complete | nothing moves — `result` and the per-line results only | inspection service |
| RQP-T4-10 | Session Complete / Cancel; GRN Cancel | nothing moves | session / GRN service |
| RQP-T4-11 | Putaway Complete | a two-line conserving movement staging → actual through `MPR-T1-01`; `whb_tasks.completion_movement_id`, `assigned_at` (when null), `started_at`, `completed_at`; `actual_location_id`, `override_reason_code_id`; outbox `putaway.task.completed` and `task.completed`; the referenced `rule_id` is frozen by `I-24`. `wh_goods_receipt_lines.putaway_location_id` records the completed putaway's actual location (H8 RESOLVED, §7 2026-09-16 P1-15 C8 fix 2); a serialised task moves exactly its `wh_putaway_task_serials`, refused `409 PUTAWAY_SERIALS_UNRESOLVED` · `quantity` otherwise | putaway service (`WH-SC-052`) |
| RQP-T4-12 | Reversal Post | a `REVERSAL` movement through `MPR-T1-08` (own idempotency key, reason); `reversal_movement_id` stored; GRN → `REVERSED`; PO line decrement and `line_status` regress (`RQP-T1-13`); header recompute (`RQP-T1-35`); the GRN lines' cancellable putaway tasks (`CREATED`, `ASSIGNED`, `EXCEPTION` — `WhbTask.canCancel`) → `CANCELLED` through `cancelForReversedSource`; `STARTED`/`PAUSED` are not cancellable on the task ladder and are not reachable in v1 (settled 2026-09-16: no reversal guard needed until they are; §7 2026-09-16 P1-16 C8 fix 5c), taking the reversal's reason code and raising **no** replacement task; free quantity goes back out through the ledger mirror but is not taken off the PO counter (mirroring the GRN post, `RQP-GRD-06`, `FR-141`). Refused while `RQP-GRD-30` holds an open inspection or a `PENDING` scrap (§7 2026-09-16 P1-16 C8 fix 3); cost per `MPR-T4-05`; outbox `stock.movement.reversed` (`MPR-T4-07`); handover per `MPR-T4-08`. Both documents stay visible | reversal service, one transaction |
| RQP-T4-13 | PO Cancel | every line → `CANCELLED`; every `DRAFT` GRN holding a line of the PO → `CANCELLED`; attached sessions are untouched | PO service |
| RQP-T4-14 | Close short — header or line | the line leaves the open-PO report; the remainder is `cancelled_quantity`; **no automatic backorder**. A header Close closes every line not already terminal; a line Close closes that line and recomputes the header by `RQP-T1-12`, or closes the header when it leaves no line open (P1-12, §7 2026-09-15) | PO service (`WH-SC-068`) |
| RQP-T4-15 | Over-receipt refused (`RQP-GRD-06`) | nothing posts. Auto-creating the `OVER_RECEIPT` reconciliation case is `WH-SC-084` (v1·P2), and **the case never moves stock** | reconciliation (`p2-13`, `FR-138`) |
| RQP-T4-16 | Scrap rejected (`RQP-T1-36`) | the `PENDING` `SCRAP` movement is rejected per `MPR-T1-06` and kept (`L-2`); the stock stays QC-held and dispositionable — the quantity is given back to the inspection line's `dispositioned_quantity` in the same transaction (`WhQualityInspectionScrapApprovalGuard`, §7 2026-09-16 P1-14 C8 fix 1); no putaway task. **Withdraw** (maker, WS-040 `POST /movements/{id}/withdraw`) of a PENDING QC scrap gives its quantity back to the inspection line exactly as Reject does, in the same transaction (`MPR-GRD-27` `Decision.WITHDRAW`; §7 2026-09-16 P1-14 C8 fix 5a) | inspection service → writer |
| RQP-T4-17 | A disposition movement is reversed (`MPR-T1-08`) before its putaway task completes | **SETTLED 2026-09-16 (P1-14): a posted disposition is NOT reversible from WS-080.** The screen offers no un-disposition action in any state. The disposition's effect *is* a ledger movement, and `L-3` makes the correction of a posted movement a `REVERSAL` through `MPR-T1-08` from the movements screen — not a second correction path on the document that caused it, which would let the inspection record and the ledger disagree. The scrap arm keeps its own decline exit while `PENDING` (`RQP-T1-36`), because a `PENDING` movement has no ledger effect to reverse. Consequence, stated: once released, the quantity is no longer QC-held and cannot be re-dispositioned (`RQP-GRD-29`), and its putaway task stands — a reversal of a release movement is a ledger correction whose putaway consequence is `P1-15`'s to state, not this task's | inspection service |

---

## 5. T5 — Screen contract (web only — see §0)

Default sorts, statistics tiles and export columns below are the ratified defaults (H16): sorts follow
the tables' own indexes (`DATA-MODEL.md` §2.2.1) and the `warehouse-base` document precedent
(`WhbNumberSeriesIssuedQueryService` — newest first); tiles are status counts, filter-aware and so with no
cache name (`FR-395`); every export is a superset of the grid columns including `createdByName` /
`updatedByName`. The owning task may refine them in its PR with a §7 row.

### 5.1 `/warehouse/inbound/receiving-sessions` — WS-075 Receiving Sessions

| | |
|---|---|
| **Type** | list + transition modals (reference: **Service Vehicle**) |
| **Grid identifier** | `wh_receiving_sessions` · filter scope `WAREHOUSE_RECEIVING_SESSION` |
| **Default sort** | `e.arrived_at DESC` (index `idx(warehouse_id, arrived_at DESC)`) |
| **Statistics tiles** | `ARRIVED` · `UNLOADING` · `COMPLETED` · `CANCELLED` (filter-aware) |
| **Columns** | `sessionNumber`, `warehouseName`, `dockDoorCode`, `supplierName` (nullable), `carrierName`, `vehicleNumber`, `driverName`, `sealNumberIn`, `sealNumberOut`, `gatePassRef`, `arrivedAt`, `startedAt`, `completedAt`, `status`, `documentCount`, `grnCount`, audit |
| **Filters** | `warehouseId` → `dockDoorId` → `status`; `supplierCounterpartyId` typeahead; `vehicleNumber` text; `arrivedFrom`/`arrivedTo` (`date`); `openOnly` |
| **Row actions** | Start → `RQP-T2-02` · Attach PO → `RQP-T2-03` · Create GRN → `RQP-T2-06` · Record seals → `RQP-T2-03` · Complete → `RQP-T2-04` · Cancel → `RQP-T2-05` |
| **Toolbar** | **Add** (Gate-in modal) → `RQP-T2-01` · Export |
| **Export columns** | every grid column above, plus `createdByName`, `updatedByName` |
| **Mobile counterpart** | none (§0) |

### 5.2 `/warehouse/inbound/goods-receipts` — WS-076 Goods Receipts · `/[id]` — WS-077

| | |
|---|---|
| **Type** | list + transition modals (reference: **Service Vehicle**); WS-077 is a route with tabs |
| **Grid identifier** | `wh_goods_receipts` · filter scope `WAREHOUSE_GOODS_RECEIPT` |
| **Default sort** | `e.received_at DESC` (index `idx(warehouse_id, received_at DESC)`) |
| **Statistics tiles** | `DRAFT` · `POSTED` · `CANCELLED` · `REVERSED` (filter-aware) |
| **Columns** | `grnNumber`, `sessionNumber`, `poNumber`, `asnNumber`, `supplierName`, `warehouseName`, `ownerName`, `receivedByName`, `receivedAt`, `isBlindReceipt`, `receivingMode`, `grnTiming`, `status`, `matchStatus` (`MATCHED`/`QTY_OVER`/`QTY_UNDER`/`ITEM_MISMATCH`), `lineCount`, `receivedQuantity`, `acceptedQuantity`, `rejectedQuantity`, `freeQuantity`, `invoiceMatched`, audit |
| **Filters** | `warehouseId` → `supplierCounterpartyId` → `status` (**multiselect**); `matchStatus`; `isBlindReceipt`; `poNumber` (reads through `po_line_id`, `RG-019`); `grnNumber`; `receivedFrom`/`receivedTo` (`date`); `awaitingQc`; `awaitingPutaway` |
| **Row actions** | View → WS-077 · **Post** → `RQP-T2-08` (`DRAFT`) · **Cancel** → `RQP-T2-09` (`DRAFT`) · **Reverse** → WS-078 (`POSTED`, `wh_receipt_reversals:create`) · **Inspect** → WS-080 · **Putaway** → WS-082 · Print GRN. Inspect, Putaway and Reverse follow the server flags `canInspect`, `canPutaway`, `canReverse`; `canInspect` = receipt POSTED **and** no inspection of it in progress (`completed_at IS NULL`), loaded once per page (§7 2026-09-16 P1-14 C8 fix 5a); `canReverse` is false while a live reversal exists (§7 2026-09-16 P1-16 C8 fix 3) · · Raise reconciliation case → WS-083 |
| **Modals** | Blind receipt (counterparty derived from the owner; picker only for a house owner) · Receive line · Over/short · Cancel · Post. **No edit modal for a posted GRN** (`RQP-UNR-01`) |
| **Export columns** | header + line grain: `lineNo`, `poLineNo`, `itemCode`, `expectedQuantity`, `receivedQuantity`, `acceptedQuantity`, `rejectedQuantity`, `freeQuantity`, `schemeReference`, `uomCode`, `conversionFactorUsed`, `lotCode`, `expiryDate`, `unitCost`, `stockStatusCode`, `putawayLocationCode`, `crossDockReference`, plus `createdByName`, `updatedByName` |
| **WS-077 tabs** | Lines · Serials · QC · Putaway · Movements · Landed costs · Reversals · Documents · Audit. **Documents is not available in v1** — no GRN document table exists; a delivery note attaches to the receiving session (§6) |
| **Mobile counterpart** | none (§0) |

### 5.3 `/warehouse/inbound/purchase-orders` — WS-072 and `/[id]` — WS-073 (the receipt-facing part)

| | |
|---|---|
| **Grid identifier** | `wh_purchase_orders` · filter scope `WAREHOUSE_PURCHASE_ORDER` · cache `dropdown.whPurchaseOrder` |
| **Receipt-facing columns** | `status`, `orderedQuantity` / `receivedQuantity` / `cancelledQuantity` (**backend-computed** Σ of line counters), `ownershipTransferPoint`, audit quartet + names (a document, not a ledger row) |
| **Receipt-facing filters** | `status` **multiselect**; `overdueOnly` (backend-computed, site timezone); `openOnly`; both date pairs `dateOnly` |
| **Statistics tiles** | open POs · overdue (`RQP-T3-01`) · fully received this month (by `received_at`, `RQP-T3-05`) · value on order |
| **Receipt-facing actions** | Receive against → WS-075 pre-filled · **Cancel** → `RQP-T2-12` · **Close short** → `RQP-T2-11`, the whole order or one line. **No Reopen** (RQP-OPEN-07) |
| **Endpoints beyond the grid** | `GET /{id}/cancel-check` — the `RQP-GRD-13` pre-check the Cancel modal reads before it confirms (received lines named by GRN line, arrived sessions, draft GRNs the cascade will cancel); the Cancel re-runs the same guard · `GET /{id}/lifecycle` — WS-073's sections, each counted or `available = false` with the owning task's reason · `PATCH /{id}/lines/{lineId}/close` — the line Close of `RQP-T2-11` |
| **WS-073** | a route, not a modal: Lines · GRNs · QC results · Putaways · Invoices / three-way match · Returns · Exceptions · Movements (`FR-036` lineage) · Documents · Audit — child grids, **no `gridIdentifier` per tab**, each built by the task that owns its document (GRNs and Movements `p1-13`, QC results `p1-14`, Putaways `p1-15`, Returns and Exceptions `p2-13`); until then the tab shows its count or *not available* with that task's reason. **Documents is not available in v1** — no PO document table exists (§6). The three quantities are shown side by side and never reconciled away (`FR-123`, `WH-SC-065`, `WH-SC-069`) |
| **Mobile counterpart** | none (§0) |

### 5.4 `/warehouse/inbound/quality-inspections` — WS-080, and `/warehouse/inbound/inspection-plans` — WS-079

| | |
|---|---|
| **Grid identifiers** | `wh_quality_inspections` · `WAREHOUSE_QUALITY_INSPECTION` (Service Vehicle); `wh_inspection_plans` · `WAREHOUSE_INSPECTION_PLAN` (Customer) |
| **WS-080 columns** | `inspectionNumber`, `grnNumber`, `planName`, `inspectorName`, `startedAt`, `completedAt`, `result` (`PASS`/`FAIL`/`PARTIAL`), `dispositionCode`, `inspectedQuantity`, `passedQuantity`, `failedQuantity`, audit |
| **WS-080 filters** | `warehouseId` → `planId` → `result`; `inspectorId` typeahead; `dispositionCode`; `startedFrom`/`startedTo` (`date`). `result` and `dispositionCode` **fetch** their values from the registry, never a TypeScript union (`OD-5`, `FR-380`) |
| **WS-080 actions** | Start → `RQP-T2-13` · Record results · Complete · **Disposition** → `RQP-T2-14` · **Approve scrap** / **Reject scrap** → `RQP-T2-15` (shown while the linked `SCRAP` movement is `PENDING`) |
| **WS-080 default sort · tiles · export** | `e.started_at DESC` · *in progress* · `PASS` · `FAIL` · `PARTIAL` · *pending scrap approval* (filter-aware) · every grid column plus `createdByName`, `updatedByName` |
| **WS-079** | columns `code`, `name`, `inspectionType` (`FULL`/`SAMPLING`/`SKIP_LOT`), `samplingPlan`, `sampleSizeFormula` (whitelisted), `aql`, `criterionCount`, `isActive`, audit; filters `inspectionType`, `isActive`, `code`/`name`; Add/Edit with a child editor over `wh_inspection_plan_criteria`; default sort `e.code ASC`; tiles total · active · inactive (the Customer reference); export every grid column plus `createdByName`, `updatedByName` |
| **Mobile counterpart** | none (§0) |

### 5.5 `/warehouse/inbound/putaway-tasks` — WS-082, and `/warehouse/inbound/putaway-rules` — WS-081

| | |
|---|---|
| **Grid identifiers** | `wh_putaway_tasks` · `WAREHOUSE_PUTAWAY_TASK` (Service Vehicle); `wh_putaway_rules` · `WAREHOUSE_PUTAWAY_RULE` (Customer) |
| **WS-082 columns** | `taskNumber` (from `whb_tasks`), `grnNumber`, `itemCode`, `quantity`, `lotCode`, `lpnCode`, `suggestedLocationCode`, `actualLocationCode`, `overrideReasonName`, `stagingLocationCode`, `ruleName`, `status`, `assignedToName` (the shared user-display helper) |
| **WS-082 filters** | `warehouseId` → `status` (**multiselect**) → `assignedTo` typeahead; `grnNumber`; `itemId` typeahead; **`hasOverride`** — the report that finds a wrong rule. `assignedTo` and `itemId` are **server-side** typeaheads. Child-grid params `goodsReceiptId` (GRN detail and the WS-076 Putaway row action) and `purchaseOrderId` (PO detail Putaways tab), bound against the task row's scopes (§7 2026-09-16 P1-15 C8 fix 2). The `/assignees` feed accepts `includeId` so a restored Assigned To filter keeps its label (§7 2026-09-16 P1-15 C8 fix 5b) |
| **WS-082 actions** | Assign → `RQP-T2-16` (`CREATED`) · **Complete** → `RQP-T2-17` (`CREATED` · `ASSIGNED`; own modal: scan location, override reason) · Cancel → `RQP-T2-18` (`CREATED` · `ASSIGNED`) |
| **WS-082 default sort · tiles · export** | `t.priority DESC, t.created_at ASC` (index `idx(status, warehouse_id, priority DESC)`) · `CREATED` · `ASSIGNED` · `COMPLETED` · `CANCELLED` · `EXCEPTION` (filter-aware) · every grid column plus `createdByName`, `updatedByName` |
| **WS-081** | columns `code`, `name`, `warehouseName`, `sequence`, `scopeCategoryName`, `scopeItemCode`, `scopeStatusCode`, `strategy` (whitelisted, fetched), `isActive`, audit; filters `warehouseId` → `strategy`, `isActive`; actions Add / Edit / **Reorder** (copy-on-write, `RQP-T1-33`) / **Test** (`RQP-T1-34`); default sort `e.sequence ASC` (`uk(warehouse_id, sequence) WHERE is_active`); tiles total · active · inactive; export every grid column plus `createdByName`, `updatedByName` |
| **Grid config** | both grids ship `default_columns` **and** `default_filters`; both scopes registered (`p1-15` Acceptance). Hidden by default per the `V510017` seed — WS-081: `targetZoneLocationCode`, `versionNo`, `createdByName`, `updatedByName`, `createdAt`, `updatedAt`; WS-082: `lpnCode`, `warehouseName`, `priority`, `assignedAt`, `startedAt`, `completedAt`, `createdByName`, `updatedByName`, `createdAt`, `updatedAt` (§7 2026-09-16 P1-15 C8 fix 2) |
| **Strategy resolution** | the first matching rule whose strategy finds a location wins; a matching rule that finds none falls through (`WH-SC-074`). `ZONE_BY_VELOCITY` and `BULK_THEN_PICK_FACE` share one resolver in v1 (zone candidates in `putaway_priority`, `pick_sequence`, `code` order, occupied locations allowed subject to `RQP-GRD-22`). A putaway may target an active, `AVAILABLE` location of any type except `RECEIVING`, `SHIPPING`, `VIRTUAL`, `DOCK`, `STAGING`, never the task's staging location (§7 2026-09-16 P1-15 C8 fix 2). Suggestions are advisory but account for open work: every strategy's placement guard excludes `RECEIVING`, `SHIPPING`, `VIRTUAL`, `DOCK`, `STAGING` and the line's staging location. Open putaway tasks — "open work" is `WhbTask.OPEN_STATUSES` (`CREATED`/`ASSIGNED`/`STARTED`/`PAUSED`/`EXCEPTION`), the same set the creation port reads before raising a task for a GRN line (§7 2026-09-16 P1-14/P1-15/P1-16 C8 fix 6) — suggested to a location, and suggestions already made earlier in the same batch, count against `max_units`, `FIXED_LOCATION`'s `max_quantity` (same item), `NEAREST_EMPTY` emptiness and the commingle policy. Complete reserves nothing. Complete refuses those location types `422 PUTAWAY_LOCATION_INVALID`. Rule Reorder locks the site's active chain in ascending `sequence` before the moved rule, the same order as the chain's `FOR SHARE` read at task creation (§7 2026-09-16 P1-15 C8 fix 5b) |
| **Mobile counterpart** | none (§0) |

### 5.6 `/warehouse/inbound/receipt-reversals` — WS-078

| | |
|---|---|
| **Type** | list + transition modals (reference: **Service Vehicle**); statistics: four filter-aware status tiles `REQUESTED` · `APPROVED` · `REJECTED` · `POSTED` (`FR-395`; §7 2026-09-16 P1-16 C8 fix 3) |
| **Grid identifier** | `wh_receipt_reversals` · filter scope `WAREHOUSE_RECEIPT_REVERSAL` |
| **Columns** | `reversalNumber`, `grnNumber`, `reasonCodeName`, `requestedByName`, `approvedByName`, `approvedAt`, `status`, `reversalMovementSequenceNo`, audit quartet + names; `warehouseName` shipped hidden. The view modal and the WS-077 Reversals tab show the request, approve and reject justifications and the reject reason (§7 2026-09-16 P1-16 C8 fix 3) |
| **Filters** | `warehouseId`; `status`; `reasonCodeId` (fetched from `whb_reason_codes`, never a union); `requestedFrom`/`requestedTo` (`date`); the `grnId` param. Deep links `?grnId=&grnNumber=` and `?reversalId=&returnTo=`; extra endpoints `/reason-codes`, `/goods-receipts`, `/vocabularies`, `/warehouses` (§7 2026-09-16 P1-16 C8 fix 3) |
| **Actions** | **Request** → `RQP-T2-19` · **Approve** → `RQP-T2-20` (`REQUESTED`) · **Reject** → `RQP-T2-20` (`REQUESTED` · `APPROVED`) · **Post** → `RQP-T2-21` (`APPROVED`) |
| **Default sort · export** | `e.created_at DESC` · every grid column plus `createdByName`, `updatedByName` |
| **Mobile counterpart** | none (§0) |

Screens this workflow feeds but does not own: WS-040/WS-041 (the movements, `MPR` — a QC scrap's
`PENDING` movement is approved from WS-080, not WS-040), WS-042 (positions, and *Change status* for
QC-held stock with no inspection — H14), WS-059 (tasks, `p0-10`), WS-064 (job runs), WS-083
(reconciliation cases), WS-084 (supplier returns).

---

## 6. Out of scope (explicit decisions)

| Baseline item | Decision | Reason | Revisit |
|---|---|---|---|
| Mobile and RF screens — `screens/whPurchaseOrder`, `whReceivingSession`, `whGoodsReceipt`, `whQualityInspection`, `whPutawayTask`; WS-229 RF Receive; WS-230 RF Putaway | **not built** | user scope rule of 2026-09-11 (§0, `RB-001`); task bodies amended 2026-09-14 | only if the user reverses the rule |
| Movement post / reverse mechanics, period gating, availability, idempotency | [`movement-post-reverse.contract.md`](movement-post-reverse.contract.md) | cited by `MPR-*` id, never restated | — |
| PO authoring ladder (Create, Submit, Approve, Return for correction) | BUILD-SPEC §0.11 rows, cited | this contract covers the PO only where receipts drive or gate it | a PO contract |
| PO **Reopen** | **not in v1** | no ladder row and no verb: a modal with neither has no guard (BUILD-SPEC §0.11) | a PO contract |
| PO document attachments — the WS-073 **Documents** tab | **not in v1** | `DATA-MODEL.md` has no PO document table; the tab shows *not available* with that reason (P1-12, §7 2026-09-15) | a PO contract |
| GRN document attachments — the WS-077 **Documents** tab | **not in v1** | `DATA-MODEL.md` has no GRN document table — `V510014` creates `wh_goods_receipts`, `_lines` and `_line_serials` only (§0 Primary tables) — because a delivery note attaches to the **receiving session** that unloaded the truck, through `wh_receiving_session_documents` (`V510013`). The tab shows *not available* with that reason (P1-13, §7 2026-09-16). **No task is allocated**: adding it would need a new GRN ↔ platform `documents` link table on the `whb_item_documents` pattern, its migration and a task to own both | a task that claims both |
| ASN as a document (`wh_asns`, WS-074), ASN attach and the ASN clause of the cancel guard | v1.1 (`P3-05`) | `wh_asns` has no status values in v1 (`RJ-018`) | v1.1 |
| Task-driven putaway, Start / Pause, claiming, the putaway optimiser | `P3-02`, `P3`, `P6` | v1 ships the evaluation harness and operator-completed tasks only (`FR-135`) | P3 / P6 |
| Inspection plan per item × supplier × site (`wh_inspection_plan_assignments`, `V510220`) | v2 | v1 keeps the category default (`RG-018`) | v2 |
| Cross-dock flow, three-way match | v2 | v1 ships the nullable `cross_dock_reference` column only (`FR-137`) | v2 |
| Reconciliation case lifecycle, supplier returns | `p2-13` | T4 cross-reference only | P2 |
| Cost layers and landed-cost arithmetic | `p0-17` / `p2-16` under `D-15` | cited, never restated | — |
| Receipt shelf-life enforcement | `p2-05` | v1 ships the columns (`p1-03`) | P2 |
| Dock appointments and the detention clock | WS-086, v1.1 scheduling | schema only in v1 | v1.1 |
| Ageing / reminders for a session left `ARRIVED`/`UNLOADING`, a `DRAFT` GRN, uninspected QC-held stock | **no v1 rule** | no source sets a clock; `warehouse.approval.pending_alert_hours` covers only pending approvals (H15) | v1.1 with WS-086 |
| Partial receipt reversal | **not supported** | `L-3` whole-movement mirroring; a partial correction is an adjustment (RQP-OPEN-20) | — |

---

## 7. Amendments

| Date | Row IDs affected | Change | Found by |
|---|---|---|---|
| 2026-09-12 | all | v0 derived from the design set at `57bd7df`; unratified | `functional-reviewer`, derive mode (task W0-2b) |
| 2026-09-14 | §0, §1.1, T1-01…-36, GRD-01…-29, UNR-03/-05/-07/-09/-14/-15, T2-01…-24, T3-02/-03/-05, T4-01/-04/-05/-08/-11…-17, §5, §6, §9 | **Ratified (v1).** All 21 `RQP-OPEN` rows and 17 holes resolved or deferred to a named task (§9). User decisions 2026-09-14: RQP-OPEN-12 = seed `REJECTED` status + `REJECT` disposition in `V500058` (claimed from `DATA-MODEL.md` §7.2's `V500058`–`V500059` gap, carried by `P1-14`); RQP-OPEN-15 + H13 = counterparty always required, derived from a non-house owner; RQP-OPEN-13 follows `MPR-OPEN-05`'s settlement (pending scrap = `PENDING` `SCRAP` movement). New rows T1-35, T1-36, GRD-28, GRD-29, UNR-14, UNR-15, T4-16, T4-17. Carried into `DATA-MODEL.md`, `IRREVERSIBLE.md`, `BUILD-SPEC-SCREENS.md`, `WAREHOUSE-FUNCTIONAL-REQUIREMENTS.md`, `SCENARIO-CATALOGUE.md`, `IMPLEMENTATION-PLAN.md`, `DESIGN-SET-DEFECTS.md` (`X-055`), `issues/02-EPIC-p1.md` and task bodies `p0-05`, `p0-10`, `p1-03`, `p1-12`…`p1-16`, `p1-20` | WAVE C0 research + write (ratify) |
| 2026-09-15 | §0, T1-12, T1-13, T1-14, T1-35, GRD-13, GRD-14, GRD-28, T2-11, T3-01, T3-05, T4-14, §5.3, §6, H11 | **P1-12 C1 fix cycle (warehouse-issues#105).** (1) The T1-14 / T4-14 deferral is settled. A header Close closes every line not already terminal. A new **line Close** (`PATCH /{id}/lines/{lineId}/close`, `wh_purchase_orders:edit`) closes one line, with GRD-14 applied to that line, and the header is recomputed by the T1-12 rule (RQP-T2-11 "or PO line"). Item (8) amends this. (2) Every PO ladder write and `applyReceipt` are serialised on the PO header row (GRD-13). (3) User decision 2026-09-15: `wh_purchase_orders.received_at` (`V510021`) is stamped on the first entry to `RECEIVED` and never cleared; the *fully received this month* tile reads it (T3-05). (4) `V510021` drops the two status `CHECK`s §0 forbids. (5) T1-13 and T1-35 now read "recomputed by the T1-12 rule". (6) `PO_HAS_RECEIVED_STOCK`, `PO_SESSION_ARRIVED` and `CLOSE_SHORT_REASON_REQUIRED` are settled, and `PO_LINE_TRANSITION_INVALID` is added (H11). `GET /{id}/cancel-check` and `GET /{id}/lifecycle` are listed in §5.3. Overdue excludes `RECEIVED` (T3-01). (7) No PO document table exists in v1, so the Documents tab shows *not available* (§5.3, §6). **Fix pass 2, same day:** (8) User decision: a line Close that leaves every line `CLOSED`/`CANCELLED` closes the header as a header Close would, with `closed_at`, the same audit entry and no header reason. A mix of `RECEIVED` and `CLOSED` lines still leaves the header `RECEIVED` (T1-12, T1-14, T1-35, T3-05, T4-14). (9) User decision (F6 follow-through): the *fully received this month* tile counts by `received_at` alone, with no `status = 'RECEIVED'` filter (T3-05). (10) `applyReceipt` refreshes the header and lines once the lock is granted and re-checks GRD-28 for a receipt: 409 `PO_NOT_RECEIVABLE`, now a settled literal (GRD-28, H11). The `p1-13` hand-off adds that the GRN post locks PO headers in ascending `poId` order before its GRD-28 read. Carried into `BUILD-SPEC-SCREENS.md` (§0.11 Approve row: maker-checker always, per `FR-408`; WS-073 tab owners), `DATA-MODEL.md` (`received_at`; §7.3 `V510021`) and task bodies `p1-13` (hand-offs H1 and H2), `p1-14`, `p1-15` and `p2-13` | `functional-reviewer` C1 gate (F3–F10, H1, H2); fix pass 2: user decisions and N1 |
| 2026-09-15 | §1.1 putaway `EXCEPTION` row, T3-02, H9 | **P0-10 C2 fix pass 1.** `RQP-T3-02` and `H9` are no longer deferred: classic `warehouse-base/docs/contracts/task.contract.md` closes both — the ageing job `TSK-T3-01` and its notification `TSK-X-05`, and the `EXCEPTION` exits `TSK-T1-02`…`-04`. The `OPEN-11` EXCEPTION-vs-REVERSED-GRN gap stays with this contract | P0-10 hand-off (driver) |
| 2026-09-16 | §1.1, GRD-06, GRD-08, GRD-09, T4-02, T4-03, T4-07, §5.1, H16 | **P1-13 fix 1 (warehouse-issues#113), settled by default.** (1) **`departed_at` is NOT a WS-075 grid column.** §5.1's ratified column list omits it, and v1 has **no departure step** — no `T1` row, no `T2` row, no verb and no UI entry point, so nothing can ever stamp it; a sortable, exportable column over a column the product never writes is a column of blanks. The **table column and the entity field stay**: §1.1 and `RQP-T3-05` name `departed_at` a recorded fact, and **WS-086's detention clock (v1.1, §6) is the feature that will write it**, adding the grid column, the export cell and the sort mapping together. `V510013`'s `grid_column_definitions` row is removed; the `IRR-23` / `PNR-3` column assertion is unchanged. (2) **`match_status` precedence is stated** (`RQP-GRD-09`, `RQP-OPEN-18` listed the four values but no order): `ITEM_MISMATCH` **>** `QTY_OVER` **>** `QTY_UNDER` **>** `MATCHED`. `ITEM_MISMATCH` is `RJ-014`'s meaning and nothing else — **a received item on no attached PO line, on a receipt that is against an order**; a wholly blind receipt has no order to disagree with and is `MATCHED`. `QTY_OVER` outranks `QTY_UNDER` on a receipt carrying both, because the over-receipt is the half that raises an `OVER_RECEIPT` reconciliation case (`RQP-T4-15`) while the short half is answered by WS-072's Close short on the PO line. The comparison is against each line's `expected_quantity`, **frozen at post from the locked PO line's outstanding quantity** (ordered − received − cancelled) before `applyReceipt` moves the counter. (3) **`RQP-GRD-06`'s "free quantity excluded" is applied to the PO line counter as well as the tolerance comparison**: a counter inflated by free quantity makes the next receipt against the same line refuse spuriously. Free quantity still reaches the ledger and the `RQP-T4-06` landed-value spread. (4) **`RQP-GRD-08`'s "written in the post transaction" needs a staging table**, because the request naming the values exists only at keying: `V510014` gains **`wh_goods_receipt_line_attributes`** (`grn_line_id`, `attribute_key_id`, the four typed value columns, `uk(grn_line_id, attribute_key_id)`, `ON DELETE CASCADE`) — the `wh_goods_receipt_line_serials` shape, for the same reason. Keying types and registry-checks the value; Post writes it to `whb_entity_attribute_values`. A cancelled or re-edited draft now leaves no attribute row behind. **`DATA-MODEL.md` §2.2.1 gains the table.** (5) `RQP-T4-02`'s derived `po_id` and `RQP-T4-07`'s `receipt.line.confirmed` (one per line, owner / lot / LPN / warehouse / the three timestamps) are emitted at post | WAVE C7 P1-13 fix 1 (reviewer findings 1–3, 5–7, 9) |
| 2026-09-16 | A3, GRD-07, T4-01, H11, H13, H16 | **P1-13 build part 2 (warehouse-issues#113), settled by default.** (1) **`RQP-GRD-07`'s `item → supplier → AVAILABLE` chain is not implementable in v1** and is built as line override → **item × site** (`whb_item_site_settings.default_receipt_status_code`) → **movement type** (`whb_movement_types.default_stock_status_code`) → **refuse `RECEIVED_STATUS_UNRESOLVED`**. `whb_item_supplier_sources` carries no status column, and adding one needs a `warehouse-base` migration outside this task's `V510013`/`V510014` band, so the **supplier rung is handed to `p1-03`**, which owns that table, to settle before P1-14. The final rung is a REFUSAL rather than a literal, so `AVAILABLE` still appears in no code path and the row's own predicate still holds. (2) **`H13`'s dual-role case is settled**: a counterparty holding both a supply-side and a demand-side role takes the **SUPPLIER** virtual location on a receipt (`RQP-T4-01`). (3) **`H11`'s P1-13 literals are settled**: `OVER_RECEIPT_TOLERANCE_EXCEEDED` (GRD-06, and the message names the LEVEL the tolerance came from — PO line / PO header / item / site / global setting), `RECEIVED_STATUS_UNRESOLVED` (GRD-07), `RECEIPT_LOCATION_UNRESOLVED`, `COUNTER_LOCATION_MISSING`, `MOVEMENT_TYPE_NOT_CONFIGURED`, `GRN_COMPANY_UNRESOLVED`, `ATTRIBUTE_KEY_NOT_REGISTERED` / `ATTRIBUTE_VALUE_INVALID` / `ATTRIBUTE_ALREADY_RECORDED` (GRD-08), `GRN_HAS_NO_LINES` (GRD-09), `SESSION_HAS_DRAFT_GRN` (GRD-10), `SESSION_HAS_POSTED_GRN` (GRD-11), `GRN_NOT_DRAFT` (GRD-12). (4) **`H16`'s WS-075 / WS-076 refinement is settled**: the sorts, tiles and export columns of §5.1 and §5.2 are built as ratified, with the export a superset of the grid. (5) The GRN post locks PO headers in ascending `poId` order before its GRD-28 read, and `applyReceipt` runs inside the post transaction | WAVE C7 P1-13 build (settled by default) |
| 2026-09-16 | A1, GRD-01, T1-07, T2-01, T2-06, §5.1, §5.2, H2 | **P1-13 fix 2 (warehouse-issues#113), settled by default.** (1) **A create modal's counterparty feed is not the filter's.** WS-075's gate-in and WS-076's receipt modal read a typeahead over **every active counterparty holding a current supply-side role** (the feed WS-072's supplier picker already used), while the FILTERS keep reading "the suppliers on record". The self-referential feed made `RQP-GRD-01` unreachable: the first house-owned blind receipt offered an empty picker while the server refused the blank with `COUNTERPARTY_REQUIRED`. The one query now lives in `warehouse-base` (`WhbItemUomOptionsService`) and all three screens read it. (2) **§5.1's ratified `carrierName` column is honoured**: the gate-in modal gains a **carrier typeahead** over the counterparties holding a current **`CARRIER`** role (`V500011`; the role is neither supply- nor demand-side, so the supplier feed can never offer one). Without it the column was permanently blank and every edit nulled a carrier set by other means. `carrier_counterparty_id` was already on the request and already persisted. (3) **`is_blind_receipt` is DERIVED, never keyed** (`RQP-GRD-01`, `RQP-T1-07`, `H2`): blind-ness is the **absence of a purchase order**, so it is read off the lines exactly as `po_id` is (`RQP-T4-02`) - a receipt is blind when no line names a PO line - and it does **not** read `session_id`, because a session is a truck and not an order. The toggle is removed from the modal and the field from `WhGoodsReceiptRequest`, so the column, the `isBlindReceipt` filter and the export can no longer disagree with the receipt. (4) **`FR-450` has a WRITE direction**: a caller without `warehouse:cost:view` receives no `unitCost` and is shown no input for one, and a DRAFT's lines are rewritten on every edit - so their edit erased the unit cost of every line. The stored figures are carried forward per item, in line order, and such a caller's keyed cost is ignored on create as well. No cost reaches them: the grid has no cost column, the view Cost tab is gated and the export is gated | WAVE C7 P1-13 fix 2 (reviewer findings 1-6) |
| 2026-09-16 | GRD-06, GRD-08, GRD-09, T1-08, T4-15, H11 | **P1-13 fix 3 (warehouse-issues#113), settled by default.** (1) **`RQP-GRD-06` and `RQP-GRD-09` are predicates on the PURCHASE ORDER LINE across the whole receipt, not on one GRN line.** `RQP-T1-08` and `RQP-OPEN-05`/`-06` put damage found at the dock on its **own** line, so one order line is legitimately charged by several lines of one GRN (two lots of one item are the same shape), and `V510014` carries no unique key over `(grn_id, po_line_id)` because none is wanted — **accumulation, not refusal, is therefore the only correct reading**. Compared a GRN line at a time against the still-unmoved `po_line.received_quantity`, 2 × 60 against an order for 100 passed a **zero** tolerance twice, and 2 × 50 against 100 posted `QTY_UNDER` on a receipt that had matched the order. The post now accumulates this receipt's billed quantity per `po_line_id` (free quantity still excluded, per the 2026-09-16 fix-1 row) before the tolerance comparison, and `match_status` compares that same accumulation against the one frozen `expected_quantity`: every GRN line charging one order line freezes the **same** figure, because the expectation is a fact about the order line and not about the GRN line, so the outcome is decided once per order line. The refusal is anchored on the first GRN line that charged the order line. (2) **`RQP-GRD-08`'s in-flight set is settled before the write.** Two lines of one receipt can resolve to the **same lot** — the `RQP-OPEN-05` damage split is exactly that shape — and the committed pre-check sees neither, so the second row violated `uk_whb_entity_attribute_values` and reached the browser as the opaque `500` p1-13's Traps name. An attribute is a fact about the **lot**: two lines asserting the **same** value assert one fact and it is written once (a number compares by value, so `180.0` and `180` agree); two lines asserting **different** values contradict each other about that lot and are refused `422 ATTRIBUTE_ALREADY_RECORDED` on `lotAttributes.<key>` — H11's settled literal, whose meaning is a second, different value for one key on one lot. (3) **`RQP-GRD-06`'s refusal names the remedies**, prefixed *"Nothing posts until one of these is chosen"*: raise the ordered quantity on the purchase order, receive at most the allowance left for this receipt, or raise an `OVER_RECEIPT` reconciliation case for the overage. **Auto-raising that case is `RQP-T4-15` and belongs to `p2-13`** (§4, §6; `WH-SC-084`, v1·P2) — it is named in the message and raised by hand from WS-083 until `p2-13` ships. **No i18n key is added:** no module keys a backend refusal into `en`/`fr`/`hi` — the house envelope's `details.errors` reaches the screen as the server's own string (`DECISIONS.md` §4, BUILD-SPEC §0.13), and a key registry for one screen would be a new pattern | WAVE C7 P1-13 fix 3 (reviewer findings 1–3) |
| 2026-09-16 | §1.1, `RQP-T1-16`, `-19`, `-20`…`-23`, `-36`, `RQP-GRD-15`…`-20`, `-29`, `RQP-T4-05`, `-17`, `RQP-UNR-04`, §5.4, H7, H11, H16 | **P1-14 build (warehouse-issues#120), settled by default.** (1) **`RQP-T4-17` / `H7` is SETTLED: a posted disposition is NOT reversible from WS-080**, in any state. The screen offers no un-disposition action. The disposition's effect *is* a ledger movement, so `L-3` makes its correction a `REVERSAL` through `MPR-T1-08` from the movements screen, never a second correction path on the document that caused it — a WS-080 "undo" would let the inspection record and the ledger disagree, which is the failure `L-2` and `L-3` exist to prevent. The scrap arm keeps its own decline exit while `PENDING` (`RQP-T1-36`) because a `PENDING` movement has no ledger effect to reverse. Stated consequence: once released, the quantity is no longer QC-held and cannot be re-dispositioned (`RQP-GRD-29`), and its putaway task stands. (2) **`H11`'s P1-14 literals are settled**: `INSPECTION_INCOMPLETE` (GRD-15, `422`, field `lines[n].results`, naming each unrecorded mandatory criterion by line number and criterion code), `INSPECTION_ALREADY_COMPLETED` (GRD-16, `409`), `INSPECTION_NOT_COMPLETED` (GRD-29, `409`), `DISPOSITION_QUANTITY_EXCEEDED` (GRD-29, `422`, field `lines[n].quantity`), `LINE_ALREADY_DISPOSITIONED` (`409`, the line's `disposition_code` is already set — which is how "not yet dispositioned" is represented, there being no dispositioned-quantity column) *(withdrawn 2026-09-16 by the P1-14 C8 fix 1 row below: `V510022` adds `dispositioned_quantity` and a line is dispositioned repeatedly)*, `INSPECTION_GRN_NOT_POSTED` (`409`, Start against a GRN that is not `POSTED`; deliberately **not** `GRN_NOT_POSTED`, which is P1-16's), `UNKNOWN_CRITERION` and `CRITERION_VALUE_TYPE_MISMATCH` (`422`, GRD-15's typed-result guards), `VALUE_NOT_WHITELISTED` (GRD-20, `422`, listing the allowed values), and `INSPECTION_PLAN_IN_USE` / `CRITERION_IN_USE` (`409`, WS-079 — deleting the checklist a verdict was reached against would leave the verdict unexplainable). (3) **`H16`'s WS-079 / WS-080 refinement is settled**: the §5.4 sorts, tiles and export columns are built as ratified, with each export a superset of its grid. (4) **`RQP-GRD-20`'s whitelist is ratified to the value** — the design set said only "whitelisted": `sample_size_formula` ∈ `ALL` · `AQL_TABLE` · `SQUARE_ROOT_PLUS_ONE` · `FIXED_QUANTITY` · `PERCENTAGE` · `NONE`, one name per `inspection_type`'s need, with the service computing the size from the name. `sampling_plan` is deliberately **not** whitelisted (BUILD-SPEC WS-079 marks only the formula as such): it names the external standard a QA department follows, and closing it would refuse the next one adopted. (5) **`criterion` IS the "criterion code"** `WH-SC-301`'s refusal names. `DATA-MODEL.md` §2.2.1 gives `wh_inspection_plan_criteria` eight columns and `criterion` is the only identifier among them, so no separate code column is invented; `uk(plan_id, criterion)` is added beside the authority's `uk(plan_id, sequence)` so the message cannot be ambiguous. (6) **The `QC_RESULT` vocabulary is registered** as `results` on WS-080's `/vocabularies` with an i18n by-code map in en/fr/hi and the `registryLabel` key → row name → code fallback (C5 hand-off, `WH-SC-147`); `result` and `dispositionCode` are fetched, never TypeScript unions (`OD-5`, `FR-380`). (7) **`RQP-T4-05`'s release-side putaway task is a recorded hand-off to `P1-15`**: `wh_putaway_tasks` does not exist yet, so release posts its `STATUS_CHANGE` and the task creation is probed for with `to_regclass` and reported, never silently skipped | WAVE C8 P1-14 build (settled by default) |
| 2026-09-16 | §5.2, §6 | **P1-13 C7 settle (warehouse-issues#113), settled by default.** **GRN document attachments are deferred, not missing.** §5.2 ratified **Documents** as a WS-077 tab while §6 carried a deferral row for the WS-073 **PO** Documents tab only, so the tab reporting `available = false` read as a build gap. It is not: `V510014` creates `wh_goods_receipts`, `wh_goods_receipt_lines` and `wh_goods_receipt_line_serials` and no document table (§0 Primary tables), because **a delivery note attaches to the receiving session that unloaded the truck**, through `wh_receiving_session_documents` (`V510013`, `RQP-T1-03`) — which is what the code states and what the design set models. §6 gains the matching GRN row and §5.2's tabs row now points at it, on the shape P1-12 used for the PO row (§7 2026-09-15 item 7). **No task is allocated** to add GRN documents and none is invented here: it would need a new GRN ↔ platform `documents` link table on the `whb_item_documents` pattern (`DATA-MODEL.md` §3.2 W16 — never a second document store), its migration, and a task claiming both. | `functional-reviewer` C7 gate (low: §6/§7 contract row missing, `receipt-qc-putaway.contract.md:385` vs `WhGoodsReceiptQueryService.java:287-289`) |
| 2026-09-16 | `RQP-GRD-15`, `-19`, `-29`, `RQP-T1-16`, `-20`…`-24`, `-27`, `-36`, `RQP-T4-01`, `-05`, `-08`, `-11`, `-16`, H7, H8, H11 | **P1-14 / P1-15 C8 fix 1 (warehouse-issues#120 / #126).** (1) User decisions 2026-09-16: (a) a serialised line is dispositioned in full, one status change (or scrap grain) per serial; its serials travel into `wh_putaway_task_serials` (`V510022`) and Complete moves exactly those serials; (b) a line is dispositioned repeatedly until its QC-held quantity is used up — `wh_quality_inspection_lines.dispositioned_quantity` (GRN-line unit); `LINE_ALREADY_DISPOSITIONED` withdrawn; `disposition_code` / `target_status_code` hold the latest disposition; (c) GRN post lands rejected units in `REJECTED`; (d) putaway Cancel raises a fresh unassigned `CREATED` task for the same stock (H8 RESOLVED); a receipt reversal's cancel raises none. (2) "QC-held" = a GRN line received into `QC_HOLD` holding (received + free − rejected) in its own unit; the cap is that minus the dispositions of the whole inspection series; quantities convert to base with the frozen `conversion_factor_used`. (3) H11 literals settled: `INSPECTION_IN_PROGRESS` `409` (Start); `DISPOSITION_STOCK_NOT_QC_HELD` `409` `lines[n].lineId`; `SERIALISED_LINE_PARTIAL_DISPOSITION` `422` `lines[n].quantity`; `INSPECTION_GRN_NOT_POSTED` `409` (also Disposition and scrap Approve); `INSPECTION_APPROVE_PERMISSION_REQUIRED` `403` (`RQP-GRD-19` / `MPR-GRD-27`); `PUTAWAY_SERIALS_UNRESOLVED` `409` `quantity`; `REJECTED_SERIALS_UNIDENTIFIED` `422` `lines[n].rejectedQuantity` (settled by default). (4) `INSPECTION_INCOMPLETE` also covers a line with neither a criterion result nor passed/failed units (`Q-006`). (5) Start proposes the inspected quantity from `sample_size_formula`; `FIXED_QUANTITY` / `PERCENTAGE` propose the whole lot until `wh_inspection_plans` gains a parameter column (**open**). (6) WS-079: editing a criterion's rule after results exist is `CRITERION_IN_USE`; retiring an in-use plan is allowed. (7) Status-change movements carry `qcResultCode`. (8) "One plan per GRN" (the first line's category plan) is unchanged; a GRN whose lines' categories name different plans is inspected under one plan (**open**) | WAVE C8 P1-14 / P1-15 fix 1 |
| 2026-09-16 | §1.1 putaway `EXCEPTION` row, `RQP-T1-24`, `-27`, `RQP-GRD-20`…`-22`, `RQP-T2-23` (new `-23a`, `-23b`), `RQP-T4-05`, `-11`, §5.4, §5.5, H8, H11, H16 | **P1-15 C8 fix pass 2 (warehouse-issues#126).** (1) H8 → RESOLVED: `wh_goods_receipt_lines.putaway_location_id` records the completed putaway's actual location. (2) `OVERRIDE_REASON_REQUIRED` proposed → settled (`422` · `overrideReasonCodeId`). (3) An override exists only when a suggestion existed: with `suggested_location_id` null no reason is required and `hasOverride` is false (`chk_wh_putaway_tasks_override_reason`). (4) `wh_putaway_tasks.stock_status_code` records the status the task moves. (5) Movement types `PUTAWAY` and `PUTAWAY_REVERSAL` are seeded by `V510017` (`PNR-1`: a type is registered by a migration first). (6) Runtime TASK series provisioning: `WhbTaskService` creates the site's `warehouse-base`/`TASK` series (one per current OPERATOR, `TASK-`, pad 6, `NEVER`, gapless) inside the creating transaction when none exists (`INSERT … WHERE NOT EXISTS … ON CONFLICT DO NOTHING`); an INACTIVE series is not replaced and still refuses `NUMBER_SERIES_NOT_CONFIGURED`. (7) Fall-through (`WH-SC-074`): the first matching rule whose strategy finds a location wins; a matching rule that finds none falls through; `NO_LOCATION_FOUND` only when every matching rule found none, `rule_id` = first matching rule. Every strategy applies `RQP-GRD-22` (temperature, dedicated owner, commingle) and capacity (`max_units`; `FIXED_LOCATION` also `whb_item_location_settings.max_quantity`) for the line's base quantity; the chain is read `FOR SHARE` at task creation and a rule is locked `FOR UPDATE` before the in-place vs copy-on-write decision. (8) User decision 2026-09-16: Cancel raises a fresh unassigned putaway task for the same stock — from WS-082 or WS-059 — except when the cancel is caused by a receipt reversal. (9) WS-059 Complete-with-exception on a PUTAWAY task is refused (`409 TASK_TRANSITION_INVALID`) unless the task is `EXCEPTION`, and the WS-059 row hides the action accordingly. (10) `RQP-GRD-22` is also raised by the port (`WhbMovementPostValidationService`) for every incoming line of a new movement at a non-virtual location; the temperature class is not asked of handling location types (`RECEIVING`, `SHIPPING`, `VIRTUAL`, `DOCK`, `STAGING`); reversals and stored-movement approvals are not re-checked (settled by default 2026-09-16). (11) `RQP-T2-23` gains rows: DELETE (`wh_putaway_rules:delete`, refused `PUTAWAY_RULE_REFERENCED` `409` when a task or successor references the rule — `RQP-T2-23a`) and Activate/Deactivate (`wh_putaway_rules:edit`; deactivate always; activate refused `PUTAWAY_RULE_SUPERSEDED` `409` on a superseded version, copy-on-write when task-referenced; row flag `canActivate` — `RQP-T2-23b`). (12) §5.5: `ZONE_BY_VELOCITY` and `BULK_THEN_PICK_FACE` share one resolver in v1 (zone candidates in `putaway_priority`, `pick_sequence`, `code` order, occupied locations allowed subject to `RQP-GRD-22`). (13) Hidden-by-default columns per the `V510017` grid seed — WS-081 (`wh_putaway_rules`): `targetZoneLocationCode`, `versionNo`, `createdByName`, `updatedByName`, `createdAt`, `updatedAt`; WS-082 (`wh_putaway_tasks`): `lpnCode`, `warehouseName`, `priority`, `assignedAt`, `startedAt`, `completedAt`, `createdByName`, `updatedByName`, `createdAt`, `updatedAt`. (14) WS-082 accepts child-grid params `goodsReceiptId` (GRN detail and the WS-076 Putaway row action) and `purchaseOrderId` (PO detail Putaways tab), bound against the task row's scopes. (15) A putaway may target an active, `AVAILABLE` location of any type except `RECEIVING`, `SHIPPING`, `VIRTUAL`, `DOCK`, `STAGING`, never the task's staging location. (16) Unknown strategy refuses `VALUE_NOT_WHITELISTED` (`422`, lists the five). (17) §5.5 WS-082 `assignedTo` and `itemId` filters are server-side typeaheads | WAVE C8 P1-15 fix pass 2 |
| 2026-09-16 | §1.3, `RQP-GRD-24`, `-25`, `-26`, `-27`, new `-30`, `RQP-T1-29`…`-32`, `RQP-T2-19`…`-21`, `RQP-T4-12`, §5.2, §5.6, H11, H16 | **P1-16 C8 fix 3 (warehouse-issues#129).** (1) The soft-close period override on WS-078 Post is API-only (`periodOverride*` on the Post body), no modal field, following the WS-076 GRN Post precedent. (2) Free quantity goes back out through the ledger mirror but is not taken off the PO counter, mirroring the GRN post (`RQP-GRD-06`, `FR-141`); `wh_receipt_reversal_lines.quantity` is received quantity only. (3) Refinements beyond the ratified screen: four filter-aware status tiles (`REQUESTED`, `APPROVED`, `REJECTED`, `POSTED`); `warehouseName` shipped hidden; the `grnId` filter param; extra endpoints `/reason-codes`, `/goods-receipts`, `/vocabularies`, `/warehouses`; request, approve and reject justifications; deep links `?grnId=&grnNumber=` and `?reversalId=&returnTo=`. (4) H11 literals settled for P1-16: `GRN_NOT_POSTED`, `RECEIPT_REVERSAL_EXISTS`, `REVERSAL_REASON_INVALID`, `REVERSAL_REASON_NOTE_REQUIRED`, `REVERSAL_SELF_APPROVAL` (`403`), `REVERSAL_NOT_REQUESTED`, `REVERSAL_NOT_OPEN`, `REVERSAL_NOT_APPROVED`, `RECEIPT_MOVED_ON`, `RECEIPT_MOVEMENT_MISSING`, `RECEIPT_INSPECTION_OPEN`, `RECEIPT_SCRAP_PENDING` (`409` unless noted). (5) `uk_wh_receipt_reversals_open_per_grn` is `RQP-GRD-24`'s database backstop. (6) The reason code must be active and from the `REVERSAL` context, checked at Request and Reject; `requires_note` makes the justification mandatory. (7) Putaway tasks cancelled by a reversal post take the reversal's reason code and raise no replacement. (8) The RREV number series is not seeded, like PO, GRN, RCV and QCINSP — ops note: configure it per site before the first Request. (9) `RQP-T1-31` "reason recorded" is stored on the row (`V510023` `reject_reason_code_id`, `reject_justification`, plus request and approve justifications), shown in the view modal and the WS-077 Reversals tab. (10) WS-076 row actions Inspect, Putaway and Reverse use server flags `canInspect`, `canPutaway`, `canReverse`; `canReverse` is false while a live reversal exists. **Also:** `RQP-GRD-26`'s predicate wording is replaced by the per-grain live on-hand test, and new `RQP-GRD-30` refuses a reversal post while an inspection of the GRN is uncompleted or a `PENDING` `SCRAP` from its inspections exists (`RQP-T4-12` cross-reference) | WAVE C8 P1-16 fix 3 |
| 2026-09-16 | `RQP-T1-16`, `-36`, `RQP-GRD-29`, `RQP-T4-16`, §5.2 | **P1-14 C8 fix 5a (warehouse-issues#120).** (1) **Withdraw** (maker, WS-040 `POST /movements/{id}/withdraw`) of a PENDING QC scrap gives its quantity back to the inspection line exactly as Reject does, in the same transaction (`MPR-GRD-27` `Decision.WITHDRAW`). (2) `RQP-GRD-29`: disposition takes the GRN row lock, then the inspection lock, before reading the series cap; Lock order (rewritten in place, §7 2026-09-16 P1-14/P1-15/P1-16 C8 fix 6): approve / reject / withdraw of a movement — from WS-040 or WS-080 alike — take the writer's order, **positions → movement → inspection** (the inspection only via the `MPR-GRD-27` guard, on reject and withdraw; on approve the guard only reads); WS-080 pre-locks neither the movement nor the inspection, and a scrap decided by someone else meanwhile is `409 MOVEMENT_NOT_PENDING`. Disposition takes **goods receipt → inspection** (then the writer's positions for its own new movement, an INSERT). (3) A line's `disposition_code` / `target_status_code` / `rejection_reason_code_id` mean **the latest decision still standing**: after a scrap is rejected or withdrawn they are re-derived from the line's newest QCINSP movement that is not rejected, not withdrawn, not a reversal and **not a reversed original** (`is_reversed`, or a reversal row that is not rejected or withdrawn names it; reversed originals added §7 2026-09-16 P1-14/P1-15/P1-16 C8 fix 6) (null if none), and the header code is recomputed from the lines. A disposition is tied to its line through the idempotency key `QCINSP:<lineId>:<code>:…`, since status-change movements carry no line number. (4) WS-076 `canInspect` = receipt POSTED **and** no inspection of it in progress (`completed_at IS NULL`), loaded once per page — supersedes P1-16 fix 3 item 10 for `canInspect`. (5) **V510024**: recomputes `wh_quality_inspection_lines.dispositioned_quantity` from effective (not rejected, not withdrawn) QCINSP movements, as inbound base quantity ÷ the GRN line's frozen `conversion_factor_used`. Backfills `wh_putaway_task_serials` for every open task status with serials that no other task carries and that stand at the task's staging location and status, up to the task quantity. Reads the ledger and positions only; safe to re-run. *Recorded, not corrected (§7 2026-09-16 P1-14/P1-15/P1-16 C8 fix 6):* `V510024` is already on origin and used the looser standing definition (reversed originals not excluded) and a looser serial grain (location + status only); harmless because no environment held pre-fix rows, so it is not edited and no corrective migration is added. (6) A re-inspection proposes its sample from the quantity still QC-held (received + free − rejected − already dispositioned) | WAVE C8 P1-14 fix 5a (driver apply) |
| 2026-09-16 | `RQP-T1-27`, `-34`, `RQP-T4-05`, §5.5, H8, H11 | **P1-15 C8 fix 5b (warehouse-issues#126), settled by default.** (1) N1: a Cancel raises **no** replacement — the cancel itself still succeeds — when the task's goods receipt is not `POSTED`, or when nothing of the task's stock is still staged at its grain (owner, item, staging location, status, lot and LPN matched exactly; when serialised, only the task's serials); a partial shortfall is replaced for what is still staged (rewritten in place, §7 2026-09-16 P1-14/P1-15/P1-16 C8 fix 6). (2) Runtime TASK series provisioning continues from the highest `TASK-` number already issued at the site: `current_value` = MAX of the numeric suffix after the provisioned prefix over `whb_tasks` at that warehouse, else 0. An OPERATOR handover therefore never re-issues a number under `uk_whb_tasks_warehouse_task_number`; still `INSERT … WHERE NOT EXISTS … ON CONFLICT DO NOTHING` (supersedes fix pass 2 item 6's implicit start at 0). A company re-appointed OPERATOR (A→B→A) keeps its existing series, whose `current_value` is raised to the site's highest issued suffix before issuing (§7 2026-09-16 P1-14/P1-15/P1-16 C8 fix 6). (3) §5.5: suggestions are advisory but account for open work — placement guard excludes handling location types and the line's staging location; open putaway tasks and earlier suggestions in the same batch count against `max_units`, `FIXED_LOCATION` `max_quantity` (same item), `NEAREST_EMPTY` emptiness and the commingle policy; Complete reserves nothing and refuses those location types `422 PUTAWAY_LOCATION_INVALID` (H11); Rule Reorder locks the site's active chain in ascending `sequence` before the moved rule. (4) `RQP-T1-34` Test takes an optional owner (picker `GET /owners?warehouseId`, filtered to the site); without one, dedicated locations and occupied commingle-constrained ones are not offered. (5) WS-082 `/assignees` accepts `includeId` so a restored Assigned To filter keeps its label | WAVE C8 P1-15 fix 5b (driver apply) |
| 2026-09-16 | `RQP-GRD-26`, `-27`, `RQP-T1-32`, `RQP-T4-12`, `RQP-OPEN-09` | **P1-16 C8 fix 5c (warehouse-issues#129).** (1) **User decision 2026-09-16: "a putaway blocks its own receipt".** `RQP-GRD-26`'s predicate is replaced (in place): a completed putaway of one of this receipt's GRN lines' tasks is charged in full up to the receipt's quantity whatever the on-hand; a putaway of another receipt's task is never charged; every other live movement is attributed by on-hand in `sequence_no` order; a pre-existing negative balance is named as such. This reconciles `RQP-GRD-26` with `RQP-OPEN-09` (reworded) and `issues/p1-16.md`. (2) Only posted movements that need no approval or are `APPROVED` are read; `PENDING` excluded; ordered by per-site `sequence_no`, never `occurred_at`; the read takes no lock (`MPR-GRD-12`, `-13` and the one-transaction post are the backstop). (3) Message names the movements that took the receipt's units out of the receiving location, not downstream; leads with the total, names at most 5 blocking movements (negative-balance row first), then *"and further movements beyond the first 5"*; `WH-SC-082` "Then" reworded. (4) `RQP-T1-32`: `RQP-GRD-30` is evaluated before `RQP-GRD-26`. (5) `RQP-T4-12` / `RQP-GRD-27`: the reversal cancels the GRN lines' cancellable putaway tasks (`CREATED`, `ASSIGNED`, `EXCEPTION` — `WhbTask.canCancel`) through `cancelForReversedSource`; `STARTED`/`PAUSED` are not reachable in v1 and need no reversal guard until they are (settled 2026-09-16) | WAVE C8 P1-16 fix 5c (driver apply) |
| 2026-09-16 | `RQP-GRD-29`, row 2026-09-16 P1-14 fix 5a (2), (3), (5), row 2026-09-16 P1-15 fix 5b (1), (2), `RQP-T1-27`, `RQP-T4-05`, `RQP-GRD-26`, `-27`, §5.5, H8 | **P1-14/P1-15/P1-16 C8 fix 6 (warehouse-issues#120 / #126 / #129), settled by default.** (1) **Lock order:** approve / reject / withdraw of a movement from WS-080 or WS-040 take the writer's order, positions → movement → inspection (via the `MPR-GRD-27` guard, which alone locks the inspection, on reject and withdraw); WS-080's Approve / Reject scrap pre-locks nothing and hands each PENDING scrap to the writer, so WS-080 and WS-040 can no longer deadlock on positions vs movement. An approve never writes the inspection row (taking it after the positions would cycle with a disposition's goods receipt → inspection → positions); the decision is stamped on the movement. A line whose scraps were all decided already is `409 MOVEMENT_NOT_PENDING`, `404` only when it never had one. Disposition keeps goods receipt → inspection. (2) **Open work** = `WhbTask.OPEN_STATUSES`, `EXCEPTION` included, for placement (§5.5) and for the creation port's "line already has an open task" read. (3) `RQP-GRD-26`: a grain's taker rows are merged by movement before naming — A +100, B +100, an unlinked issue of 150, then A's putaway of 50: reversing B names the issue once, 100. (4) `RQP-GRD-27`: a task no longer cancellable once locked is skipped; replacements raised concurrently for the same GRN lines are cancelled too. (5) `RQP-T1-27` / `RQP-T4-05`: partial-shortfall replacement for `min(staged on-hand, task quantity)`, serialised carrying only its serials still staged; none only at zero staged or a GRN not `POSTED` (replaces fix 5b N1). (6) **A→B→A handover:** `whb_warehouse_companies` permits a company to become OPERATOR again (its EXCLUDE forbids only overlapping ranges per site, company and role; the partial unique index allows one current OPERATOR), and provisioning skips a company that already has a series. The TASK issue path therefore raises the resolved series' `current_value` to the site's highest issued `TASK-` suffix when it is behind — one `UPDATE` on the series row, the lock the issuer takes next in the same transaction, a no-op when not behind. Stated consequence: the gapless assertion (`MAX(issued_value) = COUNT(*)`) reports such a series, as it already did for a series provisioned after a handover (fix 5b (2)). (7) **Standing disposition** excludes reversed originals as well as reversal rows; no runtime computation of `dispositioned_quantity` reads movements (it is maintained on the line), so the rule lands in the standing read only. `V510024` (on origin) used the looser definition and a looser serial grain (location + status only) — recorded, not corrected, since no environment held pre-fix rows | WAVE C8 P1-14/P1-15/P1-16 fix 6 |

---

## 8. Invariants and scenarios this workflow must not break

| Invariant | Rows that carry it | Scenarios that prove it |
|---|---|---|
| `L-1` conservation | `RQP-T4-01`, `RQP-T4-11`, `RQP-GRD-23` | `WH-SC-051`, `WH-SC-052`, `WH-SC-063`, `WH-SC-066`, `WH-SC-069`, `WH-SC-073` |
| `L-2` append-only | `RQP-UNR-01`, `RQP-UNR-05`, `RQP-T4-16` | `WH-SC-081` |
| `L-3` correction is reversal | `RQP-T1-29`…`-32`, `RQP-UNR-06`, `RQP-UNR-12`, `RQP-UNR-14` | `WH-SC-081`, `WH-SC-082` |
| `L-7` frozen factor | `RQP-GRD-02`, `RQP-T4-01` | `WH-SC-078` |
| `L-8` period-bound | `RQP-GRD-09`, `RQP-GRD-26` | `WH-SC-021` |
| `L-9` idempotent ingestion | `RQP-GRD-27` | `WH-SC-081` |
| `L-12` traceability | `RQP-T4-03`, `RQP-GRD-01` | `WH-SC-051`, `WH-SC-063`, `WH-SC-079` |
| `L-13` timestamps (and `IRR-23`) | `RQP-T3-05`, `RQP-T1-26` | `WH-SC-052` |
| `D-15` never restate | `RQP-T4-06` | `WH-SC-085` |
| `FR-408` maker–checker | `RQP-GRD-19`, `RQP-GRD-25`, `RQP-UNR-15` | `WH-SC-039`, `WH-SC-071` |
| Other scenarios in scope | sessions, PO roll-up, inspection, numbering, identifiers, events | `WH-SC-064`, `WH-SC-065`, `WH-SC-067`, `WH-SC-068`, `WH-SC-070`, `WH-SC-071` (v1·P2, owned by `p2-01`), `WH-SC-072` (`p2-13`), `WH-SC-074`, `WH-SC-075`, `WH-SC-076`, `WH-SC-077`, `WH-SC-080`, `WH-SC-083`, `WH-SC-084`, `WH-SC-086`, `WH-SC-142`, `WH-SC-175`, `WH-SC-176`, `WH-SC-194`, `WH-SC-294`, `WH-SC-301` |

---

## 9. Resolved rows — contradictions and holes

Every row below is `RESOLVED` (the resolution is written into the rows it names and into the owning
document) or `DEFERRED-TO-<task>` (the task fills the `TBD` in its own PR and adds a §7 row). Precedence
applied: `DECISIONS.md` owns precedence and `GLOBAL-SETTINGS-DECISIONS.md` is its adopted amendment;
`DATA-MODEL.md` owns schema; `BUILD-SPEC-SCREENS.md` owns screen contracts and §10.2 owns verb strings;
later round folds beat earlier text; the scenario catalogue owns fixtures, not schema.

**Contradictions.**

| ID | Contradiction | Resolution | Authority | Status |
|---|---|---|---|---|
| RQP-OPEN-01 | Mixed rollup literal: `PARTIAL` (DATA-MODEL, WS-080, `p1-14`) vs `CONDITIONAL` (`FR-133`, `WH-SC-070`) | `PARTIAL`; `FR-133` and `WH-SC-070` amended; `X-055` fixed | DATA-MODEL owns schema (`wh_quality_inspections.result`); `X-055`'s recommendation; IMPLEMENTATION-PLAN §2 P1-14 row | RESOLVED |
| RQP-OPEN-02 | `match_status` on the GRN header vs the PO line (`WH-SC-051`, `WH-SC-068`) | GRN header only; the PO line carries `line_status`; scenarios reworded | DATA-MODEL §2.2.1 `wh_goods_receipts`, `wh_purchase_order_lines` | RESOLVED |
| RQP-OPEN-03 | Close short on the PO (WS-072) vs on the GRN (`p1-13` Scope/Acceptance) | WS-072 only; `p1-13` Acceptance reworded | BUILD-SPEC WS-072 modals; `p1-12` line ladder | RESOLVED |
| RQP-OPEN-04 | A GRN spans POs (`RG-019`) vs one PO per GRN (`WH-SC-064`) | a GRN may span POs; `WH-SC-064`'s sentence scoped to its fixture | DATA-MODEL `wh_goods_receipts.po_id`; round-4 `RG-019` | RESOLVED |
| RQP-OPEN-05 | A damaged quantity column (`WH-SC-069`) vs none in DATA-MODEL | no column; damage at the dock is a separate GRN line in `DAMAGED` with `condition_code` / `damage_notes`; `WH-SC-069` reworded | DATA-MODEL `wh_goods_receipt_lines` | RESOLVED |
| RQP-OPEN-06 | Damaged stock enters `DAMAGED` by a second movement (`WH-SC-069`) vs on the first transaction (`FR-129`, `WH-SC-073`) | first transaction for damage found at the dock; `MPR-T1-10` only for damage found after post | `FR-129` over a fixture | RESOLVED |
| RQP-OPEN-07 | PO Reopen modal (WS-072, `p1-12`) vs `CLOSED`/`CANCELLED` terminal (§0.11) | no Reopen in v1; struck from WS-072 and `p1-12` | BUILD-SPEC §0.11 completion rule (a modal with no ladder row and no verb has no guard) | RESOLVED |
| RQP-OPEN-08 | v1 putaway task auto-completed at creation (`p0-10`, DATA-MODEL `whb_tasks`, WS-059) vs open work (`p1-15`, WS-082, `RJ-017`) | open work: created at post / release, completed by the operator's Complete request, which stamps `assigned_at` (when null), `started_at` and `completed_at` in that request; `p0-10`, DATA-MODEL and WS-059 wording clarified for putaway | round-4 `RJ-017` (later); BUILD-SPEC WS-082 actions and WS-076 `awaitingPutaway`; `WH-SC-052` fixture (stock at `RECV-01`, then putaway); `p1-15` Traps (explicit assignment) | RESOLVED |
| RQP-OPEN-09 | What *"moved on"* means (`p1-16` vs `WH-SC-081`/`-082`) | a completed putaway of this receipt's own task blocks it in full (user decision 2026-09-16, "a putaway blocks its own receipt"); every other posted, approved-or-unapproval-needing, unreversed movement taking stock out of the receipt's grain is attributed by on-hand (`RQP-GRD-26`); a putaway of another receipt's task never blocks this one; `p1-16` reworded (§7 2026-09-16 P1-16 C8 fix 5c) | `WH-SC-081`, `WH-SC-082` fixtures; `FR-131` | RESOLVED |
| RQP-OPEN-10 | `wh_goods_receipts:reverse` (`p1-20`) vs `wh_receipt_reversals:*` | `wh_receipt_reversals:create` gates WS-076 Reverse; `wh_goods_receipts:reverse` struck from `p1-20` | BUILD-SPEC §10.2 / §0.11 own verb strings | RESOLVED |
| RQP-OPEN-11 | `wh_putaway_tasks:complete` seeded (`p1-20`) but absent from §10.2 | Complete = `wh_putaway_tasks:complete` (§10.2 row added); Assign, Cancel = `wh_putaway_tasks:edit` | `p1-20` seed; §10.2 rule; `p0-10` WS-059 precedent (task transitions under CRUD) | RESOLVED |
| RQP-OPEN-12 | QC disposition vocabulary vs the frozen seed (no release / reject code, no `REJECTED` status) | release `RESTOCK_SELLABLE`, RTV `RTV`, scrap `SCRAP`; **user decision 2026-09-14**: seed status `REJECTED` + disposition `REJECT` (→ `REJECTED`) in `V500058`, carried by `P1-14`; `WH-SC-071`/`-072` and `FR-134` stay as written | user decision; `p0-05` merge-blocker rule; live seed read 2026-09-14; DATA-MODEL §7.2 gap rule (`P1-10`'s `V500057` precedent) | RESOLVED |
| RQP-OPEN-13 | Scrap approval home: document-level vs movement-level `approval_status` | the disposition posts the `SCRAP` movement at once, `approval_status = PENDING`, found by `source_document_type`/`source_document_id`; WS-080 Approve / Reject scrap delegate to `MPR-T1-05` / `-06` under `wh_quality_inspections:approve`; no approval column on `wh_quality_inspection_lines` | `MPR-OPEN-05` settled 2026-09-14 (`approval_status` carries the lifecycle); round-4 §10.2 verb; `FR-164`; seeded `SCRAP.requires_approval = true`; `WH-SC-039` | RESOLVED |
| RQP-OPEN-14 | Over-receipt ladder's last rung and the PO-line column | PO line → PO header → item → warehouse → global `warehouse.receiving.over_receipt_percent`; DATA-MODEL gains `wh_purchase_order_lines.over_receipt_tolerance_pct` | `GLOBAL-SETTINGS-DECISIONS.md` (adopted by DECISIONS) | RESOLVED |
| RQP-OPEN-15 | A blind receipt names a counterparty (`FR-120`) vs *"nothing else required"* (`FR-128`) | **user decision 2026-09-14 (B1)**: always required, derived from a non-house owner's `whb_owners.counterparty_id`; a picker only for a house-owned blind receipt; balanced against that counterparty's virtual location | user decision; DATA-MODEL `whb_owners` | RESOLVED |
| RQP-OPEN-16 | Mobile scope | web only; mobile acceptance and screen lines replaced by `RB-001` notes | user rule 2026-09-11 | RESOLVED |
| RQP-OPEN-17 | A fully received PO line "closes" (`WH-SC-066`) vs `RECEIVED` non-terminal | `RECEIVED` is non-terminal; `CLOSED` only by Close / Close short; `WH-SC-066` reworded | §0.11 and `p1-12` ladders | RESOLVED |
| RQP-OPEN-18 | `match_status` values: three (WS-076) vs four (`ITEM_MISMATCH`) | four; WS-076 amended | DATA-MODEL; round-4 `RJ-014` | RESOLVED |
| RQP-OPEN-19 | Short side bounded by the over-receipt tolerance (ladder text) vs `short_receipt_tolerance_pct` (`RE-004`) | `short_receipt_tolerance_pct`, item → warehouse → 0 (no PO column, no global key); `p1-12` and §0.11 rows reworded | round-3 `RE-004` (later than round-2 ladder text); DATA-MODEL columns | RESOLVED |
| RQP-OPEN-20 | Partial reversal (per-line quantity) vs whole-movement mirroring | whole GRN only; each reversal line's `quantity` = the full received quantity | `DECISIONS.md` `L-3`; `MPR-GRD-14`, `MPR-UNR-05` | RESOLVED |
| RQP-OPEN-21 | PO cancel cascades into its session (`WH-SC-083`) vs refused while a session is live (`RJ-018`) | cascade to lines and `DRAFT` GRNs only; no session transition; `WH-SC-083` reworded | round-4 `RJ-018` (later, written into the same scenario) | RESOLVED |

**Holes.**

| ID | Hole | Resolution | Authority | Status |
|---|---|---|---|---|
| H1 | Entry points for Gate-in, GRN Cancel, reversal Reject | WS-075 toolbar **Add**; WS-076 row **Cancel** (`DRAFT`); WS-078 row **Reject**; BUILD-SPEC amended | Service Vehicle reference (`classic` `services/frontend/src/app/dashboard/services/vehicles/page.tsx`); §0.11 GRN and reversal rows | RESOLVED |
| H2 | Whether a GRN requires a session | `session_id` nullable; set by WS-075 Create GRN, null for WS-076 Blind receipt | BUILD-SPEC WS-072 *Receive against* → WS-075; WS-076 Blind receipt modal | RESOLVED |
| H3 | Scrap Approve screen; no decline exit | WS-080 **Approve scrap** / **Reject scrap** (`RQP-T1-23`, `-36`) | `p2-12` return-scrap approval precedent; `MPR-T1-06` | RESOLVED |
| H4 | `APPROVED` reversal with a refused Post has no exit | Reject allowed from `APPROVED` (`RQP-T1-31`); `p1-16` and §0.11 amended | `p0-02` `WITHDRAWN`-after-approval precedent (`RA-004`); §0.11 completion rule | RESOLVED |
| H5 | GRN against a PO not yet `APPROVED` | `RQP-GRD-28` | §0.11 PO receipt rows | RESOLVED |
| H6 | PO header after a receipt reversal | `RQP-T1-35` recompute; §0.11 amended | §0.11 (header = a consequence of the ledger); `p1-12` line regress | RESOLVED |
| H7 | Disposition vs Complete; re-disposition; reversibility before putaway | after Complete, QC-held quantity only (`RQP-GRD-29`) — RESOLVED; **reversibility before putaway: NO — WS-080 offers no un-disposition action, the correction is a movement reversal (`RQP-T4-17`, settled 2026-09-16 by P1-14)** | `WH-SC-301`; BUILD-SPEC §0.11 *Still owed* names `P1`'s QC task; `L-3` | RESOLVED |
| H8 | Putaway Cancel: staging stock, replacement task, `staging_location_id`, `putaway_location_id` | Cancel posts nothing (`RQP-T1-27`) — RESOLVED; **Cancel raises a fresh unassigned task for the same stock, except when caused by a receipt reversal (user decision 2026-09-16), when the GRN is not `POSTED`, or when nothing of it is still staged at the task's grain; a partial shortfall is replaced for what is still staged (§7 2026-09-16 P1-14/P1-15/P1-16 C8 fix 6); `putaway_location_id` records the completed putaway's actual location; `stock_status_code` records the status moved (`RQP-T1-24`, `-27`, `RQP-T4-11`)** | contracts README (the building task adds detail); §7 2026-09-16 P1-15 C8 fix 2 | RESOLVED |
| H9 | `whb_tasks` exits of `PAUSED` / `EXCEPTION` | `PAUSED` not reachable in v1; `EXCEPTION` exits are WS-059's Reassign / Cancel / Complete-with-exception | `p0-10` WS-059; `task.contract.md` (classic `warehouse-base/docs/contracts/`) `TSK-T1-02`…`-04` | RESOLVED — closed by `task.contract.md` |
| H10 | Codes outside `FR-039` | application codes live in their raising block; kept as named | BUILD-SPEC §0.13 | RESOLVED |
| H11 | Unallocated refusal codes | shapes, fields and messages fixed in §1.3; *proposed* literals named there | BUILD-SPEC §0.13 (codes live in the raising block); `warehouse-base` naming convention | P1-12's literals SETTLED 2026-09-15 (GRD-13, GRD-14, and GRD-28's `PO_NOT_RECEIVABLE`; §7); **P1-13's SETTLED 2026-09-16** (§7); **P1-14's SETTLED 2026-09-16** (§7); **P1-15's and P1-16's SETTLED 2026-09-16** (§7 C8 fix rows), including `PUTAWAY_LOCATION_INVALID` (`422`, putaway Complete to a handling location type or the task's staging location; §7 2026-09-16 P1-15 C8 fix 5b) |
| H12 | Setters of `dock_to_stock_completed_at`, `first_receipt_at` | `RQP-T3-05` | BUILD-SPEC §8 dock-to-stock metric; `IRR-23` | RESOLVED |
| H13 | Counter-side virtual location of a non-supplier blind receipt; what *available* means | the counterparty's role's virtual location (`RQP-T4-01`) — a counterparty holding BOTH roles takes the **SUPPLIER** location on a receipt, because that is the side the stock came from (SETTLED 2026-09-16, §7); *available* = `is_allocatable = true` (`RQP-T1-24`) | user decision B1; live `whb_stock_statuses` seed | RESOLVED |
| H14 | QC-held stock with no plan exits only via WS-042 Change status | the exit stays WS-042 `MPR-T1-10`; its permission (and so whether the QA gate holds there) is the movement contract's G3 | `movement-post-reverse.contract.md` G3 | DEFERRED-TO-P0-02 |
| H15 | Untracked dated obligations | pending approvals covered by `warehouse.approval.pending_alert_hours` (`RQP-T3-03`); the rest have no v1 rule (§6) | `GLOBAL-SETTINGS-DECISIONS.md` catalogue | RESOLVED |
| H16 | Default sort, tiles, export for WS-075, WS-078…WS-082; WS-076 tiles | defaults written into §5 | DATA-MODEL indexes; `warehouse-base` sort precedent; `FR-395`; export ⊇ grid | RESOLVED (WS-075 / WS-076 SETTLED 2026-09-16 as ratified, §7; **WS-079 / WS-080 SETTLED 2026-09-16 as ratified**, §7; **WS-081 / WS-082 and WS-078 refinements SETTLED 2026-09-16**, §7 C8 fix rows) |
| H17 | Permissions for Receive line, Attach PO/ASN, Record seals, inspection Start / Record results / Complete, rule Test | `:edit` / `:create` / `:view` per §2 | BUILD-SPEC §10.1 shape and §10.2 rule; `p0-10` WS-059 | RESOLVED |

<!-- Ratified 2026-09-14. Amend with /functional-contract: every change references a row id and adds a §7 row. -->
